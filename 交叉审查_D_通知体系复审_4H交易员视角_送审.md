# 交叉审查 D：通知体系复审（4H 交易员视角）

> 交接系审查 D 篇，与 `交叉审查_A_源码实证核对.md` / `交叉审查_B_设计安全审查.md` / `交叉审查_C_测试有效性审查.md` 并列。
>
> **审查性质**：只读取证 + 结论复盘，**未修改任何代码**。
> **审查日期**：2026-09-25
> **审查触发问题**：「为什么没有持仓，系统还在发邮件？」
> **审查视角**：人类 4 小时级别比特币波段交易员（`MAX_LEVERAGE=100`）

---

## 0. 结论速览

| 序号 | 结论 | 级别 |
|---|---|---|
| **F1** | 「空仓也发邮件」的根因已实证闭环：**日报 08:05 触发本身完全不看持仓**，且**运行中的进程并未加载 `.env` 里新写入的 `EMAIL_ALERT_ONLY_WITH_POSITION=true`**——配置文件的开关和进程里生效的开关是两个东西。 | **P0** |
| **F2** | 闸门存在**双向陷阱**：进程内闸门当前是「放行」，所以日报照发；但一旦重启让磁盘上的 `true` 生效，空仓时**连 `AUTH_BLOCKED`、程序崩溃的邮件兜底也会被一并吞掉**。同一份配置，重启前后行为相反。 | **P0** |
| **F3** | 快照/日报会把「已挂单未成交」渲染成 `持仓 0.0000 (0/5层) SL❌ TP❌`，视觉上等同「裸仓、保护单丢失」，属于**假阳性资金安全告警**。 | **P0** |
| **F4** | 全代码库**零实现**强平价 / 维持保证金 / 距强平距离监控（全仓 grep `强平\|liquidation\|维持保证金\|maintenanceMargin\|liquidationPrice` → 0 命中），而杠杆上限为 100x。100x 下约 1% 不利波动即触及强平，4H 单根常态振幅 2%~5%。 | **P0** |
| **F5** | 浮盈百分比按 **notional（名义价值）** 计算，却与 `100x` 并列展示，可能被误读 **100 倍**；且 ticker 取价失败会静默回落成均价，显示 `+0.00` 的「假平静」。 | **P1** |
| **F6** | 轮询间隔在活跃批次 `≤2` 时为 10~15s，`≤4` 时骤降至 75~100s——恰好在 `RISK_MAX_ACTIVE_BATCHES=3` 处回到代码注释里被判为「不可接受」的量级，**裸仓防护被配置本身关掉**。 | **P1** |
| **F7** | 告警是**单向**的：巡检只有「出事通知」，没有「已恢复通知」；静默机制让持续存在的异常在信息层彻底消失。 | **P2** |
| **F8** | Watchdog 定时重启锚定在 `[0,4,8,12,16,20]:00`（= 每根 4H 收盘整点），当前虽 `ENABLE_SCHEDULED_RESTART=False`，但一旦开启将与收盘触发类逻辑正面相撞。 | **P2** |
| **F9** | 资金费（funding）零监控：多日 4H 持仓跨越多次结算，成本未进日报。 | **P2** |
| **F10** | **巡检邮件此刻即被闸门挡住，无需重启**：`健康巡检.py` 每次运行重读磁盘 `.env`（`:68-80`），其邮件兜底与主程序共用同一闸门 → 「当前全开」只对长驻主程序成立。 | **P0** |
| **F11** | **崩溃邮件不是真兜底**：`bot_runner.py:385-392` 仅在 **TG 发送成功**后才发崩溃邮件；TG 三轮失败进 `SILENCED` 则永不发；bot 启动熔断时无消费进程，队列永不消费。 | **P0** |
| **F12** | **日报把「尝试发送」当「已送达」**：`_daily_report_loop` 调用后即标记当日日期，TG 失败仅打印、邮件为 daemon 线程异步发送，两路皆失败当日不再重试。 | **P1** |

**最需要立刻确认的一件事**：当前 `.env` 的磁盘配置与运行进程（PID 38828，2026-09-24 09:35 启动）内的实际生效配置**不一致**。重启前后，邮件闸门的行为会 180° 反转。

---

## 1. 审查方法与取证清单

### 1.1 四轮审查的演进

| 轮次 | 视角 | 产出 | 遗留缺陷 |
|---|---|---|---|
| 第一轮 | 渠道清单（谁发、发哪、什么条件） | 通知类型 × 渠道矩阵 | 只看调用点，未看闸门生效性 |
| 第二轮 | 4H 交易员行为（作息、空仓期诉求） | 邮件分级、抗疲劳建议 | 基于「闸门已生效」的错误前提 |
| 第三轮 | 100x 高杠杆风险后果 | 强平监控、浮盈口径、轮询悬崖 | **F1 结论有误**，本轮已修正 |
| 第四轮（本报告） | 运行时实证（日志 + 进程 + 文件时间戳） | 根因闭环 + 完整问题清单 | — |

### 1.2 本轮实际执行的取证

| # | 取证命令/动作 | 结果 |
|---|---|---|
| E1 | 读取 `.env` 中 `EMAIL_ALERT_ENABLED` / `EMAIL_ALERT_ONLY_WITH_POSITION` / `MAX_LEVERAGE` / `RISK_MAX_ACTIVE*` | `true` / `true` / `100` / 未显式配置（默认 3） |
| E2 | 统计 `trade_state.json` 批次状态 | 9 个批次，**`is_active=true` = 0 个** |
| E3 | `trade_state.json` 文件 `LastWriteTime` | **2026-09-23 23:07:05** |
| E4 | `.env` 文件 `LastWriteTime` | **2026-09-24 11:19:26** |
| E5 | `.heartbeat.json` → `bot_pid=38828` → 查询进程创建时间 | **2026-09-24 09:35:14**（早于 .env 写入 1h44m） |
| E6 | 全仓 grep 三个 SMTP 发送点 | `trader_260725.py:890` / `bot_runner.py:122` / `健康巡检.py:193`，**三处全部经过 `email_gate`** |
| E7 | 检索 `logs\bot_*.log` 中 `[邮件] 已发送` / `[邮件] 已跳过` | **2 次发送、0 次跳过**（见 §2 时间线） |
| E8 | `.notify_queue/` 目录是否存在 | **不存在**（队列从未落过盘） |
| E9 | grep 强平/维持保证金/liquidationPrice | **0 命中** |
| E10 | grep 资金费 / funding | **0 命中** |
| E11 | 统计 `send_tg_notification(` 调用点 | `trader_260725.py` **131 处**，`bot_runner.py` 2 处 |
| E12 | 读取 `.patrol_alert.state.json` | 含孤儿键 `output_stalled`（2026-09-24 08:44:53），现有代码 0 命中 |


---

## 2. F1/F2：「空仓也发邮件」根因闭环（含配置漂移）

### 2.1 硬证据时间线

| 时间 | 证据 | 含义 |
|---|---|---|
| 2026-09-23 14:50 | 创建 `batch_20260923_145056_491d00` | 曾有活跃批次 |
| 2026-09-23 16:58:01 | `bot_20260923.log:11` → `活跃批次: 1 个` | 进程 A 启动时有 1 个活跃批次 |
| 2026-09-23 23:07:05 | `trade_state.json` 最后一次写入 | 此后**再无任何批次状态变化** |
| 2026-09-24 08:05:31 | `bot_20260924.log:2` → `📧 [邮件] 已发送: 每日结算报告 2026-09-23` | **日报邮件发出** |
| 2026-09-24 09:35:14 | 进程 B（PID 38828）启动 | 当前运行进程，启动时 `活跃批次: 0 个` |
| 2026-09-24 11:19:26 | **`.env` 最后写入** | **晚于进程 B 启动 1h44m** |
| 2026-09-25 08:05:43 | `bot_20260925.log:2` → `📧 [邮件] 已发送: 每日结算报告 2026-09-24` | **在 0 活跃批次下邮件发出** |
| 2026-09-25 审查时 | `trade_state.json` 9 批次全部 `is_active=false` | 当前确属空仓 |

### 2.2 推理链（每一步都有证据）

1. **日报触发完全不看持仓** —— `trader_260725.py:1671-1675` 的 `_daily_report_loop` 只判断 `now.hour==8 and now.minute==5` + 当日去重，**没有任何持仓条件**，它每天都必然尝试发送。
2. **9-25 08:05 时活跃批次 = 0** —— `trade_state.json` 自 09-23 23:07 起未被写入，当前 9 个批次全为 `is_active=false`；`bot_20260924.log` 在 09:35 启动后只有 `限流观测` 行，无任何开/平仓痕迹。
3. **但邮件确实发出去了，且日志中 0 条 `已跳过`** —— 若闸门生效，`trader_260725.py:867-869` 必然打印 `ℹ️ [邮件] 已跳过（no_active_positions）` 并 `return`，不会出现 `📧 [邮件] 已发送`。
4. **∴ 进程 B 内 `email_only_with_position()` 返回了 `False`** —— 其环境里 `EMAIL_ALERT_ONLY_WITH_POSITION` **不是 `true`**（是 `false` 或键不存在；`email_gate.py:57` 默认值为 `false`）。
5. **而 `.env` 写入于进程启动之后**（E4 vs E5）—— python-dotenv 的 `load_dotenv()` 只在 `bot_runner.py:58` 进程启动时读一次。

> **结论**：磁盘上的 `EMAIL_ALERT_ONLY_WITH_POSITION=true` **对当前运行进程无效**。系统当前处于「闸门放行」状态，日报每天照发 —— 这正是「没有持仓却收到邮件」的直接原因。
>
> **反向陷阱**：任何一次重启都会让 `true` 生效，届时行为会**整个翻转** —— 空仓期间日报、崩溃报警、`AUTH_BLOCKED` 邮件**全部**被吞（三处 SMTP 共用同一个 `email_gate`，且 `email_gate` 不区分事件类型）。

### 2.3 F2：闸门设计的三个缺陷

| # | 缺陷 | 位置 | 后果 |
|---|---|---|---|
| F2-a | **无事件类型维度** —— 一个布尔开关同时管辖日报、critical 资金安全告警、崩溃、鉴权封锁、巡检兜底 | `email_gate.py:81-98` | 致命事件被日常静音策略误杀 |
| F2-b | **「持仓」判据 = 本地 `is_active`，不区分已成交与仅挂单** | `email_gate.py:74`；`trader_260725.py:5661`（挂单即置 `is_active=True`，`last_filled_count=0`） | 你认为「没持仓」，系统认为「有持仓」—— 语义分歧 |
| F2-c | **配置热漂移** —— `.env` 改后必须重启才生效，且无任何提示 | `bot_runner.py:58` | 用户看到的配置 ≠ 生效的配置，调试结论会被带偏 |

**F2-b 的完整故事**：一个挂着未成交的限价入场单，会让闸门判定为「有持仓」→ 邮件放行 → 你收到一封「昨日已实现盈亏 0.00（无平仓）」的日报 → 你判定「没持仓还发邮件」。**分歧不在邮件，在「持仓」的定义。**

---

## 3. 通知体系全景矩阵（当前实现）

### 3.1 渠道 × 事件

| 事件 | 触发条件 | Telegram | Email | `.notify_queue` | 关键位置 |
|---|---|:-:|:-:|:-:|---|
| 普通交易状态（开仓/成交/结算） | `send_tg_notification(msg)` | ✅ | ❌ | ❌ | `trader_260725.py` ×131 处 |
| Warning | `level='warning'` | ✅ | ❌ | ❌ | `:820-821` |
| Critical 资金安全 | `level='critical'` | ✅ | ✅ **并行** | ❌ | `:817-819` |
| IP 变更 | `_record_ip_change` | ✅ | ✅ **并行** | 仅 TG 失败时 | `:573` |
| AUTH_BLOCKED / 状态锁损坏 | `_mark_auth_blocked` 等 | ✅ | ✅ | ✅ **三通道** | `:687/:734` + `:691/:738` |
| 鉴权恢复 | `_attempt_auth_recovery` 成功 | ✅ | ❌ | ❌ | `:774-778` |
| 每日结算报告 | 08:05（北京）**与持仓无关** | ✅ | ✅ | ❌ | `:1658-1660`, `:1671-1675` |
| 平仓后剩余批次快照 | `_notify_snapshot` | ✅ | ❌ | ❌ | `:1588-1595` |
| Watchdog 崩溃 | 主程序异常退出 | ✅ | ✅（TG 成功后） | ✅ | `watchdog.py:571-576` → `bot_runner.py:390-392` |
| Watchdog 定时重启 | `RESTART_HOURS`（当前禁用） | ✅ 汇总 | ❌ | ✅ | `watchdog.py:40-44`, `:580-587` |
| Watchdog 启动熔断 | 连续 5 次启动失败 | ✅ | ✅（走 crash 链） | ✅ | `watchdog.py:549-557` |
| Watchdog 通用告警 | `watchdog_alert` | ✅ | ❌ | ✅ | `bot_runner.py:372-375` |
| 健康巡检异常 | 心跳/进度/代理/进程 | ✅ 双路 | 仅 TG 双路失败后 | ❌ | `健康巡检.py:alert()` |
| 开机自启放弃 | 代理 300s 未就绪 | ✅ 直连 | TG 失败后 | ❌ | `健康巡检.py:342-357` |
| 通道自检 | `--selftest` | ✅ 双路 | ✅ 主动测试 | ❌ | `健康巡检.py:359-369` |
| TG 命令回复 | `/summary` `/system` 等 | ✅ | ❌ | ❌ | `bot_runner.py:668+` |

### 3.2 重试与静默

| 机制 | 范围 | 参数 |
|---|---|---|
| 队列有限重试 | `.notify_queue` 消费 | `NOTIFY_MAX_ATTEMPTS=3` 轮 → `SILENCED`（`bot_runner.py:87`, `:413-420`） |
| 队列降级 | Markdown → 纯文本 | `_send_notify_with_fallback`（`bot_runner.py:260-272`） |
| 同键告警抑制 | `converge` 类 | 同键 3 轮后静默（`trader_260725.py:11341-11343`） |
| 崩溃同因去重 | `crash_alert` | `CRASH_ALERT_DEDUP_WINDOW=600s`（`watchdog.py:61`） |
| 巡检去重 | 按 issue key | `ALERT_DEDUP_SECONDS=1800s`（`健康巡检.py:42`） |
| **恢复通知** | — | **❌ 全系统无**（巡检正常只写日志：`健康巡检.py:327`） |


---

## 4. 问题清单

> 每条格式：**现象 → 代码证据 → 交易员后果 → 建议**。
> 级别：P0 = 资金/漏报风险；P1 = 会造成误判与错误决策；P2 = 体验与健壮性。

### 4.1 P0-1 邮件闸门：事件无分级 + 配置漂移（F1/F2）

**现象**：一个布尔开关管辖全部邮件；磁盘配置与生效配置不一致。

**证据**：
- `email_gate.py:81-98` 判定链只有「总开关 / 持仓开关 / 状态可读性」三个维度，**没有事件类型**；
- 三处 SMTP 全部调用它：`trader_260725.py:866`、`bot_runner.py:99`、`健康巡检.py:173`；
- `.env` 写入时间（09-24 11:19:26）晚于进程启动（09-24 09:35:14）；
- 日志两次 `已发送`、零次 `已跳过`。

**交易员后果**：
1. 当前：日报每天 08:05 抵达，空仓也发（最初困扰的直接答案）；
2. 重启后：闸门生效 → 空仓时**崩溃报警与 `AUTH_BLOCKED` 邮件一起被吞**，出事时邮箱安静；
3. 你无法通过「看 `.env`」判断系统现在会不会发邮件。

**建议**：
```python
# email_gate.py 增加事件维度
def should_send_email(event: str = "generic", env=None, state_path=None) -> tuple[bool, str]:
    # 1) 总开关 EMAIL_ALERT_ENABLED
    # 2) 致命白名单永远放行：auth_blocked / crash / state_corrupt / api_banned
    # 3) daily_report 由独立开关 DAILY_REPORT_EMAIL 控制，不受持仓闸门约束
    # 4) 其余事件才受 EMAIL_ALERT_ONLY_WITH_POSITION 约束
```
配套：进程启动时打印**实际生效值**（`📧 邮件闸门: enabled=... only_with_position=... .env_mtime=...`），并在 `.env` mtime 晚于进程启动时间时告警一次。

---

### 4.2 P0-2 快照把「待成交挂单」渲染成「裸仓」

**证据**：`trader_260725.py:1555-1586`
```python
sl_txt = "SL✔️" if b_data.get('current_sl_id') else "SL❌"
# 未成交批次：current_sl_id=None, tp_order_id=None → 两者都 ❌
```
实际输出：`📈 BTCUSDT BUY | 持仓 0.0000 (0/5层) | SL❌ TP❌`

**交易员后果**：早晨 08:05 在日报里看到这条，会读成「持仓为 0 却没有止损」——即**保护单丢失**级别的事故，进而可能在交易所做多余的手工干预。误报比漏报更贵。

**建议**：三态渲染 —— `待成交挂单(0/5层) · SL/TP 未生效（正常）` / `已成交 · SL✔️TP✔️` / `⚠️已成交但 SL❌（真异常）`。**只有第三种允许出现 ❌**，且第三种应升级为 `critical`。

---

### 4.3 P0-3 强平监控零实现（F4）

**证据**：grep `强平|liquidation|maintenanceMargin|liquidationPrice|维持保证金|距强平` → **0 命中**；唯一相关文案是 `trader_260725.py:5710` 的入场提示「价格波动可能导致强平，请密切关注！」（`warning` 级，仅 TG）。
配套事实：`.env` `MAX_LEVERAGE=100`；`RISK_MAX_ACTIVE_BATCHES` 默认 3。

**交易员后果**：
- 100x 下约 **1%** 不利波动即触及强平，而 4H 单根 K 线常态振幅 **2%~5%**；
- **两根 4H 之间价格完全可以先扫强平价再回归**，系统只会在事后告诉你「已强平」；
- 保证金使用率**只在入场瞬间算一次**（`:5702-5713`），持仓期间永不复算。

**建议**：新增 `liquidation_proximity` 事件（仅对已成交批次生效）：

| 距强平 | 级别 | 渠道 | 夜间 |
|---|---|---|---|
| <0.5% | `critical` | TG + Email | **叫醒** |
| 0.5%~1.5% | `warning` | TG | 攒到 08:05 |
| 恢复 >1.5% | 恢复通知 | TG | 按 §4.8 |

数据来源优先用本地仓位/保证金推导，避免在 AUTH_BLOCKED 期间依赖额外 API。


---

### 4.4 P1-1 浮盈百分比口径在 100x 下误导 100 倍（F5-a）

**证据**：`trader_260725.py:2615-2616`
```python
capital_base = avg_price * filled_amount          # 名义价值 notional
unrealized_pnl_pct = unrealized_pnl / capital_base * 100
```
渲染处：`bot_runner.py:700` / `:2722` → ``浮动盈亏: 🟢 `+250.00` USDT (`+0.50%`)``，**同屏并列 `100x`**。

**交易员后果**：`+0.50%` 会被读成「本金 +0.5%」，实际是名义 +0.5% = **保证金约 +50%**；反向 `-0.90%` 看似安全，实为**保证金 -90%，距强平一步之遥**。这会直接改变手动干预决策。

**建议**：改为保证金口径，或双标注 `占名义 +0.50% / 占保证金 +50.0%`，并在消息中写明口径。

---

### 4.5 P1-2 取价失败静默回落 → `+0.00` 假平静（F5-b）

**证据**：`trader_260725.py:2603-2607`
```python
except Exception:
    current_price = avg_price   # 静默替换，下游算出 pnl = 0
```

**交易员后果**：断网 / 代理挂掉 / 熔断期间打开 `/summary`，看到 `均价 68000 | 市价 68000 | 浮动盈亏 +0.00` —— 看起来「刚好打平」，实际是**取价失败、真实盈亏未知**。在最需要信息的时刻收到平静假象。

**建议**：`current_price=None` 时显示 `市价: 获取失败（盈亏未知）`，**禁止用均价兜底成数字**。

---

### 4.6 P1-3 轮询悬崖：第 3 个批次起裸仓防护失效（F6）

**证据**：`trader_260725.py:3579-3596`
```python
# v6.2-P0-1（实盘）：100x 下成交到发现 SL/TP 缺失的裸仓窗口最长 ~80s 不可接受
if active_count <= 2:   base=10.0, jitter=5    # 10~15s
elif active_count <= 4: base=75.0, jitter=25   # 75~100s ← 断崖
elif active_count <= 6: base=90.0, jitter=30
```
而 `RISK_MAX_ACTIVE_BATCHES` 默认 **3**（`trader_260725.py:1500-1501`）。

**交易员后果**：从第 3 个批次开始，轮询从 10s 跳到 75s —— **恰好回到代码注释里被判为「不可接受」的量级**。你越是有多个批次（越需要风控），成交到补挂保护单的延迟就越长。

**建议**：间隔决策由「**是否存在未完成保护的成交**」驱动，而非批次数量 —— 有裸仓风险恒定 5~15s，批次多时用 API 权重预算节流，不拿保护延迟换权重。

---

### 4.7 P1-4 保证金使用率 >80% 仅 `warning` 且只算一次

**证据**：`trader_260725.py:5702-5713` —— 入场时一次性计算，`level='warning'` → 仅 TG，无邮件、无队列、持仓期不复算。

**交易员后果**：100x 下这是**唯一一条**提示「你可能已经满仓」的信号，却以最低优先级、最窄渠道、瞬时一次的方式发出，错过即永久错过。

**建议**：升级为 `critical`（TG + Email），并在每次余额刷新时复算，跨阈值（>80% / >90%）各告警一次。


---

### 4.8 P2-1 告警单向，没有「已恢复」（F7）

**证据**：`健康巡检.py:327` 巡检正常仅 `log("✅ 巡检正常: ...")`，不发任何通道；同键 1800s 去重（`:42`）。
正面样板对照：`trader_260725.py:774-778` 的「鉴权已恢复」是全系统唯一带闭环的告警。

**交易员后果**：凌晨 03:00 收到「代理不可达」→ 05:00 修好 → **永远不会收到「已恢复」**，只能自己上机器确认或猜。一次 4 小时故障 = 最多 8 条同样告警（30min 去重）+ 0 条恢复通知。

**建议**：
1. 巡检增加恢复通知 —— 仅当同一 key 在近 N 小时内告警过才发，天然限流；
2. 未闭环异常进入**每小时一条**「仍在异常（已静默 N 条）」状态行，或收进日报固定字段。

---

### 4.9 P2-2 定时重启锚定 4H 收盘整点（F8）

**证据**：`watchdog.py:40-44`
```python
ENABLE_SCHEDULED_RESTART = False
RESTART_HOURS = [0, 4, 8, 12, 16, 20]
RESTART_MINUTE = 0
```
重启链含 `CTRL_C_EVENT` → `sleep(10)` → 必要时 `terminate` → 启动 + `recover_active_batches`（`watchdog.py:516-545`）。

**交易员后果**：这套锚点 = **每根 4H K 线收盘瞬间**，而收盘瞬间正是收盘触发类条件单的判定点（D-011 `candle_close`）与 KAMA 取值点。当前 `False` 无风险，**一旦有人打开就会与收盘逻辑正面相撞**。

**建议**：若启用，`RESTART_MINUTE = 5`（收盘后 5 分钟），并加保护：存在未决订单/未完成结算时推迟重启。

---

### 4.10 P2-3 资金费零监控（F9）

**证据**：grep `资金费|funding` → **0 命中**；日报已有「费用未对账（非权威）」措辞（`trader_260725.py:1645-1646`）但只覆盖手续费。

**交易员后果**：资金费每 8 小时结算一次，一天 3 次，4H 持仓 5 天 = 15 次；100x 下资金费对**保证金**的侵蚀被放大近百倍，且完全不进日报。

**建议**：日报对跨结算点的持仓增加一行：`本持仓已跨越 N 次资金费结算，估算成本 X USDT（未计入已实现盈亏）`。

---

### 4.11 P2-4 渠道哲学：并行双发 ≠ 冗余

**现状**：

| 事件 | 方式 | 位置 |
|---|---|---|
| critical | TG **和** Email 并行双发 | `trader_260725.py:819` |
| IP 变更 | TG **和** Email 并行双发 | `:573` |
| 崩溃 | TG 成功**后**补 Email | `bot_runner.py:390-392` |
| 巡检 | TG 双路**失败后**才 Email（真串行兜底） | `健康巡检.py:alert()` |

**交易员后果**：并行双发 = 同一消息收两遍（重复噪音）；串行兜底才是真冗余（平时零噪音，TG 挂了才唤醒邮件）。

**建议**：默认改为「TG 主 + 失败即邮件兜底」；**唯一例外**保留 AUTH_BLOCKED 三通道并行（资金安全级，设计稿已论证，理由成立）。


---

### 4.12 P2-5 缺少夜间静默（quiet hours）策略

**现状**：系统没有任何时段维度；是否打扰你完全由「有没有活跃批次」这个不相干的开关决定。

**建议**（0x–8x 分级）：

| 类别 | 事件 | 处理 |
|---|---|---|
| **可叫醒** | 距强平 <0.5%、保护单失效、`AUTH_BLOCKED`、带仓崩溃 | 立即多通道 |
| **攒到 08:05** | 日报、无仓期巡检告警、Watchdog 重启、收敛静默摘要 | 合并为一条 digest |
| **永不打扰** | 自检、心跳正常、`info` 级状态 | 仅日志 |

---

### 4.13 P2-6 其它待核实项

| # | 现象 | 证据 | 说明 |
|---|---|---|---|
| a | `.patrol_alert.state.json` 残留孤儿键 `output_stalled`（2026-09-24 08:44:53） | 现有 4 个主文件 0 命中 | 旧版检查项遗留，state 文件跨版本累积不清 → **待核实**是否属已退役检查 |
| b | `.notify_queue/` 目录不存在，`.notify.state.json` 仅 2 字节 | 取证 E8 | 队列机制从未被触发过 = **从未做过真实端到端演练**，故障时是否可用未经实证 |
| c | TG 单通道承载「命令 + 告警 + 状态」共 131 处调用 | 取证 E11 | 洪泛时 critical 可能被顶出屏幕；建议资金安全类启用独立话题/置顶 |
| d | 文档与配置漂移：`README.md:111` 写 `EMAIL_ALERT_ONLY_WITH_POSITION=false`，`.env` 实际为 `true` | `git status --porcelain` | 已于 `f1c8979` 整体提交（分支 `frozen-archive`），此项已闭环；仅保留文档/实配不一致的提示 |

---

## 5. 对此前各轮结论的修正声明（重要）

本次审查过程中有两处**此前结论错误**，按证据优先原则予以更正，请以本报告为准：

### 5.1 第一/二轮：「无持仓时唯一能发出的邮件是每日结算报告」—— 部分错误

- **错误点**：日报同样走 `_send_email_alert` → 同一闸门，当时我推断「空仓时日报邮件也会被拦」。
- **实际情况**：推断忽略了**闸门在运行进程中未必生效**这一事实。日志证明 9-25 08:05（0 活跃批次）日报邮件**确实发出**。
- **修正后表述**：日报触发本身与持仓无关；它是否真的发出，取决于**运行进程当时加载的闸门值**，而非 `.env` 现值。

### 5.2 第三轮：「当前邮件通道事实上处于全静默状态」—— **结论相反，予以撤回**

- **第三轮原话**：9 个批次全 `is_active=false` ⇒ 崩溃/鉴权邮件也被吞 ⇒ 邮件通道全静默。
- **错误原因**：只读了磁盘 `.env`，**没有比对进程启动时间与 `.env` 修改时间**。
- **证据修正**：进程 B 启动于 09-24 09:35:14，`.env` 写入于 09-24 11:19:26；日志中 0 条 `已跳过`、2 条 `已发送` ⇒ **当前运行进程的闸门是「放行」，邮件通道全开**。
- **正确表述**：**当前全开（日报照发），重启后若磁盘值生效则全闭（致命邮件被吞）。两种状态都是错的，且方向相反。**

> 教训写入本次审查方法论：**判断一个开关，必须同时校验「配置文件内容 + 文件写入时间 + 进程启动时间 + 运行期日志」四者**，缺一即可能得出相反结论。


---

## 6. 4H 交易员画像与信噪比设计

### 6.1 画像假设（如与实际不符请告知，本节结论需相应调整）

- **决策节奏**：锚点为 4H 收盘（北京 00/04/08/12/16/20 点），日常主动看盘次数有限；
- **持仓周期**：数天至数周，**空仓观望是常态**，可长达数日；
- **作息**：0x–8x 大概率离线（睡眠）；
- **杠杆**：`MAX_LEVERAGE=100`；
- **信息入口**：手机 Telegram 为主，邮箱为夜间/兜底；
- **抗干扰**：对「无事报平安」类噪音容忍度极低——噪音会稀释真告警，最终导致静音。

### 6.2 当前信噪比是「倒挂」的

| 场景 | 当前行为 | 期望行为 |
|---|---|---|
| **空仓、系统健康** | 08:05 日报照发邮件（含 0.00 盈亏、`SL❌ TP❌` 快照） | 只发一行 TG：`空仓待命 · 可用余额 x · 昨日无操作`，**不发邮件** |
| **空仓、致命故障** | 闸门生效时**邮件被吞** | **必须**多通道送达（重启后即落入此陷阱） |
| **带仓、多层成交** | 单批次可能连发多条（成交/SL/TP/收敛） | 按 4H bar 聚合为一条「本根变化摘要」 |
| **带仓、接近强平** | **无任何通知** | 最高优先级、允许突破夜间静默 |
| **故障已修复** | 永不通知 | 一条带时间戳的恢复通知 |

**一句话**：现在的系统是「空仓时吵、出事时静、最危险时无声」。

### 6.3 每条通知的输出契约（建议固化为规范）

1. **第一行必须回答「我需要行动吗」**：`🔴 需立即处理 / 🟡 知悉即可 / 🟢 记录`；
2. **数字必须带口径**：`占保证金 +50.0%（占名义 +0.50%）`；
3. **未知必须显式**：`市价: 获取失败（盈亏未知）`，禁止用默认值伪装正常；
4. **每条 critical 必须带「下一步」**：查看哪份日志、执行哪条命令、是否需要人工介入；
5. **异常必须闭环**：告警发出后必须有对应恢复通知（或在下一份日报中注明状态）。

---

## 7. 行动计划（分批）— ⚠️ **本节顺序已被 §10.4 取代，仅保留批次内容参考**

### Batch A — P0 止血（不改架构，改语义）

| # | 改动 | 涉及文件 | 验收标准 |
|---|---|---|---|
| A1 | `email_gate` 增加 `event` 参数与致命白名单；日报独立开关 | `email_gate.py`, `trader_260725.py`, `bot_runner.py`, `健康巡检.py` | 致命事件在 `count=0` 时仍 `allowed=True`；日报不再受持仓闸门约束 |
| A2 | 启动时打印实际生效闸门值 + `.env` mtime 比对告警 | `bot_runner.py` | 改 `.env` 不重启 → 下次日志可见明确提示 |
| A3 | 快照三态渲染，未成交批次不再出现 ❌ | `trader_260725.py:1555-1586` | 0 成交批次输出 `待成交挂单(0/5层) · SL/TP 未生效（正常）` |
| A4 | `/summary` 取价失败显式化（禁止均价兜底） | `trader_260725.py:2603-2607` | mock ticker 抛异常 → 输出 `盈亏未知` 而非 `+0.00` |

### Batch B — P1 决策正确性

| # | 改动 | 涉及文件 | 验收标准 |
|---|---|---|---|
| B1 | 浮盈百分比改保证金口径或双标注 | `trader_260725.py:2615-2616`, `bot_runner.py:700/:2722` | 100x 下显示与保证金收益一致 |
| B2 | 轮询间隔由「裸仓风险」驱动 | `trader_260725.py:3579-3596` | 3 批次且存在未完成保护时，间隔仍 ≤15s |
| B3 | 保证金使用率升级 critical + 持仓期复算 | `trader_260725.py:5702-5713` | 跨 80%/90% 各告警一次且带邮件 |
| B4 | 新增 `liquidation_proximity` 监控与分级告警 | 新增模块 + `trader_260725.py` | 距强平 <0.5% 触发 critical；仅对已成交批次生效 |

### Batch C — P2 体验与闭环

| # | 改动 | 验收标准 |
|---|---|---|
| C1 | 巡检恢复通知（近 N 小时告警过的 key 才发） | 故障→修复→收到 1 条恢复通知 |
| C2 | 静默期状态摘要（每小时 1 条或并入日报） | 持续异常不再在信息层消失 |
| C3 | critical 改「TG 主 + 失败即邮件」（AUTH_BLOCKED 除外） | 正常时不再收到重复消息 |
| C4 | quiet-hours 分级 + 08:05 digest | 0x–8x 只有致命事件会响 |
| C5 | 资金费进入日报 | 跨结算持仓显示累计资金费 |
| C6 | `RESTART_MINUTE` 改 5 + 未决单保护 | 定时重启不再落在收盘整点 |
| C7 | 清理 `.patrol_alert.state.json` 孤儿键 / 加键版本管理 | state 文件只含当前有效检查项 |
| C8 | 队列端到端演练（人工投递一条 `crash_alert`） | `.notify_queue/` → TG → 删除 → 审计，全链路验证一次 |


---

## 8. 测试建议

沿用仓库既有 `test_email_gate.py` / `test_email_channels.py` / `test_notify_queue.py` 风格，新增：

| 测试 | 场景 | 断言 |
|---|---|---|
| `test_email_gate.py` 新增 | `event='auth_blocked'` + `count=0` | `allowed=True` |
| | `event='daily_report'` + `count=0` + 日报开关开 | `allowed=True` |
| | `event='generic'` + `count=0` | `allowed=False, reason=no_active_positions` |
| | 挂单未成交（`is_active=True, last_filled_count=0`） | 视为**无持仓**（若采纳 F2-b 修正） |
| `test_snapshot_render` 新增 | 0 成交批次 | 输出不含 `SL❌` |
| | 已成交且 `current_sl_id=None` | 输出含 `⚠️` 且 level=critical |
| `test_summary_unknown_price` 新增 | `fetch_ticker` 抛异常 | 输出 `盈亏未知`，**不出现** `+0.00` |
| `test_liquidation_proximity` 新增 | 距强平 0.3% / 1.0% / 2.0% | 分别触发 critical / warning / 无 |
| `test_poll_interval` 新增 | 3 批次 + 未完成保护 | 返回间隔 ≤15s |
| `test_config_drift` 新增 | `.env` mtime > 进程启动时间 | 启动日志含 drift 告警 |

**另外建议做一次真实演练（C8）**：手工向 `.notify_queue/` 投递一条 `crash_alert`，验证 TG → 邮件 → 汇总 → 删除 → 审计的完整链路——目前该目录从未生成过，属于**未经实证的安全网**。

---

## 9. 需要你拍板的决策

1. **邮件通道的定位**
   - (A) **备用报警铃**：TG 主 + 失败才邮件，仅日报例外 —— 推荐，去重且保真冗余；
   - (B) **并行第二通道**：每条 critical 双发 —— 当前实现，代价是重复噪音。

2. **`EMAIL_ALERT_ONLY_WITH_POSITION` 是否继续保留**
   - 保留则**必须**按 §4.1 分级，否则重启后空仓期致命邮件被吞；
   - 不设为闸门，改由 quiet-hours 统一管理可能更贴合 4H 使用习惯。

3. **睡眠红线（0x–8x）**：默认建议「距强平 <0.5% / 保护单失效 / AUTH_BLOCKED / 带仓崩溃」四类可叫醒，其余攒到 08:05。请确认或增删。

4. **`ENABLE_SCHEDULED_RESTART`**：当前 `False`。若计划开启，请先采纳 C6（错开收盘整点）。

5. **是否立即重启以让 `.env` 生效**：**不建议现在重启** —— 先完成 A1（致命白名单），否则重启即进入「空仓期致命邮件被吞」状态。

---

## 附录 A：原始取证数据

```
[E1] .env
  EMAIL_ALERT_ENABLED=true
  EMAIL_ALERT_ONLY_WITH_POSITION=true
  MAX_LEVERAGE=100

[E2/E3] trade_state.json
  总批次数 = 9，is_active=true 批次数 = 0
  LastWriteTime = 2026-09-23 23:07:05

[E4] .env LastWriteTime = 2026-09-24 11:19:26

[E5] .heartbeat.json → bot_pid=38828
  Win32_Process CreationDate = 2026/9/24 9:35:14
  CommandLine = ...python.exe G:\my-crypto-bot\bot_runner.py

[E7] logs\bot_*.log（目录内仅存 20260923/24/25 三份，更早日志已不存在）
  bot_20260924.log:2  [08:05:31] 📧 [邮件] 已发送: 每日结算报告 2026-09-23
  bot_20260925.log:2  [08:05:43] 📧 [邮件] 已发送: 每日结算报告 2026-09-24
  [邮件] 已跳过 命中数 = 0（三份日志全量检索）

  bot_20260923.log:11  活跃批次: 1 个（进程 A 启动）
  bot_20260924.log:13  活跃批次: 0 个（进程 B 启动，09:35:19）

[E8]  .notify_queue/ 不存在；.notify.state.json 存在（2 字节）
[E9]  强平/liquidation/maintenanceMargin/liquidationPrice → 0 命中
[E10] 资金费/funding → 0 命中
[E11] send_tg_notification( 调用点：trader_260725.py=131，bot_runner.py=2
[E12] .patrol_alert.state.json = {"_bot_dead_count":0,"output_stalled":1790210693.33}
      → output_stalled 在 watchdog/bot_runner/健康巡检/trader 四文件 0 命中
      → 1790210693 = 2026-09-24 08:44:53

[SMTP 发送点] trader_260725.py:890 / bot_runner.py:122 / 健康巡检.py:193（三处均过 email_gate）
```

## 附录 B：关键阈值速查

| 项 | 值 | 位置 |
|---|---|---|
| 日报时间 | 北京 08:05，去重 90s | `trader_260725.py:1671-1675` |
| 队列重试上限 | 3 轮 → SILENCED | `bot_runner.py:87` |
| 崩溃同因去重 | 600s | `watchdog.py:61` |
| 心跳写入 / 陈旧阈值 | 60s / 300s | `watchdog.py:37`, `健康巡检.py:41` |
| 业务进度停滞阈值 | 900s | `健康巡检.py:45` |
| 巡检频率 / 去重 | 15min / 1800s | `健康巡检.py:18`, `:42` |
| 巡检 bot 判死 | 连续 2 次 | `健康巡检.py:43` |
| 轮询间隔 | 10~15s (≤2) / 75~100s (≤4) / 90~120s (≤6) / 120~160s | `trader_260725.py:3579-3596` |
| 活跃批次 / 交易对上限 | 3 / 1 | `trader_260725.py:1500-1507` |
| Watchdog 定时重启 | 0/4/8/12/16/20 点整，**当前禁用** | `watchdog.py:40-44` |

---

*本报告为只读审查产出，未修改任何代码。所有结论均可按附录 A 复现。*


---

## 10. 对 ChatGPT 复审的逐条回应（2026-09-25 追加）

> 复审方基于 `f1c8979` 独立核对仓库，未接触本机进程/`.env`/日志。本节为逐条取证回应。
> **核对结果：11 条意见中，9 条完全同意、2 条部分同意（均为我方表述让步）、0 条反对。**

### 10.1 三项「优先补充」——全部成立，且第一项推翻我报告的结论

| # | ChatGPT 意见 | 我方取证 | 结论 |
|---|---|---|---|
| 1 | 巡检邮件**此刻**即被闸门挡住，不必等重启 | `健康巡检.py:68-80` `load_env()` 每次执行**重读磁盘 `.env`**；`:171-178` 将该 `env` 显式传入 `email_gate.should_send_email(env=env,...)` | **完全同意，并纠正我报告**：§2「当前全开」仅对长驻主程序成立；**巡检链此刻就是关的** —— 磁盘 `true` + 0 活跃批次 → `no_active_positions` → 巡检邮件兜底失效。已升级为 **F10 / P0** |
| 2 | 崩溃「邮件兜底」在 TG 失败时并不兜底 | `bot_runner.py:385-392`：邮件调用位于 `if ok:` 内，`ok` 即 TG 结果；`:413-420` 三轮失败 → `SILENCED` 后**永不重试**；`watchdog.py:549-557` 启动熔断时 `sys.exit(1)`，**无消费进程 → 队列永不消费** | **完全同意**。我原报告只在矩阵里记了「TG 成功后」，**未识别其失效含义**。已升级为 **F11 / P0** |
| 3 | 日报把「尝试发送」当「已发送」 | `trader_260725.py:1671-1675` 调用后立即置 `_last_daily_report_date`；`send_tg_notification` **无 return**，超时/失败仅 `print`（`:835-856`）；`_send_email_alert` 起 daemon 线程即返回（`:897`） | **完全同意**。已升级为 **F12 / P1** |

### 10.2 五项「建议修正」——三项完全采纳，两项部分采纳

| # | ChatGPT 意见 | 我方回应 |
|---|---|---|
| 4 | `MAX_LEVERAGE=100` ⇒「约 1% 即强平」不成立为**当前仓位**结论 | **部分同意（表述让步）**。我未主张当前实盘仓位的强平距离，但报告措辞确会被读成实盘结论，接受改写为「**100x 全额保证金的上限场景示意**；实盘强平距离取决于逐笔信号杠杆、逐仓/全仓、账户余额与维持保证金」 |
| 5 | 强平数据应**优先取交易所权威值**（标记价/强平价/保证金）并带获取时间；取不到就报「未知」 | **完全同意**，并补充一条边界：`AUTH_BLOCKED` 期间交易所 API 按设计全禁，因此正确显示是「**最后已知值（含时间戳）+ 当前未知**」，不得用本地估算冒充权威值，也不得静默沿用旧值 |
| 6 | `current_sl_id=None` 不能直接宣称交易所保护单失效 | **完全同意**。修正后的规则：先按 **方向 + 数量 + 交易所未结订单**核对；查询失败显示「**保护状态未知**」并升级处理；仅在**核对确认缺失**后才判定失效。待成交批次的 `SL❌` 假象（F3）不受此影响，仍然成立 |
| 7 | 暂不把 critical 改成「TG 失败才 Email」 | **完全同意，并撤回我方 C3 原写法**。已核 `send_tg_notification` **无返回值**、失败仅 `print`：直接改串行会在 TG 失败时**静默丢失全部邮件**，制造新漏报。正确顺序是**先建立每渠道独立投递结果（成功/失败/超时未知）+ 去重**，再决定串行或并行 |
| 8 | 资金费应优先对账 `FUNDING_FEE` 实际流水 | **完全同意**。按次数估算只能标注「**未对账**」，且**不得混入已实现盈亏** |

### 10.3 原则与流程类意见

| # | ChatGPT 意见 | 我方回应 |
|---|---|---|
| 9 | 通知边界 =「**是否需要在下一根 K 线前行动**」 | **完全同意**，采纳为通知分层的组织原则。与报告 §4.12 的 quiet-hours 表一致：安全事件不受夜间静默约束，**降噪不得延迟资金安全事件** |
| 10 | 实施顺序：先修三条故障通知链及失败测试 → 再做快照/未知价 → 再核对交易所权威保护与强平 → 最后日报/资金费/夜间降噪 | **完全采纳**，替换报告 §7 原顺序（见 §10.4） |
| 11 | 报告末尾「闸门仍未提交」是提交前历史描述，`f1c8979` 已提交 | **接受**，报告 §4.13-d 已同步修正 |

### 10.4 修订后的实施顺序（取代 §7 Batch A/B/C）

| 阶段 | 内容 | 验收 |
|---|---|---|
| **R0 故障链**（先做） | ① 闸门加事件维度，致命白名单豁免（覆盖 F10 巡检链）② 崩溃邮件独立于 TG 结果，bot 不可启动时由独立巡检报故障（覆盖 F11）③ 日报区分「生成 / 提交发送 / 渠道确认」，失败可当日重试（覆盖 F12） | 三条链各配**失败路径**测试：TG 失败、bot 不启动、邮件被闸门拦 |
| **R1 显示正确性** | 快照三态渲染（待成交假象 F3）+ 取价失败显式化（F5-b） | 0 成交批次输出无 ❌；ticker 异常输出「盈亏未知」 |
| **R2 权威数据** | 交易所标记价/强平价/保证金 + 时间戳；保护单按方向+数量核对，失败显「未知」（F4 修正版） | 距强平 <0.5% 触发 critical；AUTH_BLOCKED 期显示「最后已知值+未知」 |
| **R3 运营优化** | 恢复通知、静默状态摘要、资金费实际流水对账、quiet-hours、日报按 4H 聚合 | 故障→修复有闭环通知；资金费不进已实现盈亏 |

> **被撤回/降级的原建议**：C3（critical 改串行兜底）**撤回**，改为「先建投递结果模型」；B4 强平监控由「本地推导」**改为「交易所权威值优先」**；A1 扩展为 R0-① 以覆盖巡检链。

### 10.5 一致性声明

- 复审方指出的 3 项补充，**均为我报告的真实遗漏**，其中第 1 项还纠正了我对「当前状态」的误判；已全部并入正文并升级级别。
- 复审方对 5 项建议的修正，**2 项我方主动让步（强平表述、保护单判定）**，3 项完全采纳，其中 C3 的撤回是**我方方案的安全缺陷**，属实。
- **双方在「先修故障链、暂缓降噪」这一优先级上完全一致**；我方无任何保留意见。


---

## 11. R0 实施记录（2026-09-25）

> 按 §10.4 的 R0 阶段落地。三条故障链均**先补失败路径测试**，再改生产代码。

### 11.1 改动清单

| 文件 | 改动 | 对应 |
|---|---|---|
| `email_gate.py` | 新增 `FATAL_EVENTS` 白名单（critical/auth_blocked/crash/startup_breaker/health/liquidation_proximity/protection_lost）、`DAILY_REPORT_EVENT`；`should_send_email` 增加 `event` 形参，三层裁决：总开关 → 致命豁免 → 日报独立开关 → 普通持仓闸门；新增 `DAILY_REPORT_EMAIL_ENABLED` | F10 |
| `健康巡检.py` | `send_email(..., event=)`；`alert()` 与开机自启告警固定 `event="health"`；读到心跳 `fatal_alert` 时新增 `watchdog_fatal` 告警项 | F10 / F11 |
| `bot_runner.py` | `send_email_alert(..., event=)` 返回提交结果；**crash_alert 邮件移出 `if ok:`** —— TG 失败/静默时仍独立尝试，且带 `event="crash"`；崩溃后汇总仍仅在 TG 成功后发 | F11 |
| `watchdog.py` | 心跳新增 `fatal_alert` 字段 + `mark_fatal()`；启动熔断分支在写队列之外**额外落心跳**，供独立巡检出告警 | F11 |
| `trader_260725.py` | `send_tg_notification` 返回 `True/False/None`（None=未配置，不可判定）；`_send_email_alert(..., event=, wait=)` 支持致命豁免与**同步等待确认**；critical 邮件带 `event="critical"`；日报邮件带 `event="daily_report", wait=True`；`_send_daily_report` 返回渠道结果；`_daily_report_loop` 改为**双渠道确认才写日期**，否则 5 分钟重试（上限 6 次） | F12 |
| `.env.example` / `README.md` | 补 `DAILY_REPORT_EMAIL_ENABLED` 与事件维度语义说明 | F10 |
| 测试 | `test_email_gate.py` +6 例；`test_notify_queue.py` +S15；新增 `test_r0_notify_chains.py`（R1~R8） | — |

### 11.2 测试证据

| 套件 | 结果 |
|---|---|
| `test_r0_notify_chains.py` + `test_email_gate.py` + `test_email_channels.py` | **28 passed** |
| `test_notify_queue.py`（含新增 S15：TG 全失败时邮件仍发出且带 event=crash） | **15/15 场景通过** |
| `test_watchdog_guard.py` | **22/22 PASS**（首次失败为 GBK 控制台打印 `✅` 的环境问题，非代码；加 `PYTHONIOENCODING=utf-8` 后全绿） |
| 回归批次（g3_converge / r10 / tg_reply_fallback / recover_semantics / v62_green / v64_p3 / v64_partial / account_risk / auth_blocked / health_progress） | **10 passed** |
| 脚本式回归（close_confirmation_v62 / v62_staged_integration / b2_hardlock / position_close_confirmation） | **133/133、20/20、16/16、7/7** |
| `py_compile`（5 个改动文件） | 通过 |

### 11.3 上线影响与遗留

**⚠️ 变更需重启 `watchdog.py` + `bot_runner.py` 才生效**（两者都是长驻进程；巡检是计划任务，每次运行自然加载新代码）。

**重启风险已解除**：R0-① 落地后，磁盘上的 `EMAIL_ALERT_ONLY_WITH_POSITION=true` 即使生效，**致命事件（崩溃 / 鉴权封锁 / 启动熔断 / 健康巡检）与日报都会正常发邮件**——即 §0 提示的「重启后全闭」陷阱已被本次改动消除。新增配置 `DAILY_REPORT_EMAIL_ENABLED` 缺省 `true`，无需在 `.env` 中补写。

**本次未覆盖（留待 R1/R2）**：
- 快照三态渲染（F3）、取价失败显式化（F5-b）——R1；
- 交易所权威强平价 / 保护单核对（F4、F6 修正版）——R2；
- 恢复通知、静默摘要、资金费对账、quiet-hours ——R3；
- `fatal_alert` 目前**只由启动熔断写入**；崩溃等场景仍依赖队列 + bot 存活。


---

## 12. R0 实现的对抗性复审（2026-09-25，对 `c071643`）

> 范围：专挑**我自己在 R0 引入的新缺陷**（而非重述既有问题）。全部结论均有 RED/GREEN 实证。

### 12.1 发现并修复的 4 个缺陷

| # | 级别 | 缺陷 | 实证（RED） | 修复 |
|---|---|---|---|---|
| **D2** | **P1** | `bot_runner` 把崩溃邮件移出 `if ok:` 后，TG 连续失败会在 3 轮（约 10s 间隔）**重复发 3 封**崩溃邮件 → 我自己制造的通知风暴 | `test_notify_queue` S16 实测 `email=3` | state 记 `crash_email_sent`（跨重启保持），**每个事件实例只发 1 封**；`False`（被拦/未配置/提交失败）不落记忆，下轮可重试。S16 转绿：`email=1`，TG 仍按 3 轮 `SILENCED` |
| **D2b** | **P1** | 日报重试会**重发已成功的渠道** —— TG 坏、邮件好时，邮件最多重复 6 封（25 分钟内） | 用例 `test_confirmed_channel_not_resent_on_retry` 红 | `_send_daily_report(channels=...)` + 按日 `_daily_report_done` 记忆：**只补未确认渠道** |
| **D1** | **P1** | 重试计数不按日归属：前一日耗尽后，**次日首次失败即被判「已耗尽」**，只剩 1 次机会 | 见 12.2（测试首版本身无效，修正后覆盖） | 新增 `_daily_report_retry_date`，跨日重置额度与渠道确认 |
| **D4** | P2 | 巡检 `--selftest` 用 `generic` 事件 → 空仓时被闸门跳过，**自检结论会说「邮件通道不可用」**，与真实告警策略不一致 | 代码审查 | 自检改用 `event="health"`（与真实告警同策略） |

### 12.2 复审中的第二个发现：我自己写的测试是无效测试

三条新用例首版把「前一日残留 `count=6`」等状态**预设到 fake 上再驱动无限循环**——但 `_daily_report_loop` 启动时会重置这些字段（线程生命周期只发生一次），**预设被静默抹掉，用例恒定通过**（已实测其中一条恒定绿）。同名方法还被重复定义、后者覆盖前者。

修正做法：
1. 把日报状态机从无限循环抽出为 `_try_daily_report_once(today) -> 'idle'|'done'|'retry'|'exhausted'`，直接确定性验收；
2. 循环侧只保留**窗口守卫**用例（08:04 / 08:31 不得触发、08:05 必须触发且只委派）；
3. 删除重复定义与不再使用的测试辅助类。

> 教训：**测试必须能证伪**。涉及"只发生一次"的初始化逻辑时，预设状态 + 驱动无限循环是最容易写出恒定绿的模式。

### 12.3 复审后的测试证据

| 套件 | 结果 |
|---|---|
| `test_r0_notify_chains.py` + `test_email_gate.py` + `test_email_channels.py` | **32 passed** |
| `test_notify_queue.py`（含 S15/S16） | **16/16 场景** |
| `test_watchdog_guard.py` | **22/22 PASS** |
| pytest 回归批次（12 个文件） | **42 passed** |
| 脚本式回归（close_confirmation_v62 / v62_staged / b2_hardlock / position_close_confirmation） | **133/133、ALL PASS、PASS、7/7** |
| `py_compile`（7 个文件） | 通过 |

### 12.4 复审后仍存的语义与遗留（诚实声明）

- **TG 未配置或未就绪（返回 `None`）**：日报会累计 6 次尝试后标记 `exhausted` 并打印 ❌ 日志。取舍：**不重复投递已成功的渠道、不静默丢失**，代价是日志会记录一次"未确认"。不再视为「失败重试」之外的语义。
- **`fatal_alert` 仅由启动熔断写入**：普通崩溃仍依赖队列 + bot 存活；watchdog 自身被杀（非熔断）时只会触发 `stale_heartbeat`。
- **`crash_email_sent` 记忆依赖 state 文件**：state 被人工删除会解除记忆（有界重复 ≤1 轮），与 D-010「宁可漏发不重复」的既有取舍一致。
- R1（快照三态 / 取价显式化）、R2（交易所权威强平与保护单）、R3（恢复通知 / 资金费 / quiet-hours）**未动**。
