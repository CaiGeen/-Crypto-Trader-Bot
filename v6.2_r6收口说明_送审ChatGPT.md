# v6.2 r6 收口说明（回应第二轮交叉审核）— 送审 ChatGPT

> 配套材料：`v6.2_正式diff_r6_送审ChatGPT.md`（统一权威规格稿）
> 本文件只说明**这轮改了什么、怎么验证的、还缺什么**，不重复规格正文。

---

## 1. 三项 P1 的处置

### P1-1 已知 `PROGRAMMATIC_CANCELED` ENTRY 被重复验证（真实业务路径 blocker）

**我复现了你的场景**，确认会发生：

```text
E2 registry.state = PROGRAMMATIC_CANCELED
cancel E2 → Binance 返回 -2011/OrderNotFound
fetch_open_orders → []
fetch_order(E2) → OrderNotFound
→ _cancel_and_verify_entry_orders(...) == False + critical
```

**修法**（不放松 `OrderNotFound` 判据，那个判据是对的）：
- `_cancel_and_verify_entry_orders`：claimed registry 中 `PROGRAMMATIC_CANCELED`
  的 ENTRY **不进** cancel/fetch 队列
- `cancel_open_orders` 同步豁免 → 第二次 🗑️ 不再假失败
- **只豁免 `PROGRAMMATIC_CANCELED`**，不豁免 ABSENT / FAILED

**我认同并写进注释的方向辨析**：
> legacy 无 registry = **没有事实 → 不能猜**
> registry=`PROGRAMMATIC_CANCELED` = **已有确定事实 → 不该再假装不知道**
> 两者不矛盾。

### P1-2 builder 13→19 method 污染（landing blocker）

`ast.walk(整树)` 递归进方法体，把 6 个嵌套闭包提成 class method：
`_finite_pos_dv` / `_finite_zero_dv` / `_topology_ok` / `_finite_nonneg` /
`_finite_pos` / `_finite_zero`

**修法**：只遍历 `_Holder.body`，且 builder 自 assert：
```python
assert len(_method_srcs) == 13
assert set(_holder_names) == EXPECTED_HELPERS
```
实测（`verify_r6_surface.py`）：13/13 全在，污染 = 无。

### P1-3 MARKET/LIMIT 的 batch-not-found guard 被组装删掉（regression）

builder 原注释甚至写了「丢弃 target 校验」——这是我的明确疏漏。

**修法**：保留 `if not target_b_data:` 及其 return 行，位置在 `fetch_ticker` 之前。
AST 验证：MARKET / LIMIT 两侧 `guard=✅`、`guard 在 ticker 之前=✅`。

---

## 2. 第 4 项：D6g 过时注释已更新

原文（v6.1 语义，已过时）：
> gate 的 pending_ids = entry_orders[2:] = [] → 无单可撤、恒 True，且不得产生任何 cancel 调用

改为（v6.2 改动 8 实际语义）：
> gate 从 registry 恢复出被截断的 E3/E4/E5 → 撤销并逐 ID 验证终态
> （**不再是 pending_ids 恒空 / 零撤单**；v6.1 的「恒空」假设会让被截断但仍活着的 ENTRY 成为漏网之鱼）

注：只在 `test_close_confirmation_v62.py`（副本）中改，v6.1 批准基线未动。

---

## 3. 第 5 项：r6 统一权威稿

见 `v6.2_正式diff_r6_送审ChatGPT.md`。要点：
- R6-1~R6-9 变更清单（含 r5 **未明确写出**的 bid/ask、`_freeze_alerted` 初始化）
- 端点契约成对制约：TP→conditional / B-lite OID1→normal
- exact **13** helper 清单（r5 稿仍写「11 helper」，已过期）
- legacy preflight 规格：运行时 Fail-Closed + 部署条件 `LEGACY_UNRECOVERABLE == 0`
- 证据口径统一（见下）

**按你的要求，证据命名已改**：

| 名称 | 含义 | 结果 |
|---|---|---|
| canonical helper regression | v6.2 helper + 旧事故送审 doc + v6.0/v5/v4/v3 负向 | **133/133** |
| staged transaction integration | 直接跑 staged 的 MARKET/LIMIT/🗑️ 组装方法 | **19/19** |

❌ 不再使用「133@staged」—— canonical 套件不加载 staged trader。

---

## 4. 变异检查：**7/7 全部杀死**

| 变异体 | 内容 | 被杀用例 |
|---|---|---|
| M32a | B-lite cancel 强加 `stop=True` | t_limit_commit_fail_blite |
| M32b | B-lite verify 改 conditional | t_limit_commit_fail_blite |
| M34 | TP gate 放行非 TERMINAL_ZERO | t_limit_tp_gate_blocked |
| M33 | 首因保护失效 | t_market_first_abnormal_wins_midflight |
| **M35** | **删除 PROGRAMMATIC_CANCELED 豁免** | t_r6b_gate_skips_known_terminal |
| **M36** | **builder 退回 ast.walk（污染 13→19）** | t_r6d_staged_helper_surface_exact13 |
| **M37** | **删除 batch-not-found guard** | t_r6c_missing_batch_guard_no_exchange_call |

**写入 M36 时踩的坑（如实记录）**：只把 `for _n in _holder.body:` 改成
`ast.walk(...)` 而保留 exact-set assert，builder 会直接 AssertionError，
harness 判「变异体 builder 失败（不算杀死）」。必须**同时去掉 assert**，
让产物真的被污染成 19 methods，再由结构面测试 `t_r6d` 判 RED。

---

## 5. 新增的 4 个集成用例

| 用例 | 锁的是什么 |
|---|---|
| `t_r6a_second_cancel_no_false_failure` | 第一次 🗑️ → PROGRAMMATIC_CANCELED；第二次 🗑️ 不得再 cancel/fetch E2、不得 critical |
| `t_r6b_gate_skips_known_terminal` | E2=PROGRAMMATIC_CANCELED 且交易所配 OrderNotFound → close gate 仍 True，E2 cancel/fetch 次数 == 0 |
| `t_r6c_missing_batch_guard_no_exchange_call` | 批次不存在 → 纯本地拒绝；fetch_ticker / create_order / persist 全 0（MARKET + LIMIT） |
| `t_r6d_staged_helper_surface_exact13` | CryptoTrader 的 helper 必须 exact 13，嵌套闭包不得出现在 class surface |

---

## 6. 当前门禁

| 项 | 结果 |
|---|---|
| staged transaction integration | **19/19** |
| 变异检查 | **7/7 杀死** |
| canonical helper regression | **133/133** |
| canonical 133（v6.1 基线未动） | **133/133** |
| GREEN / RED | **28/28** / **23/23** |
| 43 scripts 回归 | 41 rc=0 + 2 个 v3 遗留 rc=1 |
| r6 规格与 staged 同步 | ✅ |
| legacy preflight | ✅ 规格已定，**实现待单独排期** |
| 五道门 + M11~M37 全量 | ⏳ **待你放行** |
| 生产三文件 | ❌ 继续冻结（零改动） |

**staged 终版**：9311 行，
SHA-256 `46b75f2768ab21b449cb718ab2f1ca2e0f0ea7900e111d8ee88f89b4d64c2c4f`

---

## 7. 已附上你索要的两份 harness 材料

- `test_v62_red_first.py`（最新；含 cancel endpoint routing，integration 从它 import
  `FakeExchange62 / FakeSelf62 / mk_batch`）
- `check_doc_helper_parity.py`（用于独立签 M1/M2；之后改 r6 的 13-helper parity）

另附 `verify_r6_surface.py`（三项 P1 的 AST 结构验证）、`mutate_v62_integration.py`、
`build_v62_full.py`，供你从源码侧交叉审核。
