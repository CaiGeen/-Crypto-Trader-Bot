# v6.2 设计稿 v2.4-final（attribution 收敛增量 · 设计层收口版）— 未动生产

> 本稿是 **v2.3 的 attribution delta**（ChatGPT：ΔB/ΔC/ΔD/退出判据全部批准；
> **否决「`is_programmatic_cancel` = 永久归因」**——sticky batch bool 会永久关闭
> SL/TP 自动补挂）。按铁律逐条独立源码核实：两轮共 **6 项声明全实锤，零证伪**（§零）。
> **v2.4-final 收口**：ChatGPT 复审指出最后 1 个 P0 实现错误 + 1 个收紧点，
> 本稿已就地吸收（§ΔE1-1 归因集合钉死 / §ΔE1-3 按 `terminal_ok` 分别写 /
> §ΔE2 判据只看 state / §ΔE5 +R1-q）。
> ChatGPT 裁定：吸收后**设计层正式收口，不再需要 v2.5 架构审查**，
> 可直接进入正式 v6.2 送审稿 diff + RED→GREEN 阶段。
> 文档纪律：`python` 块完整可解析（零 Ellipsis）；生产摘录用 `python-frag`。

---

## §零 本轮核实结果

| # | ChatGPT 声明 | 独立核实证据 | 结论 |
|---|---|---|---|
| 1 | **新 P0**：sticky `is_programmatic_cancel=True` 让 SL/TP 消费者误判「程序撤单」→ 关闭自动补挂（`need_recover_sl` 不置位）→ 持仓裸奔。**攻击链**：点一次 🗑️ → sticky True（棘轮清不掉）→ 之后任何时候 SL 被外部撤销 → 不补挂 | **SL 段 L5324-5337**：`is_programmatic = latest_b_data_check.get('is_programmatic_cancel', False)` → True 则 `current_sl_id=None`、**不设 `need_recover_sl`**；仅 False 且非 `user_modified` → `need_recover_sl=True` 自动补挂。**TP 段 L5465-5478 完全对称**（`need_recover_tp`）。棘轮机制（L41 + L1627-1629）前轮已实锤 | ✅ 成立（资金安全级，不只是误归因） |
| 2 | 归因应改为 **order-ID scoped**：`protection_registry` + `PROGRAMMATIC_CANCELED`（订单级终态、不可转出）+ `terminated_reason` 已是现有机制 | **L3608-3624** `_find_registry_identity_by_order_id(symbol, batch_id, order_id)` 已存在，docstring「按 order_id 反查最稳」；**L3659-3675** `_update_registry(..., state=..., terminated_reason=...)`，L3669-3671 明载「PROGRAMMATIC_CANCELED 是订单生命周期终态，**不可转出**」；L48 `_REGISTRY_TERMINAL_STATES`、L45-47 保护态佐证；且该写入**走锁内 `load→modify→_persist_states` 直写、绕开 merge 棘轮**（L3672-3675） | ✅ 成立，就地复用可行 |
| 3 | v2.3 §A-3「持续重试直到 clear 成功」对 programmatic zero-filled unresolved 不准确；sticky True 会让 monitor 不置 `manual_canceled_detected` → 不会自动转 terminal | **L4749-4763**：ENTRY 被检测 canceled 时，若 sticky `is_programmatic` → 打印「程序撤单」+ **不设置 `manual_canceled_detected`**；否则才置 True 并 TG 提醒。→ 最后一张 unresolved 后来自己变 canceled 时：`pending_close` 仍 False、`manual_canceled_detected` 亦 False → monitor 继续运行，不会自动终止 | ✅ 成立，表述须修正 |

### 复审第二轮（v2.4 复审，3 项全实锤）

| # | ChatGPT 声明 | 独立核实证据 | 结论 |
|---|---|---|---|
| 4 | **P0 实现错误**：E1-3 无条件把 TP/SL 都写成 `PROGRAMMATIC_CANCELED`，未绑定各自 cancel 结果 → SL cancel 网络失败时「交易所仍 live、registry 却说已终结」，违反 order-ID scoped 原则，且 `PROGRAMMATIC_CANCELED` 是不可逆禁建终态 | 市价 AFTER 撤保护单结构（送审稿 L606-620）确为 `if TP: try cancel except: pass` + `if SL: try cancel except: pass`——**与结果无绑定**；正确范式生产已有：限价撤 TP（L7608-7615）`if _tp_terminal_ok and _tp_old_id: _update_registry(... state='PROGRAMMATIC_CANCELED' ...)` | ✅ 成立（我的 v2.4 原稿错误） |
| 5 | E2 判据不应把 `terminated_reason` 字符串当授权证据，只认 `state == 'PROGRAMMATIC_CANCELED'`（授权看 state，解释看 reason） | **L3763-3780** `_assert_create_allowed` docstring：仲裁只看 registry state 集合（禁 `PENDING_CREATE/PENDING_VERIFY/NOT_CONFIRMED/CONFIRMED/MISMATCH`，允许 `FAILED/ABSENT/无条目`），**从不解析 terminated_reason**——项目既有规范即 state-only | ✅ 成立 |
| 6 | E1-1/E1-2 的 `cancelled_ids` 必须是 **verifier=gone 的集合**，不能是「cancel 调用成功返回过的集合」 | v2.3 §ΔB 循环里 cancel 成功先 append `cancelled_ids`，随后 verifier 若非 gone 会再进 `cancel_failed_ids` 并 break → **同一 ID 可能同时存在于两个集合**；v2.4 原稿 `for order_id in cancelled_ids` 会把未确认 gone 的 ID 也写成终态，即违反归因原则 | ✅ 成立（我的 v2.4 原稿错误） |

**最终职责模型（本稿落定）**：

```text
close_phase          = close transaction 状态
pending_close        = batch 生命周期终止资格（唯一）
programmatic 归因    = exact order identity / registry 事实（order-ID scoped）
is_programmatic_cancel = legacy / close transaction 辅助标志
                         不得作为任意未来订单事件的权威归因
```

---

## §ΔE1 ENTRY-only 撤单不再写 sticky `is_programmatic_cancel`

### E1-1 🗑️ `cancel_open_orders`（producer #1）

删除 L6872 的 sticky 写入与 L6891 的对称 pop；改为**按单归因**：每张程序撤且 verifier=gone 的 ENTRY，写 registry `PROGRAMMATIC_CANCELED`。

```python
        # 🔒 v2.4（ΔE1）：不再写 batch-global sticky
        # is_programmatic_cancel —— 它是 False→True 棘轮（L1627-1629），
        # 一旦为 True 会永久关闭 SL/TP 自动补挂（L5328/L5469）。
        # ENTRY 撤单归因改为 order-ID scoped：
        #   program cancel + verifier=gone
        #   → registry identity = PROGRAMMATIC_CANCELED + terminated_reason
        #   （_update_registry 走锁内直写，不受 merge 棘轮影响）
        cancelled_ids = []
        cancelled_layers = []
        cancel_failed_ids = []
        already_terminal_ids = []
        # 🔒 v2.4-final（复审#6 钉死）：归因写入只认这个集合——
        # 元素 = 「cancel 尝试过 + verifier 判定 gone」的具体 ID。
        # 绝不能用 cancelled_ids（= cancel 调用成功返回过的集合）：
        # v2.3 §ΔB 下最后一张可能 cancel 成功但 verifier 非 gone，
        # 它同时也在 cancel_failed_ids 里 → 不得写终态。
        programmatic_gone_ids = []
```

（撤单循环：verifier 返回 `gone` 时同时 append `programmatic_gone_ids`；非 gone 一律不进。）

```python
        for order_id in programmatic_gone_ids:
            _ident = self._find_registry_identity_by_order_id(target_symbol, batch_id, order_id)
            if _ident:
                self._update_registry(target_symbol, batch_id, _ident,
                                      state='PROGRAMMATIC_CANCELED',
                                      order_id=order_id, id_known=True,
                                      terminated_reason='cancel_open_orders')
            else:
                print(f"  └─ ⚠️ 撤单归因：{order_id} 在 registry 中无 identity"
                      f"（不影响撤单事实，但归因降级为 manual 语义）")
```

（`_find_registry_identity_by_order_id` 返回 None 时**不阻断**——撤单事实已完成；归因缺失只在后续被检测为 canceled 时按 manual 语义提醒，Fail-Noisy 方向。）

### E1-2 monitor 手动撤单路径（producer #2，L4843-4846）

```python
                        latest_all = self.load_all_states()
                        latest_b_data = latest_all.get(symbol, {}).get(batch_id, {})
                        if latest_b_data:
                            # 🔒 v2.4（ΔE1）：同样不写 sticky bool；entry_orders
                            # 原样保留（v2.1 §1.3），归因按单落 registry。
                            # 保留 is_programmatic_cancel 的读取兼容由 ΔE2 承担。
                            self.save_batch_state(symbol, batch_id, latest_b_data)
                        for _oid in programmatic_gone_this_round:
                            _ident = self._find_registry_identity_by_order_id(symbol, batch_id, _oid)
                            if _ident:
                                self._update_registry(symbol, batch_id, _ident,
                                                      state='PROGRAMMATIC_CANCELED',
                                                      order_id=_oid, id_known=True,
                                                      terminated_reason='monitor_cancel_remaining_entries')
```

### E1-3 close 事务撤保护单：**按各自 cancel 结果**分别写归因（v2.4-final P0 修正）

**规格**（ChatGPT 裁定，对齐限价既有范式 L7608-7615）：

```text
cancel_order 正常成功返回 → 该 ID 可写 PROGRAMMATIC_CANCELED
异常 / -2011（不确定失败） → 本轮不写，交 converge / 后续事实判断
TP 与 SL 各有独立的 terminal_ok，失败的那张绝不写
```

```python
            _tp_terminal_ok = False
            if _txn_vars.get('tp_order_id'):
                try:
                    self._safe_api_call(self.exchange.cancel_order, _txn_vars['tp_order_id'],
                                        target_symbol, params={'stop': True})
                    _tp_terminal_ok = True
                    print(f"  └─ 已撤销止盈单: {_txn_vars['tp_order_id']}")
                except Exception:
                    pass

            _sl_terminal_ok = False
            if _txn_vars.get('current_sl_id'):
                try:
                    self._safe_api_call(self.exchange.cancel_order, _txn_vars['current_sl_id'],
                                        target_symbol, params={'stop': True})
                    _sl_terminal_ok = True
                    print(f"  └─ 已撤销止损单: {_txn_vars['current_sl_id']}")
                except Exception:
                    pass

            # 🔒 v2.4-final（P0 修正 / order-ID scoped）：只有各自
            # cancel 正常返回的那一张才写 PROGRAMMATIC_CANCELED。
            # 异常（含 -2011/OrderNotFound）= 不知道它为何消失 → 不记
            # 「程序已终结」，交 converge/后续事实判断（同 ENTRY verifier 哲学）。
            for _oid, _ok in ((_txn_vars.get('tp_order_id'), _tp_terminal_ok),
                              (_txn_vars.get('current_sl_id'), _sl_terminal_ok)):
                if not _oid or not _ok:
                    continue
                _ident = self._find_registry_identity_by_order_id(target_symbol, batch_id, _oid)
                if _ident:
                    self._update_registry(target_symbol, batch_id, _ident,
                                          state='PROGRAMMATIC_CANCELED',
                                          order_id=_oid, id_known=True,
                                          terminated_reason='close_requested_canceled')
```

（限价路径已有同款 `_tp_terminal_ok` 范式，保持不变；市价撤 SL 的归因由此补齐。）

（helper 计数不变——`_find_registry_identity_by_order_id` / `_update_registry` 均为生产既有方法。）

---

## §ΔE2 SL/TP/ENTRY 消费者改用 order-scoped 归因（不再读 sticky bool）

### E2-1 SL 段（替换 L5324-5337）

```python
                            # 🔒 v2.4（ΔE2）：归因改为 order-ID scoped。
                            # batch-global sticky bool 会因一次 🗑️ 永久为 True，
                            # 导致 SL 被外部撤销时永不补挂（持仓裸奔）。
                            _sl_ident = self._find_registry_identity_by_order_id(
                                symbol, batch_id, current_sl_id)
                            # 🔒 v2.4-final（收紧：授权看 state，解释看 reason）——
                            # 与项目既有规范一致：_assert_create_allowed（L3763-3780）
                            # 只看 registry state 集合，从不解析 terminated_reason。
                            # 字符串前缀匹配会把未来的 program_cancel_failed /
                            # program_reconcile_unknown 等审计 reason 误当授权。
                            _sl_reg = ((latest_b_data_check.get('protection_registry') or {})
                                       .get(_sl_ident) if _sl_ident else None)
                            _sl_prog = (isinstance(_sl_reg, dict)
                                        and _sl_reg.get('state') == 'PROGRAMMATIC_CANCELED')
                            if _sl_prog:
                                print(f"ℹ️ [程序撤单] 批次 {batch_id} 止损单已由程序终结 (ID: {current_sl_id})")
                                current_sl_id = None
                            elif user_modified:
                                print(f"ℹ️ [用户主动修改] 批次 {batch_id} 止损单已被用户撤销，不再自动补挂")
                                current_sl_id = None
                            else:
                                print(f"⚠️ ⚠️ [风控异常] 止损单已在外部撤销，准备按策略自动补挂...")
                                current_sl_id = None
                                need_recover_sl = True
```

### E2-2 TP 段（替换 L5465-5478，同构，`_tp_prog` → `need_recover_tp`）

```python
                                _tp_ident = self._find_registry_identity_by_order_id(
                                    symbol, batch_id, tp_order_id)
                                _tp_reg = ((latest_b_data_check.get('protection_registry') or {})
                                           .get(_tp_ident) if _tp_ident else None)
                                _tp_prog = (isinstance(_tp_reg, dict)
                                            and _tp_reg.get('state') == 'PROGRAMMATIC_CANCELED')
                                if _tp_prog:
                                    print(f"ℹ️ [程序撤单] 批次 {batch_id} 止盈单已由程序终结 (ID: {tp_order_id})")
                                    tp_order_id = None
                                elif user_modified:
                                    print(f"ℹ️ [用户主动修改] 批次 {batch_id} 止盈单已被用户撤销，不再自动补挂")
                                    tp_order_id = None
                                else:
                                    print(f"⚠️ ⚠️ [风控异常] 止盈单已在外部撤销，准备按策略自动补挂...")
                                    tp_order_id = None
                                    need_recover_tp = True
```

### E2-3 ENTRY 段（替换 L4749-4763）

```python
                                _en_ident = self._find_registry_identity_by_order_id(
                                    symbol, batch_id, order_id)
                                _en_reg = ((latest_b_data_check.get('protection_registry') or {})
                                           .get(_en_ident) if _en_ident else None)
                                _en_prog = (isinstance(_en_reg, dict)
                                            and _en_reg.get('state') == 'PROGRAMMATIC_CANCELED')
                                if _en_prog:
                                    # 这张具体 ENTRY 由程序终结 → 不发手动撤单提醒
                                    print(f"ℹ️ [程序撤单] 第 {idx + 1} 层开仓条件单已由程序终结 (ID: {order_id})")
                                    # 不设置 manual_canceled_detected
                                else:
                                    manual_canceled_detected = True
                                    print(f"⚠️ 🛑 [手动撤单提醒] 第 {idx + 1} 层开仓条件单被撤销 (ID: {order_id})")
                                    self.send_tg_notification(
                                        f"⚠️ 🛑 **[撤单提醒]** 批次 `{batch_id}` 第 {idx + 1} 层条件单已被手动撤销/失效。"
                                    )
```

- **归属缺失（registry 无条目）→ 按 manual 语义提醒**：Fail-Noisy 方向，宁可误报提醒也不静默吞掉；
- sticky `is_programmatic_cancel` **本轮不从 `_MERGE_RATCHET_BOOL_FIELDS` 删除**（ChatGPT：避免扩大安全模型），但此后只作为 legacy/close 辅助标志（L4890 结算跳过、L6324 清理仍在读），**不再参与任何订单事件归因**。

---

## §ΔE3 `limit_creating` transient grace 时点前移（ΔD 精确化）

BEGIN 内：**先 seed 进程内 grace → 再 persist `limit_creating` → persist 失败则回滚 seed**，消除「磁盘已可见、grace 未生效」的极小 race：

```python
        # 🔒 v2.4（ΔE3）：grace 必须**先于**磁盘可见性建立，否则 monitor 可能
        # 在未 seed 的窗口内看到 limit_creating 并立即 false critical。
        _prev_freeze_alerted = self._freeze_alerted.get(batch_id)
        if reason == 'limit_creating':
            self._freeze_alerted[batch_id] = time.time()
        _ok = self._persist_states(all_states)
        if not _ok:
            # persist 失败 → 磁盘未变，grace 一并回滚，不留幽灵窗口
            if reason == 'limit_creating':
                if _prev_freeze_alerted is None:
                    self._freeze_alerted.pop(batch_id, None)
                else:
                    self._freeze_alerted[batch_id] = _prev_freeze_alerted
            return False, _op_id, 'persist_failed', None
```

（键名以生产 `_freeze_alerted` 实际键为准——送审稿改动 4 现用 `batch_id`。crash 语义不变：内存 dict 随进程消失 → 重启后 `limit_creating` 立即 loud。）

---

## §ΔE4 operational 表述修正（替换 v2.3 §A-3 第 1 条）

v2.3 的「monitor 手动撤单路径改为持续重试直到 clear 成功」**对 programmatic zero-filled unresolved 不成立**（§零#3）。准确表述：

> **zero-filled unresolved 之后：批次保持 ACTIVE + `pending_close=False` + `close_phase=0`，ID 完整保留，monitor 继续管辖；当最后一张 unresolved 后来自行变 canceled 时，batch 不会自动转 terminal（归因仍是 program 语义，不置 `manual_canceled_detected`），需用户再点一次 🗑️（重新逐 ID 验证全部 gone → 置 `pending_close`）或人工 reconcile 完成终止。本轮不新增自动 terminal 状态机。**

这在资金上是 Fail-Closed（批次与 ID 都在、监控在线、风险可见），只是终止动作需要一次用户交互。

---

## §ΔE5 测试增量（2 个关键反例，接 v2.3 §ΔG）

| 编号 | 场景 | 必须证明 |
|---|---|---|
| R1-o | 批次有持仓 + SL；曾执行 ENTRY-only cancel（🗑️）；随后 SL 在**外部**被 cancel（registry 中该 SL 无 `PROGRAMMATIC_CANCELED` 记录） | **`need_recover_sl=True`**——不得因为批次历史上有 ENTRY 程序撤单就把 SL 当 programmatic cancel（直接锁死本轮 P0）；sticky bool 版本此用例必须 RED |
| R1-p | E5 由程序 cancel + verifier=gone（registry 记 `PROGRAMMATIC_CANCELED`），E4 由用户手工 cancel | **E5 → programmatic（不发手动撤单提醒）；E4 → manual（发提醒）**——归因必须 order scoped，不能全批次一个语义 |
| **R1-q** | 市价平仓：TP cancel 成功、SL cancel 网络失败 | **TP registry == `PROGRAMMATIC_CANCELED`；SL registry != `PROGRAMMATIC_CANCELED`**——直接锁死 E1-3 这个最后的 P0（原无条件写法此用例必 RED） |
| R1-r | ENTRY 撤单：cancel 调用成功但 verifier 返回 `filled`/`open`/`unknown` | 该 ID 的 registry **不得**出现 `PROGRAMMATIC_CANCELED`（归因集合必须是 verifier=gone 子集，锁死复审#6） |

（v2.3 的 R1-l/m/n、R3-c 与 v2.2 的 R1-i/j/k、R2-f/g、R3-b 全部保留；R1-l 的 GREEN 断言不变——退出判据仍只看 `pending_close`。）

---

## §Gate 状态（设计层收口）

- **已批准且不变**：ΔB 每层 verifier、ΔC shape+hole 硬门、ΔD `limit_creating` 迁移（+ΔE3 时点精确化）、ΔA 退出只看 `pending_close`、generic pre-create rollback CAS、commit source-state guard、first-abnormal-wins、registry hardening、helper 13；
- **本稿闭环**：归因从 batch-global sticky bool 迁到 **order-ID scoped registry 事实**（producer #1/#2 按 verifier=gone 集合写入；close 撤保护单按各自 `terminal_ok` 写入；SL/TP/ENTRY 三个消费者只认 `state == PROGRAMMATIC_CANCELED`）；
- **v2.4-final 两处小修已吸收**：①E1-3 改为 TP/SL 独立 `terminal_ok`，失败的那张不写不可逆终态；②E2 判据删除 `terminated_reason` 前缀匹配，只认 state（授权看 state，解释看 reason）。两处均已独立源码核实（§零 #4/#5/#6）。
- helper 计数仍 **13**（复用生产既有 `_find_registry_identity_by_order_id` / `_update_registry`）；生产零改动（HEAD=e953d79）。

## §下一步（ChatGPT 裁定的正式流程）

1. 出正式 v6.2 送审稿 diff（AFTER 块全量 + 落点表 + checker 名单 13 helpers + `cancel_open_orders`/monitor 段/`_begin_close_request_if_active` 入 `PROD_SIGNATURE_OVERRIDES`）；
2. RED-first → GREEN（v2 §六 + v2.1 §Δ9 + v2.2 §ΔF + v2.3 §ΔG + 本稿 §ΔE5 共 R1-a~r / R2-* / R3-*）；
3. 旧 133 场景回归 → 五道 checker → mutation；
4. 交 ChatGPT 做「精确 diff + 测试终审」→ 批准后按五段式落生产。

> 正式 diff 会在 ChatGPT 终审的 4 个重点上逐条给出：
> ①市价 TP/SL 各自 `terminal_ok`；②ENTRY `cancelled_ids` 只含 verifier=gone；
> ③SL/TP/ENTRY 消费者只认 exact order ID 的 `state=PROGRAMMATIC_CANCELED`；④R1-q。
