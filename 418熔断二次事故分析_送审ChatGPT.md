# 418 熔断二次事故分析报告（送审 ChatGPT 交叉评审）

- 日期：2026-08-19 23:45
- 作者：WorkBuddy（本地排查）
- 性质：只读排查报告，未修改任何代码
- 背景：与 2026-08-19 凌晨的首次 418 事故（已闭环，见《418封禁事件总结_送审ChatGPT.md》）同日发生的**第二次 418 事件**

---

## 一、事件概述

用户于晚间收到 API 熔断告警邮件（已确认为 **418 IP 封禁**级别），回到电脑时发现 PyCharm 终端持续打印熔断倒计时（大量"等待 X 秒"），PyCharm CPU 100% 卡死，终端输出丢失。期间交易所侧 4 层挂单陆续成交，但程序因熔断无法感知处理。

## 二、时间线（基于 watchdog.log / 进程快照 / trade_state mtime / 用户终端日志交叉复原）

| 时间 | 事件 | 证据 |
|---|---|---|
| 17:49:45 | 用户在 PyCharm 以 watchdog 配置重启系统：watchdog(PID 23580) + bot_runner(PID 25540)。**这是 C5 代码（commit f20ae62）首次生产运行** | watchdog.log L 最后段；进程 StartTime |
| 17:49 之后某时刻 | **418 触发全局熔断**，R1 critical 告警 → TG + 邮件（用户收到的邮件）。熔断期间监控线程的 fetch_open_orders 调用全部阻塞在 `_wait_for_api_cooldown`，**无法感知挂单成交** | 用户邮件确认 418；trade_state.json mtime 停留在 10:56（17:49 实例从未写入状态） |
| 17:49-22:18 | 熔断等待期：监控线程每 5 秒打印一条 `🚫 [API熔断] 等待 X 秒...`（`_wait_for_api_cooldown` L537-538），经 watchdog 的 stdout PIPE 转发进 PyCharm 终端，**持续累积数千行** | trader_260725.py L528-538；watchdog.py L130-150（PIPE 转发） |
| 交易所侧（熔断期间） | BTCUSDT 4 层买单成交：65483.7 / 65590.6 / 65697.6 / 65804.9（22:53 重启后程序逐层发现） | 用户 22:53 终端日志 |
| ~22:18 | 用户回电脑，发现 PyCharm 卡死，重启 PyCharm（22:18:48）。**旧 watchdog+bot_runner 成为孤儿进程继续运行** | PyCharm 进程 StartTime |
| 22:22 | WorkBuddy 现场快照：bot_runner(25540) 存活，**4.5 小时 CPU 时间仅 23 秒**（无 busy loop），6 线程 111MB | Get-Process 快照 |
| 22:22-22:53 之间 | 孤儿 bot_runner(25540) 死亡（原因未记录，推测用户清理或手动杀），孤儿 watchdog(23580) 检测后**自动重启新 bot（PID 25184，持有互斥体）** | 锁文件残留内容 25184；当前进程表无 python 进程 |
| 22:53:23 | 用户新 PyCharm 会话重跑 watchdog → 新 bot_runner(15320) 启动成功（此时 25184 已死）→ 恢复流程发现 4 层成交 → **触发 C5 Verify 假阴性重复挂单风暴（另一份报告）** | 用户提供的终端日志 |
| 22:57:20 | 用户手动停止 watchdog，进程树清理 | 终端日志 |

## 三、418 根因分析

### 3.1 程序侧请求速率核查（排除内部风暴）

- 单实例运行（互斥体锁有效，锁文件 PID 与进程一致）
- 活跃批次 1 个，监控轮询基准间隔 **60 秒**（`_calculate_monitoring_interval` L1458-1470，P1-2 已上调）
- `_safe_api_call` 限速：Semaphore(1) + `_min_api_interval` + 429 触发 30-60 秒全局熔断
- **结论：bot 自身请求速率不足以触发 418**

### 3.2 最可能根因：共享 IP 权重池耗尽（与首次 418 事故同款诱因）

首次 418 事故诊断结论：**同 WiFi 手机 Binance App + bot 请求共享同一公网 IP 的权重池**，叠加频繁重启导致 IP 级封禁。本次为晚间时段（用户手机看盘活跃期），单实例 bot + 手机 App 的组合仍可能击穿 IP 权重。

**待用户补充确认**：418 告警邮件的时间戳、当日手机 App 使用情况、当前 .last_ip.txt 记录的 IP 是否变化。

### 3.3 已知缺口（首次事故已识别、尚未实施）

- **P1-API-01 ban-until 跨进程持久化**：熔断状态（api_cooldown_until）仅存于进程内存。重启进程后熔断记忆丢失，新进程立即发起请求可能撞在剩余封禁期上（本次 22:53 重启即碰上"熔断刚解除"的窗口，所幸恢复健康检查通过）。
- **P1-API-02/API-03**（调用计数器、启动请求削减）均未实施。

## 四、熔断期间程序行为链与"堆积感"定性

用户感知"程序一直在堆积错误"，实际堆积的是**终端输出而非程序缺陷**：

1. `🚫 [API熔断] 等待 X 秒...` 每 5 秒 × 监控线程数（`_wait_for_api_cooldown` L537）
2. watchdog 以 `subprocess.PIPE` 捕获 bot 全部 stdout 并逐行转发（watchdog.py L130-150）→ PyCharm 终端
3. 418 封禁典型时长 2 小时以上 → 单线程即 ~1440 条，加异常 traceback（L674 `traceback.print_exc()`）累积数千行
4. PyCharm 终端缓冲膨胀 → 渲染卡死 → 用户看到"CPU 100%"（**实测 bot_runner 4.5h 仅 23 秒 CPU，烧 CPU 的是 PyCharm 渲染**）

**风险定性**：该打印频率本身无功能危害，但存在两个真实问题：
- **可观测性丢失**：数千行倒计时淹没真正的关键日志，且 PyCharm 卡死后全部丢失（本次取证依赖用户手动复制终端）
- **REPL 假象**：用户误以为程序在空转堆积，增加误操作（重启）风险——而频繁重启恰恰是 418 的诱因之一，形成恶性循环

## 五、通知/打印频率审查（用户问题 3 专项）

| 通知/打印源 | 频率 | 熔断期间行为 | 问题 |
|---|---|---|---|
| 熔断倒计时（L537） | 5 秒/线程 | 持续 | **无节流**，长时间封禁下千条级堆积 |
| R1 熔断进入告警 | 每熔断周期 1 次 critical（TG+邮件） | 正常 | 已节流 ✅ |
| R1 熔断解除通知 | 每周期 1 次 | 正常 | 已节流 ✅ |
| SL/TP 挂单失败 critical（邮件） | **每次失败都发** | 熔断期间不触发 | **无节流**——见另一报告"邮件风暴"（22:53-22:57 四分钟 8 封） |
| TG Markdown 解析失败提示 | 每次 TG 发送失败 | - | 消息格式 bug，见另一报告 |
| watchdog 日志 | 事件驱动 | 正常 | 落盘 ✅（唯一落盘日志） |

## 六、单实例/Ctrl+F5 防护核查结论（用户问题 4）

三层防护全部有效，**Ctrl+F5 本身不会造成双实例**：
1. bot_runner.py L2049 互斥体锁（CreateMutexW "Global\\my_crypto_bot_single_instance"）——第二实例 exit 42
2. trader_260725.py L4057-4059 直接运行拒绝
3. watchdog.py L225-229 识别退出码 42 → 停止并 sys.exit(0)，不进重启循环

**但存在运维盲区（本次实际发生）**：关闭/重启 PyCharm **不会杀死** PyCharm 启动的 run 进程（watchdog/bot_runner 成为孤儿继续运行）；孤儿 watchdog 在其 bot 死亡后仍会**自动重启新 bot 并持有互斥体**——用户随后重跑看到"已有相同实例在运行"的提示，来源正是这个孤儿 watchdog 派生的 bot（锁文件残留 PID 25184）。**当前进程表已无 python 进程，互斥体已释放，现在重跑可正常启动。**

正确停止顺序：**先停 watchdog（它会清理 bot 进程树），再关 PyCharm**。仅杀 bot 会被 watchdog 重启；仅关 PyCharm 全部存活。

## 七、待 ChatGPT 评审的问题

1. 熔断倒计时打印是否应节流（建议 60 秒一条 + 状态变化时立即打印）？watchdog PIPE 转发是否应同步落盘文件（解决取证难）？
2. P1-API-01（ban-until 持久化）是否应升级为立即实施项（本次事故再次暴露）？
3. "关闭 PyCharm 不杀孤儿"的运维盲区是否需要程序侧兜底（如 watchdog 心跳文件 + 启动时检测孤儿 watchdog 并拒绝启动/接管提示）？
4. 418 共享 IP 假说的验证手段（Binance 权重响应头监控？IP 检查频率调整？）

---

## 附录：ChatGPT 交叉评审修正（2026-08-20 00:40 增补）

ChatGPT 4/5 星评审通过，两处表述修正如下：

### 修正 1：第三节"最可能根因：共享 IP 权重池耗尽" → **降级为"高概率假设，待证实"**

原表述"最可能根因"过强。当前仅证实：418 发生（用户邮件）、程序侧请求速率低（排除内部风暴）。共享 IP 假说与首次事故经验一致但**缺乏闭环证据链**（418 时刻的公网 IP、该 IP 权重占用、手机 App 流量共享证明）。完整证实需要：418 告警时间戳 + 当时段请求记录 + IP 变化史交叉比对。**在证据闭环前，本报告结论应为"程序侧已排除，外部诱因高度怀疑共享 IP，待证"。**

### 修正 2：第四节"烧 CPU 的是 PyCharm" → **"强烈指示，非唯一原因闭环"**

已有证据：bot_runner 4.5h CPU 时间 23 秒（进程快照）+ watchdog PIPE 转发链路确定 + 倒计时打印频率确定。该证据链足以排除"python busy loop"，**但"PyCharm 终端渲染是 100% CPU 的唯一/主要构成"未直接实测**（事发时无 PyCharm CPU 采样）。采纳 ChatGPT 建议：若再发生，立即采样四项指标——watchdog CPU / bot CPU / PyCharm CPU / stdout lines/sec——彻底闭环。

### 采纳的连带修正

- watchdog 孤儿问题（第六节）确认为 P1 运维安全项，非本次 24 孤儿单事故的第一根因（主因链见另一报告附录 B.2 验证 3）
- 修复优先级已并入另一报告附录 B.3 统一排序（本报告第五、六节的建议项分别对应 P1-B/P1-C 与运维项）
