# 送审稿：通知体系 R0 加固与五轮复审（2026-09-25）

> 供独立复审使用。本稿自包含：除注明外，结论均可用下方「可复现验证」自行核对。
> 完整审查报告（含全部取证、矩阵、逐轮复审细节）见仓库内
> `交叉审查_D_通知体系复审_4H交易员视角_送审.md`（§0–§15，约 880 行）。

---

## 1. 一句话结论

针对「无持仓却收到邮件」这一起点，已完成：根因取证 → 事件维度分级（R0）→ **五轮对抗性复审**，
累计修复 **12 处缺陷**（§4 表格 10 项 + 脚注 2 项 P2，每项均有 RED→GREEN 证据；
其中 3 处为阻断级：F11 崩溃邮件永不送达、D8 SMTP 未确认即标记已送达、D9 事件循环被同步等待饿死）。
**但系统尚未上线**：代码需重启生效，且仍存在 **7 项**已声明的残留不确定性（见 §6），不宣称「告警链已闭环」。

---

## 2. 提交清单（分支 `frozen-archive`，按时间顺序）

| 提交 | 内容 |
|---|---|
| `f1c8979` | 提审前快照：邮件闸门在途实现 + 首版审查报告 |
| `be1d44a` | 并入首轮 ChatGPT 复审回应（F10/F11/F12，修订实施顺序） |
| `c071643` | **R0**：邮件闸门事件维度、崩溃邮件独立于 TG 结果、日报双渠道确认 |
| `c3f860a` | 复审第 1 轮：崩溃邮件按事件实例只发 1 封、日报只补未确认渠道、跨日重试额度重置 |
| `f5349e8` | 复审第 2 轮：策略关闭≠投递失败、日报状态持久化、`event` 拼写可读 |
| `a1a884e` | 复审第 3 轮：崩溃邮件只认 SMTP 确认（阻断项）、修正被漏收的测试 |
| `dd83faf` | 复审第 4 轮：等待 SMTP 移出事件循环（新阻断项）、三处声明收窄 |

---

## 3. 决策记录（B5，用户已拍板）

| 项 | 决策 | 落地方式 |
|---|---|---|
| 空仓是否收日报邮件 | **选项一：关闭日报邮件，保留 Telegram 日报** | 实盘 `.env` 新增 `DAILY_REPORT_EMAIL_ENABLED=false`（该文件不入库） |
| 效果 | 日报只走 TG（每天 08:05 固定一行留痕）；邮件通道让给致命告警 | `trader_260725._daily_report_required_channels()` 返回 `()`，状态机据此只等 TG |
| 生效条件 | **必须重启** `watchdog` + `bot_runner`（`.env` 仅在进程启动时由 `load_dotenv()` 读取一次） | — |

配置改动已在本机验证（加载 `.env` 后）：

```
DAILY_REPORT_EMAIL_ENABLED -> False
EMAIL_ALERT_ENABLED         -> True
ONLY_WITH_POSITION          -> True
日报必需渠道                -> ()        # 预期：只等 TG
```

> **→ 2026-09-25 16:35 已重启生效**（新进程加载该配置），运行期验收证据见 §9.2。

---

## 4. 缺陷清单（表格 10 项 + 脚注 2 项 P2 = **合计 12 项**，全部有 RED→GREEN 证据）

| # | 级别 | 缺陷 | 关键证据 |
|---|---|---|---|
| F10 | P0 | 空仓期巡检邮件被持仓闸门吞掉（健康巡检每次重读磁盘 `.env`，与主程序策略不一致） | `健康巡检.py` `load_env()` + `email_gate` 共用判据 |
| F11 | P0 | 崩溃「邮件兜底」只在 TG 成功后触发；TG 三轮失败进 `SILENCED` 则永不发；bot 启动熔断时无消费进程 | `bot_runner` 邮件原在 `if ok:` 内；`watchdog` 新增 `mark_fatal()` 供独立巡检出告警 |
| F12 | P1 | 日报把「尝试发送」当「已送达」，两路皆失败当日不再重试 | `send_tg_notification` 原无返回值；邮件为 daemon 线程异步 |
| D2 | P1 | 修复 F11 后自引入：TG 连续失败会在 3 轮重复发 3 封崩溃邮件 | S16 RED：`email=3` → 修复后 `email=1` |
| D2b | P1 | 日报重试会重发已成功渠道（邮件最多重复 6 封） | `channels` 参数 + 按日 `_daily_report_done` |
| D1 | P1 | 重试计数不按日归属，次日首次失败即判「已耗尽」 | 新增 `_daily_report_retry_date` |
| D5 | P1 | 关闭日报邮件开关被当成投递失败 → 每天 6 次重试 + ❌误报 | RED：`'retry' != 'done'` |
| D6 | P1 | 日报投递状态仅在内存 → 窗口内崩溃重启重复发送 | `.daily_report.state.json` 原子落盘 |
| **D8** | **P0** | 崩溃邮件「线程启动」被记为「已发送」，SMTP 后续失败仍标记已通知 | S17：`crash_email_sent` 不得为 `True`；需 `wait=True` 拿真实 SMTP 结果 |
| **D9** | **P0** | **D8 的修复引入**：`wait=True` 的同步 `join(20s)` 跑在 Telegram 事件循环上，饿死整个 loop | S18 RED：SMTP 阻塞 0.6s 期间事件循环心跳 **1 次**；修复后 **12 次** |

另有 D4（自检用错事件口径）、D7（`event` 拼写不可读）两项 P2。

---

## 5. 测试证据

| 套件 | 结果 |
|---|---|
| `test_r0_notify_chains.py`（`--collect-only` **26 tests**） | 与下列 pytest 批次合计 **56 passed** |
| `test_notify_queue.py`（S1–S19 场景） | **19/19** |
| `test_watchdog_guard.py` | **22/22** |
| `test_email_gate.py` / `test_email_channels.py` | 含于 56 passed |
| 既有回归：g3_converge / r10 / tg_reply_fallback / recover_semantics / v62_green / v64_p3 / v64_partial / account_risk / auth_blocked / health_progress | 含于 56 passed |
| 脚本式：close_confirmation_v62 / v62_staged_integration / position_close_confirmation / b2_hardlock | **133/133、ALL PASS、7/7、PASS** |
| `py_compile`（bot_runner / trader_260725 / watchdog / 健康巡检 / email_gate / 3 测试文件） | 通过 |

**可复现验证**（Windows + `cmd`，`PYTHONIOENCODING=utf-8`）：

```bat
.venv\Scripts\python.exe -m pytest test_r0_notify_chains.py test_email_gate.py test_email_channels.py test_g3_converge.py test_r10_limit_close_alert.py test_tg_reply_fallback.py test_recover_semantics.py test_v62_green.py test_v64_p3_lifecycle.py test_v64_partial_close.py test_account_risk.py test_auth_blocked.py test_health_progress.py -q
.venv\Scripts\python.exe test_notify_queue.py
.venv\Scripts\python.exe test_watchdog_guard.py
```

> 口径声明：上述数字**已与 `--collect-only` 核对一致**。此前曾出现一条用例缩进在 `if __name__ == '__main__':` 之下、从未被收集却被计入通过数的情况（已在 `a1a884e` 修正，22 → 26 tests）。

---

## 6. 残留不确定性（**不宣称闭环，请重点复审**）

1. **SMTP 接受 ≠ 到达收件箱**：`wait=True` 的 `True` 只证明 SMTP 服务器接受投递；QQ 侧拦截/垃圾箱不可观测。
2. **TG 成功即结束事件**：崩溃告警 TG 成功但邮件失败时，事件文件本轮删除、邮件无第二次机会（D-010 语义）。
   已补审计留痕：`.notify_audit.log` 写 `TG_DELIVERED_EMAIL_UNCONFIRMED`。
3. **`SILENCED` 后不自动恢复**：两路皆败三轮后停止自动重试，需人工介入或重启后重新评估。
4. **日报投递状态的固有窗口**：发送确认成功与状态写盘之间崩溃、或写盘失败（只打印不回滚）→ 仍可能重复发送。
   当前口径为「不保证绝不重复」，不做强保证。
5. **D7 仅为可观测性**：未知 `event` 仍走持仓闸门；拼错的致命事件在空仓时仍可能无邮件。
6. **日报 ≠ 监控健康证明**：`fetch_balance` 失败时日报仍会发送。
7. **实盘未生效**：当前运行的进程仍是 R0 之前的代码，需重启才加载。

---

## 7. 请重点复审的问题

1. D9 的 `asyncio.to_thread` 修复是否彻底？是否还有其它在事件循环协程中执行阻塞调用的路径？
2. 「TG 成功即删除、邮件不重试」是否应改为**分渠道确认**（TG/Email 分别持久化，邮件未确认则保留事件）？当前保留会引入重复与状态复杂度，故未改。
3. `wait=True` 在崩溃场景阻塞 20s（已移出事件循环）是否仍过长？是否应改为 SMTP 超时收敛到 5–8s。
4. D6 的日报状态持久化采用「宁可偶发重复、不可丢失」，是否符合你的取舍预期。
5. 是否同意「关闭日报邮件、保留 TG 日报」的产品定位（邮件通道专供致命告警）。
6. 下一阶段优先级建议：`R1-1 取价失败显式化`（唯一会让用户看到错误数字的问题）> `R1-2 快照三态` > `R2-1 强平监控`。

---

## 8. 附：文件清单

| 文件 | 作用 |
|---|---|
| `交叉审查_D_通知体系复审_4H交易员视角_送审.md` | 完整审查报告（取证、矩阵、§5 结论修正声明、§10–§15 五轮复审） |
| `email_gate.py` | 邮件闸门（事件维度 + 致命白名单 + 日报独立开关 + 未知事件告警） |
| `bot_runner.py` | 崩溃告警链（邮件独立于 TG、每事件 1 封、SMTP 确认、事件循环不阻塞、审计留痕） |
| `watchdog.py` | `mark_fatal()` + 心跳 `fatal_alert`（bot 未启动场景的唯一出口） |
| `健康巡检.py` | 致命事件豁免持仓闸门；读取 `fatal_alert` 独立告警；自检口径对齐 |
| `trader_260725.py` | 日报状态机（渠道确认/重试/持久化/策略降级）与通知返回值语义 |
| `test_r0_notify_chains.py`、`test_notify_queue.py`、`test_email_gate.py`、`test_email_channels.py` | 对抗测试（含 S17/S18/S19 三条阻断项用例） |


---

## 9. 第六轮复审与 R0 运行期验收（2026-09-25 16:3x）

### 9.1 ChatGPT 复核 `b02aae3` 的结论与处置

| 复核意见 | 处置 |
|---|---|
| 「D9 已用 `asyncio.to_thread` 把 SMTP 等待移出事件循环；**这轮没有发现新的 R0 源码阻断项**」 | 接受 |
| 正文称「9 处缺陷」但表格列 10 项 | ✅ 已更正为 **12 项**（表格 10 + 脚注 D4/D7 两项 P2），`784eda1` |
| 开头称「4 项残留」但 §6 列 7 项 | ✅ 已更正为 **7 项** |
| 测试数字/`.env`/进程状态是「本机声明」，GitHub 无法独立确认 | 接受 —— 本节补充运行期证据（下 §9.2） |
| R0 可进入重启验收；整张修复地图未完成 | 接受 —— 修正后地图见 §9.4 |

### 9.2 R0 运行期验收（ChatGPT 要求「不能用已推送代替」）—— **已执行并留证**

**停机窗口**：`16:33:20 → 16:35:05`（约 105 秒）。原因说明：首次用 `Start-Process cmd /c 启动Bot.bat` 拉起时，
本机工具链在 30s 超时处连带回收了子进程，导致首启失败；改用 WMI `Win32_Process.Create`（脱离调用方进程树）后成功。
**停机前已确认空仓**：`trade_state.json` 9 个批次全部 `is_active=False`、代理 `--wait-proxy 15` 就绪（exit 0）。

| # | 验收项 | 证据（可复现） |
|---|---|---|
| A1 | 新进程已接管 | `watchdog PID 25472` / `bot_runner PID 36488`，创建时间均为 `2026-09-25 16:35:05`；旧树 `13316→41184→25428→38828→4080` 已 `taskkill /F /T` 终止，无残留 |
| A2 | **运行的确为 R0 代码（客观证据）** | 新心跳出现 `"fatal_alert": null` 字段 —— **旧版心跳没有这个键**（重启前基线可比对：旧心跳只有 `watchdog_started_at/bot_pid/bot_started_at/restarts/last_reason/stopped/stopped_reason/ts/ts_str/watchdog_pid/bot_alive`）。该字段只由 R0 的 `watchdog.mark_fatal()` 引入 |
| A3 | 磁盘 == 提交 | `git rev-parse --short HEAD` = `b02aae3`（验收时工作区 clean；`.env` 不入库） |
| A4 | 启动恢复链干净 | `logs/bot_20260925.log`：`SYSTEM SAFETY CHECK` → `Binance API: ✅ 已连接` → `活跃批次: 0 个` → `[恢复前健康检查] 通过` → `[状态恢复] 共接管 0 个历史活跃批次` → `[启动检测] 历史任务恢复校验完成！系统 READY` |
| A5 | 心跳存活 | `.heartbeat.json` `bot_alive: true`（`16:36:05` 起持续刷新）、`restarts: 0`、`fatal_alert: null` |
| A6 | **真实通道自检** | `健康巡检.py --selftest` → `=== 自检结果: TG经代理=True TG直连=True 邮件=True ===`（`16:37:54`）。邮件自检用 `event="health"`，**空仓且 `EMAIL_ALERT_ONLY_WITH_POSITION=true` 下仍发出 → R0 的致命事件豁免持仓闸门在实盘路径生效**。**注**：此自检走**巡检进程**路径，**不等价于**崩溃邮件路径（§10.1 收窄 1） |
| A7 | 独立巡检计划任务在运行 | `CryptoBot-HealthPatrol`：`Ready`、`LastRunTime 16:14:14`、`LastTaskResult 0`、`NextRunTime 16:29:29`（每 15 分钟）；`logs/patrol.log` 逐次 `✅ 巡检正常 … bot_alive=True 活跃批次 0`。**重启后首轮（`16:44:53`）已确认认到新实例**：`✅ 巡检正常: 心跳 47s 前 | watchdog_pid=25472 | bot_pid=36488 bot_alive=True | restarts=0 | 活跃批次 0`（任务 `LastRunTime 16:44:44` / `LastTaskResult 0`） |
| A8 | 停机窗口未产生误告警 | `16:33:20–16:35:05` 期间无巡检运行（相邻运行 `16:32:07` 与下一轮 `~16:44`），`.patrol_alert.state.json` `_bot_dead_count: 0` |
| A9 | 日报配置已加载（**待次日 08:05 终验**） | `.env` `DAILY_REPORT_EMAIL_ENABLED=false` → 日报必需渠道 `()`；进程 `16:35:05` 由 `load_dotenv()` 读入。**注**：尚无「进程内有效值」的直接展示 → 见 §10.1 收窄 3；M4 启动横幅将永久解决 |

**仍未验证的部分（不宣称）**：
1. 日报邮件开关的**实盘效果**要等次日 `08:05` 的日报（届时日志应只有 TG 送达、无 `📧 [邮件] 已发送: 每日结算报告`）。
2. **崩溃告警链路的实盘触发无法主动验证**——只能靠真实崩溃或故障注入；本轮只验证了通道可达性与代码身份，不等于「崩溃链已实测」。
3. 运行期验收**不覆盖** R1/R2（取价、快照、保护单、强平）。

### 9.3 本轮新增实测发现（回答 §7 问题 3：`join(20s)` 是否够）—— **口径已按 §10.5 修正**

**实测：不够，且会造成「假未确认」。**

- 通道自检的邮件步在 `16:37:23 → 16:37:54` 完成，**耗时约 31 秒**（该次发送最终成功）。
- 根因：`smtplib.SMTP_SSL("smtp.qq.com", 465, timeout=10)` 的 `timeout=10` 是**单个阻塞操作**的上限，
  `connect` + `login` + `sendmail` 三段串行累计可超 20s（本机实测 31s），并非整体 10s 预算。
- 后果：`wait=True` 的 `t.join(timeout=20)` 会先超时返回 `False`（打印「等待确认超时(20s)，按未送达处理」），
  **而邮件随后仍然送达**。在「TG 失败 + 邮件超时被记为未确认」的组合下，事件保留 → 下一轮重试 → **同一封崩溃邮件可能到达两次**。
- 影响面：D9 修复后该等待已不在事件循环上，**不会**再阻塞 TG；但会带来重复邮件与审计失真（`TG_DELIVERED_EMAIL_UNCONFIRMED` 可能误记）。
- 建议（列入 R1，需伴随重启复验）：把 SMTP 三段收敛到**总预算**（如整体 15s）或把 `join` 提到实测 P95 之上（≥35s）；
  两者取其一即可，不必同时。

### 9.4 修正后的下一阶段地图（接受 ChatGPT 的排序与补漏）

| 优先级 | 事项 | 触发条件 / 说明 |
|---|---|---|
| ✅ 已完成 | R0 受控重启与运行期验收 | 本节 §9.2 |
| **P0（前置）** | **修「第 3 批次起轮询断崖」，或把活跃批次硬上限限定为 2** | 允许同时存在第 3 个活跃批次**之前**必须处理。`trader_260725.py` L3784-3808（第 3 批次起间隔 75–100s）、L7293-7302（网络错误放大至最多 300s）；成交后补建保护单的窗口随之变长，3 秒快轮询无法缩短**首次发现**前的等待。此项在旧清单 B2 中，未列入新版 R1/R2 地图 —— 本轮补入 |
| P1 | **取价失败显示未知 + 浮盈百分比分母标注** | 把 `/summary` 当人工决策依据之前。L2815-2828 取价异常仍以均价代替市价 → 显示 `0.00`；百分比以仓位名义价值为分母而消息未说明 |
| P1 | **快照三态** | 有挂单未成交的活跃批次仍显示「持仓 0 / SL❌ TP❌」（L1644-1669），会误导人工判断 |
| P2 | R2：交易所保护单核对 + 强平数据 + `SILENCED` 后的发现/恢复责任 | 把通知当无人值守仓位安全监控之前。`bot_runner.py` L481-493 仍是「`SILENCED` 后停止重试」 |
| P3 | R3：恢复通知 / 资金费 / 夜间降噪 | 可继续延后 |

> **口径声明**：本轮重启只意味着 **R0 邮件闸门修复已受控生效**，
> **不等于**「整套通知与仓位监控可以无人值守」——R1/R2 未做，§6 的 7 项残留不确定性依然成立。

---

## 10. 第七轮复审（ChatGPT 复核 `f0df163`）：三处收窄 + 一项新增缺陷

### 10.1 三处收窄（**全部接受**）

**收窄 1 —— 31 秒自检不能证明崩溃邮件路径已超时。**
§9.3 的 31s 来自 `健康巡检.py send_email`（L171-205），崩溃邮件走 `bot_runner.py send_email_alert`（L93-161），
**两条不同代码路径**。该实测只能证明「**同机 SMTP 可能超过 20 秒**」且「代码确实存在『先报未确认 → 线程后来发送成功 → 随后重试』的路径」，
**尚未在崩溃邮件路径实测到一次重复投递**。→ §9.3 的结论已在 §10.5 按此口径重写。

**收窄 2 —— 「总预算 15s」与「`join≥35s`」都不是完整修复。**
前者若只缩短 `join`，发送线程仍继续跑，**更易**产生假未确认；后者只是降低概率，不能保证下次不超 35s。
下一轮必须显式区分三态：**发送失败 / 超时但线程仍在途 / 确认送达**，且**同一进程内不得在前一封仍途时启动重复发送**；
进程崩溃导致的外部投递重复属**必须接受的边界**（无法做到绝对不重）。
风险等级修订：D9 已把等待移出事件循环 → **当前风险是「重复邮件 + 状态误记」，不阻塞 TG，不值得单独制造一次停机**。

**收窄 3 —— A9 尚待次日验证。**
仓库只能证明代码读 `os.environ`（`trader_260725.py:362-374`），送审稿只能记录磁盘 `.env=false` 与新进程启动时间，
**没有直接展示 `bot_runner` 进程内的有效值**。巡检自检证明的是**巡检进程**的致命邮件通道可用，不能代替此项。
→ A9 改标为「**待 2026-09-26 08:05 日报验证**」（届时应只见 TG 送达、无 `📧 [邮件] 已发送: 每日结算报告`）。

### 10.2 新增缺陷 D10（**已源码验证，P1，运维安全）**

**现象**：`trader_260725.py:1582` 注释「限额调用时读 env 不缓存（**改 .env 即时生效，无需重启**）」
与 `:5521` 的用户提示「如确需开仓：调整 .env 限额（**即时生效无需重启**）或先平掉部分仓位」
—— **在本部署方式下不成立**。

**核实**：
- `load_dotenv()` 只在 `bot_runner.py:58`（进程启动 / 模块导入）执行一次，运行中进程的 `os.environ` 是**启动快照**；
- 因此磁盘改 `.env` 后，运行中进程读到的仍是旧值；`健康巡检.py` 因**每次运行都重读磁盘**而不受影响（两进程行为不一致，与 F10 同源）。

**实测现场**：`.env` 中**根本没有设置** `RISK_MAX_ACTIVE_BATCHES`（仅有 `MAX_LEVERAGE=100`），
→ 有效值 = 默认 **3** → **第 3 个活跃批次当前会被放行**，F6 轮询断崖（`≤2` 时 10–15s → `≤4` 时 75–100s）**仍处于暴露状态**。

**危害**：运营者（含本轮的我）会以为存在「改 `.env` 即止血」的不停机手段 —— 实际没有；
危机时刻按此操作会**得出错误的安全感并错过真正的处置窗口**。

**另需记录的事实**：当前代码库**没有**任何「暂停新开仓」的运行时开关
（`manual_pause|PAUSE_FILE|.pause|pause_flag` → 0 命中；`HARD_LOCK` 是**保护单级**锁，不是新批次冻结）。
→ 在合并维护落地前，**唯一可行约束是运营侧：不提交可能产生第 3 个活跃批次的信号**。

### 10.3 合并维护批次设计（**一次重启**，接受 ChatGPT 的排序）

| 项 | 内容 | 依据 |
|---|---|---|
| M1 | SMTP 等待语义重写：三态（失败 / 超时但在途 / 确认送达）；在途期间**禁止同进程重复发送** | §10.1 收窄 2 |
| M2 | 轮询断崖：修分级（`≤2` 档不再直接跳 75s）**或**把 `RISK_MAX_ACTIVE_BATCHES` 落为 2 | F6 / §16.4 P0 前置 |
| M3 | cap=2 落 `.env`（**该值不写 `.env.example` 之外**）+ 修 D10 文案 | §10.2 |
| M4 | **启动配置横幅**：进程启动时打印有效值（`RISK_MAX_ACTIVE_BATCHES` / `RISK_MAX_ACTIVE_SYMBOLS` / `MAX_LEVERAGE` / `DAILY_REPORT_EMAIL_ENABLED` / `EMAIL_ALERT_ONLY_WITH_POSITION` / `FATAL_EVENTS`） | 直接消灭 A9 类「无法从外部确认进程内有效值」的问题 |
| M5 | 次日 `08:05` 补验日报邮件关闭的实盘效果 | §10.1 收窄 3 |

**实施状态（2026-09-25 17:1x）：M1~M4 已完成编码并全绿，**未部署**（按 ChatGPT 决定，合并一次重启）。**

| 项 | 落地位置 | 落地内容 |
|---|---|---|
| M1 | `email_gate.py`（新增共享原语）+ `bot_runner.send_email_alert` + `trader_260725._send_email_alert` | `MAIL_RESULT_*` 五态常量、`mail_send_key` / `claim_mail_slot` / `release_mail_slot`（只释放自己的槽）/ `mail_in_flight` / `record_mail_result`；两条发送路径统一「**在途不启动第二封**」，`join` 超时改记 `timeout_in_flight`（≠ `failed`）且**保持槽位**，下一轮被抑制 |
| M2 | `trader_260725._calculate_monitoring_interval` | 分级改为连续：`<=2` 10~15s（不动）、`<=4` **20~30s**、`<=6` 30~40s、`>6` 45~60s；断崖 6.7 倍 → ≤2 倍 |
| M3 | `.env` + `trader_260725` 注释/`_check_account_risk` 文档串/TG 文案 | `RISK_MAX_ACTIVE_BATCHES=2`、`RISK_MAX_ACTIVE_SYMBOLS=1`（**需重启才生效**）；「即时生效无需重启」→「**需重启 watchdog/bot_runner 才对运行中进程生效**」 |
| M4 | `bot_runner.log_effective_config()`（在 `main()` 单实例锁之后立即调用） | 打印进程内有效值：4 个风险/杠杆项 + 3 个邮件开关 + `FATAL_EVENTS` 列表 + 「改 `.env` 需重启」提醒 |
| M5 | 待次日 `08:05` | 日报只走 TG 的实盘验证 |

**新增测试** `test_m1_m4_maintenance.py`（9 例）：M1 在途抑制/槽位释放/失败与超时可区分/在途期间槽位保持；
M2 9 个批次数的区间 + 上界连续性（≤2 倍）+ 第 3 档起 ≤60s + 源码不再含 `75.0/120.0`；
M3 误导文案已清除且新文案存在；M4 横幅反映进程内 env / 未设置时显示默认。

**回归**：`test_m1_m4_maintenance` **9/9**；pytest 13 套件 **56 passed**；`test_notify_queue` **19/19**；
`test_watchdog_guard` 全通过；`test_close_confirmation_v62` **133/133**；`test_position_close_confirmation` **7/7**；
`test_v62_staged_integration` **ALL PASS**；`test_b2_hardlock` **16/16**；`py_compile` 4 文件通过。

> ⚠️ **部署注意**：当前实盘进程仍跑**旧代码**（`.env` 的 cap=2 也未生效）。但 watchdog 具备崩溃自动重启 ——
> 若发生一次意外崩溃重启，本批次会被**提前部署**。届时只需核对启动日志里的
> `⚙️ [配置生效] RISK_MAX_ACTIVE_BATCHES=2` 横幅即可确认，无需再等维护窗口。


**维护窗口要求**：空仓、代理就绪、非整点 4H 收盘附近；单次停机（参考本轮：约 105s，含恢复链）。

### 10.4 决策记录（**本轮明确不做的事**）

- ❌ **不**为把 `join(20s)` 改成 35s 而单独停机；
- ❌ **不**在维护前把 `RISK_MAX_ACTIVE_BATCHES=2` 写进 `.env`（写了也不生效，反而制造 D10 式假安全感）；
- ✅ M1~M4 合并为一次重启；M5 次日验证。

### 10.5 §9.3 的口径修正（生效版本）

> **修正后表述**：本机巡检路径实测邮件发送 **31 秒**（`16:37:23 → 16:37:54`，最终成功），
> 证明**同机 SMTP 可能超过 20 秒**；结合 `send_email_alert` 的 `join(timeout=20)` 与「超时即记未送达」逻辑，
> **崩溃邮件路径存在「假未确认 → 后续重试」的重复投递风险**，但**尚未在该路径实测到一次重复**。
> 风险为**重复邮件与状态误记**，不阻塞 TG 事件循环，**不单独停机修复**。


---

## 11. 第八轮复审（ChatGPT 复核 `a8db596`）：M1 两处阻断项成立并已重做（**仍未部署**）

### 11.1 ChatGPT 指出的两个阻断项 —— **全部成立，根因确认**

| 阻断项 | 我的核实 | 后果 |
|---|---|---|
| ① 在途槽位键为 `(event, subject)`，而所有 `critical` 资金安全告警**共用主题**「🚨 资金安全告警」 | ✅ 确认（`trader_260725.py:880`）。且 C5 事故史证明 critical 会**成串触发**（4 分钟 8 封） | 第一封在途（SMTP 可达 31s）期间，**内容不同**的第二条资金安全告警被抑制且**无补发** → 不同事件丢失邮件兜底 |
| ② `join` 等满 20s 判「未确认」后，线程若在第 31s 成功，结果只留在观测变量；`crash_email_sent` 仍为假，槽位释放后下一轮重发 | ✅ 确认（`bot_runner.py` 崩溃分支 + 10s 轮询）。在途去重只挡**并发**重复，挡不住这种**先后**重复 | 「在途去重已消除重复」**不成立**；M1 原实现不可部署 |

**我的自我更正**：原 M1 的 9 条全绿**不能**排除这两条路径 —— 测试只覆盖了「同键在途抑制」与「结束后可再发」，未覆盖「同主题不同内容」与「超时后成功再重试」。全绿数字曾被我当作充分证据，这是方法错误。

### 11.2 M1' 重做方案（按 ChatGPT 处方）

| 要求 | 落地 |
|---|---|
| 用**稳定事件 ID** 区分崩溃队列邮件 | `send_email_alert(..., dedup_key=f"crash:{event_id}")`；`email_gate.mail_send_key(event, subject, dedup_key, text)` 有 `dedup_key` 时用稳定身份 |
| 其他资金安全告警按**具体告警**区分 | 未传 `dedup_key` 时**默认用内容指纹**（主题+正文 SHA1）→ 同主题不同内容**绝不**互相抑制（默认即安全，未来调用方无需记得传参） |
| 超时后完成的 SMTP 结果**回到该事件投递状态** | 新增 `on_late_result(ok)` 回调：发送线程**最终**结束时调用；崩溃链回调 `_mark_crash_email_late` → 登记到进程内 `_LATE_CRASH_CONFIRM`；队列消费轮在处理该事件时 `_consume_late_crash_confirm` 吸收并置 `crash_email_sent=True`（含 `crash_email_late_confirmed` 时间戳）后随本轮状态落盘 → **下一轮不再重发** |
| 区分失败 / 超时在途 | 崩溃分支读 `email_gate.mail_last_result_for(...)`，仍在途时不宣告「未送达」；TG 已成功时审计状态改用 `TG_DELIVERED_EMAIL_IN_FLIGHT`（与真失败的 `TG_DELIVERED_EMAIL_UNCONFIRMED` 区分） |

**实现中我自己发现并修掉的第 3 个隐患（未在 ChatGPT 清单内）**：
迟到回调若**直接写** `.notify.state.json`，会与队列消费轮末尾的 `_save_notify_state(内存快照)` 竞争，
后写者用旧快照覆盖前者 —— 等于把重复投递换成状态丢失。处置：回调**只写进程内登记表**，
由队列线程在下一轮读取合并（无跨线程写文件）✓。代价与边界：登记表**不跨重启**（线程随进程消失），
这属于已声明的「进程崩溃时在途邮件命运不可知」边界。

**已知未闭环项（不隐瞒）**：日报链（`trader._send_daily_report`）的**迟到回灌尚未接线** —— 日报邮件超时后若线程迟到成功，
状态机仍会重试。当前实盘 `DAILY_REPORT_EMAIL_ENABLED=false`，**无实际暴露面**；若重新开启日报邮件，
需一并补上（列入 R1）。

### 11.3 新增的两条对抗测试（ChatGPT 指定）

| 用例 | 覆盖 | 结果 |
|---|---|---|
| `test_a1_same_subject_different_alerts_not_suppressed` | 两条**同主题、不同正文**的 critical 告警，第一封在途时第二封**必须**发出 | ✅ 2 封均发出，SMTP 会话 2 次 |
| `test_a2_timeout_then_late_success_no_resend` | 端到端队列：第 1 轮 `join` 超时（事件保留）→ 线程迟到成功并登记 → 第 2 轮吸收确认 → **不得重复投递** | ✅ `crash_email_sent=True` + `crash_email_late_confirmed` 存在，SMTP 会话**仅 1 次** |

### 11.4 测试证据

`test_m1_m4_maintenance` **11/11**（原 9 + 2 条对抗）｜pytest 13 套件 **56 passed**｜`test_notify_queue` **19/19**｜
`test_watchdog_guard` 全通过｜`test_close_confirmation_v62` **133/133**｜`test_position_close_confirmation` **7/7**｜
`test_v62_staged_integration` **ALL PASS**｜`test_b2_hardlock` **16/16**｜`py_compile` 4 文件通过。

> 过程中我的测试自身也错了两次并已修正：① `KEY` 少传正文导致键不匹配（测试问题，非实现问题）；
> ② M2 连续性判据在随机采样上做 2.0× 边界比必然抖动 → 改为对**声明档位**做确定性断言。
> 记录在此以免复审时把「测试失败」误读为「实现缺陷」。

### 11.5 部署状态

**M1'–M4 全部编码完成且全绿，仍未部署**（ChatGPT 决定：补完这两条对抗测试后，再合并一次空仓、避开 4H 收盘的重启）。

- 若需**提前启用批次上限**：M2–M4 与 M1' 无耦合（分别改 `trader` 轮询分级 / `.env` 与文案 / `bot_runner` 横幅），
  可单独准备「排除 M1'」的版本供复核；代价是崩溃邮件的重复风险保持现状（当前实盘本身即为此状态，无新增风险）。
- ⚠️ 提前部署风险仍在：watchdog 崩溃自动重启会一次性加载全部改动（含 M1'）。届时核对启动横幅
  `⚙️ [配置生效] RISK_MAX_ACTIVE_BATCHES=2` 即可确认。


---

## 12. 第九轮复审（ChatGPT 复核 `d539da9`）：M1' 仍不可部署 → **单独出 M2–M4 可部署版**

### 12.1 两条新时序推断 —— **均成立并已核实**

| 发现 | 我的核实 | 后果 |
|---|---|---|
| ① **在途被抑制也消耗失败轮次** | ✅ `failed_attempts` 只看 TG 结果（与邮件在途无关）；`SILENCED` 早退**位于**吸收逻辑之前 | SMTP 若在第 3 轮之后才失败且 TG 一直失败 → 事件**只真正发过一次邮件就永久静默**，且迟到结果永不被吸收 |
| ② **吸收迟到标记与占槽之间仍有竞态** | ✅ 线程序：`release_mail_slot` → `_notify_late`；队列序：`_consume` → `claim` | 线程恰在两步之间完成时，队列既读到空标记又占到空槽 → **同进程重复邮件窗口**，故「迟到已完全闭环」不成立 |

**结论**：M1' 的两条主路径已修好，但**时序上仍不完整** → 不部署。已按 ChatGPT 决策执行下述拆分。

### 12.2 本轮落地：M2–M4 单独可部署版（**已提交，待 ChatGPT 复核**）

`frozen-archive` 上已**摘除全部 M1' 代码**，只保留 M2–M4：

| 项 | 保留内容 |
|---|---|
| M2 | `_calculate_monitoring_interval` 分级连续化（`<=2` 10~15s / `<=4` 20~30s / `<=6` 30~40s / `>6` 45~60s） |
| M3 | `.env` `RISK_MAX_ACTIVE_BATCHES=2` + `RISK_MAX_ACTIVE_SYMBOLS=1`（需重启生效，文件内已注明）；D10 误导注释与 TG 文案修正 |
| M4 | `log_effective_config()` 启动横幅（进程内有效值 + 「改 .env 需重启」提醒） |
| 护栏 | `test_m2_m4_maintenance.py::DeployableVersionGuardTests` —— 断言 `email_gate`/`bot_runner`/`trader` **不含** M1' 任何符号或参数。**M1' 若被误合入可部署分支，测试会响亮失败**（M1'' 落地时再移除该护栏） |
| 留痕 | `send_email_alert` / `_send_email_alert` 的 docstring 内**明确写出当前两条已知缺陷**，避免后来者误以为已修 |

**M1' 完整实现保存在分支 `notify-m1-timeline`（= `d539da9`）**，未合并、未部署；其两条对抗测试（A1/A2）随分支保留。

### 12.3 M1'' 修法草案（下一轮，按 ChatGPT 补测要求设计）

1. **槽位状态机取代「独立迟到表」**：把「在途 / 已确认 / 已失败」并入**同一个 key 的槽位状态**，
   让「迟到确认」通过**占槽操作本身**返回（`claim` 返回 `ALREADY_CONFIRMED` 时不再发第二封）——
   读取与占槽变成**一次原子操作**，从根本上消除 12.1② 的两步竞态。
2. **在途期间不计失败轮次**：崩溃链若判定「邮件仍在途」，本轮**不**累加 `failed_attempts`
   （邮件尚未被否定，不应消耗有界重试预算）；迟到结果回来后再正常计账，最终仍会进入 `SILENCED`（有界不被破坏）。
3. **吸收点前移**：`SILENCED` 早退**之前**先吸收迟到确认（迟到成功需能被记入状态/审计）。
4. **补测两条时序**：① 长时间在途后**失败**（跨 3 轮）→ 不得「只发一次即永久静默」；
   ② 吸收与占槽交错（线程恰在两步之间完成）→ 不得重复投递。

### 12.4 测试证据（M2–M4 可部署版）

`test_m2_m4_maintenance` **8/8**（M2 2 + M3 2 + M4 2 + 护栏 2）｜pytest 13 套件 **56 passed**｜
`test_notify_queue` **19/19**｜`test_watchdog_guard` 全通过｜`test_close_confirmation_v62` **133/133**｜
`test_position_close_confirmation` **7/7**｜`test_v62_staged_integration` **ALL PASS**｜
`test_b2_hardlock` **16/16**｜`py_compile` 4 文件通过。

> 说明：摘除 M1' 后 `test_notify_queue` 仍 19/19 —— 说明 M1' 的移除**没有**削弱既有覆盖
> （S17 断言的是「SMTP 失败不得标记已发送」，S19 断言的是「TG 成功 + 邮件未确认 → 删除但留审计」，
> 二者均与在途去重无关）。

### 12.5 状态与下一步

- **待 ChatGPT 复核**：M2–M4 单独版的提交号（见下方回复）。
- **复核通过后**：在**空仓 + 避开 4H 收盘**的窗口重启，并核对启动日志
  `⚙️ [配置生效] RISK_MAX_ACTIVE_BATCHES=2`（M4 横幅即为「进程内有效值」的直接证据，一并解决 A9 缺口）。
- **维护前仍需**：不提交会产生第 3 个活跃批次的信号（旧进程尚未加载 cap=2）。
- **M1''** 待补两条时序测试后再评审。


---

## 13. 第十轮：M2–M4 部署实盘验收 + **新缺陷 D11**（重启死循环，2026-09-25 21:29~21:33）

### 13.1 部署结果：成功，但过程发现 D11（**我的 M4 引入的实盘缺陷**）

批次上限按用户决策定为 **3**（`ba99e6c`）。21:29 执行部署重启，**首次失败**：watchdog 连续 4 次拉起
`bot_runner`（`21:29:22` / `21:29:41` / `21:30:03` / `21:30:25`），每次启动约 3~5s 后被判定
「主程序异常」并杀死，**日志无任何 traceback**。

**根因（D11，P1）**：`watchdog.monitor_process` 以**纯子串**判定崩溃：

```python
if "CRASH" in line or "FATAL" in line or "Unhandled exception" in line:  # 历史实现
```

而 M4 启动横幅输出 `FATAL_EVENTS=['auth_blocked', 'crash', ...]` —— **含 `FATAL`** →
横幅自己被判为崩溃 → 杀进程 → 重启 → 再打横幅 → **死循环**。
bot 本身无缺陷：前台直接运行 `bot_runner.py` 一切正常（当时「看似卡住」是 stdout 重定向到文件时的块缓冲假象）。

**为什么前三轮复审都没抓到**：M4 只是「加一行启动横幅」，静态审查看它无害；
**只有真正重启实盘、盯住进程与日志，才会暴露「输出文本被下游当作控制信号」这类耦合**。
这是「跑起来才算验收」的又一次实证——与 §9 的 A9 缺口同源：外部行为必须实测。

**第一版双重修复**（`a2813d8`）：
1. 横幅标签改为中文「致命事件白名单=」，不再输出大写 `CRASH`/`FATAL` 词；
2. 判定抽为 `_looks_like_crash()`：改为**词边界**正则 `\b(CRASH|FATAL)\b`
   （`FATAL_EVENTS`/`CRASH_ALERT` 这类标识符不再误判；`FATAL: ...`/`CRASH ...` 仍命中），
   并补 `Traceback (most recent call last)`。

> 后续复审确认第 2 步仍有 P1：业务层已捕获并自行降级的异常也会打印 traceback，不能据此杀整个进程。
> §14 已将 watchdog 收窄为只记录输出，进程死亡只认 poll/returncode。

**回归护栏**（`CrashDetectorHardeningTests`，3 例）：标识符与中文日志不误判；四类真崩溃信号仍命中；
**M4 横幅的真实输出不得被判为崩溃**（即本次死循环的根因）。

### 13.2 21:33 重新部署：验收通过

| 验收项 | 证据 |
|---|---|
| 启动链完整 | 横幅 → `🚀 Telegram Bot 监听服务已启动` → `SYSTEM SAFETY CHECK` → `Binance API ✅ 已连接` → `活跃批次: 0 个` → `[恢复前健康检查] 通过` → `[状态恢复] 接管 0 个` → **`[启动检测] 历史任务恢复校验完成！系统 READY`** |
| 无重启循环 | `watchdog.log` 只有一次「🚀 启动主程序」，无「检测到主程序异常」 |
| 进程内有效值 | 横幅显示 `RISK_MAX_ACTIVE_BATCHES=3`（**这是「进程内真正生效」的直接证据**，A9 类缺口就此闭合） |
| 心跳 | `bot_alive: true`、`restarts: 0`、`fatal_alert: null`（`21:34:06`） |
| 停机影响 | 本地账本空仓；无持久化信号入口审计，不能严格宣称“期间无信号丢失”；死循环产生的 **1 条 crash_alert + 3 条 summary_restart** 被新进程正常消费（仅 crash 走 TG+邮件，三条空仓汇总跳过实际发送） |

### 13.3 现场副产品：M1'' 要解决的「迟到成功」在实盘首次显形

死循环产生的崩溃告警中，一封的 SMTP 在 `join(20s)` 超时后，日志于 `21:33:36`（约 30s 后）才出现
`📧 [邮件] 已发送: 💥 程序崩溃报警` —— 即**先判未确认、邮件随后仍送达**。
这正是 §12.3 M1'' 要闭环的现象，**首次获得实盘证据**（此前只有 31s 的耗时推断）。
同时可见 `⚠️ 崩溃告警：TG 已送达但邮件未获 SMTP 确认…` 与随后的实际送达并存 —— 状态误记与重复投递风险真实存在。

### 13.4 教训（写给下一轮）

1. **任何写入子程序 stdout 的内容，都要过一遍「下游是否拿它做控制判断」这一关**：
   本项目里 stdout 不是纯日志，`watchdog` 会对它做模式匹配。本轮之前的所有复审都没意识到这条隐式契约。
2. **新增任何输出型功能（横幅/自检/摘要）都要问**：下游有没有在监听它？命中会不会触发动作？
3. 验收不能只看「测试全绿 + 静态审查」——**必须真跑一次并盯住进程、日志、告警三者**。

---

## 14. 第十一轮：开放第 3 批次前的三项安全修复（已部署 c462233）

1. **watchdog 文本误杀收口**：删除 `_looks_like_crash()`；`monitor_process()` 只负责排空并落盘 stdout/stderr，
   不再凭 `CRASH` / `FATAL` / `Traceback` 杀进程。进程死亡由主循环 `process.poll()` / returncode 裁决；
   进程存活但停止推进由业务进度心跳 + `健康巡检.py` 的 output-stalled 检测负责。
2. **日报 `required` 作用域**：08:05 首次 `done` 分支先固定本轮 required 渠道，再调用状态机；
   消除发送/落盘成功后打印渠道列表时的 `UnboundLocalError` 与错误 `sleep(300)`。
3. **批次骨架 Fail-Closed**：`save_batch_state()` 返回真实落盘布尔值；新建批次仅在 True 时允许进入
   `create_order` 循环。墓碑拒绝或写盘失败均告警并返回 False，保证零交易所副作用。

新增负测：已捕获 traceback 不得触发 watchdog 重启；真实 M4 横幅完整落盘；管道读取异常不据此重启；
日报 done 路径必须走 `sleep(90)`；骨架持久化失败必须返回 None、发送 critical 且 `create_order=0`。

专项结果（经 c462233 后补测扩充）：`test_m2_m4_maintenance + test_r0_notify_chains` 47 项全绿；
现役 pytest 门禁 77 项全绿；watchdog 22/22、notify queue 19/19、account risk 13/13、hardlock 16/16、
close confirmation 133/133、position close 7/7、staged 20/20、D-009 16/16、Batch C 23/23、
TP 24/24、R12 6/6、B2 crash-safe 18/18、crash_injection 归档全绿。

> 根目录 `pytest -q` 仍无法完整收集（12 个既有环境错误：`strategies_backtest/*.parquet` 缺失、
> `trader_test.py` 导入不存在的 `parse_signal`、`送审附件_*` 与根目录同名测试模块冲突）。
> 这些是基线问题，不由本轮改动引入，故验收采用「可收集集合 + 专项脚本」双轨。
### 14.1 受控重启验收（2026-09-25 22:15:48）

- 部署提交：`c462233`（本地与 `origin/frozen-archive` 一致，启动前工作区 clean）。
- 新进程树：cmd `2552` → venv watchdog `40032` → real watchdog `32708` →
  venv bot `15252` → real bot `18044`；`.bot_instance.lock=18044`。
- 日志：cap=3 横幅 → Telegram 监听 → Binance API ✅ → 活跃批次 0 → 恢复前检查通过 →
  接管 0 → `系统 READY`。
- `22:15:48` 后 watchdog 无“检测到主程序异常/正在终止”记录；`restarts=0`、`fatal_alert=null`、
  `bot_alive=true`，通知队列 0。业务控制进度持续刷新。
- 结论：三项代码修复已在进程内生效；M5 仍待 2026-09-26 08:05 行为验收。

---

## 15. 第十二轮：独立复审发现的两项收口（a91798a）

> 触发：`c462233` 上线后启动独立差异复审（只读，不改代码），结论为「三条生产目标均实现，
> 无新增 P0」，但附带两项需要立即收口的问题。

### 15.1 P1（既有安全缺口）：墓碑条目级损坏 Fail-Open

`_load_tombstones()` 只校验根节点是不是 `dict`；`save_batch_state()` 又只对**目标条目**做
`isinstance(entry, dict)` 判定。于是 `{"batch_bad": "not-a-dict"}` 这类「根合法、条目损坏」的
局部损坏会被当成「没有墓碑」：

```text
旧：return=True, degraded=False, trade_state.json 写入 batch_bad → 已清理批次复活
新：return=False, degraded=True, trade_state.json 不变 → Fail-Closed
```

修复：任一条目类型非法即整体置 `_tombstones_degraded`；`data` 原样返回（**不丢弃证据**）。
`save_batch_state` 沿用 D-009 Q3 分治——**全新批次拒绝、已存在批次更新放行**。
`_prune_tombstones()` 在 DEGRADED 下直接跳过，禁止基于不可信数据改写墓碑。

### 15.2 P2（c462233 引入的通知回归）：重复且误导的 critical

c462233 让 `execute_signal` 在骨架保存失败时自行发 critical，而 `save_batch_state` 的
墓碑分支与 `_persist_states` 的账本损坏分支**已经各自发过**。实测后果：

- 同一事件收到两条 critical（critical 级会触发邮件兜底 → 可能重复邮件）；
- 墓碑拒绝/账本损坏都被措辞为「修复 trade_state.json 的磁盘/权限问题」→ 误导排查方向；
- `_persist_states` 在**持有 `_state_lock`** 时发 TG，把网络 IO 带进状态锁。

修复（告警所有权收敛到单一来源）：

- `save_batch_state` 成为写盘失败的**唯一**告警源：锁外发送、按真实原因措辞（账本损坏 vs
  磁盘写入失败）、按键去重；
- `execute_signal` 只做副作用阻断（返回 None、零 `create_order`），不再发泛化告警；
- `_persist_states` 支持 `_defer_state_corrupt_alert`，把损坏告警推迟到锁外统一发出；
  其余调用方行为不变。

### 15.3 测试与夹具

- 新增 6 项：条目损坏拒绝新建 / 条目损坏仍放行已存在批次 / DEGRADED 跳过 prune /
  落盘失败告警唯一且锁外 / 旧 fake 返回 `None` 同样零下单 / 调用方零泛化告警。
- **RED 基线已验证**：这 4 项新断言在 `c462233` 上全部失败（独立复现 Fail-Open 与重复告警），
  修复后转绿——证明测试真的锁住了行为，不是事后补写的空断言。
- 修正归档夹具漂移：`test_crash_injection.py` 成功桩显式 `return True`、4 参冲突检查、
  持仓模式与指纹桩（该脚本此前因 `None` 成功桩会在新硬门下假失败）；`test_b2_crashsafe_entry.py`
  的 T6a 断言按新告警契约改为「调用方零泛化 critical」。

### 15.4 受控重启验收（2026-09-25 22:36:47）

- 部署提交：`a91798a`（本地与 `origin/frozen-archive` 一致，启动前工作区 clean、活跃批次 0、
  代理 7890 可用、通知队列 0）。
- 新进程树：cmd `38036` → venv watchdog `44100` → real watchdog `42260` →
  venv bot `42180` → real bot `12752`。
- 日志：cap=3 横幅 → Binance API ✅ → 活跃批次 0 → 恢复前检查通过 → 接管 0 → `系统 READY`。
- `22:36:47` 后 watchdog 无「检测到主程序异常/正在终止」；`restarts=0`、`fatal_alert=null`、
  `bot_alive=true`、队列 0。
- 独立巡检 `22:44:53`：心跳 6s、`bot_alive=True`、`restarts=0`、控制进度 1s、活跃批次 0，
  任务返回码 0。
- 说明：watchdog 启动后首次心跳的 `bot_alive` 为 `false`（写于子进程确认存活之前），
  下一轮 60s 刷新即转 `true`，与 22:15 那次启动行为一致，非本轮回归。
- 结论：两项收口已在真实进程内生效；M5 仍待 2026-09-26 08:05 行为验收。

### 15.5 仍未闭环（不在本轮范围）

- `save_batch_state` 的布尔契约目前**只有骨架路径**消费；TP/SL 更新与最终提交等约 30 处调用
  仍忽略返回值（既有行为，非本轮引入）。若要把「durable write」升级为全局契约，需单独批次。
- 根目录 pytest 无法完整收集（12 个基线环境错误），验收依赖可收集集合 + 专项脚本双轨。

---

## 16. 第十三轮：对 `a91798a` 的独立复审反例（41bfc2c）

> 触发：ChatGPT 复核已部署的 `a91798a`，认可 watchdog 与 `required` 两项主修复，
> 但判定「墓碑 Fail-Closed 已收口」过早，并给出两条具体反例。本轮**先复现、再修复**，
> 两条反例均在旧基线上取得 RED。

### 16.1 反例一（P1）：`{"batch_x": {}}` 仍能 Fail-Open

`a91798a` 的条目级校验只做 `isinstance(v, dict)`。空 dict 能通过，且缺 `cleared_at`
会被 `float(t_entry.get('cleared_at', 0) or 0)` 兜底成 `0` → `age = now - 0` 远超 7 天
TTL → 被判成「墓碑已过期」。于是既不触发复活告警，也不置 DEGRADED，**全新批次直接放行**；
由于 `_degraded=False`，TTL prune 还会把这条损坏证据一并剪掉。

实测（旧基线）：

```text
反例1: save返回=True degraded=False state含batch_x=True   ← 期望 False
```

修复：

- 新增模块级 `_tombstone_entry_valid(entry)`：非空 dict 且 `cleared_at` 可解析为**有限正数**
  （拒绝 `bool`、`NaN`、`inf`、缺失、非数字、非正数）。
- `_load_tombstones` 改用该判据 → 任一条目结构非法即整体 DEGRADED（`data` 原样返回不丢证据）。
- `save_batch_state` 的 TTL 判定前置结构校验，不再走 `age` 兜底。
- `_prune_tombstones` 对结构非法条目**永不删除**（纵深防御），且 prune 写盘失败不再静默。
- 判据放在**模块级而非方法**：在 Mock 单测夹具上 `self.<name>` 会被自动创建成 Mock 并静默
  返回真值（假绿陷阱），模块级函数无法被夹具偷换。本轮实现过程中就真实踩到了这个坑——
  初版写成 `self._tombstone_entry_valid(...)`，5 个测试因此假失败，改为模块级后消除。

### 16.2 反例二（P1）：`_persist_tombstones()` 返回 `False` 时清理仍继续

`clear_batch_state` 此前调用 `self._persist_tombstones(tombstones)` 却**不检查返回值**，
随后删除活跃账本并 `return True`。若墓碑写失败而账本写成功，就产生
「批次已清理、反复活墓碑不存在」的孤儿态——这是比条目类型更直接的闭环缺口。

实测（旧基线）：

```text
反例2: clear返回=True 账本已被删除          ← 期望 clear=False 且账本保留
```

修复：**墓碑落盘升级为删账本的前置硬门**。

| 情形 | 旧行为 | 新行为 |
| --- | --- | --- |
| 墓碑原子写返回 `False` | 删账本 + `return True` | 保留账本、`return False`、锁外 critical |
| `_persist_tombstones` 抛异常 | 打印警告、删账本 + `return True` | 同上（`_tomb_write_fail` 记录真实异常） |
| 墓碑条目损坏（如 `{}`） | 视为「已存在」→ 跳过写入 → 删账本 | 视为缺失并**重写**，写失败即拦截 |
| 墓碑已有效（重复 clear） | 跳过写入 | 不变（幂等放行） |
| 清理后 `_persist_states` 失败 | 忽略 | 打印批次级上下文（磁盘仍保留该批次，下轮 converge 自动重试；墓碑已 durable，无复活风险） |

墓碑拒绝与 proof 门拒绝**分流为两个告警键**：`('clear_tomb_failed', symbol, batch_id)`
vs `('clear_rejected', symbol, batch_id)`，均在 `_state_lock` 外发送、按键去重，
措辞写明真实原因（墓碑落盘失败 → 检查磁盘空间/权限），不再误导为 proof 问题。

### 16.3 测试

- 新增 2 项（`test_m2_m4_maintenance.py`，现 22 项）：
  `test_empty_dict_entry_must_degrade_and_block_new_batch`（含 DEGRADED 下 prune 不得改写证据）、
  `test_clear_keeps_ledger_when_tombstone_write_fails`（断言返回 `False` + 账本保留 + 墓碑文件原样 + 锁外 critical）。
- **RED 基线已验证**：两项在 `a91798a` 上分别得到 `save=True/degraded=False` 与
  `clear=True/账本被删`，均失败；修复后转绿。
- 夹具补绑 `_collect_batch_order_ids` / `_verify_clear_proof` / `_converge_alert` /
  `_tp_breaker_alerted` / `_converge_alert_counts`，避免 MagicMock 吞掉依赖造成假绿/假红。

### 16.4 回归结果

| 套件 | 结果 |
| --- | --- |
| 根目录 pytest（可收集集合） | **79 passed**（基线 77 + 2 新增） |
| `test_m2_m4_maintenance.py` | 22/22 |
| Batch C 专项 `test_c_batch.py` | 23/23（含 TC22 墓碑 TTL prune 正常路径） |
| D-009 `test_d009_state_persistence.py` | 16/16 |
| R12 `test_r12_state_backup.py` | 6/6 |
| 归档 `test_crash_injection.py` | 20 PASS / 0 FAIL |
| 归档 `test_b2_crashsafe_entry.py` | 18 PASS / 0 FAIL |
| `test_close_confirmation_v62.py` | 133/133 |
| `py_compile trader_260725.py` | 通过 |

**既有失败（非本轮引入）**：`tests_archive/test_close_race_replay.py` 2 项
`UNEXPECTED-FAIL`（驱动异常：`close_position_limit` → `_begin_close_request_if_active`
解包不足）。已 `git stash` 对照验证：在基线 `f0a21d2` 上输出完全相同，与本轮改动无关。

**收集错误更正**：§15.5 记的「12 个基线环境错误」实测为 **13 个**
（8 个 `strategies_backtest/*.parquet` 缺失、`trader_test.py` 导入不存在的 `parse_signal`、
4 个 `送审附件_*` 与根目录同名模块冲突）。基线 `f0a21d2` 与本轮 `41bfc2c` 收集数一致
（77 → 79），错误数一致（13 → 13），故非本轮引入。

### 16.5 受控重启验收（2026-09-25 23:11:37）

- 部署提交 `41bfc2c`（已推 `origin/frozen-archive`，启动前工作区 clean、活跃批次 0、
  `.notify_queue/` 条目 0、代理 `127.0.0.1:7890` 返回 200）。
- 重启方式与前两轮一致：`taskkill /F /T` watchdog 树 → 按原命令行重启。
- 新进程树：cmd `28860` → venv watchdog `26288` → real watchdog `7148` →
  venv bot `11980` → real bot `37160`。
- 日志：`23:11:37` 启动 → 风控参数横幅 `RISK_MAX_ACTIVE_BATCHES=3` → Binance API ✅ →
  活跃批次 0 → 恢复前检查通过 → 接管 0 → `系统 READY`（`23:11:42`）。
- 心跳：`23:13:37` → `bot_alive=true`、`restarts=0`、`fatal_alert=null`、`stopped=false`
  （启动后首次心跳 `bot_alive=false` 为既有行为，下一轮刷新即转 true）。
- `watchdog.log` 自 `23:11:37` 起**无**「检测到主程序异常/正在终止」类记录。
- `logs/bot_20260925.log` `23:11` 之后 19 行内**无** Traceback/异常/邮件发送记录。
- 独立巡检 `23:15:18`：心跳 41s、`bot_alive=True`、`restarts=0`、最后原因 `None`、
  控制进度 2s、活跃批次 0，任务返回码 0。

### 16.6 仍未闭环（不在本轮范围）

- 第 3 个活跃批次信号**继续冻结**（网络错误后 60~90s 监控放大窗口未收口，需用户明确接受）。
- `save_batch_state` 布尔契约未被约 30 处 TP/SL 更新与最终提交调用消费（沿 §15.5，另起批次）。
- M5 待 2026-09-26 08:05 实盘验收（TG、状态文件、无循环异常、无邮件、08:10 不重发）。

---

## 17. 第十四轮：清理链成功语义 + 我方两处测试回归（9d9526c）

> 触发：ChatGPT 复审 `41bfc2c` 评 **82/100**，认可两条原反例已关闭，但指出
> 「整条批次清理的成功语义尚不能标为闭环」。本轮核实成立，且问题比它描述的更重。

### 17.1 P1：账本落盘失败仍报「清理完毕」（成立，且更重）

`clear_batch_state` 在墓碑写成功后调用 `_persist_states()` 却只判断不阻断：
失败时仅打印警告，随后照样打印「🧹 清理完毕」并 `return True`。

核实两点：

1. **13 个调用方全部把 `False` 当作「未清理、下轮重试」**——`recover_active_batches`
   打印「清理未完成」、`_finalize_batch` 返回 `clear_failed（保留在 close_phase=2）`、
   手动止损分支打印后不 `break`、中间平仓分支置 `_cleanup_pending=True`。
   于是 `True` 确实是契约谎言：调用方报告批次已结束，磁盘上批次还在。
2. **该路径原本零告警**：`_persist_states` 只在 `_state_corrupted` 分支发 critical，
   普通写盘失败只 `print("保存状态文件失败")`。所以是「既说谎、又无人知晓」，
   比复审描述的更重。

RED 实测（`rc1=True`、stdout 含「清理完毕」、critical 列表为空）全部复现后修复：

| 情形 | 旧行为 | 新行为 |
| --- | --- | --- |
| 墓碑写失败 | 保留账本 + `False` + critical（§16 已修） | 不变 |
| 账本写失败 | 删内存对象 + 打印「清理完毕」 + `True` + 无告警 | `False`、不打印「清理完毕」、锁外 critical |
| 两者成功 | `True` | 不变 |

- 新增 `_ledger_write_fail` 分支，告警键 `('clear_ledger_failed', symbol, batch_id)`，
  按 `_state_corrupted` 分流措辞（损坏现场 vs 磁盘空间/权限）；
- 调用 `_persist_states` 前置位 `_defer_state_corrupt_alert`，锁外统一发，
  避免与它自身的损坏告警**重复**（重演 §15.2 的反模式）；
- 正确性依据：`_persist_states` 是 `os.replace` 原子写 → 失败时磁盘批次原样保留，
  「状态保留、下轮重试」可如实宣称；墓碑已 durable 且幂等，重试无复活风险。
- docstring 写入返回值契约：``True`` **只**在「墓碑 durable 且账本 durable」时返回。

### 17.2 P2：`_tombstone_entry_valid` 口径定界（按兼容性反向收）

复审指出「目前仅验证有效正数时间戳，若要宣称完整结构校验需先定义旧版兼容规则」。
核实后**采纳其口径要求，但结论是不加校验**：

- `test_c_batch` 夹具写的墓碑没有 `close_phase`；Batch B 之前写入的真实墓碑同样没有。
  要求 `close_phase` 会把合法历史文件判成 DEGRADED 并连带禁止一切新建批次——
  把兼容问题升级成可用性事故。
- 定界写入 docstring：**「TTL/存在性判据所需的最小结构」，不是完整 schema 校验**。
  只强制 `cleared_at`（TTL 与「是否已过期」唯一依赖字段）；
  `close_phase` / `symbol` / `converged_order_ids` 是溯源审计字段，不参与
  TTL、存在性或复活判定。
- 新增 `test_tombstone_entry_valid_boundary_and_legacy_rule`（13 用例：3 种合法
  含旧版形态，10 种非法含 `{}`/`0`/`-1`/`True`/`NaN`/`inf`/缺字段）钉住该规则，
  日后收紧必须先给迁移方案。

### 17.3 我方两处测试回归（复审看不到，因为它只读仓库）

| # | 现象 | 基线实测 | 根因 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | `test_tp_validation.py` T5c | `f0a21d2`=**24/24**，`41bfc2c`=**23/24** | 夹具未绑 `_load_tombstones`/`_persist_tombstones`，MagicMock 使 `is not True` 恒成立 → 清理被当成墓碑落盘失败拦掉 | 补绑真实墓碑四件套（临时文件）+ `_persist_states` 内存闭环 |
| 2 | `test_v64_p3_lifecycle.py` `NameError: _tombstone_entry_valid` | — | AST 源码提取基建把函数体 `exec` 进**合成命名空间 `NS`**（非 `trader_260725` 的 globals），新增模块级名未登记 | NS 注入该函数 + `TOMBSTONE*` 常量 |

第 2 项修复后验证为**基线等价**：`f0a21d2` 3/9 → 现 3/9，9 项 marker 逐条一致
（`r5c` 两边均通过），即该文件 6 项为既有失败，非本轮引入。

**门禁盲区（根因）**：这两个文件根目录 pytest **收集 0 项**（脚本式测试，
`scenario_*` 不是 `test_*`），所以 §16 的「79 passed」根本没覆盖到它们。
本轮起验收加入**全量脚本门禁**：逐个以 `python test_*.py` 直跑根目录 48 个文件——
正是这一步抓到了上面两处回归。

### 17.4 回归结果

| 套件 | 结果 |
| --- | --- |
| 根目录 pytest（可收集集合） | **81 passed**（基线 77 + 4 新增） |
| **脚本式全量（新门禁）** | **48 OK** / 1 项见下 |
| `test_orphan_guard.py` | **5/5 PASS，exit=0**（停机窗口内验证） |
| m2_m4 / Batch C / Batch B / G3 / R12 | 24 / 23 / 23 / 28 / 6 |
| close_confirmation | 133/133 |
| 归档 crash_injection / b2_crashsafe | 20 / 18 |
| `py_compile`（4 个改动文件） | 通过 |

`test_orphan_guard` 在 Bot 运行期间**必然** rc=42：它第一件事是
`acquire_instance_lock()`，而运行中的 Bot 持有 `Global\my_crypto_bot_single_instance`
（`bot_runner.py:566`）。这是设计使然、与本轮无关，已借本次重启的停机窗口跑通 5/5。
注意 `acquire_instance_lock()` **无返回值**，成功 = 不抛 `SystemExit`。

### 17.5 受控重启验收（2026-09-25 23:52:25）

- 部署提交 `9d9526c`（已推 `origin/frozen-archive`）。停机前检查：活跃批次 0、
  `.notify_queue/` 0、代理 200、工作区 clean（仅 `AGENTS.md` 未跟踪，用户新增，未动）。
- **停机窗口内**先跑 `test_orphan_guard.py`（5/5）再启动，避免互斥体假失败。
- 进程树：cmd `11180` → venv watchdog `42080` → real watchdog `38068` →
  venv bot `16724` → real bot `36884`。
- 日志：`23:52:25` 启动 → 风控横幅 `RISK_MAX_ACTIVE_BATCHES=3` → Binance API ✅ →
  活跃批次 0 → 恢复前检查通过 → 接管 0 → `系统 READY`（`23:52:30`）。
- 心跳 `23:53:25`：`bot_alive=true`、`restarts=0`、`fatal_alert=null`、`stopped=false`；
  队列 0；`watchdog.log` 自 `23:52:25` 起无误杀/终止记录；
  bot 日志 `23:52` 后无 Traceback、无邮件。
- 独立巡检 `23:54:42`：心跳 17s、`bot_alive=True`、`restarts=0`、最后原因 `None`、
  控制进度 8s、活跃批次 0，返回码 0。

### 17.6 仍未闭环（不在本轮范围）

- 第 3 个活跃批次信号**继续冻结**（网络错误后 60~90s 监控放大窗口未收口）。
- `save_batch_state` 布尔契约未被约 30 处 TP/SL 更新与最终提交调用消费（沿 §15.5）。
- M5 待 2026-09-26 08:05 实盘验收（TG、状态文件、无循环异常、无邮件、08:10 不重发）。
- **`test_v64_p3_lifecycle.py` 基线即 3/9**（`f0a21d2` 与 `9d9526c` 逐项一致）：
  6 项既有失败（线程时序 / `AttributeError: 'function' object has no attribute 'get'`），
  需单独批次排查，不属通知体系范围。

---

## 18. 第十五轮：损坏账本分支 + 门禁运行器（`3df36f9` / `e834dcb`）

> 触发：ChatGPT 复审 `9d9526c` 评 **87/100**，认可清理链已修，给出两条新指正。
> 核对后**两条均成立**——一条是真缺陷，一条是我的**失实陈述**。

### 18.1 P1：损坏账本 →「不知道批次是否还在」被当成「已清理」

这不是新口径争议，`load_all_states()` 的 docstring 早就写死了契约：

> 「损坏时置位 `self._state_corrupted=True` 并返回 `{}`（占位，返回值在损坏态
> 下无意义）。**调用方读取返回值前必须先判 `_state_corrupted`**。」

`clear_batch_state` 违反了它：

```python
all_states = self.load_all_states()
b_data = (all_states.get(symbol) or {}).get(batch_id)
if b_data is None:
    return True          # ← 账本损坏时恒成立
```

后果链（三段都已核实）：

1. 损坏时 `load` 返回占位 `{}` → `b_data` 必为 `None` → **所有**清理请求返回 `True`；
2. 13 个调用方把 `True` 当成批次已结束：打印「清理完毕」、`_finalize_batch` 返回
   `finalized`、中间平仓 `break`、`recover_active_batches` 报告归档完成；
3. 而磁盘 `trade_state.json` 仍是损坏状态，无人重试、无人察觉——**且这条路径调不到
   `_persist_states`，`load_all_states` 对损坏又只 print 不发 TG → 该路径原本零告警**
   （与第十四轮是同一类「既说谎又静默」）。

修复（最小改动，复用既有「锁外拒绝告警」结构）：

- 读取后先判 `_state_corrupted` → 置 `_ledger_unreadable`，并置 `_reject` 阻断后续
  proof/authorization 分支（`b_data` 为 `None`，放行会 `.get` 抛 `AttributeError`）；
- **账本可读且批次不存在**才走幂等 `return True`；
- 新增锁外 critical 分支，告警键 `('clear_state_unreadable',)` 用**全局 1 元组**：
  损坏的是整个文件，按 symbol/batch 分键会让 N 个批次各发 3 轮，且这些键会随
  `clear_batch_state` 入口的批次键修剪被误删；
- 返回值契约升级为**三前提**：`账本可读 ∧ 墓碑 durable ∧ 账本 durable`。

**无自旋风险（已核查）**：13 处调用全为一次性判定 `if clear(...): break/return`，
无 `while` 轮询，`False` 只推迟到下一轮 converge。

RED 实测（修复前）：`rc=True`，断言 `True is not false`，25 项中恰 1 失败；
修后 25/25 OK。

### 18.2 P2：我上轮的「脚本式 49/49 OK」是失实陈述

ChatGPT 指出「同时承认 p3 是 3/9，就不能说 49 个退出码全 0」。核对后我错在三处：

| # | 我说的 | 实际 |
| --- | --- | --- |
| 1 | 49 个脚本 | 根目录 `test_*.py` **50 个** |
| 2 | 49/49 退出码 0 | `test_v64_p3_lifecycle.main()` 是 `return 0 if passed == len(tests) else 1`，6 项失败**必然 rc=1** |
| 3 | 门禁已跑 | 临时 PowerShell 循环**不在仓库**，复审方无从复现 |

根因是循环里的计数变量只统计「脚本是否抛异常」，**没有读 `returncode`**——
我把「50 个都跑起来了」写成了「49/49 全过」。这不是口径含糊，是失实陈述。
另外它也点对了：仓库里没有独立的门禁运行器，所以我的统计无法被核验。

### 18.3 `run_test_gate.py`：把口径钉死

逐行打印每个文件的 rc 与耗时，按四态分类，**禁止归并成「全绿」**：

| 状态 | 含义 | 计入通过？ |
| --- | --- | --- |
| `PASS` | rc=0 | ✅ |
| `BASELINE-FAIL` | rc=1，已登记基线依据 | ✅（须同时列出） |
| `NOT-VERIFIED` | rc=42，Bot 持互斥体，需停机窗口 | ❌ |
| `FAIL` | rc 超出预期集合 → 真回归 | ❌ |

- `EXPECTED` 登记非 0 退出码**必须写明依据**，否则判 `FAIL`；
- `--strict` 让 `NOT-VERIFIED` 也致命（停机窗口内用）；
- 汇总行固定输出**四个计数**，措辞上无法再声称「全绿」。

**顺带修掉一个一直没暴露的坑**：`--ignore=送审附件_*` 无效——pytest 的 `--ignore`
是**路径前缀**不是通配符。实测该写法仍报 4 个 `collection error` 并 `Interrupted`
中断整轮；v6.1 / v6.2 目录内本就没有 `test_*.py`，所以问题从未显现。改为运行时
枚举 `送审附件_*` 目录逐个展开。

### 18.4 门禁实测（2026-09-26 08:23~08:35，Bot 运行中）

| 段 | 结果 |
| --- | --- |
| pytest 收集集 | **82 passed**（上轮 81 + 本轮新增 1），exit=0 |
| 脚本式 50 项 | **PASS 48 \| BASELINE-FAIL 1 \| NOT-VERIFIED 1 \| FAIL 0** |
| `--ignore` 展开 | 6 个送审附件目录，0 collection error |

- `BASELINE-FAIL`：`test_v64_p3_lifecycle.py` rc=1（基线即 3/9，与 `f0a21d2` 逐项一致）
- `NOT-VERIFIED`：`test_orphan_guard.py` rc=42 —— **已另在停机窗口 08:19 补验
  5/5 PASS、exit=0**，故该项证据完整，只是不能计入门禁的通过计数
- **安全实测**：门禁运行前后 `trade_state.json` mtime **完全未变**
  （`2026-09-23T15:07:05Z`），`.notify_queue/` 前后均 0 → 测试不触碰生产账本

### 18.5 M5 实盘验收（2026-09-26 08:05，**已完成**）

由旧进程（`9d9526c`，23:52:25 起连续运行）执行，窗口证据完整保留：

| 判据 | 实测 |
| --- | --- |
| TG 日报送达 | `[08:05:28] ✅ [日报] 2026-09-26 已确认送达（渠道：tg）`，当日**仅 1 条** |
| 08:10 不重发 | 全文件搜索 `08:10` → **0 行** |
| 无循环异常 | `Traceback` / `Exception` / `循环异常` / `is_cycling` → **各 0** |
| 无邮件 | 当日 `邮件` / `send_mail` / `smtp` 关键字 → **各 0** |
| 状态文件未被污染 | `trade_state.json` mtime = `2026-09-23 23:07:05`（整个窗口未变） |
| 全程健康 | `logs/patrol.log` 2026-09-26 **33 条巡检全部「巡检正常」**，`bot_alive=True`、`restarts=0`、`last_reason=None`；`watchdog.log` 从 `23:52:25` 直接跳到 `08:20:18`，**8 小时零重启零误杀** |

**代码口径说明**：M5 由 `9d9526c` 执行，`3df36f9` 在窗口之后（08:20:18）才上线。
本轮改动仅在 `clear_batch_state` 与测试/门禁，**未触及日报路径**，故该结论对新代码
同样成立；如需在 `3df36f9` 上复验，下一个观察点是 2026-09-27 08:05。

**关于 patrol.log 里 11 条「邮件」行的甄别（重要）**：`00:09:58 / 00:11:13 /
00:11:14 / 00:12:59 / 00:15:42` 出现 5 条 `✅ 邮件告警已发送`，看着像真实外发。
逐层核实结论是**当日零真实外发**：

1. `健康巡检.py:201` 的 `✅ 邮件告警已发送` 只在 SMTP 成功后打印，但走到邮件兜底的
   前置是 `alert()` 里 TG 双路全失败，而那条路径**必先**打印 `TG 告警(...)` 行——
   当日该行 **0 条**，且全部 44 行里没有任何 `alert()` 产物 → `alert()` 从未被调用；
2. 这 11 行全部来自 `patrol.send_email` 的**直接调用点**，4 个调用点
   （`test_email_channels.py:42/53/65`、`test_r0_notify_chains.py:430`）
   **全部 patch 了 `smtplib.SMTP_SSL`**；
3. 三行一组（发送 / 禁用 / 无持仓）恰是 `test_email_channels` 的三个分支，
   单条 `已发送` 来自 `test_r0_notify_chains::test_patrol_health_email_sends_without_position`。

### 18.6 受控重启部署（2026-09-26 08:20:18）

- 部署提交 `e834dcb`（代码 `3df36f9`，已推 `origin/frozen-archive`）。
- 停机前检查：活跃批次 0、`.notify_queue/` 0、代理 200、工作区 clean（仅 `AGENTS.md` 未跟踪）。
- **停机窗口内先跑 `test_orphan_guard.py`：5/5 PASS、exit=0**（把门禁里那条
  `NOT-VERIFIED` 变成实证），随后启动。
- 进程树：cmd `46116` → venv watchdog `38636` → real watchdog `33968` →
  venv bot `42176` → real bot `46752`。
- 日志：`08:20:18` 启动 → 风控横幅 `RISK_MAX_ACTIVE_BATCHES=3` → Binance API ✅ →
  活跃批次 0 → 恢复前健康检查通过 → 接管 0 → `08:20:23 系统 READY`。
- `08:35:18` 心跳：`bot_alive=true`、`restarts=0`、`fatal_alert=null`、队列 0。

### 18.7 新发现（**不在本轮修复范围**，仅记录）

1. **测试污染生产观测日志**：`test_email_channels` / `test_r0_notify_chains` 的输出
   通过 `健康巡检.py` 的 `log()` 直接追加进生产 `logs/patrol.log`，并打印与真实
   告警**完全相同**的 `✅ 邮件告警已发送`。今后真出邮件告警时，运维无法从日志分辨
   它是测试噪声还是真事件——观测性缺陷，与本轮通知体系主题同域，但属另一处所有权。
2. **测试与生产 Bot 并存运行**：本轮门禁在 Bot 运行中执行。已实测 `trade_state.json`
   mtime 未变、队列未变，但这是**观察到的安全**而非**结构上的保证**，尚无隔离
   （临时目录 / 只读挂载 / 显式 `TEST_MODE` 闸门）。

### 18.8 仍未闭环

- 第 3 个活跃批次信号**继续冻结**（网络错误后 60~90s 监控放大窗口未收口）。
- `save_batch_state` 布尔契约未被约 30 处 TP/SL 更新与最终提交调用消费（沿 §15.5）。
- `test_v64_p3_lifecycle.py` 6 项既有失败待独立排查。
- §18.7 的两条新发现（测试污染 patrol.log、测试无隔离）待另起批次。

---

## 19. 第十六轮（合法 JSON 的结构损坏 + 门禁口径 + 测试日志隔离）

> ChatGPT 对 `adb6225` 给 **78/100**：认可 `3df36f9` 与 `e834dcb`，但指出
> "全链路完成、全部验收通过"仍超出证据。三条意见逐条复核，**全部成立**；
> 另外它对 M5 证据的三条方法论质疑也成立，本节一并补证。

### 19.1 P1：合法 JSON 但内部结构损坏 —— 反例成立，且比报告的更重

`load_all_states()` 旧实现只校验**根节点是 dict**，于是下面三种文件都能通过：

| 载荷 | 旧实现行为（RED 实测） |
| --- | --- |
| `{"BTC/USDT:USDT": []}` | `([] or {})` → `{}` → `.get(batch_id)` → `None` → **`return True`**（谎报已清理，且 `_state_corrupted=False`，`_persist_states` 会照常覆盖抹掉现场）|
| `{"BTC/USDT:USDT": "x"}` | `"x".get(batch_id)` → **`AttributeError` 抛在 `_state_lock` 内**（ChatGPT 未发现，比误报更严重）|
| `{"BTC/USDT:USDT": {"b1": 123}}` | 靠墓碑写入的 `try/except` 兜底返回 `False`，**不是有意设计** |

RED 实测：`test_m2_m4_maintenance.py` 新增 `test_clear_rejects_valid_json_with_corrupt_inner_structure`
在修复前 `Ran 26 tests ... FAILED (failures=2)`。

**修复落点选在唯一读取入口 `load_all_states`，而不是 `clear_batch_state` 局部补丁**，理由：

1. 该函数的 docstring 本身就是"调用方读取返回值前必须先判 `_state_corrupted`"的
   契约所有者，在这里补校验等于把契约补完整，而不是给某一个调用方打补丁；
2. 修在读取层 → 第十五轮已实现的 Fail-Closed 路径**自动生效**：`clear` 返回 `False`
   + 锁外 critical + 磁盘原样保留；`_persist_states` 拒绝覆盖写入；
3. 保护**所有**调用方（`save`/`recover`/`finalize`…），不只 `clear`。

校验深度到不变量 `{symbol: {batch_id: dict}}` 两层，任一层类型非法即
`_state_corrupted=True` + `_state_corruption_detail` 写明是哪一层哪个键 +
返回占位 `{}`。docstring 的三态里 ③ 从"读取失败/根非 dict"扩为"读取失败 / 根非
dict / **内部节点类型非法**"，因为"根是 dict 不代表内容可信"。

**爆炸半径已核**：全库唯一 symbol 节点写入是 `all_states[symbol] = {}`（dict）；
6 处测试写 `trade_state` 的 payload 全是合法两层结构；`bot_runner.py:2743` 与
`trader_260725.py:5353` 早已把 `_state_corrupted` 接进 `recover → False → _ready=False`，
故内层损坏同样落在"禁止接管/补单/新建"这条既有 Fail-Closed 上，不新增分支。
修复后 `Ran 26 tests ... OK`。

### 19.2 P2：门禁默认 PASS 含义偏宽 —— 已改成三态结论 + 基线漂移检测

ChatGPT 的两点都成立：

1. `BASELINE-FAIL` / `NOT-VERIFIED` 不阻止最终打印裸 `PASS`；
2. 基线脚本**只要仍返回 1 就被接受**，失败项从 6 个涨到 8 个照样过——退出码比对
   对"恒为非 0"的脚本是无效校验。

`run_test_gate.py` 对应两处改动：

- **结论三态**：只有 `FAIL=0 ∧ BASELINE-DRIFT=0 ∧ NOT-VERIFIED=0 ∧ BASELINE-FAIL=0`
  才输出 `ALL-GREEN`；否则输出 `COMPLETED-WITH-EXCEPTIONS —— 运行完成，但**不是全绿**`；
  有真回归/漂移/哨兵变化则 `FAIL`（rc=1）。任何情况下都不再出现孤立的 `PASS`。
- **新增 `BASELINE-DRIFT`（致命）**：登记 `BASELINE_GREEN = {'test_v64_p3_lifecycle.py': (3, 9)}`
  并从脚本 stdout 正则解析 `GREEN: n/m` 实测值比对。已验证该文件 L535 打印
  `GREEN: 3/9`、rc=1，与登记一致。**变好或变坏都判漂移**——变好说明基线该摘除，
  变坏说明真回归，两个方向都必须暴露。

### 19.3 P3：测试污染生产日志 —— 已修，并给出实测

`patrol.log()` 会追加进生产 `logs/patrol.log`，于是测试输出顶着与真实告警一字不差的
`✅ 邮件告警已发送`。全部 **8 个**触碰 `patrol.send_email / alert / run_check` 的
调用点（`test_email_channels.py:42/55/67/85`、`test_health_progress.py:40`、
`test_r0_notify_chains.py:400/416/429`）现在**全部 patch `patrol.log`**，并保留原验证
意图（断言"确实记录了成功"，只是记到被 patch 的 log 上）。

**实测**：单独跑 `test_email_channels.py` + `test_r0_notify_chains.py` 前后
`logs/patrol.log` 字节数 `69446 → 69446`；整轮门禁（约 7 分钟，含这 8 个文件）跑完
字节数仍 `69446`。

**存量污染如实登记，未删除**（改写生产日志属破坏性动作，需另行授权）：
`logs/patrol.log` 全文件含测试标记 **142 行**，其中 2026-09-26 当日 **20 行**
（`邮件告警已发送` 10 + `邮件已跳过` 10）。

### 19.4 对 M5 证据三条质疑的补证

ChatGPT 说"源码证据中没有现场日志/邮箱/状态文件，不能独立确认"。三条逐条落到源码：

| 质疑 | 补证 |
| --- | --- |
| `trade_state.json` mtime 不能证明日报状态已保存 | 直接给出 `.daily_report.state.json` 内容 `{"date":"2026-09-26","retry_date":"2026-09-26","retry_count":0,"done":{"tg":true,"email":false}}`，mtime `08:30:51`。`_persist_daily_report_state()` 写的正是 `date = self._last_daily_report_date`，故该文件**就是**状态机自己记录的"今日已发" |
| `08:30:51` 是不是测试写的 | **是生产写的**：`_daily_report_loop` 的重试窗是 `08:05..08:30 每 5 分钟`，**每次尝试后都 `_persist_daily_report_state()`**。反证：单独跑 5 个嫌疑测试（`t1c_fees` / `r0_notify_chains` / `v62_staged` / `v64_partial` / `p5_closecancel`）mtime **均未变** |
| "不重发"不能只靠日志缺行 | 是**机制保证**：`_try_daily_report_once` 首行 `if self._last_daily_report_date == today: return 'idle'`，`'idle'` 不打印；`_load_daily_report_state_into()` 又把文件里的 `date` 还原回 `_last_daily_report_date` → **08:20 重启后同样不会重发**。实测 `[日报]` 全日**仅 08:05:28 一行** |
| "已确认送达"只是发送函数返回成功 | 承认，故不单靠这行。且 `_daily_report_done[ch]` 只在 `result.get(ch) is True` 时置位 |
| 没有 `TG 告警` 行不足以排除邮件 | 补三条独立路径：① 运行中配置横幅 `DAILY_REPORT_EMAIL_ENABLED=False` → `_daily_report_required_channels()` 返回 `()` → `required=("tg")`，**邮件通道从未被尝试**，`done.email:false` 是设计而非失败；② `健康巡检` 里另两条能直呼邮件的分支当日标记 **`通道自检=0`、`开机自启=0`**；③ 巡检 `fail/ERROR/⚠` 各 0、`watchdog.log` 末条停在 `08:20:18`、心跳 `restarts=0 fatal_alert=null` → 无 health/critical 事件可绕过 `EMAIL_ALERT_ONLY_WITH_POSITION` |

**仍标 NOT VERIFIED**：邮箱本身我读不到，"未收到邮件"无法独立复核。

**一处对 §18.4 的更正**：该节写的"`.notify_queue/` 前后均 0"是**无意义的观察**——
该目录**根本不存在**（D-010 已迁移），`os.listdir` 恒抛 `FileNotFoundError` 被吞成 0。
这条不构成任何隔离证据，予以撤回；替代物见 §19.2 的生产哨兵——但那**只是事后检测、
不是隔离**（判 FAIL 时写入或外发已经发生），其边界与第十七轮的收窄见 §20。

### 19.5 新发现：`bot_runner` 把两种失败混进同一个 `except`（未修，仅登记）

`bot_runner.py:2855-2865`：

```python
try:
    await _process_notify_queue_once(...)
    write_progress('control', _HEALTH_INSTANCE_ID, ...)
except Exception as e:
    logging.warning(f"⚠️ 处理通知队列失败: {e}")
```

`write_progress()` 抛错时，前面的 `_process_notify_queue_once()` **其实已经成功**，
日志却说"处理通知队列失败"，把运维往错误方向引；反过来真·队列处理失败也与进度写
失败无法区分。今日 08:31:51 / 08:55:00 两条即属后者：真实原因是
`.bot_health\control.json` 原子替换 `WinError 5 拒绝访问`。
`09-23 / 09-24 / 09-25` 各 **0 次**，今日新增 2 次，**成因未定位**
（无法排除并发读者持有该文件），不猜。通知本身未丢失（`_notify_queue` 处理在前、
进度写在后，且异常被吞只降级为一条 warning）。

### 19.6 门禁实测（本轮，可由单次运行复现）

日志：`logs/gate_round16_final.log`（`python run_test_gate.py`，rc=0）。

| 项 | 实测 |
| --- | --- |
| pytest | **83 passed, 3 subtests passed**，exit=0，11.6s |
| 脚本式 50 项 | **PASS 48 \| BASELINE-FAIL 1 \| NOT-VERIFIED 1 \| BASELINE-DRIFT 0 \| FAIL 0** |
| `--ignore` 展开 | 6 个送审附件目录，0 collection error |
| 生产 Bot 运行中检测 | `pid=[38636, 33968, 42176, 46752]` —— 已提示 |
| **生产哨兵前后指纹** | `trade_state.json` `(14245, 308e87f3…)`、`.daily_report.state.json` `(106, e4043f6d…)`、`logs/patrol.log` 测试标记 `142` → **三项全部未变** |
| **门禁结论** | `COMPLETED-WITH-EXCEPTIONS —— 运行完成，但**不是全绿**`（rc=0） |

- `BASELINE-FAIL`：`test_v64_p3_lifecycle.py` rc=1，`GREEN: 3/9` 与登记的 `BASELINE_GREEN=(3,9)` **一致 → 无漂移**
- `NOT-VERIFIED`：`test_orphan_guard.py` rc=42（Bot 持互斥体，设计使然；已于停机窗口
  08:19 单独取得 5/5、exit=0 的实证，故证据完整，只是不计入门禁通过计数）
- **测试不再污染生产日志**：整轮门禁（约 7.5 分钟）跑完 `logs/patrol.log` 测试标记
  恒为 **142**，未新增一行；文件字节数的增量全部来自合法巡检行（`09:14:53`、`09:29:53`）
- `test_m2_m4_maintenance.py` 1.1s PASS —— 含本轮新增的 3 个子用例与
  "必须由 `_state_corrupted` 返回 `False`"的直接契约断言

### 19.7 仍未闭环

- 第 3 个活跃批次信号**继续冻结**（网络错误后 60~90s 监控放大窗口未收口）。
- `save_batch_state` 布尔契约未被约 30 处 TP/SL 更新与最终提交调用消费（沿 §15.5）。
- `test_v64_p3_lifecycle.py` 6 项既有失败待独立排查（基线 `3/9`，`f0a21d2`/`9d9526c` 一致）。
- ~~**§19.1 的修复尚未在运行中的 Bot 生效**：进程 08:20:18 启动，晚于该次编辑 →
  需重启才加载~~ **已闭环**：2026-09-26 **09:38:38** 受控重启，`09:38:43` 打出
  「历史状态校验完成，系统 READY」，`trader_260725.py` mtime `09:33:11` 早于启动
  → 新代码已加载，结构校验（`load_all_states` L2464/L2472）生效。实证见 §20.7。
- patrol.log 存量 **142 行**测试标记（当日 20 行）待授权清理——改写生产日志属破坏性动作。
- §19.5 的 `control.json` `WinError 5` 成因待查（截至 09:31 未复现，仍为 2 条）。

---

## 20. 第十七轮：退出码三态 + 基线逐项身份 + 生产运行时拒绝执行

ChatGPT 对 `5d3ab5d` 打 **85/100**，四条扣分我逐条对源码核实，**全部成立**，
其中两条是我在上一轮注释/文档里的**失实表述**。

### 20.1 四条扣分的核实结论

| ChatGPT 的扣分 | 核实 | 证据 |
| --- | --- | --- |
| **退出码仍宽**：结论写 `COMPLETED-WITH-EXCEPTIONS` 却返回 `0` | 成立 | 旧代码 `run_test_gate.py` 末行 `return 0 if overall else 1` |
| **基线漂移只检数量、不检组成**，而代码自称"组成变了也会报警" | 成立，**且是我写在 UI 标签里的失实声明** | 旧注释 `'BASELINE-DRIFT': '（失败项数量/**组成**变了 → 基线失效）'`，实现只有 `GREEN: n/m` 计数比对 |
| **生产哨兵是事后报警不是隔离**，源码"结构性保证"应收窄 | 成立，**是我过度声称** | 旧注释 `# 生产哨兵（测试隔离…）…结构性保证`、交叉审查 §26.3「同时补上**结构性隔离**」 |
| 门禁默认不拒绝执行可能触及实盘的脚本 | 成立（当时只打印一行 `⚠` 照跑） | 旧代码 `if live: print('⚠ 检测到生产 Bot 运行中…')` 后继续 |

### 20.2 P1：退出码与结论一一对应（三态 + 拒绝态）

```
0 = ALL-GREEN                   全部退出码 0，无任何待验证项
1 = FAIL                        真回归 / 基线漂移 / 生产哨兵变化 / --strict 下未验证
2 = COMPLETED-WITH-EXCEPTIONS   运行完成，仅含**已登记**的基线失败与待验证项，无回归
3 = REFUSED                     生产 Bot 运行中且未给 --allow-live，本次**未执行任何测试**
```

理由不是"多加个数字"，而是把「真失败」（1）、「跑完了但不是全绿」（2）、「根本没跑」（3）
拆开：只看 `rc` 的发布脚本把 0 当成功、把 2 当需人确认、把 3 当未执行，
不再出现「文字说未全绿、退出码却是 0」的歧义。

**这是退出码契约变更（原先恒为 0/1），需注意**：原本判 `rc==0` 即放行的调用方，
现在在存在已登记基线失败时会拿到 `rc=2`。上一轮的 `logs/gate_round16_final.log`
记录的 `rc=0` 是**旧行为**，不改写。

### 20.3 P2：基线按**逐项失败身份**比对（含自查出的第四个口子）

新增 `BASELINE_FAIL_SET = {'test_v64_p3_lifecycle.py': frozenset({6 个用例名})}`，
基线依据写在登记处：2026-09-26 在 `5d3ab5d` 实测 `rc=1`、`GREEN: 3/9`、
6 项打印 `❌ <name>: <reason>`、3 项打印 `✅ <name>`。

- 解析按行锚定 `✅`/`❌` 两个标记字符（`CASE_LINE_RE`），名字不含冒号，
  因此不依赖 emoji 编码；`GREEN: 3/9` 汇总行天然不匹配。
- **解析不出来返回 `None` 并判漂移**，不返回空集合——宁可误报一次让人去看，
  也不能在看不见组成的情况下声称"组成未变"。
- 实测反例（合成输出）：把 `r2` 由通过改为失败、`r6` 由失败改为通过，
  `GREEN` **仍是 3/9**，旧的纯计数完全失明，组成比对判定 `BASELINE-DRIFT`
  并打印 `新增失败=['r2_toctou_second_guard'] 转为通过=['r6_normal_batch_unchanged']`。

**自查出的同一不变量上的第四个口子**（ChatGPT 未指出）：原判定是
`if name in BASELINE_GREEN and rc in allowed`（p3 的 `allowed={1}`），
于是 p3 一旦**全绿 rc=0** 就跳过整个基线校验、直接落进 `elif rc == 0: PASS`——
§19.2 声称的"变好或变坏都必须暴露"在这条路上并不成立。现改为 `rc in (allowed | {0})`。
崩溃（rc=1 但无 `GREEN` 行）仍判 `BASELINE-DRIFT`（`GREEN 行未找到`），
这是**第十六轮既有**行为，本次未改分类，语义也正确：登记的 GREEN 行不再出现 = 登记失效。

### 20.4 P3：把"结构性隔离"收窄为"事后检测"（承认过度声称）

三处失实表述已改：

1. `run_test_gate.py` 生产哨兵注释 `（测试隔离…）…结构性保证`
   → `（**事后检测，不是隔离**）`；
2. 交叉审查 §26.3「**同时补上结构性隔离**」→ 改为"门禁侧的事后检测"，
   并明确列出检测不到的四条边界；
3. R0 §19.4「替代物见 §19.2 的生产哨兵」→ 补注"那只是事后检测、不是隔离"。

四条**检测不到**的边界（原文写进 `run_test_gate.py` 文件头，不再是口头保证）：

- 判 `FAIL` 时文件写入或外发**已经发生**，哨兵只是事后告诉你；
- 未列入哨兵清单的文件不在检测范围；
- "改了又改回原样"的短暂写入检测不到；
- 测试直接外发（不落任何状态文件）完全检测不到。

真正隔离 = **临时状态目录 + 独立外发通道**，登记为 `UPGRADE_TRIGGER`（未做，不声称做了）。

### 20.5 生产 Bot 运行中默认**拒绝执行**（不是警告）

```
$ python run_test_gate.py
⛔ 门禁拒绝执行：检测到生产 Bot 运行中（pid=[36404, 10864, 32656, 19056]）。
   …继续请二选一：1) 先停机  2) 追加 --allow-live
   本次未执行任何测试。
$ echo $LASTEXITCODE → 3
```

拒绝发生在 `_snapshot()` 与任何测试执行**之前**，不是跑完再提醒。
`--allow-live` 是显式接受"测试与生产并存"，接受后仍打印"哨兵是事后检测，不是隔离"。

**明确的局限**：这只是**人工同意**，不是隔离（`KNOWN_LIMITATION` 已写进源码）。
同一条 `UPGRADE_TRIGGER` 才是根治。

### 20.6 门禁自身第一次有了测试：`test_gate_baseline.py` 6/6

`run_test_gate.py` 是"判断别的测试可不可信"的那个东西，此前**零测试**——
`BASELINE-DRIFT` 判错它照样打印一套看起来正常的结论。新增 6 项：

| 用例 | 钉住的不变量 |
| --- | --- |
| `check_parse_edge_cases` | 空输出 → `None`；`GREEN` 行与杂散行不得被当成用例行 |
| `check_composition_beats_count` | **计数不变（仍 3/9）但组成变了 → 必须 DRIFT** |
| `check_count_still_caught` | 原有计数校验不能因本次改动失效（`实际 4/9`） |
| `check_rc_zero_exposes_improvement` | p3 全绿 rc=0 → 必须暴露，不能直接 `PASS` |
| `check_unexpected_rc_is_fail` | 未登记退出码（rc=2）→ `FAIL` |
| `check_registered_baseline_matches_reality` | **真跑一次 p3**，逐项结果必须等于登记的 6 项 |

最后一条即 `BASELINE_FAIL_SET` 的**基线依据的自动化复核**。
命名用 `check_*` 而非 `test_*`，与其它根目录脚本一致：pytest 收集 0 项、只在脚本段跑一次，
不重复执行。单跑 `python test_gate_baseline.py` → `GATE-BASELINE: 6/6`，rc=0，约 22s。

### 20.7 运行实测

**受控重启闭环（§19.7 第 4 条）**：`46268(cmd) / 32656(venv bot) / 19056(real bot)`
进程创建时间 **2026-09-26 09:38:38**，日志 `09:38:43` 打出「历史状态校验完成，系统 READY」；
`trader_260725.py` mtime `09:33:11` 早于启动 → 新代码已加载，§19.1 的结构校验已生效。

**本轮门禁**：日志 `logs/gate_round17.log`（`python run_test_gate.py --allow-live`，**rc=2**）。

| 项 | 实测 |
| --- | --- |
| pytest | **83 passed, 3 subtests passed in 9.78s**，exit=0，10.9s |
| 脚本式 **51** 项 | **PASS 49 \| BASELINE-FAIL 1 \| NOT-VERIFIED 1 \| BASELINE-DRIFT 0 \| FAIL 0** |
| `test_gate_baseline.py` | PASS，rc=0，22.1s（**新文件已进枚举**，50 → 51 项） |
| `--allow-live` 提示 | `pid=[36404, 10864, 32656, 19056]`，并声明"哨兵是事后检测，不是隔离" |
| **生产哨兵前后指纹** | `trade_state.json` `(14245, 308e87f3…)`、`.daily_report.state.json` `(106, e4043f6d…)`、`logs/patrol.log` 测试标记 `142` → **三项全部未变**，且与 §19.6 第十六轮**逐字相同** |
| **门禁结论 / 退出码** | `COMPLETED-WITH-EXCEPTIONS —— 运行完成，但**不是全绿**`，**rc=2** |

哨兵三项与上一轮**完全相同**是重点：两轮之间生产在 `09:38:38` 重启过，
`trade_state.json` mtime 仍停在 `2026/9/23 23:07:05`、JSON 合法（顶层 `BTCUSDT` → dict），
`patrol.log` 测试标记两轮都是 `142` → 测试既没改账本、也没再往生产日志写字。

> **证据边界（不夸大）**：`logs/` 整目录被 `.gitignore:81` 忽略、**0 个受跟踪文件**，
> 所以上面这些日志是**本地证据**，仓库里只有可复现的**命令与期望计数**；
> 复审方无法从仓库独立看到日志正文，只能重跑复现。进程树 / 心跳 / 日志同样是本地观察，
> **邮箱**读不到，"未收到邮件"维持 **NOT VERIFIED**。

### 20.8 仍未闭环

- 第 3 个活跃批次信号**继续冻结**（网络错误后 60~90s 监控放大窗口未收口）。
  **重启到达 READY、门禁改口径，都不等于这条闭环**。
- 真正隔离（临时状态目录 + 独立外发通道）未做，现为 `UPGRADE_TRIGGER`；
  `--allow-live` 只是人工同意。
- `save_batch_state` 布尔契约未被约 30 处 TP/SL 更新与最终提交调用消费（沿 §15.5）。
- `test_v64_p3_lifecycle.py` 6 项既有失败待独立排查（基线 `3/9`，组成已登记为
  `BASELINE_FAIL_SET`，任何一项变化都会判 `BASELINE-DRIFT`）。
- patrol.log 存量 **142 行**测试标记（当日 20 行）待授权清理——改写生产日志属破坏性动作。
- §19.5 的 `control.json` `WinError 5` 成因待查（成因未定位，不猜）。


---

## 21. 第十八轮：**D12 —— 门禁进程探测的 Fail-Open 收口**（第十七轮 ChatGPT P1，2026-09-26）

### 21.1 接受裁定，并复核出比裁定多的一条

ChatGPT 复核 `3f4b0f8`：88/100，唯一 P1 = `_live_bot_pids()` 探测不确定时返回 `[]`。
读代码复核**成立**，而且旧实现比裁定的描述**还多一条 fail-open 路径**：

| # | 触发条件 | 旧返回 | `main()` 的读法 | 旧实际行为 |
| --- | --- | --- | --- | --- |
| 1 | powershell 超时 25s（`TimeoutExpired`） | `[]` | 「没有 Bot」 | **照跑全部测试** |
| 2 | powershell 不存在 / 启动失败（`OSError`） | `[]` | 同上 | 照跑 |
| 3 | 非零退出码 + 空 stdout（WMI 故障、策略拦截 CIM） | `[]` | 同上 | 照跑 |
| 4 | **非 Windows 平台**（`os.name != 'nt'`） | `[]` | 同上 | 照跑（裁定未提） |
| 5 | 进程名只认 `Name='python.exe'` | 漏 `pythonw.exe` / `py.exe` 等 | — | 漏检后照跑 |

根因不是「漏了个异常分支」，而是**把「查不出来」和「确认没有」编码成了同一个值**（空列表）。

### 21.2 修法：三态 + 「空输出永远不等于没有 Bot」

1. **三态**：`LIVE_RUNNING` / `LIVE_NONE` / `LIVE_UNKNOWN`。
   `main()` 只在 `LIVE_NONE` 时继续；`RUNNING` 与 `UNKNOWN` **同样 rc=3 拒绝**，
   拒绝文案分开（后者说明「探测不出来 ≠ 没有 Bot，按最坏情况处理」）。
2. **显式哨兵**（关键）：探测脚本对两种结论都**必须显式表态**——
   命中打 `PID <pid> <进程名>`，确认零命中打 `NO_MATCH`。
   于是**空 stdout 不再是「没有 Bot」的证据**，只能是 `UNKNOWN`。
   `$ErrorActionPreference='Stop'` + try/catch → CIM 任何异常都非零退出；
   连一条进程记录都取不到（WMI 整体不可用）也非零退出。
3. **不可解析即不确定**：未预期文本、`PID` 字段非数字、同时给出 `PID` 与 `NO_MATCH`
   → 一律 `UNKNOWN`（不猜）。
4. **不再限定进程名**：改为「排除 `powershell/pwsh/cmd/conhost` 与自身 PID 后按命令行匹配」，
   覆盖 `pythonw.exe` / `py.exe` / 改名解释器；误报方向是安全的（多拒绝一次，
   文案给出 pid+进程名让人一眼甄别），且 `--allow-live` 可越过。
5. **第二信号源（超出裁定的补充）**：`.heartbeat.json` 由 watchdog 每 60s 写。
   若进程查询说「零命中」而心跳在 150s 内报 `bot_alive=true` → **两信号矛盾**
   → 判 `UNKNOWN` 拒绝。心跳陈旧/损坏/缺失 → 「无信息」，**不反证**「没在跑」。

`KNOWN_LIMITATION`：仍靠命令行里的 `bot_runner.py` / `watchdog.py` 字样识别生产进程。
`UPGRADE_TRIGGER`：出现任一「门禁拒绝但实际无生产 Bot」或「生产在跑却探测成 NONE」的现场案例，
即改为以 `acquire_instance_lock()` 用的**具名互斥体**为唯一判据（跨进程可见、不受命令行影响）。

### 21.3 证据（本轮全部实跑，非推断）

**自测 `test_gate_baseline.py`：13/13**（原 6 项 + 新 7 项，`rc=0`）

| 新用例 | 钉住的不变量 |
| --- | --- |
| `check_probe_failures_are_unknown` | 上表 1/2/3 + 空 stdout + 未预期输出 + `PID` 字段损坏，6 条统统 `UNKNOWN` |
| `check_probe_decisive_answers` | 只有显式 `NO_MATCH` 才算 `NONE`；`(pid, 进程名)` 解析；自相矛盾 → `UNKNOWN` |
| `check_probe_decisive_against_real_powershell` | **真跑 powershell**：正常脚本必须给决定性结论；4 种坏输出必须 `UNKNOWN`（打桩测不出脚本写错/哨兵拼错/CIM 类名错） |
| `check_heartbeat_overrides_impossible_none` | 「零命中 + 心跳刚报活」必须 `UNKNOWN`，不得放行 |
| `check_heartbeat_reads` | 缺失/损坏/陈旧 → 无信息且不抛异常 |
| `check_main_refuses_on_unknown_and_running` | `UNKNOWN`/`RUNNING` → **rc=3 且一次测试都不跑**，文案含 pid |
| `check_main_proceeds_only_when_confirmed_none` | 只有 `NONE` 照常跑；`--allow-live` 在不确定时放行但必须打印「无法确认」 |

**真 subprocess 五态（不打桩，逐条实跑）**：

```
✅ 正常脚本（生产在跑）        → RUNNING [(36404,'python.exe'), (10864,'python.exe'), …]
✅ 真 powershell 未预期输出     → UNKNOWN | 探测输出无法解析：'some noise'
✅ 真 powershell rc=0 零输出    → UNKNOWN | 探测输出为空（**空输出不等于没有 Bot**）
✅ 真 powershell 非零+空 stdout → UNKNOWN | powershell 退出码 1
✅ 真 powershell CIM 类不存在   → UNKNOWN | powershell 退出码 1，stderr: Get-CimInstance : 无效类 …
REAL-PS-PROBE: 5/5
```

**端到端（真实机器）**：

| 场景 | 结果 |
| --- | --- |
| `python run_test_gate.py`（生产在跑） | `⛔ 门禁拒绝执行：检测到生产 Bot 运行中（pid=36404(python.exe), pid=10864(…), pid=32656(…), pid=19056(…)）`，**rc=3**，未执行任何测试。四个 pid 与第十七轮 §20 记录**逐字相同** → 覆盖面未因改写而缩窄 |
| `$env:PATH='C:\nonexistent-dir'` 后跑门禁（即旧实现 fail-open 的第 2 条） | `⛔ 门禁拒绝执行：**无法确定**生产 Bot 是否在运行 —— powershell 无法启动：FileNotFoundError: [WinError 2] …（心跳佐证：报活）`，**rc=3**。旧实现在这里会把 51 个脚本全跑一遍 |
| 整轮门禁 `--allow-live`（本轮改动即门禁自身） | `logs/gate_round18.log`：pytest `exit=0` 14.6s；脚本式 **51 项 PASS 49 / BASELINE-FAIL 1 / NOT-VERIFIED 1 / BASELINE-DRIFT 0 / FAIL 0**（与第十七轮同分布，无回归）；`test_gate_baseline.py` PASS rc=0（23.0s，含 13 项）；结论 `COMPLETED-WITH-EXCEPTIONS`，**rc=2** |
| 生产哨兵事后比对 | 未发现改动；事后复算指纹与第十六/十七轮记录**逐字节相同**：`trade_state.json (14245, 308e87f3…)`、`.daily_report.state.json (106, e4043f6d…)`、`patrol.log 测试标记 142 行` |

### 21.4 同意裁定中的边界，并补一句

同意：生产哨兵是事后检测、`--allow-live` 仍会在实盘目录跑测试、停机 ≠ 测试与外发已隔离，
因此**不建议**把 `python run_test_gate.py --allow-live` 当日常默认命令。
补充：**本轮之后 `--allow-live` 的语义被拓宽了一点**——它现在也代表「接受探测不确定」。
所以文案在 `UNKNOWN` 时额外打印「本次**无法确认**生产 Bot 是否在运行，请先自行确认」，
避免 `--allow-live` 被误当成「已确认没有生产进程」。

### 21.5 下一步（建议，非授权）

1. 停机窗口跑一次 `run_test_gate.py`（不带 `--allow-live`）：`test_orphan_guard.py` 才能拿 `rc=0`，
   消掉 `NOT-VERIFIED`；同时验证 `LIVE_NONE` 分支在真实停机态下成立。**需要停机授权**（涉及生产暂停）。
2. `第 3 个活跃批次信号` 继续冻结。
3. 若 ChatGPT 认可本次三态口径，剩下最有价值的两条是：
   以具名互斥体替换命令行匹配（§21.2 UPGRADE_TRIGGER），以及真正隔离（临时状态目录 + 独立外发通道）。

---

## 22. 第十九轮：**门禁循环收口 + 优先级转向**（ChatGPT 第十八轮裁定，2026-09-26）

### 22.1 裁定与接受

ChatGPT 把「修得对不对」与「值不值得继续做」分开裁定：**D12 代码修复通过、可以收口**（技术 ~89/100），
但**任务优先级 ~65/100，应停止继续打磨 D12**；并点出本轮「为验证门禁又用 `--allow-live` 在生产 Bot
运行时跑了全量测试，这项测试的增量价值不值得反复承担并存风险」。

**全部接受。** 自我更正一条（不等人指出）：本轮那次全量 `--allow-live` 的作用是「证明门禁自身没被我改坏」，
这个作用**只需要一次**；把它当成门禁改动的例行验证手段，等于把门禁工程凌驾于并存风险之上。
今后的口径：门禁改动只由 `test_gate_baseline.py`（13 项，其中 `check_probe_decisive_against_real_powershell`
已经真起 powershell、覆盖真 subprocess 契约）验证，**不再为门禁改动跑全量 `--allow-live`**。
现场 PID、心跳、13/13 等数字属仓库外观察，ChatGPT 无法独立核验 —— 这点如实标注，不辩解。

### 22.2 三条「下一步」按裁决改道

| 原建议 | 裁决 | 落地方式 |
| --- | --- | --- |
| 为验证 `LIVE_NONE` 单独停机 | **不做** | 撤销。挂到**下一次计划维护**顺带跑一次 `run_test_gate.py`（不带 `--allow-live`）；`test_orphan_guard` 已有停机窗口成功记录，无需为门禁再中断 Bot |
| 设计 M1''（原子槽位抢占） | **暂缓** | 见 22.3：留档**已存在**，本轮只补一条代码级复核结论 |
| 明早新建定时任务验证日报 | **不做** | 撤销。Bot 已定时执行日报；明早只做一次**只读核对**（TG 结果 + `.daily_report.state.json` + 是否重发）。为验证一次既有调度而引入第二套调度器，是扩大运维面 |

### 22.3 M1''：留档已存在，不重复写；但补一条新事实

复核确认「迟到成功 → 重复投递」不是推测，而是**代码里已承认的缺陷**，且**有两处留档**：

- `bot_runner.py` L112-116（生产代码内注释，第八轮复审 / 复核 `d539da9`）：
  ① 崩溃邮件在 `join` 超时后若线程迟到成功，事件状态不回灌 → 下一轮**重复投递**；
  ② 无在途去重 → 慢 SMTP 时同事件可**并发多封**。M1' 实现与修法保存在分支
  `notify-m1-timeline`（**未合并、未部署**），「修复需第二轮（槽位状态机 + 失败轮次计账）后再评审」。
- 送审稿 §11-§12。

本轮**新增**一条对 M1'' 有直接影响的事实：同一套发送逻辑在生产代码里**存在两份同构实现**——
`bot_runner.py` L130-160+ 与 `trader_260725.py` L995-1026，两侧时序一致且都有
`t.join(timeout=20)` → `if t.is_alive(): return False`（「按未确认处理」）→ 调用方按失败重试，
而那一刻线程可能**已经把信发出去**。含义：M1'' 到位后**必须同时改两处**，只改一处等于缺陷仍在。
（现在不动代码，按裁决暂缓。）

### 22.4 转向目标 A：**第 3 批次**——量化事实（本轮只读，未改任何代码）

| 事实 | 证据 |
| --- | --- |
| 上限**是**代码硬闸门，但默认值 = 3 | `trader_260725.py` L1634/1636 `RISK_MAX_ACTIVE_BATCHES` 缺省 `"3"`；L1654 `total_batches >= max_batches` → 拒绝新批次。`.env` 实配 `RISK_MAX_ACTIVE_BATCHES=3`、`RISK_MAX_ACTIVE_SYMBOLS=1` |
| 「冻结第 3 批次」**没有任何代码闸门** | 全仓无 freeze/禁用第 3 批次的开关 → 纯靠**人为不发信号** |
| 正常网络下 3 批次轮询 20~30s | `_calculate_monitoring_interval()` L4015-4053：`<=2`→10~15s、`<=4`→20~30s、`<=6`→30~40s、`>6`→45~60s（M2 已消除断崖，断崖曾是 75~100s） |
| 网络错误后是**平坦三倍**，不是指数 | L7551-7553 `if consecutive_network_errors > 0: sleep_interval = min(sleep_interval * 3, 300.0)`；每轮用 base 重算 → 3 批次实盘 = **60~90s/轮** |
| `min(…, 300)` 封顶**目前是死代码** | 可达最大 base=60s（>6 批次）→ 60×3=180s < 300；当前上限 3 → 最大 90s |
| **紧随成交的首查没被 ×3 拖慢** | L7642 触发 `fast_poll_count = 3`；L7554-7556 `min(sleep_interval, 3.0)` 位于 ×3 **之后** → 快轮询覆盖退避。被拖慢的是**之后的**补建/复核轮 |
| 另有一个独立的 300s 窗口（与 ×3 无关） | L7599-7604 `AuthBlockedError` → 盲区安全模式 `time.sleep(300)` 纯本地等待 |

→ 结论：ChatGPT 说的「代码仍允许上限 3，而网络错误会把正常轮询间隔乘 3」**成立且已量化**：
真实代价是 3 批次时**每轮保护复核间隔 60~90s**，而不是「最长 5 分钟」。

### 22.5 转向目标 B：`save_batch_state` 的返回契约——量化事实

| 事实 | 证据 |
| --- | --- |
| 契约由函数自己写明 | `save_batch_state()` docstring：返回 `True` **仅表示 merge 后完整状态已成功持久化**；墓碑拒绝或写盘失败均返回 `False`；「新建批次的调用方必须以该布尔值作为 `create_order` 前的硬门」；并声明它是写盘失败的**唯一告警源** |
| **33 个调用点里只有 1 个消费返回值** | 全文件 `save_batch_state(` 出现 34 次（1 定义 + 33 调用）；唯一消费者是 L6012 `if self.save_batch_state(...) is not True: → 阻断`（正是 docstring 要求的新批次硬门） |
| 「告警」半边已闭环，缺的是**「阻断」半边** | 锁外告警 + 按真实原因措辞 + 按键去重都在函数内；调用方「只负责阻断副作用」这一半，除 L6012 外**无人执行** |

按写入字段给 32 个未消费点分类（决定修法强度，不是一刀切）。
**计数闭合**：14 + 12 + 6 = 32，加 L6012（唯一消费返回值者）= 33 = 全部调用点。

- **交易所真相指针类 14 处**——写盘失败 = 磁盘账本与交易所真实挂单**失去同步**，重启恢复会按旧指针行动：
  `protection_registry` L6162；`current_sl_id` L8375；`tp_order_id` L8664 / L10063；
  `pending_sl_orders` L7215 / L7690 / L8973 / L9741 / L9905；`close_phase` L11358 / L11650；
  `sl_failed_layers` L8242 / L9168；L9419（`batch_state_data` 收尾提交，含 `sl_failed_layers`）。
- **熔断与降级记账类 12 处**——丢失后果是重启后熔断计数归零或降级标记消失，**不是立即错单**：
  `sl_fail_count` L9771 / L9933；`tp_fail_count` L9301 / L9339；`sl_error_time` L9140；
  `monitor_error` L9463；`tp_param_invalid` 置位/清除 L2097 / L2123；`is_programmatic_cancel` L7813；
  `user_modified` L3624 / L3726 / L8789。
- **准备/收尾路径 6 处**（L3456 / L3565 / L3957 / L7188 / L11313 / L11349）**未逐点读完**，
  分类待下一轮补，不猜。

设计判断（供评审，尚未实施）：把 32 处都改成 `if not ok: <分支>` 会把 300+ 行监控循环铺满分叉，
而多数调用点的**副作用早已发生**（单子已在交易所），「阻断」无处可阻。
更可能正确的边界：只对**交易所真相指针类**引入 strict 写入 —— 失败即 `critical` + 置该批次降级位 →
**停止该批次后续保护变更**（Fail-Closed，宁可不再动保护单，也不让后续动作建在未落盘的假设上）；
记账类维持 best-effort + 函数内既有告警。此为可评审的最小方案，**不是**政策决定。

### 22.6 唯一需要用户拍板的问题（**已于第二十轮作废**，保留原文供追溯）

> ⚠️ 下列 A/B 选项已作废，裁定见 §23.1。原文保留仅为记录当时的判断，**不要再按它执行**。

`第 3 批次`要不要变成**代码硬闸门**？（当轮未改任何代码，未停机，未动 `.env`）

- **选项 A（推荐）**：`.env` 设 `RISK_MAX_ACTIVE_BATCHES=2`，随下一次计划维护受控重启生效。
  代价：少一批次的敞口；收益：把「人为不发信号」的约定变成 L1654 那个已有闸门的硬事实，
  **不需要任何新时序验收**，回滚同样是一行配置。
- **选项 B**：保持 3，则须先收口 22.4 的 60~90s 复核间隔并做异常网络下的保护发现窗口验收（需隔离环境 + 时序证据）。
- 需要授权的动作：A = 改 `.env` + 受控重启（**属生产状态变更，需明确授权**）；B = 代码改动 + 验收。

---

## 23. 第二十轮：保护单写盘门禁离线注入（4 项实测，characterization）

### 23.1 先记录本轮收口的产品决定（避免后续复审重提）

§22.6 当时提出的 A/B 选项（`cap=2` 冻结 / 保 3 但先修窗口）**已作废**，用户明确裁定：

| 问题 | 裁定 | 理由 |
| --- | --- | --- |
| 「冻结第 3 批次」要不要落成 `cap=2` | **不落**。`cap` 保持 **3** | 冻结是复审期的**运营约定**，不是业务参数；把它写进上限旋钮等于把临时约定固化成经营规则 |
| `cap` 的性质 | **容量设置**（受 API 预算约束），不是安全额度 | **目标**是让安全由时间 SLA 承担、与 N 解耦。⚠️ **现状不是这样**：现役 `_calculate_monitoring_interval()` 仍按全局活跃批次数分档（N↑ → 所有人基础间隔变长），§22.4 的 ×3 退避缺陷**也尚未修复**。「与 N 解耦」是待做目标，**不是已达成的事实**；`cap=3` 也不代表扩容安全已闭环 |
| 账户资金上限 / 日亏损上限 / 相关性约束 | **不做**，由用户自己的账本把控 | 用户明确决定；本轮及可预见未来不再作为建议提出 |
| 埋点方案（`_update_registry` 追加一行） | **撤回** | 见 §23.5：写入点不是唯一的，且借用语义 ≠ 假设还能落盘 |

由此「至少 3 个活跃批次、今后可增可减」与「不预先做三套时钟/共享巡检重写」达成一致。

### 23.2 范围与性质：characterization，不是 RED

4 项离线注入，**只加测试文件，不改任何生产逻辑、不埋点、不部署**。断言的是**代码当前实际行为**，
因此全绿——这是刻意选择：根目录 `test_*.py` 非 0 退出会被 `run_test_gate.py` 记为
`FAIL` / `BASELINE-FAIL`，留一批永久红测试只会污染门禁语义。**修法设计时再补 RED 负测。**
每条缺陷断言都在名称里标注「当前行为=缺陷」，修复后会失败以提醒同步更新，是有意的变更探测器。

> ⚠️ **「13/13 全绿」= 缺陷被成功复现，不代表保护链通过验收。** 门禁日志里这两个文件
> `PASS rc=0` 的行**不可**读成「止损写盘安全已验证」——它们钉住的正是当前两处门禁缺失。
> 真正的验收要等定点修复落地、并把表征测试改写成检验期望行为的负测之后。

### 23.3 注入方法（确定性，不靠真损坏磁盘）

`_persist_states` 的写盘链是 `NamedTemporaryFile → flush → os.fsync → os.replace`（L2519-2524），
任何异常 → 打印并 `return False`（L2527-2529）。故 **patch `os.replace` 抛错**即可确定性注入
写盘失败。`STATE_FILE` 全程重定向到临时目录，**不触碰生产 `trade_state.json`**。
**对照组 C1**（不注入）证明注入机制本身有效：`PENDING_CREATE → CONFIRMED` 正常落盘。

### 23.4 实测结果（13/13，两文件均 rc=0）

**`test_protection_write_gates.py`（8 项）**

| 用例 | 实测 |
| --- | --- |
| C1 对照组 | 无注入时 **完整链** `PENDING_CREATE → CONFIRMED` 落盘，终态恰为 `CONFIRMED`，`create_order` 1 次 |
| **T3a** | 意图写盘失败 → **`create_order` 仍被调用** |
| **T3b** | 意图写盘失败 → 磁盘 `protection_registry={}`，**无任何锚点** |
| T3c | 该保护单创建路径**未发 critical**（⚠️ 仅限该路径；未驱动新开仓信号验证风险闸门、未覆盖独立巡检 → **全系统告警结论未验证**） |
| **T3d** | 重启后 `_recheck_registry_self_heal` 调 `fetch_order` **0 次**、registry 仍 0 条 → 该路径**无机制**收编交易所已存在的孤儿 SL |
| **T4a** | 确认写盘失败 → **仍返回 `'committed'`** |
| **T4b** | 确认写盘失败 → 磁盘停在 `PENDING_CREATE`，**无 CONFIRMED** |
| T4c | 该确认路径**未发 critical**（⚠️ 同 T3c 的口径边界） |

**`test_monitor_poll_recovery.py`（5 项）**

| 用例 | 实测 |
| --- | --- |
| T1a | `fetch_open_orders` 失败轮**跳过**成交识别；后续成功轮完成识别（`fetch_order` > 0） |
| T1b | 成交被识别后**进入**补挂止损路径（入口探针命中）——⚠️ **只是「进入该路径」**，未断言 SL 被 `create_order` 挂出、未断言交易所确认 |
| T1c | 失败轮未消费状态：`is_active` 仍为真、未被置 `monitor_error` |
| **T2** | 该监控失败分支**未发 TG、未设本地「停止新风险」标志**（⚠️ 仅限该分支，**不等于全系统零告警**） |
| T2b | 连续失败只累加计数，**无升级、无次数上限**（`fetch_order` 0 次 = 成交识别整段被 `continue` 跳过） |

### 23.5 三处已确认缺陷（实测，非推断）

1. **L9677 意图门禁缺失**：`_update_registry(state='PENDING_CREATE', intent=...)` 的持久化
   结果**无人检查**（L6621 裸调用），失败后仍 `create_order` → 交易所已有 SL、磁盘零锚点。
   **与入场路径不对称**：入场有硬门（L6012 `save_batch_state(...) is not True` → Fail-Closed、
   零 `create_order`），保护路径只有注释。
2. **L6395 确认门禁缺失**：`_commit_protection_with_g3` 持锁段内直写 `CONFIRMED` 后
   `self._persist_states(all_states)` 不检查返回，仍 `return 'committed'`。
3. **轮询失败静默**：`fetch_open_orders` 普通异常分支只有计数 + `print` + `continue`，
   持续失败 = 保护补挂无限期延后，**全程无告警、无升级**。

### 23.6 NOT VERIFIED（如实标注，不猜）

- **「循环内 `create_order` 成功挂出 SL」未由自动化断言覆盖**：`_start_monitoring` 1185 行、
  依赖面过宽，测试替身保真度不足以让内层补挂走完；T1 的监控线程是被**替身缺陷**提前打断的
  （证据：输出里的 `[W1] 已写入 monitor_error`，该标记只由循环外层 `except Exception` 写入）。
  补挂路径本身由 C1 对照组直接验证（STOP_MARKET 真实挂出并落盘 CONFIRMED）。
- **重启后能否识别孤儿 SL：未获保证**。T3d 只证明 `_recheck_registry_self_heal` 这条
  registry 驱动的路径**没有**锚点可用；是否存在其它启动期对账路径（如扫描未结单）**未验证**。
- **交易所时间字段语义**（哪个字段对应哪一笔成交、分层/部分成交测哪笔）仍 `NOT VERIFIED`，
  需真实样本。

### 23.7 给修法设计的输入（不是修法本身）

修 §23.5 的 1、2 两条前**必须先回答**：

1. 状态写不进去时，**运行中的进程**如何禁止再次创建同一张止损单？
2. **重启后**如何凭原先已落盘的意图 + 交易所订单重新对账？

已核实的约束：

- 现有词表里已有「交易所存在、账本未确认」的语义（`PENDING_VERIFY(id_known=True, order_id=...)`、
  `NOT_CONFIRMED(...)`），但**不能假设磁盘故障时还能把它持久化**——`_update_registry` 自身
  也不报持久化成败，再调一次是「对同一份坏账本的第二次写入，且结果不检查」。
- 仓库内**已有正确模式**：入场路径 L6012 的「意图落盘失败 → Fail-Closed、零副作用」。
  修法应是把该模式**传播到保护路径**，而不是新造状态机。
- 告警必须在锁外（L2498-2500 已规定：不得在 `_state_lock` 内做网络 IO）。
- 必须审计所有调用方，**不能只增加第三个返回值**。

### 23.8 副作用知会

新增 2 个根目录 `test_*.py`，门禁脚本段 **51 → 53 项**。本轮**未跑** `--allow-live` 全量门禁
（第十八轮已裁定：门禁改动只由 `test_gate_baseline.py` 验证，不再为验证跑全量并存）。
生产哨兵核对见提交记录。

---

## 24. 定点修复契约（待评审，一页）

### 24.1 只保证两件事

| | 契约 |
| --- | --- |
| **G1** | 创建 SL **之前**，意图写盘未确认 → **零 `create_order`**，并阻断副作用 |
| **G2** | 创建**之后**，确认写盘未确认 → **保留「交易所可能已有该单」的事实**、**禁止盲目重建**、**锁外**发 critical |

不含监控循环改动、不含状态机词表扩展、不含启动期对账（见 24.6）。

### 24.2 覆盖面修正：同一根因是**四条**提交路径，不是两条

写契约时逐一枚举发现，比 §23.5 登记的更宽：

| # | 提交机制 | 位置 | 返回值语义（现状） | 缺口 |
| --- | --- | --- | --- | --- |
| 1 | `_update_registry` | L6621 | 返回 `new_fail_count` | **不报**持久化成败 |
| 2 | `_commit_protection_with_g3` | L6395 | `'committed'` | 写盘失败**仍**返回 committed |
| 3 | `_commit_registry_txn` | L6663 | `True`（docstring 只承诺「批次不存在 → False」） | 写盘失败**仍**返回 True。**自愈收编路径同缺** |
| 4 | `execute_signal` 批次提交 | L6162 | 裸调用 `save_batch_state` | 返回值未检查（骨架门禁 L6012 是**另一次**写，不覆盖此处） |

只修 1+2 会留下 3、4 同样的洞。**故修复必须覆盖四条**，这是把边界放在
「持久化成败必须可观测」的唯一理由。

### 24.3 条款

- **C1（G1）** `_place_prepared_orders_immediately` 在 `PENDING_CREATE` 意图写入之后、
  `create_order` 之前，必须确认意图**已落盘**；未确认 → Fail-Closed 跳过 create、
  不 Commit、不计数，critical **锁外**发出。
- **C2（G2）** `_commit_protection_with_g3` 写盘未确认时**不得**返回 `'committed'`；
  必须返回一个可区分的第三态并**携带已知 `order_id`**。上层
  `_verify_and_update_registry`（锁外）据此把该 identity 记入
  **进程内**「已知订单、未确认」集合（可复用 `PENDING_VERIFY(id_known=True, order_id=...)`
  的**语义**，但**不得假设它还能落盘**），并锁外告警。
- **C3（禁止盲目重建）** `_assert_create_allowed` 必须把该进程内集合视为
  `禁止 = {PENDING_CREATE, PENDING_VERIFY, NOT_CONFIRMED, CONFIRMED, MISMATCH}`
  的同组成员 → 同一 identity **不得再 create**。这是回答「状态写不进去时运行期如何
  禁止重建」的答案：**只能靠进程内状态**，因为那一刻磁盘不可信。
- **C4（不改共享契约）** 不得为 accomplish C1 而改变 `_update_registry` 的返回类型
  （约 60 处调用方）。允许在函数内记录一个实例级「本次持久化是否成功」标志供调用方读取
  ——与既有 `_state_corrupted` / `_defer_state_corrupt_alert` / `_tombstones_degraded`
  同范式。**不自造新持久化层。**
- **C5（告警位置）** 所有新增告警在 `_state_lock` **之外**发出，理由见 L2498-2500
  （锁内网络 IO 最坏阻塞数秒）。`save_batch_state` 仍是写盘失败的**唯一**告警源，
  新增告警须按键去重、不重复。
- **C6（不扩大范围）** 不改监控循环、不改退避、不改词表、不改 cap、不部署。

### 24.4 验收标准（改写成负测后逐条可执行）

| 负测 | 期望 |
| --- | --- |
| N1 注入意图写盘失败 | `create_order` 调用数 **= 0**；critical 已发；磁盘无该 identity 的 PENDING_CREATE |
| N2 注入确认写盘失败 | 返回**第三态**（非 `'committed'`）；进程内集合**含**该 identity；锁外 critical 已发；磁盘无 CONFIRMED |
| N3 N2 之后尝试重建同一 identity | `_assert_create_allowed` **拒绝**（不得再 `create_order`） |
| N4 注入 `_commit_registry_txn` 写盘失败 | **不返回 True** |
| N5 `execute_signal` L6162 写盘失败 | 批次提交被判定为未确认并告警 |
| C 对照组 | 无注入时 `PENDING_CREATE → CONFIRMED` 完整链与现有行为**逐字一致** |
| R 回归 | 现有 registry / 重启语义 / 自愈相关测试全绿 |

### 24.5 落地顺序（单批次、可 revert）

1. C1+C4（意图门禁）→ N1
2. C2+C3（确认第三态 + 进程内防重建）→ N2、N3
3. 补 24.2 的第 3、4 条 → N4、N5
4. 全量回归 → 送审（**不部署**）

### 24.6 明确不解决（KNOWN_LIMITATION）

- **重启后仍可能认不出孤儿 SL**：若两次写盘都失败，磁盘上零锚点，进程内集合随进程消失。
  需启动期拿交易所未结单做对账（`_self_heal_no_id` 是候选），属**另一批次**，不在本契约内。
  当前状态是**未获保证**，不得当作已覆盖。
- 24.2 第 1 条的约 60 个 `_update_registry` 调用点中，**只有保护单创建前的那个**需要
  Fail-Closed 阻断；其余调用点是否需要同等处理属审计范围，本契约不预先裁定。



---
