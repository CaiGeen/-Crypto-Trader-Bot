# v6.2 设计稿 v2.1（收敛增量）— 待用户过目后送审，未动生产

> 本稿是 **v2 的增量修正**（ChatGPT：架构方向批准、D-4/D-5 批准，但 4 个设计层连接处须先修）。
> 按铁律，ChatGPT 本轮全部声明已逐条独立源码核实：**11 项全部实锤，零证伪**（§零）。
> 只覆盖 4 件事 + 连带修正 + 4 个新测试反例；v2 其余章节不变。
> 文档纪律：`python` 块完整可解析（零 Ellipsis）；生产摘录用 `python-frag`。

---

## §零 本轮核实结果（11 项全实锤）

| # | ChatGPT 声明 | 独立核实证据 | 结论 |
|---|---|---|---|
| 1 | registry `layer` 是**原始 signal 层号**，不是压缩列表 idx；v2 逐 idx 构造 identity 查错层（P0） | trader L3141 `for idx, (...) in enumerate(signal.entries)` + L3148-3157 价格不合法 `continue`（跳层）+ L3172 `entry_orders.append`（压缩）+ L3178-3181 registry identity 用原始 idx。**先例**：`_rebuild_entry_orders_from_registry`（L4410-4435）按 `int(entry['layer'])` 升序重建链，从不拿压缩 idx 猜 layer | ✅ v2 §2.2 helper 设计错误 |
| 2 | zero-filled + 部分撤单失败，v2 仍会杀掉监控线程（P0） | L4858-4865：`batch_filled_count == 0` 且磁盘 `is_programmatic_cancel or pending_close` → **break**（只看磁盘标志，不看 🗑️ 返回值）；L6324 同标志还导致监控跳过 SL/TP 维护。v2 zero-filled 分支先写标志再 return False → 下一轮监控即退出，E2 无人管辖 | ✅ 成立 |
| 3 | D-4 永不压缩的连带：`pending_count = len(entry_orders) - last_filled_count`（L6866）与文案（L6918）用 list 长度推断 pending | L6864-6868、L6918 | ✅ 成立 |
| 4 | prefix 假设：`sum(target_amounts[:last_filled_count])` 默认前 N 层即仓位；非连续成交（hole）时破碎 | L4552-4553 `filled_layers/canceled_layers = [False]*len(entry_orders)`（bitmap）、L4659-4666 按层计数、**L6226 `last_filled_count = batch_filled_count`**（纯计数非 bitmap）。hole 例：E4 canceled、E5 成交 → lfc=4 → `sum(target[:4])` 算进 E4 漏掉 E5 | ✅ 成立（隐含不变量需显式化） |
| 5 | `_get_registry_entry` 生产不存在；registry 就存在批次 dict 内 | grep 无定义；L4421 `b.get('protection_registry', {})`、L3137 skeleton `'protection_registry': skeleton_registry` | ✅ 成立 → v2.1 直接从 claimed 快照构建只读链（更符合 D-5） |
| 6 | `_set_close_reason_if_current` 现实现无条件覆盖 reason，无 first-abnormal-wins | v6.1 helper L390 `b['close_reason'] = reason` 无条件 | ✅ 成立 |
| 7 | 限价函数有自己的 outer except（`close_order_placed=True` → critical + return），无 reason 切换 | L7656-7665 | ✅ 成立 |
| 8 | cleanup=PENDING 文案「无需人工干预」与 settlement_stuck 周期 critical 矛盾 | L7100-7104 文案 | ✅ 成立 |
| 9 | helper 计数 13 正确 | 11 + `_pending_entry_ids_for_gate` + `_commit_limit_close_order_if_current` | ✅ |
| 10 | LIMIT crash window：BEGIN 写 `limit_pending_normal` → create → 【崩溃】→ commit 未跑 → 磁盘正常态冻结静默 | 送审稿 L813-814（BEGIN 传入 `limit_pending_normal`）；create（L7584-7594）与 commit 之间无任何落盘 | ✅ 成立 |
| 11 | B-lite 的 `pos_before=0.0` 不破坏六态 verdict | `_confirm_close_filled` 主依据是指定 order ID 的 `fetch_order`，pos_before 参与二级敞口交叉检查 | ✅ 接受，不改 |

**D-4 措辞修正（采纳 ChatGPT 版）**：`entry_orders` 作为 **positional/audit ledger**，本轮永不压缩；**terminal 与 pending 状态不得再由 list 长度推断**（ID 留档 ≠ 订单仍 pending）。

---

## §Δ1 registry 恢复重做（修 P0-①，含删除 orphan scan）

### 1.1 算法（替换 v2 §2.2 helper 全部内容）

禁止用压缩 idx 构造 identity。改为：**从 claimed `b_data['protection_registry']` 扫描 → 按 layer 升序构建「真实创建过的 ENTRY 链」→ 三重一致性证明 → 取尾部为待验证集**。

```python
    def _pending_entry_ids_for_gate(self, symbol: str, batch_id: str,
                                    b_data: dict, last_filled_count: int):
        """gate 的 pending ENTRY ID 视图（v2.1：registry 层号坐标系）。

        🔒 v2.1（P0 修正）：registry 的 layer 是【原始 signal.entries 层号】
        （创建循环 L3141 enumerate(signal.entries)，跳层 continue 后 registry
        仍用原始 idx；entry_orders 只 append 成功层 = 压缩列表）。两者不保证
        同坐标系 —— v2 用压缩 idx 构造 identity 会查错层。先例：
        _rebuild_entry_orders_from_registry（L4410-4435）正是按 entry['layer']
        升序重建链。本 helper 复刻同一坐标系：
          ① 扫 claimed b_data['protection_registry']：role=ENTRY 且有 order_id
            （不限 state——PENDING_VERIFY/CONFIRMED/TERMINAL 都是真实创建过的单）
          ② 按 layer 升序 → registry_ids（真实创建顺序链）
          ③ 一致性证明：len(registry_ids) == len(target_amounts)
            且 entry_orders == registry_ids[:len(entry_orders)]
            （registry 与台账必须是同一条 ENTRY 链，否则 Fail-Closed）
          ④ 待验证 = registry_ids[last_filled_count:]
        全程只读 claimed 快照（D-5），零交易所 API、零生产 helper 依赖。
        返回 (ids: list[str], recoverable: bool, chain: list[str])。
        """
        reg = b_data.get('protection_registry', {})
        target_amounts = b_data.get('target_amounts', []) or []
        entry_orders = b_data.get('entry_orders', []) or []
        if not isinstance(reg, dict):
            return [], False, []
        confirmed = []
        for identity, entry in reg.items():
            if not isinstance(entry, dict):
                continue
            if entry.get('role') != 'ENTRY':
                continue
            oid = entry.get('order_id')
            if not oid:
                continue
            confirmed.append((int(entry.get('layer', 0)), str(oid)))
        confirmed.sort(key=lambda x: x[0])
        chain = [oid for _, oid in confirmed]
        # 一致性证明：registry 链必须与台账计划层同长、与现存台账前缀一致
        if len(chain) != len(target_amounts):
            return [], False, chain
        if entry_orders != chain[:len(entry_orders)]:
            return [], False, chain
        ids = chain[last_filled_count:]
        return ids, True, chain
```

### 1.2 gate 接入（替换 v2 §2.2 gate 段；**orphan scan 删除**）

```python
        entry_orders = b_data.get('entry_orders', []) or []
        if (len(b_data.get('target_amounts', []) or []) > last_filled_count
                and len(entry_orders) == last_filled_count):
            # 🔒 v2.1（P0 修正 / D-1 裁定）：legacy 截断形状 → registry 链恢复。
            # symbol-wide orphan scan 删除（ChatGPT §五）：known 集只含目标批次
            # 会把同 symbol 其他合法批次的单误判成 orphan；且 open-orders 快照
            # 看不见已成交单，永远不能构成放行或否决的充分证据。
            # registry 链恢复成功 → 逐 ID 验证（主判据）；失败 → Fail-Closed。
            rec_ids, recoverable, _chain = self._pending_entry_ids_for_gate(
                symbol, batch_id, b_data, last_filled_count)
            if not recoverable:
                self.send_tg_notification(
                    f"🚨【资金安全】截断台账无法与 protection_registry 对上链！\n"
                    f"🆔 批次: {batch_id}\n"
                    f"⚠️ registry ENTRY 链与 target_amounts/entry_orders 前缀"
                    f"不一致，不能归因缺失层，请立即人工 reconcile！",
                    level='critical')
                return False
            pending_ids = list(rec_ids)
        else:
            pending_ids = [oid for idx, oid in enumerate(entry_orders)
                           if idx >= last_filled_count and oid]
        if not pending_ids:
            return True
```

（后续逐 ID cancel + `fetch_order` 终态验证循环不变——消费 `pending_ids`。）

---

## §Δ2 zero-filled 分流重做（修 P0-②）

### 2.1 规则

> zero-filled（`last_filled_count == 0`）批次：**只要存在任何未确认 terminal 的 pending ENTRY，就绝不写 `pending_close` / `close_phase=1` / `is_programmatic_cancel`**——批次保持 ACTIVE，监控继续；只有 pending 区**逐 ID `fetch_order` 确认 terminal** 后才允许进入终止形态。

### 2.2 🗑️ `cancel_open_orders` zero-filled 分支（替换 v2 §1.2 对应段）

（摘录：落点为该函数 zero-filled else 分支，正式 diff 中随完整函数区过检。）

```python-frag
        else:
            # 🔒 v2.1（P0 修正）：zero-filled 终止标志的写盘前置 = 逐 ID terminal 确认。
            # v2 先写 pending_close/close_phase 再 return False —— 监控看磁盘不看
            # 返回值（L4858-4865 break），E2 仍 active 却失去实时管辖。
            # INV-1v2 措辞本身要求「交易所确认终态」——代码必须同样遵守。
            # 🔒 存量失败 ID 先计入未确认集合，再对全部 pending ID 逐一定案
            unresolved = list(cancel_failed_ids)
            # 逐 ID 终态确认（cancel 成功 ≠ terminal，须 fetch_order 定案）
            for order_id in entry_orders:
                try:
                    o = self._safe_api_call(self.exchange.fetch_order, order_id,
                                            target_symbol, retries=1)
                    st = (o or {}).get('status')
                    filled = (o or {}).get('filled')
                    if st in ('canceled', 'expired', 'rejected') and filled == 0:
                        continue  # terminal，安全
                    unresolved.append(order_id)
                except Exception:
                    unresolved.append(order_id)  # 查询失败 = 未确认，宁可保守
            if unresolved:
                # 🔒 存在未确认终态的 ENTRY → 批次保持 ACTIVE，监控继续管辖。
                # 绝不写 pending_close/close_phase/is_programmatic_cancel。
                target_b_data.pop('is_programmatic_cancel', None)
                self.save_batch_state(target_symbol, batch_id, target_b_data)
                return False, (f"⚠️ 批次 `{batch_id}` 撤单后仍有 "
                               f"{len(unresolved)} 张 ENTRY 未确认终态"
                               f"（{unresolved}，ID 已保留，监控继续）\n"
                               f"💡 请重试或人工核对")
            # 全部 terminal → 才允许进入终止形态（entry_orders 原样保留，D-4）
            target_b_data['pending_sl_orders'] = []
            target_b_data['pending_close'] = True
            target_b_data['close_phase'] = 1
            self.save_batch_state(target_symbol, batch_id, target_b_data)

            result_msg = (
                f"🗑️ **撤单完成**\n\n"
                f"🆔 批次：`{batch_id}`\n"
                f"🪙 标的：`{target_symbol}`\n"
                f"📊 已撤销：{len(cancelled_ids)} 个挂单\n"
                f"├─ 层数：{cancelled_layers}\n"
                f"├─ 订单ID：{cancelled_ids}\n"
                f"📊 当前持仓：0\n"
                f"📊 剩余待成交层数：0（全部 ENTRY 已确认终态）\n\n"
                f"💡 批次已无成交层，监控将自然退出"
            )
```

（`last_filled_count > 0` 分支不受影响——监控不退出，v2 版本保留。）

---

## §Δ3 D-4 连带修正：pending 计数事实化 + `-2011` 幂等特判

### 3.1 撤单循环加 `-2011` 特判（否则永不压缩后 terminal 层每轮都被当成「撤单失败」）

```python
        cancel_failed_ids = []
        already_terminal_ids = []
        for idx in reversed(range(last_filled_count, len(entry_orders))):
            order_id = entry_orders[idx]
            try:
                self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol, params={'stop': True})
                cancelled_ids.append(order_id)
                cancelled_layers.append(idx + 1)
                print(f"  └─ 已撤销第 {idx + 1} 层挂单: {order_id}")
            except Exception as e:
                if '-2011' in str(e) or 'Unknown order' in str(e):
                    # 🔒 v2.1（D-4 连带）：ID 留档 ≠ 订单仍 pending。
                    # 永不压缩后，此前已撤/已终态的层会被再次尝试，-2011
                    # 是预期幂等信号，绝不能计入失败。
                    already_terminal_ids.append(order_id)
                    print(f"  └─ 第 {idx + 1} 层挂单已不存在（视为已终态）: {order_id}")
                else:
                    cancel_failed_ids.append(order_id)
                    print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单失败: {order_id} ({e})"
                          f"（ID 保留在台账，监控继续管辖）")
```

（`reversed` 的理由见 §Δ4。）

### 3.2 计数与文案事实化

- 入口 `pending_count = len(entry_orders) - last_filled_count`（L6866）：**语义降级**为「待撤尝试区大小」，仅用于 `pending_count <= 0` 的提前返回判断；该判断下已 terminal 层会进循环并被 `-2011` 幂等吸收，无资金影响；
- 结果文案（L6910-6920 / v2 版）：「剩余待成交层数」改为「本轮待撤尝试 N 层；未确认终态 M 张（ID 已保留）」，N=len(entry_orders)-lfc、M=len(cancel_failed_ids)+len(unresolved)；
- D-3 返回值：`cancel_failed_ids` 非空 → False（不变）；`already_terminal_ids` 不影响返回值（幂等成功路径）。

---

## §Δ4 prefix 不变量显式化 + 撤单方向策略（问题 4）

### 4.1 新增 INV-3a（prefix invariant，写入设计文档）

> **INV-3a**：大量调用方以 `sum(target_amounts[:last_filled_count])` 等形式假设「前 N 层 = 已成交仓位」（生产 L6226 `last_filled_count = batch_filled_count` 是纯计数、非 bitmap）。**非连续成交（hole：成交层之下存在 canceled 层）会破坏该假设。** 本轮不重构 filled bitmap，但：①producer 不得主动制造 hole；②monitor 检测到 hole 必须 critical 暴露；③测试必须覆盖 hole 场景。

### 4.2 撤单方向策略（producer #1 与 #2 统一）：**从最高层往最低层撤 + 遇第一张无法撤下的立即停止**

- 依据：canceled 层若总在所有 active 层之上，filled bitmap 永远是**前缀连续**的（成交只发生在 active 区下缘）——`last_filled_count` 模型保持成立；
- 现状从低往高撤（L6879 / L4826）+ 失败继续，会制造 hole（E4 canceled + E5 成交）；
- 实现上即 §Δ3 的 `reversed(range(...))` + `cancel_failed_ids` 非空时 `break`（不再向更低层继续）：

```python-frag
            else:
                cancel_failed_ids.append(order_id)
                print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单失败: {order_id} ({e})"
                      f"（ID 保留在台账，监控继续管辖）")
                # 🔒 v2.1（INV-3a）：从高往低撤，遇阻即停——保持成交区前缀连续，
                # 不主动制造 hole。更低层保持 active，由监控继续管辖。
                break
```

- **monitor 手动撤单路径（producer #2）同改**：`reversed(range(len(entry_orders)))` 循环 + 失败 break + v2 §1.3 的「只标记不删」不变；
- **monitor hole 检测**：监控轮询内（`filled_layers` bitmap 已就绪处）增加——

```python
                _hole_idx = [i for i in range(min(batch_filled_count, len(filled_layers)))
                             if not filled_layers[i]]
                if _hole_idx:
                    # 🔒 v2.1（INV-3a）：成交计数 N 但前 N 层存在未成交 = hole。
                    # last_filled_count 的 prefix 假设已破碎，必须 loud 暴露。
                    self.send_tg_notification(
                        f"🚨【资金安全】批次出现非连续成交（hole）！\n"
                        f"🆔 批次: {batch_id}\n"
                        f"📌 已成交 {batch_filled_count} 层，但层位 {_hole_idx} 未成交\n"
                        f"⚠️ 按层数平仓/结算的数量口径可能失真，请人工核对持仓与台账！",
                        level='critical')
```

（检测为**纯暴露**，不改 last_filled_count 语义——本轮不重构 bitmap，ChatGPT 已认可此范围。）

---

## §Δ5 reason 改为 first-abnormal-wins（问题 6）

`_set_close_reason_if_current` 增加一条规则（修改既有 helper，**不新增**）：

```python
            cur_reason = b.get('close_reason')
            if cur_reason not in ('market_confirming', 'limit_pending_normal',
                                  'limit_creating'):
                # 🔒 v2.1（first-abnormal-wins）：当前已是异常态 → 不覆盖。
                # 通用 outer except 的 settlement_error 不得抹掉
                # market_confirm_unknown / rollback_rejected 等第一现场原因。
                # 对调用方返回 True：磁盘已 abnormal，本次切换的目标已达成。
                return True, f'reason_already_abnormal（{cur_reason}）'
            b['close_reason'] = reason
```

- 白名单 = 正常持久态（`market_confirming`、`limit_pending_normal`）+ 中间态（`limit_creating`，见 §Δ7——它必须可被 commit 覆盖之外的路径保护，但 abnormal 保护规则天然适用）；
- `limit_creating → limit_pending_normal` 的正常推进**不经过本 helper**（由 §Δ7 的 commit helper 在锁内直接写），无冲突。

---

## §Δ6 限价 post-create outer except 补 reason（问题 7）

生产 L7659 分支（限价函数 own except）加 reason 切换（市价 §3.5 已有，限价补齐）：

```python
            if close_order_placed:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'settlement_error')
                self.send_tg_notification(
                    f"🚨【资金安全】限价平仓单已挂出但后续流程异常（未回滚关闭标记）！\n"
                    f"🆔 批次: {batch_id}\n💡 原因: {str(e)[:150]}\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n")
                    + f"⚠️ 限价单仍在交易所，请立即人工核对！",
                    level='critical')
                return False, f"❌ 限价平仓后续流程异常（平仓单已创建，close_phase 保持）: {e}"
```

- commit 成功后（reason=`limit_pending_normal`，白名单内）→ `settlement_error` 可写入 ✓；
- commit 前异常（reason=`limit_creating`，白名单外）→ first-abnormal-wins 保护，保持 `limit_creating` ✓ 第一现场不丢。

---

## §Δ7 LIMIT crash window：`limit_creating` 中间态 + 4 字段原子 commit（问题 10）

### 7.1 状态机

```text
BEGIN（写 limit_creating，不在正常态白名单）
  ↓ ENTRY gate
  ↓ create LIMIT 成功
  ↓ _commit_limit_close_order_if_current()（4 字段原子落盘，含 reason）
limit_pending_normal（只有 ID durable 之后才获得「正常 pending」资格）
```

- 崩溃窗口 `create ↔ commit`：磁盘停在 `limit_creating` → 重启后冻结告警按白名单外 reason **周期 critical**（不再被 `limit_pending_normal` 静默）；
- 市价路径的残留窗口（BEGIN `market_confirming` 后崩溃）：**本轮不改**——市价 create+confirm 是秒级同步流程，且 P2 观察项记录在案（重启自愈属 D-00x 范畴）。

### 7.2 `_commit_limit_close_order_if_current` 升级为 4 字段（替换 v2 §4.1 尾段）

```python
            b['limit_close_order_id'] = order_id
            b['limit_close_price'] = limit_price
            b['limit_close_mode'] = price_mode
            # 🔒 v2.1（crash window）：durable commit = normal-state transition。
            # ID 真正落盘成功的那一刻，才原子切到正常 pending 态；
            # 此前任何时点死亡，磁盘都停在 limit_creating（异常告警接管）。
            b['close_reason'] = 'limit_pending_normal'
            if not self._persist_states(all_states):
                return False, 'persist_failed'
            return True, 'committed'
```

（前置校验段不变：op_id CAS + `close_phase==1` + `pending_close` + `not settled`。`why` 枚举补充：`op_id_mismatch` / `not_in_close` / `already_settled` / `persist_failed` / `committed`。）

### 7.3 连带

- 限价 BEGIN 调用处：`_begin_close_request_if_active(..., 'limit_creating')`（替换 v6.1 的 `limit_pending_normal`）；
- `settlement_stuck` / `limit_persist_failed` 等异常 reason 表不变；
- **B-lite 不变**（§零#11 已确认 `pos_before=0.0` 安全）。

---

## §Δ8 cleanup=PENDING 文案对齐（问题 8）

```python
            if _cleanup_pending:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'settlement_stuck')
                result_msg += (
                    f"\n\n⚠️ **[残单收敛提示]** 市价平仓已成交（持仓=SUCCESS），"
                    f"但批次清理暂未收敛（cleanup=PENDING，proof 未通过）。"
                    f"系统仍会继续尝试收敛；若告警持续存在，请人工检查残单。"
                    + ('' if _rs_ok else
                       f"\n⚠️ close_reason 切换失败（{_rs_why}），请人工关注该批次。"))
```

（删除「无需人工干预」——与 settlement_stuck 周期 critical 对齐。）

---

## §Δ9 测试增量（4 + 1 个精准反例，编号接 v2 §六）

| 编号 | 场景 | RED（v2 设计若实施） | GREEN（v2.1） |
|---|---|---|---|
| R1-f | signal layer0 被 skip，registry layers=[1,2,3]，entry_orders 压缩 idx=[0,1,2] | v2 逐 idx 查 `ENTRY layer0` → 误 Fail-Closed 或错 ID | 恢复链按 layer 排序 → 恢复正确 ID；三重一致性校验通过 |
| R1-g | `last_filled_count=0`，E1 cancel 成功 / E2 unresolved | phase=1 + pending_close=True → 监控 break | **phase 仍 0、pending_close=False**、批次 ACTIVE、return False；只有全 terminal 才写终止标志 |
| R1-h | hole 场景：E4 canceled + E5 成交（filled=[T,T,T,F,T]） | —（无检测） | monitor hole 检测 critical 暴露；撤单方向策略下 producer 不再制造新 hole |
| R2-f | `market_confirm_unknown` 已成功落盘 → outer generic except 再调 `settlement_error` | reason 被覆盖 | **reason 仍 = market_confirm_unknown**（first-abnormal-wins，返回 reason_already_abnormal） |
| R3-crash-state | LIMIT BEGIN 后、commit 前模拟进程死亡（磁盘 phase=1 + reason=limit_creating，无 limit_close_order_id） | `limit_pending_normal` 静默 | `limit_creating` 不在白名单 → 冻结分类为 **abnormal** 周期 critical |

既有 R1-a~e / R2 / R3 断言随 §Δ1-§Δ7 微调（R1-d 的 registry 恢复数据源改为 claimed 快照链；R1-e 不可恢复条件变为「链长 ≠ 计划层数或前缀不一致」）。

---

## §Gate 状态

- v2 四条主线：批准（ChatGPT）✅；D-4=A / D-5=A：批准 ✅（D-4 措辞已修正）；
- 本增量落实 ChatGPT 四件事：①registry 坐标系重做 + orphan scan 删除 ②zero-filled unresolved 禁写终止标志 ③first-abnormal-wins + 限价 outer except + cleanup 文案 ④`limit_creating` 中间态 + 4 字段原子 commit；
- helper 计数仍为 **13**（`_pending_entry_ids_for_gate` 原地重写、无新增；`_get_registry_entry` 不引入）；
- 生产零改动（HEAD=e953d79）。

## §下一步

1. 用户过目本增量稿 → 送审 ChatGPT（只核 §Δ1-§Δ8 + 5 个新测试反例）；
2. 批准后出正式 v6.2 送审稿 diff（AFTER 块全量 + 落点表 + checker 名单 13 helpers + `cancel_open_orders`/monitor 段入 `PROD_SIGNATURE_OVERRIDES`）；
3. RED→GREEN（v2 §六 + 本稿 §Δ9）→ 五道门 + 变异 → 二次送审 → 五段式落生产。
