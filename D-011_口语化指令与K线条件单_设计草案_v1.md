# D-011 口语化指令与 K 线条件单 · 设计草案 v1

> 状态：**设计定稿（v1.7），零代码改动**——Q1–Q6 已拍板；v1.5 复审修订；v1.6 外部评审修订；**v1.7 二轮外审修订（5 点全部接受）**：①快照三元组改**四元组**（v1.6 的 `signal_generation` 经代码取证为生产不存在的幽灵字段，弃用，改用 `_batch_net_position()` 净量 + `last_filled_count` + `realized_reduce_amount`，ABA 场景可捕获）；②"无法确认兜底仍持有执行权则取消自动转市价"原则原话入文 + `fallbacks` marker 持久化与启动对账通知；③"纯函数风险为零"措辞更正（易测≠不会错）；④迟到上限 30 分钟明示为产品授权参数、确认卡显示最迟执行绝对时刻；⑤TP/SL 确认卡显"原值→新值"、P1.5 与 P2 解耦可并行排期。待评审后进入 P1a 编码
> 日期：2026-09-23
> 范围：P1 口语化即时指令（DeepSeek 意图解析）+ P2 K 线收盘条件单
> 明确不含：P3 语音条（Telegram voice message STT）——DeepSeek 无语音接口，另行设计

---

## 一、背景与目标

用户希望在 TG 里用口语（自然语言**文字**输入——非命令语法、非 JSON；与语音无关，语音条属 P3 另行设计）下达交易指令，而不是记忆 `/close`、`/tp` 等命令格式。示例：

> "在下一个4小时K线收盘的时候，如果收盘时阴线就马上用最优价平仓。"

### 目标

| # | 目标 | 阶段 |
|---|---|---|
| G1 | 口语文本 → 结构化意图 → 人工确认 → 复用现有命令链执行**即时**操作（平仓/开仓信号/改TP/改SL） | P1 |
| G2 | **条件单**：确认后持久化，4H K 线收盘时本地判定条件并自动执行，重启不丢失 | P2 |
| G3 | LLM 永远不直接下单：只做"文本 → JSON"，一切交易动作走确定性代码 | 全阶段 |

### 非目标

- 不替代现有 `/close`、`/signal` 等命令（两条通道并存，命令通道永远可用，作为 LLM 故障时的兜底）。
- 不让 LLM 接触交易所 API、密钥、仓位计算。
- 不做开放式聊天/行情咨询问答（首期只做白名单意图）。

---

## 二、现状盘点（接入点实证）

### 2.1 可复用的既有能力

| 能力 | 位置 | 说明 |
|---|---|---|
| 自由文本入口 | `bot_runner.py` `handle_json_or_pending_input`（L1937 起，`MessageHandler(filters.TEXT & ~filters.COMMAND)` 注册于 L2885） | 已先查 `TEST_CONFIRM_STATE`、`USER_PENDING_INPUTS`，再落 JSON 信号解析——**NL 层插在"pending 检查之后、JSON 解析之前"** |
| 鉴权 | `is_authorized()`（该 handler 第一行即校验） | 单用户白名单，NL 层天然继承 |
| 命令注册 | `bot_runner.py` L2863-2885 | `/close` `/partial` `/tp` `/sl` `/signal` `/be` `/cancel` 等 |
| 平仓执行 | `trader_260725.CryptoTrader.close_position_market(batch_id) -> (bool, str)`；`close_position_limit(batch_id, price=None) -> (bool, str)` | v6.x 已内建 `close_phase==0` 原子 claim、孤儿单清理、`clear_batch_state` 全链——条件单触发也走这里，获得**执行期**竞态保护；**但"调用旧入口 ≠ 跨步骤新流程自动安全"**（v1.6 删除绝对化表述）：兜底/登记/恢复等跨步骤契约仍需 §4.6/§7 显式设计 + V1–V6 实测 |
| 限价单生命周期（Q1 兜底复用） | `_monitor_limit_close`（挂单后 3s 轮询：成交 → `_finalize_limit_full_fill` 结算；被撤/过期 → `_adjudicate_closed_limit_close` 四态裁决恢复 ACTIVE，与 `/closecancel` 同路径）；`_cancel_limit_close_order`（幂等撤限价平仓单，-2011 视同成功） | **"60s 未成交转市价"兜底直接复用这条链（§4.6）**：NL 层只新增"到期判定"与**撤单归因薄封装**（记录 cancel 返回值/-2011 以区分本线程撤单与人工撤单，§4.6.2③），裁决/恢复全走既有代码 |
| 平仓 UI 语义 | `bot_runner.py` 平仓菜单按钮（L1178-1187） | "🚀 市价平仓" = `close_position_market`；"💎 最优价挂单" = `close_position_limit(price=None)`——口语"最优价"对应**后者**（见 §4.3） |
| 改 TP/SL | `trader.update_take_profit(batch_id, price)` / `update_stop_loss(batch_id, price)` | 命令处理器内以 `run_in_executor` 调用 |
| 信号开仓 | `run_trader_execution(update, context, signal_snapshot=...)` | v6.4 快照机制防连发覆写，NL 开仓意图复用 |
| 低频 daemon 线程范式 | `trader_260725._daily_report_loop`（`__init__` L469 启动） | 条件单监听线程照此模式新建 |
| 状态持久化 | `_persist_states` → `trade_state.json`（D-009 fsync 链、崩溃恢复演练已验收） | 条件意图存储参照同等原子写标准 |
| TG 线程安全发送 | `trader.send_tg_notification`（后台线程可直接调，带 Markdown 降级） | 监听线程的执行结果通知走它，**不能**用 `safe_reply`（需 Update 对象） |
| K 线数据管道 | ⚠️ **生产代码中尚无 `fetch_ohlcv`**（`trader_260725.py` 仅注释预留 KAMA 字段；`strategies_backtest/data_loader.py` 有回测版实现） | P2 需新建，但 ccxt `fetch_ohlcv(symbol,'4h',limit=2)` 是交易所原生接口，风险低 |

### 2.2 与既有设计文档的关系

- **D-001（KAMA 跟踪止盈）** 的讨论稿里已设计过"4H 收盘检测 + 独立低频线程 + fetch_ohlcv 独立限流通道（N6）"，但 KAMA 本体**尚未在生产代码落地**。本设计不依赖 KAMA，独立实现收盘监听；若日后 D-001 落地，两者共享同一数据管道与限流通道（§7.3）。
- **v6.x 平仓竞态体系（close_phase 原子 claim）** 是本设计的执行前提：条件单线程只是另一个"调用方"，不新增任何交易所直连路径。

---

## 三、总体架构

```
        TG 用户（唯一授权用户）
              │ 口语文本
              ▼
┌─ bot_runner.py ─────────────────────────────────────────────┐
│ handle_json_or_pending_input                                │
│   1. is_authorized ✅（已有）                                │
│   2. TEST_CONFIRM_STATE / USER_PENDING_INPUTS（已有，不动）  │
│   3. 【新】NL 意图层 nl_route(text)                          │
│        ├─ 文本像 JSON({开头) → 走原 JSON 信号路径（不动）    │
│        ├─ NL_ENABLED=false / 解析失败降级 → 提示用命令       │
│        └─ 意图 JSON → validate_intent()（白名单+schema 手写校验）│
│   4. 确认按钮（InlineKeyboard，回显解析结果原文）             │
│        ├─ nl_confirm_{intent_id} → 落盘 / 执行               │
│        └─ nl_cancel_{intent_id}  → 丢弃                      │
└─────────────────────────────────────────────────────────────┘
      │ 即时意图（P1）                │ 条件意图（P2）
      ▼                              ▼
 run_in_executor              pending_intents.json（原子写，
 调用现有 trader方法           参照 _persist_states 标准）
 close_position_market/…             │
                              ┌──────┴───────────────────────┐
                              │ intent_watcher（新 daemon 线程│
                              │  类比 _daily_report_loop）    │
                              │  ① 轮询 4H 收盘（数据驱动）   │
                              │  ② 本地判定条件（阴线等）     │
                              │  ③ 状态机 pending→firing→done│
                              │  ④ 调 close_position_market/ │
                              │     limit（执行期既有保护）    │
                              │  ⑤ send_tg_notification 结果  │
                              └───────────────────────────────┘

DeepSeek API ◄── 仅在"确认前解析"一步被调用（超时/失败→降级提示）
（与交易所网络、TG 网络相互独立，故障互不影响）
```

**铁律：LLM 输出只到 `validate_intent()` 为止；`validate_intent()` 之后的所有代码路径中不再出现任何 LLM 调用。**

---

## 四、意图 Schema（P1+P2 统一）

### 4.1 顶层结构

```json
{
  "intent": "conditional_close | immediate_close | set_signal | set_tp | set_sl | query | clarify",
  "scope":  {"kind": "batch | all_active", "batch_id": null},
  "params": {},
  "trigger": null,
  "confidence_note": "LLM 对歧义的说明（仅回显给人看，不参与执行）"
}
```

### 4.2 字段约束（`validate_intent()` 手写校验，不新增 jsonschema 依赖）

| 字段 | 允许值 | 校验规则 |
|---|---|---|
| `intent` | 上表 7 项**白名单** | 不在白名单 → 整条拒绝，回复"无法识别，请改用命令" |
| `scope.kind` | `batch` / `all_active` | `batch` 时 `batch_id` 必须非空字符串；执行前还要在 `trade_state` 中核实批次存在且 active，**不存在 → 拒绝而非猜测** |
| `scope.kind=all_active` | — | 仅 `immediate_close` / `conditional_close` 允许；**确认时枚举活跃批次由用户勾选，锁定为 `target_batch_ids` 清单持久化（v1.6 评审修订：严禁触发时重新枚举扩大授权范围）**；触发时仅处理清单内仍 active 且通过身份快照复核的批次 |
| `target_batch_ids` + `batch_snapshot` | 代码生成 | **非 LLM 字段**（LLM 输出即整条拒绝）：确认时由代码写入——批次清单 + 每批**四元组**快照 `{side, net_qty, last_filled_count, realized_reduce_amount}`（v1.7 更正：v1.6 的 `signal_generation` 经代码取证为生产不存在的幽灵字段，弃用；四元组全部取自持久化批次状态——`net_qty`=`_batch_net_position()` 净量，`last_filled_count`/`realized_reduce_amount` 为 v6.4 既有账本字段，重启后从 trade_state 原样恢复不漂移）；触发时复核：批次消失→跳过；**`last_filled_count`↑（加仓，含 ABA"减仓再加回原值"——lfc/realized 双双变化必被捕获）或 side 变化→暂停该批并 TG 要求重新确认**；**仅 `realized_reduce_amount`↑ 且 lfc 不变（纯减仓）→平剩余净量**；字段缺失或组合不可解释→**fail-closed 按已变化处理**（v1.6/v1.7） |
| `params.close_mode` | `market` / `optimal_limit` / `custom_limit` | 平仓类必填（见 §4.3）；**口语未指明方式 → 不猜测**：按 optimal_limit 语义直接出 §4.6.1 四档卡，按钮即方式选择（fb=mkt 等价市价），卡首行注明"未指明平仓方式" |
| `params.price` | float > 0 | 仅 `custom_limit` 时必须为正数；`optimal_limit`/`market` 时必须为 null |
| `params.close_fallback` | 仅 `"market_after_60s"` / null | **LLM 不得输出此字段**（输出即整条拒绝）；由确认卡按钮在登记/执行时由**代码**注入（§4.6，Q1 拍板） |
| `params.value` | float > 0 | `set_tp` / `set_sl` 必填 |
| `trigger` | 见 §4.4 | `conditional_*` 必填；即时意图必须为 null |
| `intent=clarify` | — | LLM 判定信息不足时输出，Bot 回问；**信息不足类 clarify 最多 5 轮**（`NL_CLARIFY_MAX_ROUNDS`，9/23 由 3 调升），每轮须带 §4.7"理解回显+默认建议+明确问题"三件套，仍不收敛则放弃提示用命令；**风险告知类澄清不计轮**——限价类确认卡必须停留到用户用按钮明确接受，或 `NL_CONFIRM_TTL_SECONDS=300` 过期作废（§4.6，Q1 拍板） |

**拒绝而非猜测**：任何校验失败都不 partial 执行，整条回复失败原因 + 建议的等价命令（如 `/close <BatchID>`）。

### 4.3 "最优价"语义映射（对齐现有 UI）

现有平仓菜单已定义三档（`bot_runner.py` L1178-1187）：

| 口语说法 | LLM 输出 | 执行 |
|---|---|---|
| "市价平仓" / "马上卖掉" / "市价出" | `close_mode=market` | `close_position_market(batch_id)` |
| "最优价平仓" / "最优价出" / "挂单平掉" | `close_mode=optimal_limit` | `close_position_limit(batch_id, price=None)` |
| "在 68000 平" | `close_mode=custom_limit` + `params.price=68000` | `close_position_limit(batch_id, 68000)` |

Prompt 中明确写入该映射表，确认消息回显时**使用 UI 同款措辞**（"💎 最优价挂单"），避免"最优价"被 LLM 自行理解为市价。

**歧义强制澄清（v1.6 评审修订）**：

- "卖/卖掉"在**持有空仓**语境下有歧义（平空 vs 开空）→ 必须 clarify，不得默认平仓；持多仓时"卖掉"才允许映射 market 平仓。
- "在 68000 平"有歧义（挂限价单 vs 跌到 68000 触发）→ clarify 二选一：`custom_limit` 挂单 或 `close_at_or_below` 条件单，不得擅自选择。
- 所有风险提示语、条件定义回显、确认卡文案由**代码模板按已校验参数渲染**，LLM 只产出参数、不撰写文案（LLM 复述可能失真）。

**Q1 已拍板（2026-09-23）**：「最优价」= **限价挂单**（上表不变，不改成市价）。但限价类确认卡**必须**像真人一样明确告知"可能不成交"，并给出兜底选项、持续澄清直到用户完全接受——具体交互与 60s 兜底机制见 §4.6。

### 4.4 trigger（条件单，P2）

首期只支持**一种**触发器，刻意收窄：

```json
{
  "type": "candle_close",
  "timeframe": "4h",
  "condition": {"kind": "bearish_candle"},
  "deadline_klines": 1,
  "fires_once": true
}
```

- `timeframe`：首期白名单仅 `"4h"`（`1h`/`1d` 留接口，暂不开放）；用户未指明周期 → 默认 4h 并在确认卡回显；说"1小时线"等白名单外周期 → clarify 告知仅支持 4h。
- `condition`：**结构为对象 `{"kind": ...参数}`（v1.4 参数化白名单）**——判定多样性来自"白名单枚举 + 数值参数 + 多轮澄清（§4.7）"，**绝非 LLM 自由发挥**：

  | `kind` | 参数 | 判定（代码纯函数，LLM 不参与） | 覆盖的口语 |
  |---|---|---|---|
  | `always` | — | 到 deadline 即触发 | "到点就平" |
  | `bearish_candle` | — | `close < open` 严格小于；`close == open`（十字星）**不算阴线**（Q6 拍板） | "收阴线就平" |
  | `bullish_candle` | — | `close > open` 严格大于 | "收阳线就平" |
  | `close_at_or_below` | `price` float>0 | 收盘价 ≤ price | "收盘不超过 70000" |
  | `close_at_or_above` | `price` float>0 | 收盘价 ≥ price | "收盘站上 75000" |
  | `long_upper_shadow` | `preset` ∈ strict/normal/loose | **前置硬条件（v1.6 修复零振幅误判）：`range=high-low > 0` 且 `upper > 0`，否则一律不触发**（O=H=L=C 时两个"≥"会同时成立造成误判）；上影线 = `high-max(open,close)`；normal 档 ≥2×实体 且 ≥全幅/3，strict ≥3×实体，loose ≥1.5×实体（**常量集中于代码 `SHADOW_PRESETS`，可标定**） | "长上影线" |

  - kind/参数白名单外（含任意表达式）→ `validate_intent` **整条拒绝**（F14）。
  - 全部定义**原文写入 System Prompt**，clarify 时 LLM 原样引用给用户确认（§4.7），不得即兴改写数值。
  - 非收盘类触发（`price_touch` 盘中触及等）首期拒绝并说明，`trigger.type` 留扩展位。
- `deadline_klines`：**Q5 拍板（9/23）方案 A**——用户未指明时 LLM 输出 `null`，**登记时由代码归一为 `1`**（默认值由代码注入，LLM 不得自行臆造）；**区间表述不猜**（"接下来一两根"→ clarify 索确定根数——1 根与 2 根是不同授权，v1.6）；**拒绝无限等待**：用户说"一直等"→ clarify 要求给出根数上限。**deadline 计数锚点 = 登记后的第一次收盘**（数据驱动 open_time 判，§7.2）。超时未满足 → 标记 `expired` 并 TG 通知，**绝不无限期挂着不报**。确认卡回显"⏳ 将等待 N 根 4h 收盘（未满足自动失效并通知）"。
- **扫描语义（v1.6 明确）**：`deadline_klines=N` = **第 1~N 根逐根判定、首根满足即触发**；`kind=always` 例外 = **第 N 根收盘时无条件执行**。"只在第 N 根才判断"的语义首期不支持（→ clarify 改写或拒绝）。确认卡必须显示**目标收盘绝对时间（含时区**，如"2026-09-24 00:00 (+08:00) 收盘"**）**；卡片展示期间若首个目标收盘时间已过 → 卡作废，需重新确认。
- **K 线数据源（v1.6 明确）**：Binance USDT-M 永续**最新成交价** K 线（ccxt `fetch_ohlcv` 默认）；标记价格 K 线不在白名单，提到"标记价"→ clarify 告知仅支持成交价。
- `fires_once`：固定单次触发；首期不做周期性/重复条件单。

### 4.5 典型样例（用户原句的解析目标）

输入：

> 在下一个4小时K线收盘的时候，如果收盘时阴线就马上用最优价平仓。

期望输出：

```json
{
  "intent": "conditional_close",
  "scope": {"kind": "all_active", "batch_id": null},
  "params": {"close_mode": "optimal_limit", "price": null},
  "trigger": {"type": "candle_close", "timeframe": "4h",
              "condition": {"kind": "bearish_candle"}, "deadline_klines": 1,
              "fires_once": true},
  "confidence_note": "未指明批次，按全部活跃仓位理解"
}
```

若当时**多空批次混存**，确认消息中逐条列出受影响批次要求人工勾选（默认全不勾），不默认全选。

### 4.6 限价不成交澄清与 60s 兜底（Q1 拍板，P1/P2 通用）

#### 4.6.1 确认卡（限价类强制结构）

任何 `close_mode=optimal_limit / custom_limit` 的确认卡必须包含：

```
📋 解析结果
动作: 💎 最优价挂单平仓
范围: 批次 batch_xxx (BTCUSDT LONG 0.001)
⚠️ 限价挂单可能不立即成交，行情快速移动时可能一直不成交。
   建议：60 秒内未能限价成交则自动撤单、转为市价平仓。
   [💎 限价挂单·60s未成交自动转市价]   ← nl_confirm_{id}|fb=60
   [💎 纯限价挂单（可能不成交）]        ← nl_confirm_{id}|fb=0
   [🚀 改为市价平仓]                   ← nl_confirm_{id}|fb=mkt
   [❌ 取消]                           ← nl_cancel_{id}
```

- **点击按钮 = 用户"完全接受该风险"的凭证**，点击时间/选择写入 `.nl_audit.log`。
- 文本澄清（LLM clarify 或用户追问）最多 **5 轮**（全局 `NL_CLARIFY_MAX_ROUNDS`，9/23 调升）；但确认卡本身**不受轮数限制**，停留到被接受或 300s TTL 过期——"一直澄清到用户能完全接受为止"由**按钮显式接受**收敛，避免无限文本拉锯。
- 执行参数（fb 值）由**按钮 data 注入**，不是 LLM 输出——`validate_intent` 见到 LLM 给的 `close_fallback` 直接拒绝（防幻觉）。
- `close_mode=market` 无此风险行，沿用两按钮 `[✅ 确认执行] [❌ 取消]`。

#### 4.6.2 兜底执行序列（铁律：先撤干净、后市价，两单绝不并存）

```
① close_position_limit 成功（拿到 order_id / limit_close_order_id）
② 启动兜底定时器（新方法 _nl_fallback_after_limit，daemon；**零轮询**——睡眠
   NL_LIMIT_FALLBACK_SECONDS 后仅做 1 次只读 fetch_order；订单的 3s 轮询仍由
   既有 _monitor_limit_close 负责，成交结算通知以 _finalize_limit_full_fill 为准）
   ├─ 到期查询 status=filled/closed → 结束
   ├─ 到期查询 status=canceled/expired（非本线程动作）→ 判定人工/他者介入：
   │    放弃转市价，TG 通知"限价单已不在（可能人工撤单），60s 兜底取消"
   ├─ 到期查询仍 OPEN：
   │    ③ 兜底撤单（绑定本单 order_id，**归因到线程**，v1.6）：
   │       ├─ cancel_order 返回成功 = 本线程撤掉 → 继续 ④
   │       └─ 返回 -2011（单已不在）→ fetch_order 消歧：
   │            filled → 结束（按既有结算通知）；canceled → 人工/他者
   │            在"查询与撤单之间"介入 → 放弃转市价 + TG 通知；查询失败 → ⑦
   │    ④ 既有 _monitor_limit_close ≤3s 发现 canceled
   │       → _adjudicate_closed_limit_close 四态裁决 → 批次恢复 ACTIVE、close_phase 回 0
   │    ⑤ 兜底线程轮询确认恢复成功（超时 15s）
   │    ⑥ 市价前复核（批次仍 active、持仓>0、本线程撤单归因完整）后调 close_position_market 平掉残余
   │       （部分成交余量由 _close_amount_guard 防超平；重新 BEGIN claim 此时可成功）
   └─ 任一步失败（撤单失败 / 裁决超时 / 恢复未确认）
        → ⑦ 绝不叠加市价单，TG critical 告警交人工
⑧ 到期前后恰好成交 → 以 fetch_order status=filled 为**唯一成交证据**（BEGIN 拒绝不作数——也可能是另一平仓正在执行）
   回复"已按最优价成交"，不重试、不告警、不算失败
```

- **并发竞态**：兜底撤单后 `_monitor_limit_close` 自然接管裁决，NL 层不自己写任何状态机；与用户手动 `/close`、`/closecancel` 并发时，以 close_phase claim 输赢家语义为准（输家收到既有提示文案）。
- **人工介入优先（v1.5 复审新增）**：60s 窗口内用户若在交易所/他处手工撤掉限价单，兜底**放弃**而非转市价（尊重显式人工动作）；仅"到期仍 OPEN 且由本线程撤掉"才允许进入 ⑥。测试见 V5。
- **承诺收窄（v1.6 评审修订）**：若双方撤单落在**同一 API RTT 内**则无法归因——承诺仅限"**可检测的**人工介入必然优先"；不可检测微窗口以 fetch_order 终态为准保守处理（canceled→放弃）。窗口内用户可用既有 `/closecancel` 主动取消兜底（效果=外部撤单→兜底放弃）。
- **执行权原则（v1.7，外审原话入文）**：**无法确认兜底仍持有原平仓操作的执行权时，取消自动转市价；订单已取消本身不足以授权转市价。** fetch_order 只能判定订单现状、不能证明"谁撤的单"——故 -2011→fetch_order=canceled 一律按"可能人工介入"放弃，绝不在归因存疑时转市价。
- **崩溃窗口与 marker（v1.7 修订"重启无感知"缺口）**：兜底启动时在 `pending_intents.json` 增写 `fallbacks` 区段 marker `{batch_id, order_id, deadline_ts, fb}`（复用同文件原子写+锁，不再"完全不持久化"），兜底完成/放弃即删除。**启动对账**：凡存在 marker → 逐笔 `fetch_order` + 批次状态核查 → **TG 通知结论与建议操作（单仍在挂/已撤/已成交），但不自动行动**。重启仍不自动补兜底；崩溃可能落在三个子窗口：①挂单后撤单前（限价单仍在，监控线程重启后继续管辖）；②**撤单后、裁决恢复前**（批次处于关闭事务中——恢复行为属**未验证项 V6**，实测前不得宣称"已恢复 ACTIVE + 保护"）；③恢复后市价前（批次 ACTIVE，仅漏兜底）。三个窗口共同底线：**绝不双平**。
- **条件单（P2）同链**：兜底选项在**登记确认卡**时选定并由代码注入 `intent_payload.params.close_fallback`；watcher 触发 `close_limit` 成功后启动同一套兜底（触发时刻无人值守，逻辑完全一致）。
- 新增配置：`NL_LIMIT_FALLBACK_SECONDS=60`；`NL_LIMIT_FALLBACK_ENABLED=false`（兜底总闸门，**V1–V6 实测通过前保持 false，确认卡不显示 fb=60 按钮**，P1c 才开放——v1.6 评审修订）。

#### 4.6.3 实施前验证点（V1–V4 = P1b 开工门槛；V5–V6 = P1c 兜底放开门槛）

| # | 验证项 | 方法 |
|---|---|---|
| V1 | `_adjudicate_closed_limit_close` 恢复 ACTIVE 后 TP/SL 是否自动补挂（限价入口已撤过 TP） | 复现 `/closecancel` 后观察主监控是否重建保护 |
| V2 | 撤单 → close_phase 回 0 的实际时序（决定 ⑤ 轮询预算） | 打日志实测一轮 |
| V3 | 部分成交后撤单，残余仓位由 `close_position_market` 全量平掉的口径 | FakeExchange 场景测试 |
| V4 | `_cancel_limit_close_order`（trader_260725.py **L11245**，v1.5 修正）用于"撤了再市价"场景安全性（幂等 + 不触发结算副作用） | 读实现 + 单测 |
| V5 | 人工撤单竞态两子窗口：(a) 到期查询时已 canceled；(b) **查询 OPEN 之后、撤单返回之前**被人工撤（-2011 + fetch_order=canceled）——两种都必须放弃而非转市价（v1.6 补强） | FakeExchange 注入时序 → 断言市价 0 次调用 + TG 通知文案 |
| V6 | "撤单后、裁决恢复前"进程崩溃的重启恢复路径（批次状态/限价单终态/是否双平） | 实测一轮并记录；未通过前 `NL_LIMIT_FALLBACK_ENABLED` 保持 false |
| V7 | 快照四元组语义实测（v1.7 新增，二轮外审要求"等价于身份版本须证明"）：①`_execute_partial_close` 减仓后 realized↑/net↓；②加仓后 lfc↑；③**ABA**（减 0.3 再加 0.3 回原值）lfc/realized 双变化被捕获；④重启后快照复核不漂移（trade_state 恢复同值）；⑤批次清理重开 → batch_id 变化 → 目标清单外 → 跳过 | FakeExchange 场景测试 + 一轮真实实测；未通过前快照复核一律 fail-closed |

**P1b 开工另需**：记录 trader_260725.py 当前版本 SHA + V1–V4 实测证据附件（v1.6 评审修订：所有"既有方法已保证安全"的引用须以实测为准）+ **V7 快照四元组语义实测（v1.7）**——P1b 即时平仓确认→执行之间即做身份复核，快照语义必须先证实。

### 4.7 模糊条件澄清协议（多轮建议，直到需求可机读）

**动机**：用户第一句往往不精准（"长上影线多少算长？"）。多样化需求靠**参数化白名单 + 多轮澄清**获得，判定权始终在代码——LLM 只负责"把人话磨成白名单参数"。

**每轮 clarify 必须输出三件套**：

1. **理解回显**：白名单 `kind` + 参数的机读 JSON **和**人话定义——**人话定义由代码模板按参数渲染**（v1.6：LLM 只选 kind/preset/数值，不复述定义文本，杜绝转述失真）；
2. **默认建议**：给出预设档位/数值；
3. **明确问题**：二选一或带选项，让用户一句话就能回答。

**收敛规则**：

- 信息补齐 → 完整 intent → `validate_intent`（kind 白名单、`price>0`、`preset∈{strict,normal,loose}`、`deadline` 整数 ≥1、`timeframe=4h`）→ 确认卡。
- 用户报任意数值（"要 2.7 倍实体"）→ **明示不支持原值**："不支持任意倍数；最近可选档为严格档（≥3×实体）或普通档（≥2×实体），选哪个？"——替代方案必须经用户明确确认，**不得静默近似**（v1.6 评审修订）；首期只收三档 preset，**不接受任意公式**。
- 信息不足类 clarify 上限 **5 轮**（9/23 由 3 调升，对齐"多次建议直到澄清"）；5 轮未收敛 → 放弃 + 提示改用命令。风险告知类（§4.6）不计轮。**轮次计数存内存会话状态**（重启清零 = 重新澄清，安全方向，不落盘）。
- **明确拒绝并说明当前支持范围**（不猜、不 partial 执行）：复合条件（"连续 2 根长上影且放量"）、盘中触发（"盘中触到 70000"）、白名单外周期。

**两例落样**：

例 A（信息已足，零澄清）：

> "如果下一根K线收盘不超过70000美元就平仓"
> → `{"trigger":{"type":"candle_close","timeframe":"4h","condition":{"kind":"close_at_or_below","price":70000},"deadline_klines":1,"fires_once":true}}`
> → `confidence_note`："未指明周期，按 4h 收盘价判定"；确认卡回显"4h 收盘 ≤ 70000 → 💎 最优价挂单平仓（⏳ 等待 1 根）"。

例 B（多轮澄清）：

> 第 1 句："后面3根K线收盘出现长上影线就平仓"
> → clarify："我的理解：**长上影线·普通档 = 上影线 ≥ 2×实体 且 ≥ 全幅 1/3**（BTCUSDT 4h）。按普通档？还是更严的**严格档（≥3×实体）**？"
> 第 2 句（用户）："普通档就行"
> → `{"condition":{"kind":"long_upper_shadow","preset":"normal"},"deadline_klines":3}` → 确认卡登记。

**下游零改动**：`condition` 随 `intent_payload` 原样入 `pending_intents.json`（§7.1）；watcher 只调 `evaluate_condition` 纯函数（§7.2）。**新增一种 kind = 白名单表加一行 + prompt 加一句 + 一个纯函数，不碰状态机、不碰执行链。**

---

## 五、DeepSeek 接入层（新模块 `nl_intent.py`）

### 5.1 职责边界

单文件、纯函数为主，**不含任何交易所调用**：

```
parse_intent(text: str, context_brief: str) -> Intent | IntentError
```

- `context_brief`：由 bot_runner 注入的**只读**持仓摘要（各活跃批次的 batch_id/symbol/side/当前价），供 LLM 消歧；由代码生成，LLM 无法修改。
- HTTP 调用放 `run_in_executor` 中执行（同步调用），不阻塞事件循环；httpx 已随 python-telegram-bot 在 .venv 内，**零新增依赖**。解析并发上限 `NL_PARSE_MAX_CONCURRENT=2`（独立 executor/信号量），超出直接拒绝——**LLM 请求堆积不得拖慢 `/close` 等命令**（共享线程池风险，v1.6 评审修订）。

### 5.2 请求要点

| 项 | 值 |
|---|---|
| Base URL | `https://api.deepseek.com`（env：`DEEPSEEK_API_KEY` 配置才启用） |
| Model | `DEEPSEEK_MODEL`，默认 `deepseek-chat` |
| 输出 | `response_format={"type":"json_object"}` + system prompt 内嵌 §4 schema 全文 |
| 超时 | `NL_TIMEOUT_SECONDS` 默认 10s；超时/非 200/JSON 解析失败 → `IntentError` |
| 重试 | **0 次**（命令通道是兜底，快速失败并提示比重试重要；防外部依赖放大故障——418 事故教训） |
| System Prompt | 固定白名单说明 + 意图映射表 + "信息不足输出 clarify，禁止臆造 batch_id/价格" + 阴线定义 + **限价类意图必须输出不成交风险提示语**（回显给用户，§4.6）+ **§4.4 条件白名单全量定义原文（含 SHADOW_PRESETS 数值），clarify 时原样引用（§4.7）** |
| 温度 | 低（0.1），减少同一句话两次解析不一致 |

### 5.3 失败降级（Fail-not-Silent）

| 故障 | 行为 |
|---|---|
| `DEEPSEEK_API_KEY` 未配置 | NL 层整体禁用，静默走原路径（JSON 信号不受影响）；`/help` 不提口语功能 |
| API 超时/5xx/限流 | 回复："⚠️ 口语解析暂不可用（{原因}），请改用命令：/close /tp /sl …"——**一次提示，不重试** |
| JSON 合法但 schema 校验失败 | 回复拒绝原因 + LLM 原文 JSON（便于人工用命令复述意图），落审计日志 |
| LLM 输出非 JSON | 同上 |
| LLM 返回空内容 / 输出截断（finish_reason=length） | 视同 IntentError → 提示改用命令（DeepSeek 官方确认可能出现空内容/截断，v1.6） |

### 5.4 成本与审计

- 仅 1 个授权用户；解析失败不重试；单条输入 **>500 字整条拒绝并请用户缩短**（截断可能丢掉尾部"但不要平空单"类限制条件，v1.6 评审修订）。日均调用个位数~几十，成本可忽略。
- 审计：每次解析（含失败）追加一行到 `.nl_audit.log`（JSONL：时间/原文前 200 字/意图/结果），与 D-007 事件日志风格一致。

---

## 六、交互流程

### 6.1 即时意图（P1）

```
用户: "把 batch_xxx 平了"
  → nl_route → parse_intent → intent=immediate_close
  → Bot 回显确认卡片:
      📋 解析结果
      动作: 💎 最优价挂单平仓
      范围: 批次 batch_xxx (BTCUSDT LONG 0.001)
      [✅ 确认执行] [❌ 取消]        ← nl_confirm_{id} / nl_cancel_{id}
  → 用户点确认
  → run_in_executor(trader.close_position_market/limit, ...)   ← 与 /close 按钮完全同路
  → 回发执行结果（复用现有返回的 (ok, msg)）
```

- 确认卡片**必须**列出：动作、模式、批次、方向、数量——全部由代码从 `trade_state` 读出，**非 LLM 提供**。
- **close_mode 缺失**（"把 batch_xxx 平了"未指明方式）→ 不猜默认：按 optimal_limit 出四档卡（§4.6.1），按钮即方式选择，卡首行注明"未指明方式"；上图两按钮卡对应已明确市价的路径。
- `NL_DRY_RUN=true` 时确认卡照常弹出；点击确认 → 回复"🧪 DRY-RUN：未执行，动作已记审计"，trader 零调用（`test_nl_fallback` 覆盖）。
- `scope=all_active` 勾选交互（Q2 方案 A，即时/条件同机制）：InlineKeyboard 多选，回调 `nl_pick_{intent_id}_{batch_id}` 切换勾选（默认全不勾），"✅ 平掉所选（N）"按钮汇总执行；**勾选结果在确认时锁定为 `target_batch_ids` 持久化，执行/触发时不重新枚举**（§4.2，v1.6）。
- **确认按钮防重放（v1.6 评审修订）**：每次回调重新 `is_authorized`；确认卡**一次性原子消费**（intent_id 在内存 pending dict 中 pop，重复点击/旧卡重放 → 回复"该卡片已失效"）；修改需求/取消/TTL 过期后旧按钮一律作废；执行前以 trade_state 现状复核（批次/数量实质变化 → 要求重新确认，而非照旧执行）。`close_phase` 不能替代本保护（重复改 TP/SL 无 claim 概念）。
- 条件单确认卡必须显示五要素：**检查哪根 K（绝对时间含时区）、什么条件、平哪些仓（清单）、挂单不成交怎么办（兜底选项）、最迟何时仍允许执行（渲染为绝对时间戳=目标收盘+迟到上限，见 §7.2）**。
- **`set_tp`/`set_sl`（P1c）确认卡必显（v1.7 二轮外审）**：作用批次、**原值 → 新值**；执行前复核批次仍 active 且 TP/SL 订单未被他处变更（实质变化 → 拒绝执行并重新出卡）。"与 `/sl` 命令同级授权"**不能替代**这些校验——防的是口语层把价格/对象/"上移下移"理解错。
- `intent=clarify` → **最多 5 轮**（`NL_CLARIFY_MAX_ROUNDS`）回问，每轮须带 §4.7"理解回显 + 默认建议 + 明确问题"三件套，5 轮未收敛放弃并提示用命令；**限价类风险告知不计轮**：确认卡停留至用户点按钮接受或 300s TTL 过期；限价类确认卡为四档按钮（60s 兜底 / 纯限价 / 改市价 / 取消），见 §4.6（Q1 拍板）。
- `set_signal`（口语开仓）不自造参数：LLM 只允许填 symbol/side/leverage/entries/take_profit/initial_stop_loss 七字段，缺任一 → clarify；确认后写 `signal.json` 并走现有 `run_trader_execution(signal_snapshot=...)`（含 D-005 重复信号拦截）。**Q3 已拍板（9/23）：不进 P1/P1b，随 P1.5 单独灰度**——机制同上，仅放行时点后移。
- `conditional_close`（P2）确认卡对 `scope=all_active` **逐批列出、默认全不勾**（Q2 方案 A，9/23 拍板）；`deadline_klines` 默认 1 根、拒绝无限（Q5 方案 A，9/23 拍板）。

### 6.2 条件意图（P2）

```
用户口语 → 解析 → 确认卡片（含触发器全文回显）
  "⏰ 条件单已登记 (id=ci_20260923_a1b2c3)
   触发: 下一根 4H K 收盘时若阴线(close<open)
   动作: 全部活跃批次 · 💎 最优价挂单平仓
   等待中，可用 /intents 查看，/intent_cancel ci_... 撤销"
  → 点确认 → 写 pending_intents.json（先落盘再回复成功）
  → intent_watcher 线程持有并监控
```

---

## 七、条件单子系统（P2 新增）

### 7.1 存储 `pending_intents.json`

```json
{
  "version": 1,
  "intents": [
    {
      "id": "ci_20260923_a1b2c3",
      "status": "pending",
      "created_at": "2026-09-23T10:00:00+08:00",
      "updated_at": "2026-09-23T10:00:00+08:00",
      "raw_text": "用户原话",
      "intent_payload": { "……§4 完整 JSON……": {} },
      "target_batch_ids": ["batch_a", "batch_b"],
      "batch_snapshot": {"batch_a": {"side": "LONG", "net_qty": 0.001, "last_filled_count": 1, "realized_reduce_amount": 0.0}},
      "target_close_times": ["2026-09-24T00:00:00+08:00"],
      "close_order_id": null,
      "execution_stage": null,
      "batch_results": [],
      "next_kline_open": 1758000000000,
      "fired_kline_open": null,
      "last_error": null,
      "attempts": 0
    }
  ]
}
```

- `status` 状态机：`pending → firing → done/failed`；`cancelled/expired/verify_needed` 为其余终/中间态。**"已触发 / 已挂单 / 已平仓"三态分离**（v1.6 评审修订）：另存 `execution_stage`（triggered/submitted/limit_open/confirmed）、`close_order_id`、`batch_results[]`（逐批结果），限价单提交成功**不等于**平仓完成，用户可逐批追踪。
- **firing 崩溃恢复标 `verify_needed` 而非 failed**（v1.6 评审修订：订单可能已成功，直接标 failed 不准确）：启动时对账（读批次状态 + `close_order_id` 终态）→ 能定论则落 done/failed；不能定论 → 保持 verify_needed + TG 人工核实。仍**绝不自动补触发**。
- `firing` 是崩溃窗口（对账规则见上条）。
- **重叠去重按目标批次交集**（v1.6 评审修订，取代"同 symbol 同方向"粗粒度）：新登记的 `target_batch_ids` 与任何 pending 条件单清单有交集 → 拒绝并提示先 `/intent_cancel` 旧单。
- **并发安全（v1.6 评审修订）**：原子写（tmp+replace+fsync）只防"写一半"，**不防两线程旧快照互相覆盖**——登记/取消/触发全部经同一把 `_intent_store_lock` 串行，状态迁移在锁内 compare-and-set。取消与触发并发：锁内先到者赢；intent 已 `firing` → 取消请求回复"正在执行，无法取消"，不得假装取消成功。
- 写入采用**临时文件 + `os.replace` 原子替换 + fsync**（与 `_persist_states` 同标准），**先落盘、后回复 TG 成功**——回复成功而没落盘 = 重启丢单，禁止。
- 新增 TG 命令：`/intents`（列 pending 条目）、`/intent_cancel <id>`（撤销）。

### 7.2 `intent_watcher` 线程（trader_260725.py 内，类比 `_daily_report_loop`）

- **启动**：`__init__` 中 `threading.Thread(target=self._intent_watcher_loop, daemon=True).start()`。文件中无非终态 intent 时**完全休眠**（不轮询、零 API 消耗）；一旦登记新条件单，由登记路径唤醒（`threading.Event`）。
- **收盘检测（数据驱动，不依赖本机时钟）**：
  ```
  每次 tick: fetch_ohlcv(symbol, '4h', limit=2)
    last = 当前未收盘根；prev = 上一根
    if last.open_time != state.last_seen_open:
        → 上一根已收盘（用 prev 的 O/H/L/C 判条件）
  ```
  - 桶边界由交易所 `open_time` 定义，不担心机器时钟漂移/时区。
  - Binance 新根在边界后约 1-2s 内出现；tick 轮询天然覆盖，无需精确对表。
  - **幂等**：按 `open_time` 判"未处理过的收盘"，同一根 K 不会二次触发（F6 补偿：某次 fetch 恰好失败，下一 tick 仍能补判该根，直到超过 deadline）。
  - **断线补判（v1.6 评审修订）**：重启/断网恢复后 `limit=2` 可能已不含目标 K 线 → 改用 `fetch_ohlcv(symbol,'4h',since=target_open_time,limit=deadline+2)` 按**已持久化的目标时间**补取；**严禁用最新一根替代目标 K 线**，也不得重置 deadline 起点。
  - **迟到上限**：目标 K 收盘已超过 `NL_TRIGGER_MAX_LATE_MINUTES=30` 分钟才首次判定到 → **不执行**，标 `expired(late)` + TG 通知（不追旧信号交易）。**30 分钟为建议默认值、属产品授权参数（v1.7 二轮外审）**：确认卡"最迟何时仍允许执行"渲染为绝对时间戳 = 目标收盘 + 迟到上限（含时区），用户点确认按钮即接受该时间授权；调整仅经 .env，不得静默扩大。
- **轮询节奏**：
  - 常态：60s（仅有 pending intent 时才轮询）。
  - 估算距下一 4H 边界 < 120s：加密到 5s。`last.open_time + 4h - now` **只用于调速，不用于判定收盘**（判定永远以数据为准）。
- **条件判定**：纯本地 `evaluate_condition(condition, prev)`——§4.4 白名单纯函数（阴/阳线、收盘价阈值、长上影 preset），**表达式与数值不可由 LLM 注入**；数据质量校验（长度 <2、OHLC 乱序、NaN → 本轮跳过并计数，连续 3 次失败 → TG 告警 + 指数退避）。
- **执行**：
  1. 命中 → `pending→firing` 落盘；触发时逐批复核 `batch_snapshot` 四元组（§4.2，v1.7）：批次消失 → 跳过该批；**lfc↑ 或 side 变化 → 暂停该批并 TG 要求重新确认**；仅 realized↑（纯减仓）→ 平剩余净量；组合不可解释 → fail-closed 暂停；
  2. 调 `close_position_market/limit`（获得 close_phase 原子 claim / 孤儿单清理等全部既有保护；**与用户手动 `/close` 并发时 claim 失败属正常** → `failed(reason=close_phase_busy)`，TG 说明"已被手动平仓/正在平仓"）；limit 路径且登记时 `close_fallback=market_after_60s` → 挂单成功后启动 §4.6 同款兜底（受 `NL_LIMIT_FALLBACK_ENABLED` 闸门；无人值守，逻辑与即时一致）；
  3. `done/failed` 落盘 → `send_tg_notification` 结果（含触发 K 的 O/C、判定依据）；
  4. `all_active` 时**串行**逐批执行，单批失败不阻断后续，结果汇总为一条通知。
- **与手动操作的交互**：用户手动平掉全部相关批次 → watcher 发现批次消失 → intent 标 `expired(not_position)` 并通知。

### 7.3 API 与限流（沿用 D-001 讨论稿的 N6 思路）

- `fetch_ohlcv` 走**独立轻量通道**：独立的最小请求间隔（≥2s）与失败计数，**不共享**交易主路径 `_safe_api_call` 的常规冷却——4H 轮询绝不能把交易 API 打进冷却而拖累挂单/平仓；**但 IP 级限流/封禁信号（418/429）全局生效时行情轮询同样退避**（币安限额按 IP 计，持续违反可致封禁，v1.6 评审修订）。
- 频率预算：1 条 intent ≈ 常态 1 次/60s + 边界加密窗口 ≈ 每 4H 约 100-150 次轻量请求，远低于 Binance 权重限额。
- DeepSeek、TG、交易所三方**请求通道**独立（本地阻塞不互相传染）；**"故障域独立"不豁免交易所 IP 级限额**——K 线请求服从全局权重与封禁状态（v1.6 弱化 §3 图中"故障互不影响"的绝对表述）。

---

## 八、配置（.env 新增）

```bash
# ============ 可选：口语化指令（DeepSeek） ============
NL_ENABLED=false            # 总开关，默认关；**仅控制新指令入口**——已登记条件单/兜底属已授权动作，不受其影响（停止已授权动作用 /intent_cancel /closecancel，v1.6 明确：停止接收新指令 ≠ 取消已有授权）
DEEPSEEK_API_KEY=           # 未配置 = NL 层禁用
DEEPSEEK_MODEL=deepseek-chat
NL_TIMEOUT_SECONDS=10
NL_DRY_RUN=true             # true: 解析+确认卡全通但不执行（灰度期强制 true 上线）
NL_LIMIT_FALLBACK_SECONDS=60  # 限价单兜底：多少秒未成交则撤单转市价（§4.6，按钮级选项）
NL_LIMIT_FALLBACK_ENABLED=false # 60s 兜底总闸门：V1–V6 实测通过前保持 false，确认卡不显示 fb=60 按钮（v1.6，P1c 开放）
NL_CLARIFY_MAX_ROUNDS=5       # 信息不足类 clarify 上限（9/23 由 3 调升，对齐"多轮建议直到澄清"；风险告知类不计轮）
NL_CONFIRM_TTL_SECONDS=300    # 确认卡有效期，过期作废
NL_TRIGGER_MAX_LATE_MINUTES=30  # 条件单迟到上限：目标收盘超过该时长才判定到 → expired(late) 不执行（§7.2，v1.6）
NL_PARSE_MAX_CONCURRENT=2     # LLM 解析并发上限（独立 executor，防堆积拖慢命令通道，v1.6）
```

`.env.example` 同步补充注释。

---

## 九、失败模式总表

| # | 故障 | 后果 | 对策 |
|---|---|---|---|
| F1 | LLM 幻觉出不存在的 batch_id | 想平 A 平成 B | schema 校验 + trade_state 实存校验，不存在即拒绝；确认卡回显**代码读出**的批次详情 |
| F2 | LLM 把"最优价"解析成市价 | 成交方式不符预期 | prompt 映射表 + 确认卡用 UI 同款措辞（§4.3） |
| F3 | 确认后、落盘前崩溃 | 丢单（用户以为已登记） | 先落盘后回复；回复文案明确"以 /intents 为准" |
| F4 | `firing` 中崩溃 | 不确定是否已下单 | 启动恢复标 **verify_needed** + 对账（批次状态 + `close_order_id` 终态），能定论落 done/failed，不能定论 TG 人工核实；绝不自动补触发（§7.1，v1.6） |
| F5 | 条件单触发与手动 /close 并发 | 双方抢平 | close_phase 原子 claim（既有），输家收到明确提示 |
| F6 | 收盘瞬间 fetch 失败 | 错过本根判定 | 按 `open_time` 幂等补判"未处理过的收盘"；超过 deadline → expired |
| F7 | DeepSeek 挂了 | 口语不可用 | 降级提示改用命令；命令通道零依赖 LLM |
| F8 | 误触发一条不该下的单 | 资金损失 | NL_DRY_RUN 灰期 + 强制确认按钮 + 意图白名单三重闸 |
| F9 | watcher 线程异常退出 | 条件单静默失效 | 循环顶层 try-except + 连续异常计数告警（参照 watchdog R3 去重防通知风暴） |
| F10 | 用户拿 NL 层当聊天机器人刷 | API 成本/延迟 | 单用户 + 500 字截断 + 非意图输入 clarify 一次后拒绝 |
| F11 | 60s 兜底撤单与 `_monitor_limit_close`/裁决竞态 | 双平或漏平 | 铁律"先确认撤净、裁决恢复，再市价"：恢复未确认前**零市价调用**；撤单/裁决全复用既有 `_cancel_limit_close_order` + `_adjudicate_closed_limit_close`（§4.6.2、V1-V4） |
| F12 | 兜底到期瞬间限价单刚好成交 | BEGIN 拒绝被误报成失败 | 按既有 `(ok, msg)` 语义区分"claim 失败/持仓已平"→ 回复"已按最优价成交"，不重试不告警（§4.6.2 ⑧） |
| F13 | 兜底三子窗口崩溃（挂单后/撤单后裁决前/恢复后） | 仓位未平（但绝不双平） | ①③安全；②"撤单后裁决前"恢复行为属 **V6 未验证项**，实测前不得宣称已恢复；重启不补兜底，但 **`fallbacks` marker 持久化 + 启动对账 TG 通知**消除"中断无感知"（v1.7） |
| F14 | LLM 自创条件定义 / 在 `condition` 塞任意表达式 | 误触发或注入面 | kind+参数白名单（price>0、preset 枚举、deadline≥1、timeframe=4h）整条拒绝；判定为代码纯函数，LLM 只填枚举与数值（§4.4/§4.7） |
| F15 | 确认后目标批次被加仓/换仓/ABA 减仓再加仓（身份变化） | 按旧授权平掉新仓位 | `batch_snapshot` **四元组**复核：lfc↑ 或 side 变 → 暂停该批 + TG 重新确认；纯减仓（仅 realized↑）→ 平剩余净量；组合不可解释 → fail-closed（§4.2/§7.2，v1.7）；字段语义实测见 V7 |
| F16 | 取消与触发并发；确认按钮重放 | 取消后仍触发 / 重复执行 | `_intent_store_lock` 内 compare-and-set，`firing` 后取消拒绝；确认卡一次性原子消费 + 回调重鉴权 + TTL（§6.1/§7.1，v1.6） |
| F17 | 断线跨多根 K 线后恢复 | 拿错 K 线判定 / 追旧信号 | 按持久化目标时间 `since` 补取、严禁替代；迟到 >`NL_TRIGGER_MAX_LATE_MINUTES` → `expired(late)` 不执行（§7.2，v1.6） |

---

## 十、开放问题（✅ 6/6 已于 2026-09-23 全部拍板，存档备查）

| # | 问题 | 建议 |
|---|---|---|
| Q1 | "最优价平仓"确认按 §4.3 映射为**最优价挂单**（`close_limit(None)`，可能不立即成交）还是**市价**？ | **✅ 已拍板（9/23）**：限价挂单；确认卡强制"可能不成交"风险告知 + 四档按钮（60s 兜底转市价 / 纯限价 / 改市价 / 取消），澄清到用户按钮明确接受为止——§4.6 |
| Q2 | `scope=all_active` 时多空批次混仓（多+空并存）怎么办？ | **✅ 已拍板（9/23）**：方案 A——确认卡逐批列出受影响批次，**默认全不勾**，勾哪批平哪批（§4.5 尾） |
| Q3 | P1 是否包含口语**开仓**？（术语澄清：口语=文字输入；"口语开仓"= 直接发"开 0.05 BTC 多单、止损 65000"这类**文字开新仓**，经 NL 转 signal 执行，与语音无关） | **✅ 已拍板（9/23）**：采纳建议，**放 P1.5** 单独灰度（走 D-005 拦截 + v6.4 快照）；P1/P1b 不含开仓 |
| Q4 | 确认交互用按钮还是沿用 `YES/NO` 文本？ | **✅ 随 Q1 定为按钮**（四档按钮即 Q1 答案的载体；如需改文本另行说明） |
| Q5 | `deadline_klines` 默认值？"下一个收盘"=1 根 | **✅ 已拍板（9/23）**：方案 A——默认 **1 根**，确认卡回显等待时长；要多等必须明说根数；**拒绝无限等待**（说"一直等"→ clarify 索要根数）——§4.4 |
| Q6 | 阴线=严格 `close<open`（十字星不算）？ | **✅ 已拍板（9/23）**：符合预期，维持严格小于，不加 doji 开关 |

---

## 十一、测试计划（沿用现有独立脚本 + RED/GREEN 风格）

| 测试文件 | 覆盖 | 要点 |
|---|---|---|
| `test_nl_intent_schema.py` | `validate_intent` | 白名单外 intent 拒绝；缺 batch_id 拒绝；clarify 轮次规则（5 轮上限 + 三件套）；价格边界（≤0、`optimal_limit` 带价拒绝）；**LLM 输出携带 `close_fallback` 即整条拒绝**；condition 对象校验（未知 kind、`price≤0`、preset 越界、deadline<1 拒绝）；**LLM 输出 `target_batch_ids`/`batch_snapshot` 即拒绝（v1.6）**；区间根数（"一两根"）→ clarify；>500 字整条拒绝 |
| `test_limit_fallback.py` | §4.6 兜底链路 | 撤单成功→裁决恢复→恰好 1 次 `close_position_market`；撤单失败→市价 0 次+告警；到期前已成交→市价 0 次；裁决 15s 超时→市价 0 次+critical；按钮 fb 值注入正确；**V5a：到期时已被人撤→放弃，市价 0 次；V5b：查询 OPEN 后撤单前被人撤（-2011+fetch_order=canceled）→放弃，市价 0 次；撤单归因（仅本线程撤成才允许转市价）**；兜底零轮询（单点定时器） |
| `test_condition_eval.py` | §4.4 `evaluate_condition` | 十字星不触发阴线（Q6）；`close_at_or_below` 边界（close==price 触发）；三档 SHADOW_PRESETS 边界值；**零振幅 O=H=L=C 不触发长上影（v1.6）**；NaN/乱序数据跳过不触发 |
| `test_intent_store.py` | pending_intents.json | 原子写（模拟写中断文件不损坏）；状态机非法跃迁拒绝；**启动加载遇 `firing` → verify_needed 对账（v1.6）**；**目标批次交集重叠拒绝（v1.6）**；**锁内并发取消/触发只胜一家（v1.6）**；**`fallbacks` marker 写入/完成删除/启动对账通知输出（v1.7）** |
| `test_intent_watcher.py` | 收盘检测与执行 | FakeExchange 给两根 K：open_time 变化即收盘事件；`close<open` 触发 / `==` 不触发 / `>` 不触发；幂等（同一 open_time 不二次触发）；deadline 过期；close_phase busy 并发（claim 失败→failed 文案）；fetch 连续失败退避+告警；**断线补判按目标时间 since 补取、禁用替代根（v1.6）；迟到超时 → expired(late) 不执行（v1.6）；batch_snapshot 加仓暂停该批（v1.6）** |
| `test_nl_fallback.py` | 降级 | 无 KEY 静默禁用；超时→命令提示文案；DryRun 下确认通过但 trader 零调用；**空内容/截断输出 → IntentError（v1.6）** |
| `test_nl_confirm_guard.py` | 按钮防重放（v1.6） | 回调重鉴权；一次性原子消费（重复点击/旧卡重放失效）；TTL 过期作废；执行前复核批次实质变化 → 要求重新确认（F16） |

Mock 手法对齐现有用例：`mock.patch.object(CryptoTrader, '_daily_report_loop', ...)` 防真实后台线程、`ccxt.binanceusdm → MagicMock` 防联网、`STATE_FILE` 直接赋值指向 tmp 目录。

---

## 十二、实施分期

| 阶段 | 内容 | 前置 | 上线姿态 |
|---|---|---|---|
| **P1a** | `nl_intent.py` + schema 校验 + 确认卡（含限价风险提示与按钮）+ `NL_DRY_RUN=true`（只回显不执行） | Q1–Q6 拍板 + v1.6 评审修订落稿 | 灰度 ≥1 周，核对 `.nl_audit.log` 解析正确率 + 歧义澄清表现 |
| **P1b** | 放开**单批次**即时平仓执行（market + 纯限价 fb=0；`NL_DRY_RUN=false`）；**不含 60s 兜底、不含口语 TP/SL、不含 all_active** | P1a 解析正确 + **trader_260725.py 当前 SHA 记录 + V1–V4 实测证据** | 单批先行 |
| **P1c** | 60s 兜底（fb=60，`NL_LIMIT_FALLBACK_ENABLED=true`；含 `fallbacks` marker 持久化 + 启动对账通知）+ 口语改 TP/SL（确认卡显**原值→新值**+作用批次，执行前复核） | **V1–V6 全部实测通过**（重点 V1 TP/SL 补挂、V5a/V5b 人工撤单竞态、V6 撤单后崩溃恢复） | 单独灰度 |
| **P1.5** | 口语开仓 set_signal（**Q3 已拍板 9/23**） | D-005 拦截回归通过 + P1c 稳定 | 更长灰度 |
| **P2** | 条件单：**单批次 scope** + 白名单 kind 全开（**措辞更正（v1.7 二轮外审）**："纯函数"只意味着可穷举测试，**易测≠不会错**——零振幅 bug 即反例；保留长上影 preset 的依据是授权扩大风险集中在 scope 而非条件定义，且三档公式边界明确+边界用例有测试+不支持倍数不擅自映射+确认卡显示实际定义）+ `pending_intents.json` + watcher + `/intents` `/intent_cancel` | P1b 稳定 + `test_intent_watcher` 全绿（**与 P1.5 口语开仓相互独立、可并行排期**——P2 解决的是收盘条件平仓，不以 P1.5 为前置，v1.7） | 先 dry-run（复用 `NL_DRY_RUN=true`：触发只记日志不执行）观察一个 4H 周期，再实开 |
| **P2.5** | `all_active` 多批条件单（勾选清单锁定 + 串行执行 + 逐批结果通知） | P2 稳定 + F15/F16 场景回归 | — |
| **P3** | 语音条 STT | 独立设计 | — |

**阶段验收清单（v1.6 评审修订，替代"观察一周"的模糊标准）**：各阶段放行前须实测通过——旧确认按钮重放、确认后目标批次加仓、跨收盘确认卡失效、断线跨多根 K 恢复、60s 窗口人工撤单竞态（V5a/V5b）、撤单后崩溃恢复（V6）、取消与触发并发（F16）、LLM 超时期间 `/close` 等命令响应延迟实测、**快照四元组 ABA 场景（V7，v1.7）**。

**改动足迹预估**：

- 新增 `nl_intent.py`（~300 行）、`test_nl_*.py` / `test_intent_*.py`；
- `bot_runner.py`：`handle_json_or_pending_input` 插 1 个 NL 分支（~30 行）+ 2 个确认回调 + 2 个命令 + 注册行（~60 行）；
- `trader_260725.py`：`_intent_watcher_loop` + 意图存取方法（~200 行，daemon 线程范式）；
- **不改**：平仓/挂单/保护链/主循环任何既有函数体。

---

## 十三、设计原则备忘（审查用）

1. **LLM 只翻译，不决策**——决策 = 白名单 + 校验 + 人确认，全在确定性代码里。
2. **执行只走老路**——条件单/即时单最终都汇入 `close_position_*` / `run_trader_execution`，获得 v6.x **执行期**竞态保护；**但"调用旧入口 ≠ 跨步骤新流程自动安全"**——兜底/登记/恢复等跨步骤契约须显式设计并实测（V1–V6），禁止绝对化承诺（v1.6）。
3. **外部依赖只能降级不能阻塞**——DeepSeek 故障时命令通道 100% 可用（对照 418/通知风暴教训：快速失败 + 不重试 + 去重告警）；解析并发与排队受限（`NL_PARSE_MAX_CONCURRENT`），LLM 堆积不得拖慢命令通道；交易所 IP 级限额全局生效，"独立通道"仅隔离本地阻塞（v1.6）。
4. **先落盘后承诺**——一切"已登记"回复以 `pending_intents.json` 写成功为前提（对照 D-009 状态持久化标准）。
5. **宁可漏触发，不可重复触发**——崩溃窗口标 failed 交人工，幂等按 `open_time` 判。
6. **兜底宁可不平，不可双平**——限价兜底任一环节不确定（撤单未确认/裁决未恢复）→ 停手告警，绝不叠加市价单（对照 v6.x 平仓单向安全边界不变量）。

