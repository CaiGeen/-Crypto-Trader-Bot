# v6.3 设计稿：开仓幂等（ENTRY intent 单次执行）· 一页

> 2026-09-01 ｜ 三方共识冻结版 ｜ 范围一句话：**同一交易意图无论被重复发送多少次，最多只能形成一个有效开仓批次。**

## 0. 已核实的事实基础（不改任何东西）

| 事实 | 证据 |
|---|---|
| intent-before-effect **已成立** | L3151 `save_batch_state(skeleton)` 先于 L3153 create 循环；skeleton 含 `PENDING_CREATE` intent（B2-6） |
| 信号执行天然串行 | `TRADER_LOCK = asyncio.Lock()`（bot_runner L413）+ `async with TRADER_LOCK`（L2195）包裹 `run_in_executor(execute_signal)`；PTB 默认串行 |
| 现有冲突扫描无意图判据 | `_check_existing_conflicts`（L2736）只比 ①同 batch_id active ②交易所孤儿单（双通道 Fail-Closed） |

## 1. 核心不变量（5 条冻结）

1. **现有 intent-before-effect 不动**（skeleton 先于首个 create 持久化 + PENDING_CREATE）。
2. **新增 batch 级 `signal_fingerprint`**：基于规范化**原始用户意图**——`symbol + side + entries(price,amount) + stop_steps + TP`；与当前市价无关，**不用 post-skip 层集合**。数值一律过现成 `price_to_precision()/amount_to_precision()`（`77880/77880.0` 不漂移）；stop_steps 不足层按 skeleton 同款 `0.0` 补齐。
3. **`_check_existing_conflicts()` 增加唯一核心规则**：存在 active batch 且 `signal_fingerprint` 相同 → 拒绝（在现有 batch_id 检查之后、API 扫描之前，纯本地判断）。
4. **`entry_orders=[]` 不豁免**：空 skeleton 携带 PENDING_CREATE/PENDING_VERIFY/NOT_CONFIRMED = reservation 有效（create 可能已发出），必须拒绝第二批次；对账/收编由既有恢复机制完成（只 Commit 不 Create）。
5. **不做其他东西**：无 120s 墓碑去重、无 `signal_dedup.json`、无新锁、无 TTL、无新恢复机制、不碰仓位归属、不加 /force、无 checker/mutation 扩张。`bot_runner(6).py` 那套重型方案（已不在工作区）不沿用。

## 2. 实现落点（生产仅 trader_260725.py）

| # | 改动 | 位置 |
|---|---|---|
| 1 | 纯函数 `_compute_signal_fingerprint(signal)`：规范化拼接，返回可读 canonical 字符串 | 新增私有方法（TradeLogger/相关区） |
| 2 | `execute_signal` 冲突扫描前计算 `fp`，传入 `_check_existing_conflicts` | L2941 调用点 |
| 3 | `_check_existing_conflicts` 增加指纹判据（同 symbol 下 active 批次 `signal_fingerprint` 相同 → 拒绝 + TG 提示） | L2736 函数内，batch_id 检查后 |
| 4 | skeleton 增加字段 `'signal_fingerprint': fp` | L3120 skeleton dict |

向后兼容：旧批次无此字段 → `get()` 返回 None → 永不误匹配，无迁移。

## 3. 测试预算（6 个，RED-first，锁死不扩）

1. 首次信号 → 放行
2. active 同 fingerprint → 拒绝
3. active 同 fingerprint 且 `entry_orders=[] / PENDING_CREATE` → 仍拒绝
4. 市价变化导致 skip 层不同 → fingerprint 仍相同（拒绝）——验证与市价无关
5. entries/SL/TP 实质不同 → 放行
6. 原 batch 已正常 clear → 相同 signal 合法重入 → 放行

## 4. 假设注记（记入设计，不是现在的活）

串行性依赖两个现状：TRADER_LOCK 显式锁 + PTB 默认 `concurrent_updates=False`。任一未来被改动（如设 True / 换分发框架），需补最小原子 claim（fingerprint 检查 + skeleton 落盘合并为锁内事务）。届时由假设注记触发，不预防性建设。

## 5. 明确暂缓（记录在案，另开版本）

同方向多批次仓位归属一般化规则（v6.4 候选，优先复用守恒不变量 + 审计聚合读点）；重复结算播报；手动撤单假提醒残余；文案瑕疵。
