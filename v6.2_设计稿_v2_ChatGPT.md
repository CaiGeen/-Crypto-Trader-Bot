# v6.2 设计稿 v2（按 ChatGPT 复审收敛四条）— 定稿待送审，未动生产

> v1 → v2：ChatGPT 复审否决初稿 §1.3（层号漂移 = 新 P0）并指出 3 个漏报出口、
> helper 计数错误、`_persist_checked` 两处设计问题。本稿按其四条收敛要求重写。
> **本轮全部 13 项可核实声明已逐条独立源码核实，全部成立（见 §零），无一条被证伪。**
> 文档纪律：`python` 块完整可解析（零 Ellipsis）；生产摘录用 `python-frag`。
> 用户已裁定：批次 2 已人工处置完毕（撤全部挂单 + 停程序）；验证工具已固化 `verification_tools/`。
> **D-4/D-5 已由用户确认采纳推荐项**：D-4=A（台账永不压缩）、D-5=A（registry 恢复视图只读）。

---

## §零 初稿 v1 被否决项核实结果（全部实锤）

| # | ChatGPT 声明 | 独立核实证据 | 结论 |
|---|---|---|---|
| 1 | `entry_orders` 是**层序向量**（idx 对齐 target_amounts/filled_details/费用），初稿 1a 压缩列表会让失败 ID 层号左移 | trader L4659-4716：`for idx, order_id_raw in enumerate(entry_orders)` → `batch_filled_amount += target_amounts[idx]`、`filled_details[idx] = executed_price`、`layer_entry_fee = executed_price * target_amounts[idx] * ...`；L6386 `sl_params = layer_sl_params[idx].copy()` | ✅ **初稿 1a 证伪**——我只证明了「仍在 pending 区」，没证明「仍对应原来的层」 |
| 2 | `last_filled_count==0` 时，初稿 1a 保留的失败 ID 会被后续分支再次清空 | trader L6925：else 分支无条件 `target_b_data['entry_orders'] = []` + `pending_close=True` + `close_phase=1` | ✅ 成立 |
| 3 | 生产有**第二个同型 producer**（monitor 手动撤单路径），N-3 的「丢 ENTRY ID」部分升级为本轮 P0 | trader L4826-4846：撤失败只 print（L4835）→ `remaining_orders = [entry_orders[i] ... if filled_layers[i]]`（L4842-4843）无条件只保留已成交层 | ✅ 成立 |
| 4 | open-orders 对账有决定性反例（E5 已成交则不在 open_orders → 误判安全），只能做辅助不能做 release criterion | 逻辑核实成立：快照只能证明快照时刻的集合，不能恢复失去的 transaction identity | ✅ 成立 |
| 5 | registry 可恢复 ENTRY ID（`role='ENTRY'` + `layer` + `order_id`） | trader L3094-3106（骨架 registry）、L3178-3181（create 成功即 `_update_registry(..., order_id=order['id'], id_known=True)`）、L7609 已有 `_find_registry_identity_by_order_id` 反查先例 | ✅ 成立，恢复路径可行 |
| 6 | helper 计数是 11+3=**14** 不是 13 | 初稿新增 `_persist_checked` / `_verify_no_untracked_stop_orders` / `_freeze_reason_is_normal` 三个 | ✅ 我数错 |
| 7 | 冻结告警是 **3600 秒窗口**（每 60 分钟可再提醒），不是「3 轮去重」；3 轮是 `_converge_alert` 的机制 | 送审稿改动 4 L2407-2408（`_freeze_alerted` 3600s）；trader L7205-7217（`_converge_alert_counts` 3 轮） | ✅ 我写错 |
| 8 | parity checker 已规定「前导下划线未申报函数」Fail-Closed | check_doc_helper_parity.py L199 | ✅ 成立 |
| 9 | 漏报出口①：TERMINAL_ZERO **rollback 被拒** → raise → outer except 一发 critical，reason 停在正常态 | 送审稿 L526-539：`_rb_ok=False` → `raise RuntimeError(...转人工处置)` → L628 `if close_order_placed:` critical + return，reason 仍是 `market_confirming` | ✅ 成立 |
| 10 | 漏报出口②：create 后**通用异常**（含 `_confirm_close_filled` 意外异常）→ `if close_order_placed:` critical + return，reason 不切 | 送审稿 L622-634 原文 | ✅ 成立 |
| 11 | 漏报出口③：**cleanup=PENDING** 时照报成功 `return True`，从不写 `settlement_stuck`，converge 3 轮后静默 | trader L7082-7113：`_cleanup_pending=True` 只在文案加提示 → `return True`；`settlement_stuck` 在 v6.1 只是「保留值」（送审稿 L2349），无任何写入路径 | ✅ 成立 |
| 12 | `_persist_checked` 写 `close_reason` 丢 op_id CAS（重开第二套 reason 写入口） | 设计核实成立：初稿 §2.2 出口③用 CAS、出口④不用，自相矛盾 | ✅ 成立 |
| 13 | generic `_persist_checked` = escape hatch，与已删除的 `allow_flag_rollback` 同型 | 设计判断成立：无 op_id/phase 校验，任意调用方可 `b.update(anything)` 绕开 merge 规则 | ✅ 成立 |

**补充发现（本轮自查）**：第三处静默撤单 `_cancel_remaining_entries`（trader L2612-2621，`except: pass`）——但**不动台账**、其调用点（L4813 zero-filled 终止路径）后有 converge proof 门兜底，不违反 INV-1v2，仅在 v2 中记录。

**四条收敛主线（ChatGPT 终局要求，本稿逐条落实）**：
1. ENTRY positional ledger 不压缩部分尾段；**所有 producer** 遵守 ID 不丢 + index 不漂移（§一）；
2. legacy 截断从 registry 恢复 ID 做逐单验证，不靠 open-orders 快照单独放行（§二）；
3. N-1 白名单 + **所有**异常出口统一 CAS abnormal reason，含 rollback 拒 / generic post-create / cleanup pending（§三）；
4. LIMIT ID 用专用 op-id CAS durable commit；commit 失败尝试 cancel + 六态确认，保持 abnormal frozen（§四）。

---

## §一 INV-1v2：positional ledger 不压缩（收敛一）

### 1.1 不变量（修订版）

> **INV-1v2（ID 不丢 + index 不漂移）**：`entry_orders[i]`、`target_amounts[i]`、`filled_details[i]`、`layer_sl_params[i]` 的 `i` 必须始终表示同一层。任何 producer 不得：①让未确认终态的 pending ENTRY ID 从台账消失；②通过压缩/重排改变任何 ID 的层号。**producer 一律只标记、不删改 `entry_orders`。**

截断资格（保留定义，本稿不使用）：只有 pending 区**全部** ID 逐 ID `fetch_order` 确认 terminal 后才允许 `entry_orders[:last_filled_count]`。且 `cancel_order()` 不抛异常 ≠ 终态确认——INV-1v2 的「确认终态」措辞与代码必须一致。

**v2 设计取向（决策项 D-4，推荐 A）**：
- **A（推荐）：永不截断**。已 terminal 的 ID 留在台账无任何危害（gate 逐 ID 查询直接确认 terminal；批次 clear 时台账整体删除）；producer 改动最小、层号风险为零。
- B：gate 全 terminal 后截断。省几个后续 fetch_order 调用，但多一条写路径、多一类竞态，收益不成比例。

### 1.2 改动 1a-v2：🗑️ `cancel_open_orders`（producer #1）

```python
        cancel_failed_ids = []
        for idx in range(last_filled_count, len(entry_orders)):
            order_id = entry_orders[idx]
            try:
                self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol, params={'stop': True})
                cancelled_ids.append(order_id)
                cancelled_layers.append(idx + 1)
                print(f"  └─ 已撤销第 {idx + 1} 层挂单: {order_id}")
            except Exception as e:
                # 🔒 v6.2（INV-1v2）：失败只标记，台账不动（ID 原位保留、层号不变）。
                cancel_failed_ids.append(order_id)
                print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单失败: {order_id} ({e})"
                      f"（ID 保留在台账，监控继续管辖）")

        if not cancelled_ids and cancel_failed_ids:
            # 全部撤单失败：台账原样，批次保持现状，监控继续管辖这些层
            target_b_data.pop('is_programmatic_cancel', None)
            self.save_batch_state(target_symbol, batch_id, target_b_data)
            # 🔒 v6.2（D-3 裁定）：部分失败就是失败（全失败同理）
            return False, (f"⚠️ 批次 `{batch_id}` 挂单全部撤销失败"
                           f"（{len(cancel_failed_ids)} 张未确认终态，ID 已保留），"
                           f"请重试或人工核对")

        # 🔒 v6.2（INV-1v2 / 决策 D-4=A）：**不再截断 entry_orders**。
        # 初稿 1a 的「只删成功 ID」会让失败 ID 层号左移（idx 对齐被破坏）；
        # 现状「整体截断」会直接丢 ID（F-1）。v2 = 台账原样落盘，
        # 已撤/未撤 ID 均保留原位，后续 gate 逐 ID 按交易所事实归因。
        remaining_orders = entry_orders
        target_b_data['entry_orders'] = remaining_orders
```

后续两个分支的连带修改：

```python
        if last_filled_count > 0:
            self.save_batch_state(target_symbol, batch_id, target_b_data)
            result_msg = (
                f"🗑️ **撤单完成**\n\n"
                f"🆔 批次：`{batch_id}`\n"
                f"🪙 标的：`{target_symbol}`\n"
                f"📊 已撤销：{len(cancelled_ids)} 个挂单\n"
                f"├─ 层数：{cancelled_layers}\n"
                f"├─ 订单ID：{cancelled_ids}\n"
                f"📊 当前持仓：{current_持仓}\n"
                f"📊 剩余待成交层数：{len(remaining_orders) - last_filled_count}\n\n"
                f"💡 {last_filled_count} 层已成交，止盈止损单已保留，监控继续运行"
            )
        else:
            # 🔒 v6.2（INV-1v2）：zero-filled 分支**不再清空 entry_orders**。
            # 初稿实锤（trader L6925）：`entry_orders=[]` 会把撤单失败 ID 抹掉，
            # 且 close_phase=1 冻结后无人管辖。保留台账原样；
            # 批次终止/退出判断由 batch_filled_count==0 主导（与台账长度无关，
            # diff 阶段逐点验证退出路径不依赖 entry_orders 长度）。
            target_b_data['pending_sl_orders'] = []
            target_b_data['pending_close'] = True
            target_b_data['close_phase'] = 1
            self.save_batch_state(target_symbol, batch_id, target_b_data)

            result_msg = (
                f"🗑️ **撤单完成**\n\n"
                f"🆔 批次：`{batch_id}`\n"
                f"🪙 标的：`{target_symbol}`\n"
                f"📊 已撤销：{len(cancelled_ids)} 个挂单\n"
                f"├─ 层数：{cancelled_layers}\n"
                f"├─ 订单ID：{cancelled_ids}\n"
                f"📊 当前持仓：0\n"
                f"📊 剩余待成交层数：0\n\n"
                f"💡 批次已无成交层，监控将自然退出"
            )

        if cancel_failed_ids:
            # 🔒 v6.2（D-3 裁定）：部分失败 = False（部分成功也不谎报全成功）
            return False, (f"⚠️ 批次 `{batch_id}` 撤单部分完成：已撤 {len(cancelled_ids)} 张，"
                           f"{len(cancel_failed_ids)} 张未确认终态（{cancel_failed_ids}，"
                           f"ID 已完整保留，监控继续）\n💡 请重试或人工核对")

        return True, result_msg
```

### 1.3 改动 1c-v2：monitor 手动撤单路径（producer #2，N-3 升级部分）

trader L4826-4846 现状：失败只 print → `remaining_orders = [entry_orders[i] ... if filled_layers[i]]` 无条件只保留已成交层。修改（保留 cancel 循环原文，只改落盘段）：

```python
                        # 🔒 v6.2（INV-1v2 / 第二 producer）：不再按 filled_layers
                        # 截断 entry_orders。撤单失败的 ID 必须保持可追踪；
                        # 重复撤单对已撤 ID 幂等（-2011 视为已撤，与现状一致——
                        # canceled_layers 本就是循环内存态、不落盘）。
                        latest_all = self.load_all_states()
                        latest_b_data = latest_all.get(symbol, {}).get(batch_id, {})
                        if latest_b_data:
                            latest_b_data['is_programmatic_cancel'] = True
                            self.save_batch_state(symbol, batch_id, latest_b_data)
```

（删除 L4842-4843 两行截断；TG 文案「已取消 N 层剩余挂单」后追加失败提示，同 D-3 语义。）

---

## §二 改动 1b-v2：registry 恢复取代 open-orders 对账（收敛二，D-1 裁定落实）

### 2.1 设计

- **正常账本**（pending 区有 ID）：按 `entry_orders` 逐 ID——不变；
- **legacy 截断形状**（计划层 > 已成交层且台账 pending 区为空）：新 helper `_pending_entry_ids_for_gate` 从 protection_registry 按 `_protection_identity(batch_id, 'ENTRY', idx, side)` 逐层恢复缺失 ID → 恢复视图**只读使用**（不写回台账，决策项 D-5）→ 与台账 pending 区合并后进入既有逐 ID `fetch_order` 验证循环；
- registry 也不完整（缺失层无条目或无 order_id）→ **Fail-Closed**：gate=False + critical + 人工 reconcile；
- open-orders 扫描降级为**二级诊断**：只能提供「台账+registry 之外还存在 stop 单」的**否决权**（Fail-Closed 证据），永远不构成放行依据。

### 2.2 新 helper（第 12 个）

```python
    def _pending_entry_ids_for_gate(self, symbol: str, batch_id: str,
                                    b_data: dict, last_filled_count: int):
        """gate 的 pending ENTRY ID 视图：台账 pending 区 + registry 恢复的缺失层。

        🔒 v6.2（D-1 裁定 / INV-1v2）：legacy 截断形状不能凭 open-orders
        快照放行——快照看不见已成交的遗失 ENTRY（E5 已成交 → 不在
        open_orders → 误判安全）。改从 protection_registry 逐层恢复：
        identity = _protection_identity(batch_id, 'ENTRY', idx, position_side)，
        生产在 ENTRY create 成功时已写入 order_id + id_known=True
        （trader L3178-3181）。恢复视图只读，不写回台账。
        返回 (ids, recoverable)：
          ids          合并后的 pending ID 列表（台账区 + registry 恢复区，带来源）
          recoverable  False = 存在台账与 registry 都无法归因的层
                       → 调用方必须 Fail-Closed（gate=False + critical）
        """
        ids = []
        recoverable = True
        entry_orders = b_data.get('entry_orders', []) or []
        target_amounts = b_data.get('target_amounts', []) or []
        side = b_data.get('side')
        position_side = 'LONG' if side == 'BUY' else 'SHORT'
        for idx in range(last_filled_count, len(target_amounts)):
            if idx < len(entry_orders) and entry_orders[idx]:
                ids.append({'id': str(entry_orders[idx]), 'layer': idx, 'src': 'ledger'})
                continue
            identity = self._protection_identity(batch_id, 'ENTRY', idx, position_side)
            reg = self._get_registry_entry(symbol, batch_id, identity)
            if reg and reg.get('order_id'):
                ids.append({'id': str(reg['order_id']), 'layer': idx, 'src': 'registry'})
            else:
                recoverable = False
        return ids, recoverable
```

（`_get_registry_entry` 为 registry 读取的既有内部访问方式的包装名，正式 diff 阶段对齐生产实际访问器；若生产已有等价读取函数则直接复用、不新增。）

gate 侧接入（`_cancel_and_verify_entry_orders` 的 pending 推导替换 + Fail-Closed 分支）：

```python
        entry_orders = b_data.get('entry_orders', []) or []
        if (len(b_data.get('target_amounts', []) or []) > last_filled_count
                and len(entry_orders) == last_filled_count):
            # 🔒 v6.2（D-1 裁定）：legacy 截断形状 → registry 恢复后逐 ID 验证
            rec_ids, recoverable = self._pending_entry_ids_for_gate(
                symbol, batch_id, b_data, last_filled_count)
            if not recoverable:
                self.send_tg_notification(
                    f"🚨【资金安全】截断台账存在无法归因的 ENTRY 层！\n"
                    f"🆔 批次: {batch_id}\n"
                    f"⚠️ 台账与 protection_registry 均无法提供缺失层 ID，"
                    f"不能排除无主开仓单，请立即人工 reconcile！",
                    level='critical')
                return False
            pending_ids = [e['id'] for e in rec_ids]
            # 二级诊断：open-orders 否决权（只否决、不放行）
            _oo = self._safe_api_call(self.exchange.fetch_open_orders,
                                      symbol, params={'stop': True})
            if isinstance(_oo, list):
                _known = {e['id'] for e in rec_ids}
                _known.add(str(b_data.get('tp_order_id') or ''))
                _known.add(str(b_data.get('current_sl_id') or ''))
                _unknown = [str(o.get('id')) for o in _oo
                            if isinstance(o, dict) and str(o.get('id')) not in _known]
                if _unknown:
                    self.send_tg_notification(
                        f"🚨【资金安全】对账发现台账/registry 之外的条件单！\n"
                        f"🆔 批次: {batch_id}\n📌 无主订单: {_unknown}\n"
                        f"⚠️ 请立即人工处理！",
                        level='critical')
                    return False
        else:
            pending_ids = [oid for idx, oid in enumerate(entry_orders)
                           if idx >= last_filled_count and oid]
        if not pending_ids:
            return True
```

（后续逐 ID cancel + fetch_order 验证循环不变——它消费 `pending_ids`，数据源已换。）

---

## §三 N-1 补全：白名单 + 全出口统一 CAS（收敛三）

### 3.1 出口穷举表 v2（9 项，新增⑤⑥⑦）

| # | 出口 | v6.1 现状 | v6.2 处置 |
|---|---|---|---|
| 1 | BEGIN 失败 | critical + return | 不变（磁盘不属于本事务） |
| 2 | derive 失败 + rollback 成功 | critical + return | 不变（磁盘已回 phase=0） |
| 3 | derive 失败 + **rollback 失败** | critical 但 reason 停正常态 | **改动 2a**：CAS 写 `txn_aborted_rollback_failed` |
| 4 | 六态非确认分支写 `market_confirm_unknown` | `except: pass` | **改动 2b**：统一走 `_set_close_reason_if_current` |
| 5 | **TERMINAL_ZERO rollback 被拒** | raise → outer except 一发 critical，reason 停正常态 | **改动 2c**：CAS 写 `rollback_rejected` |
| 6 | **create 后通用异常**（`close_order_placed=True` 的 outer except） | critical + return，reason 停正常态 | **改动 2d**：CAS 写 `settlement_error` |
| 7 | **cleanup=PENDING 照报成功** | `return True`，reason 停正常态，converge 3 轮后静默 | **改动 2e**：CAS 写 `settlement_stuck`（保留值首次启用） |
| 8 | ENTRY gate 失败 | 已 CAS 写 `market_entry_unknown` | 不变（v6.1 已修） |
| 9 | 结算段通用 except 通道 | critical（fail-noisy） | 由 ⑥ 覆盖 reason 责任 |

**统一纪律**：所有 reason 写入**只用** `_set_close_reason_if_current()`（op_id CAS + phase≥1 + persist checked），不建立第二套写入口（初稿 `_persist_checked` 写 reason 的做法废除）。

### 3.2 改动 2a（第四类出口，与初稿同，保留）

derive 失败 + `_rb_ok=False` → `_set_close_reason_if_current(..., 'txn_aborted_rollback_failed')` → critical（CAS 失败文案如实追加）→ return False。市价/限价同构。

### 3.3 改动 2b（六态分支，废除 `_persist_checked` 写法）

（摘录：落点在市价 AFTER 块六态分支内，正式 diff 中随该块整体过检。）

```python-frag
            else:
                # PARTIAL / PENDING / UNKNOWN / NOT_CONFIRMED —— 一律不回滚
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'market_confirm_unknown')
                raise RuntimeError(
                    f"市价平仓单结果未确认（{_verdict}）：{_detail}。"
                    f"不回滚，保持冻结等人工处置"
                    + ('' if _rs_ok else
                       f"；⚠️ close_reason 切换失败（{_rs_why}），"
                       "冻结告警可能不再周期触发，请立即人工核查！"))
```

### 3.4 改动 2c（TERMINAL_ZERO rollback 被拒，送审稿 L537-539 处）

```python
                # CAS 拒绝 = 状态已被其他操作接管 → 绝不强行覆盖，走 critical
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'rollback_rejected')
                raise RuntimeError(
                    f"平仓单未成交且回滚被拒绝（{_rb_why}），转人工处置"
                    + ('' if _rs_ok else
                       f"；⚠️ close_reason 切换失败（{_rs_why}），"
                       "冻结告警可能静默，请立即人工核查！"))
```

### 3.5 改动 2d（create 后通用异常，outer except 的 `close_order_placed` 分支）

```python
            if close_order_placed:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'settlement_error')
                self.send_tg_notification(
                    f"🚨【资金安全】市价平仓单已发出但后续结算异常（未回滚关闭标记）！\n"
                    f"🆔 批次: {batch_id}\n💡 原因: {str(e)[:150]}\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n")
                    + f"⚠️ 请立即人工核对持仓与挂单！",
                    level='critical')
                return False, f"❌ 市价平仓结算异常（平仓单已创建，close_phase 保持）: {e}"
```

### 3.6 改动 2e（cleanup=PENDING，生产 L7100 段）

```python
            if _cleanup_pending:
                # 🔒 v6.2（P0-2 出口⑦）：cleanup=PENDING 首次启用 settlement_stuck。
                # v6.1 它只是「保留值」——批次留在 close_phase=1 + 正常态 reason，
                # converge 3 轮告警后静默。显式切异常态后由冻结告警（60 分钟窗口）
                # 周期接管。
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'settlement_stuck')
                result_msg += (
                    f"\n\n⚠️ **[残单收敛提示]** 市价平仓已成交（持仓=SUCCESS），"
                    f"但批次清理暂未收敛（cleanup=PENDING，proof 未通过，"
                    f"系统将自动重试收敛后清理，无需人工干预）。"
                    + ('' if _rs_ok else
                       f"\n⚠️ close_reason 切换失败（{_rs_why}），请人工关注该批次。"))
```

### 3.7 改动 2f：冻结告警白名单（内联，不加 helper；周期修正）

`NORMAL_CLOSE_REASONS = {'market_confirming', 'limit_pending_normal'}` 常量 + 改动 4 分支内联 `in` 判断；除白名单外一切 reason（异常态/None/缺失/未知）→ 周期 critical。**告警周期 = `_freeze_alerted` 3600 秒窗口（每 60 分钟可再提醒）**——初稿误写「3 轮去重」已修正；3 轮是 `_converge_alert` 的独立机制，两者不混用。

---

## §四 P0-3 重做：专用窄 helper + B-lite（收敛四，D-2 裁定落实）

### 4.1 删除 `_persist_checked`，新增 `_commit_limit_close_order_if_current`（第 13 个 helper）

```python
    def _commit_limit_close_order_if_current(self, symbol: str, batch_id: str,
                                             close_op_id: str, order_id: str,
                                             limit_price: float, price_mode: str):
        """限价平仓单 ID 的 durable commit（专用窄入口，禁止泛化）。

        🔒 v6.2（P0-3 / INV-3）：写盘失败绝不作为正常 limit_pending 返回成功。
        与初稿 generic `_persist_checked` 的区别（ChatGPT §十）：本 helper 校验
        事务所有权（op_id CAS + phase==1 + pending_close + 未被限价成交结算），
        且只允许写三个字段——与项目删除 allow_flag_rollback 同一原则：
        危险事务迁移用窄 helper，不做通用逃生门。
        锁内：load_all_states 重读 → 校验 → 改白名单字段 → persist must True。
        返回 (ok, why)。why ∈ 'state_unreadable（{异常}）' / 'batch_missing'
        / 'op_id_mismatch' / 'not_in_close' / 'already_settled'
        / 'persist_failed' / 'committed'。
        """
        with self._state_lock:
            try:
                all_states = self.load_all_states()
            except Exception as e:
                return False, f'state_unreadable（{e}）'
            b = (all_states.get(symbol, {}) or {}).get(batch_id)
            if not isinstance(b, dict):
                return False, 'batch_missing'
            if (b.get('close_op_id') or '') != (close_op_id or ''):
                return False, 'op_id_mismatch（已有其他操作接管，不覆盖）'
            if int(b.get('close_phase', 0) or 0) != 1 or not b.get('pending_close'):
                return False, 'not_in_close'
            if b.get('settled_by_limit_close'):
                return False, 'already_settled'
            b['limit_close_order_id'] = order_id
            b['limit_close_price'] = limit_price
            b['limit_close_mode'] = price_mode
            if not self._persist_states(all_states):
                return False, 'persist_failed'
            return True, 'committed'
```

### 4.2 限价路径接入（B-lite：commit 失败 → cancel + 六态确认 → abnormal frozen，绝不自动 rollback）

```python
            order_id = order['id']

            # 🔒 v6.2（P0-3 / INV-3 / D-2=B-lite）：durable commit
            _cm_ok, _cm_why = self._commit_limit_close_order_if_current(
                target_symbol, batch_id, close_op_id, order_id, limit_price, price_mode)
            if not _cm_ok:
                # commit 失败 = 交易所有活 LIMIT、账本没有 ID。
                # B-lite：立即尝试撤掉这张单，用既有六态确认器按交易所事实定案；
                # 无论结果如何都不自动 rollback（close_order_placed 纪律），
                # 一律 abnormal frozen 待人工。
                try:
                    self._safe_api_call(self.exchange.cancel_order, order_id,
                                        target_symbol, params={'stop': True})
                    _cx_note = "已发出撤单请求"
                except Exception as _cx_e:
                    _cx_note = f"撤单请求异常（{_cx_e}）"
                _verdict_c, _detail_c, _filled_c = self._confirm_close_filled(
                    target_symbol, side,
                    target_b_data.get('is_hedge_mode', False),
                    order_id, current_filled_amount, 0.0)
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'limit_persist_failed')
                self.send_tg_notification(
                    f"🚨【资金安全】限价平仓单 {order_id} 账本写盘失败，"
                    f"已按 B-lite 处置！\n"
                    f"🆔 批次: `{batch_id}`\n💰 价格: {limit_price}\n"
                    f"🔄 {_cx_note}；六态确认 = {_verdict_c}（{_detail_c}）\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n")
                    + f"🚫 批次保持冻结（close_phase=1），绝不自动回滚，"
                      f"请立即人工核对该限价单与持仓！",
                    level='critical')
                return False, (f"❌ 限价平仓单账本写盘失败"
                               f"（六态={_verdict_c}），批次冻结待人工处置")
```

B-lite 语义说明（对齐 ChatGPT §十一）：

- `TERMINAL_ZERO` → 至少证明 live untracked order 已消失 → 仍 abnormal frozen（本轮不自动 rollback）；
- `CONFIRMED_FULL / PARTIAL` → 仓位已变化 → abnormal frozen + critical；
- `PENDING / UNKNOWN / NOT_CONFIRMED` → 仍可能是活单 → abnormal frozen + critical；
- 全路径复用既有六态确认器，不新设计确认逻辑。

---

## §五 helper 计数修正（收敛后）

| helper | 状态 |
|---|---|
| v6.1 既有 11 个 | 不变 |
| `_pending_entry_ids_for_gate` | **新增**（§二，registry 恢复视图） |
| `_commit_limit_close_order_if_current` | **新增**（§四，durable commit） |
| `_persist_checked` | **删除**（初稿设计，generic escape hatch） |
| `_verify_no_untracked_stop_orders` | **删除**（被 registry 恢复 + 二级诊断取代） |
| `_freeze_reason_is_normal` | **不加**（白名单内联判断） |

**合计 11 + 2 = 13**。parity checker「前导下划线未申报函数 Fail-Closed」（checker L199）在名单同步时一并更新。

---

## §六 测试计划 v2（初稿 R1 期望值本身是错的，重写）

| 组 | 用例 | RED（v6.1 行为） | GREEN（v6.2 行为） |
|---|---|---|---|
| R1-a **positional invariant** | `target_amounts=[0.001,0.002,0.003,0.004,0.005]`，E4 cancel 成功 / E5 失败 | `entry_orders` 被截断为 [E1..E3]（E5 丢） | `entry_orders` 原列表不压缩——**必须仍能证明 E5 对应 0.005，不能变成 0.004** |
| R1-b **zero-filled partial failure** | `last_filled_count=0`，E1 cancel 成功 / E2 失败 | L6925 `entry_orders=[]` 清掉 E2 + `pending_close=True` 假装全撤成功 | E2 ID 绝不能被清掉；台账原样；返回 False |
| R1-c **第二 producer** | monitor `manual_canceled_detected` → cancel 剩余 ENTRY → 某张失败 | L4842 只保留 filled 层，失败 ID 丢 | 失败 ID 仍可追踪（台账原样） |
| R1-d **legacy orphan 已成交** | 截断账本 [E1,E2,E3] + registry 有 E4/E5；E5 `fetch_order → filled` | v6.1：D6e/D6g 凭形状放行 → gate=True | registry 恢复 E4/E5 → 逐 ID 验证发现 E5 已成交 → gate=False（**正好杀掉「只扫 open orders」的不足**） |
| R1-e **registry 不可恢复** | 截断形状 + registry 缺条目 | 放行 | Fail-Closed gate=False + critical |
| R2 出口③④⑤⑥⑦ | 五个出口各一例（`persist_ok=False` / rollback 拒 / generic 异常 / cleanup PENDING） | reason 停正常态 | 落盘 reason ∈ 新异常集；CAS 失败时 critical 文案含「切换失败」 |
| R2 分型/周期 | 白名单内联判断 + `_freeze_alerted` 3600s | v6.1 黑名单 | 白名单外一律周期 critical（60 分钟窗口） |
| R3 durable commit 失败 | `persist_ok=False` | return True 账本无单 | commit False → cancel + 六态确认 → abnormal frozen，绝不自动 rollback |
| R3 正向 | `persist_ok=True` | — | 正常 limit_pending GREEN |

**双向验证纪律**不变：v6.2 文档 GREEN + 对应变异 RED；变异清单新增项随四条收敛重排（回退 1a/1c 的 producer 修复 → R1-a/c 红；registry 恢复分支删除 → R1-d 红；五个出口 CAS 删除 → R2 红；commit 失败仍 return True → R3 红）。**基建缺口**：`FakeExchange.cancel_order` 需加 `cancel_seq` 失败注入（repeat-last 语义）；registry 需要内存态 Fake（现有测试未覆盖 registry 读取路径）。

---

## §七 决策项 v2（ChatGPT 已裁定三项，新增两项待用户确认）

| # | 决策 | 裁定/推荐 |
|---|---|---|
| D-1 | gate 对账方式 | **ChatGPT 裁定：A/B 都不选**——registry 恢复 ID → 逐 ID 验证为主，open-orders 扫描仅作二级诊断（只有否决权）。本稿已落实 |
| D-2 | LIMIT durable commit 失败处置 | **ChatGPT 裁定：B-lite**（cancel + 六态确认，不自动 rollback，abnormal frozen）。本稿已落实 |
| D-3 | 部分 ENTRY cancel 失败返回值 | **ChatGPT 裁定：False**。本稿已落实 |
| **D-4** | 台账压缩策略：永不压缩（A）vs gate 全 terminal 后压缩（B） | **用户确认采纳 A**——已 terminal 的 ID 留在台账无危害（gate 逐 ID 确认 terminal 直接放行），clear 时台账整体删除；「全 terminal 后截断」的资格定义保留在 §1.1，本稿不实现截断 |
| **D-5** | registry 恢复视图：只读使用（A）vs 写回修复台账（B） | **用户确认采纳 A**——恢复视图仅在 gate 内只读合并，不写回台账；写回自愈作为后续增强另立项 |

## §八 下一步

1. 本设计稿送审 ChatGPT（核四条收敛 + D-4/D-5 采纳），批准后才出正式 diff；
2. 出正式 v6.2 送审稿 diff（AFTER 块全量 + 落点表 + checker 名单同步 13 helpers + `cancel_open_orders`/monitor 段纳入 `PROD_SIGNATURE_OVERRIDES`）；
3. R1-a~e / R2 / R3 测试实现（先 RED 后 GREEN，双向验证）；
4. 五道门 + 变异清单（按 §六重排）+ 133 场景回归；
5. 重新送审 ChatGPT（核 diff 与测试），批准后按既有五段式流程落生产。
