# v6.1 送审说明（给 ChatGPT 的投递摘要）

> 配套主文档：`事故_市价平仓-4061_精确diff_送审ChatGPT.md`（v6.1）
> 本文只做两件事：**①告诉你相对 v6 改了什么 ②把要你核的点收窄到最小集**。
> 生产代码**零改动**（HEAD 仍 `e953d79`，四个生产文件 `git diff` 全空）。

## 0. 一句话

你复审 v6 提的 **3 个 P0 + 2 项修正 + 测试/checker 假绿**，我逐条源码核实后
**全部成立、全部处置**。此外我自己新加的一道机器检查又抓出 **1 个落地级 P0
（C-7）**，也已修。测试 133/133，变异检查 10/10。

---

## 1. 相对 v6 的 5 处改动

| # | 改动 | 你的出处 | 落点 |
|---|---|---|---|
| 1 | **BEGIN / rollback / reason 三处必检 `_persist_states()` 返回值**。写盘失败 = **未取得所有权**，绝不返回 op_id、绝不谎称已回滚、绝不谎称 reason 已切换 | 你的 P0-1 | 第 11 个 helper `_set_close_reason_if_current`；测试 B11/B11b/B11c、B12/B12b、B13/B13b + 三组 v6.0 负向 |
| 2 | **限价路径撤 ENTRY 升级为「撤销确认 gate」**（改动 1d） | 你的 **P0-3** | 测试 L1/L1b/L2/L2b/L2c/L3/L3c |
| 3 | `_derive_close_txn_vars` 三项完整性校验：`target_amounts_short` / `side_invalid` / `entry_orders_missing|short` | 你的修正 2 | 测试 D3c/D3cb、D3d/D3db、D6c/D6cb/D6d/D6db |
| 4 | `_derive_close_txn_vars` 契约确定为 **10 raw + 1 derived = 11 vars** | 你的修正（我自查补的 6 个） | 见 §3 |
| 5 | 限价 gate 失败**优先 CAS 回滚**，回滚失败才落 `limit_entry_unknown` | 你的修正 3 | 测试 L2b/L2c |

**plus C-7（我自己抓的，见 §2）**。

---

## 2. C-7：只给调用、不给定义（🔴 落地级 P0，v6.1 前稿的阻断项）

v6.1 前稿在结算处写了

```python
self._record_realized_pnl(..., "市价平仓", pnl_partial=_pnl_partial)
```

但**全文 `def _record_realized_pnl` 命中 0 次** —— 生产 `trader_260725.py`
L678-680 只有 8 个必填位置参数，没有 `pnl_partial`。

照前稿落地 → 结算阶段 `TypeError`，而且**炸在平仓已成交之后**：结算/clear
被打断，批次钉在 `close_phase=1` → 监控线程冻结、跳过全部 SL/TP 维护，
**与本次事故的最终症状完全一致**。

处置（主文档「改动 1b 追加 2 / 追加 3」）：

1. 补出生产签名改动 diff：`pnl_partial: bool = False` + record dict 落
   `prior_reduction_unknown`。生产实证：4 个调用点（L5408 止损 / L5579 止盈 /
   L7109 市价 / L7818 限价）全是位置传参 → **其余 3 处零影响**。
   （前稿写的「其余 5 处调用点」是笔误，4 是调用点合计。）
2. **日报读取侧同步改**：`_send_daily_report`（L871）原本无条件累加 `net_pnl`，
   只落标记不改读取方等于没做 —— 新增「其中 N 笔为部分成交记账，累计值不完整」提示。
   （只加提示、**不改累加口径**：少记总比把不完整值展示成"精确值"安全。）

> 这条是新建的第三道机器检查（`check_doc_helper_calls.py`【4】关键字参数名校验）
> 抓出来的。前两道门都守不住它：`ast.parse` 完全合法，只在运行时炸。
> 同类前科是 v5 的 `close_op_id` 生成太晚 → `NameError`。

---

## 3. 请你优先核这 2 个事务边界

你说过 v6.1 只需核事务边界 + 对应负向测试。当前新增/改动的事务边界就两条，
我把精确测试编号列出来，你可以只看这些：

### 边界 A：BEGIN 的原子性现在**包含写盘**（claim 的完成条件是落盘成功）

- 正向：`B11` 写盘失败→BEGIN 拒绝 / `B11b` 不返回 op_id / `B11c` 原因含 `claim_persist_failed`
- **负向（v6.0 实证）**：`B11-v60` —— v6.0 忽略写盘失败**仍发 op_id**，双 MARKET 窗口成立
- 对称：`B12/B12b`（回滚写盘失败绝不谎称已回滚）+ `B12-v60` 负向
- 对称：`B13/B13b`（reason 写盘失败绝不谎称已切换）
- 变异体：`M1-begin-persist` / `M2-rollback-persist` / `M3-reason-persist`，
  撤销检查后上述用例**必须**失败

**要你判的**：把 `_persist_states()` 的返回值纳入 claim 的原子性，
是否是这个事务边界的正确闭合方式？还是应该把「claim 成功」与「落盘成功」
拆成两个可区分的状态分别上报？

### 边界 B：限价 ENTRY gate 失败后的回滚

- `L1` gate 通过→落后续生产段 / `L1b` gate 通过时绝不回滚、零 persist
- `L2` ENTRY 查不到→gate=False→**不撤 TP、不挂 LIMIT** / `L2b` CAS 回滚成功
  phase=0 / `L2c` 回滚成功时不切异常 reason
- `L3` 快照 None→gate=False→回滚 / `L3c` **必须干净拦截，不得依赖异常兜底**
  （防 `or []` 回归 —— 事故原型的 UNKNOWN→EMPTY）
- 变异体：`M8-verify-always-gone` / `M9-snapshot-check`

**要你判的**：限价 gate 失败时「优先 CAS 回滚、回滚失败才落 `limit_entry_unknown`」
这个分治是否成立？我判断的依据是**限价 gate 失败时仓位零变化**（尚未挂单），
而市价 gate 失败时 MARKET **已成交**（仓位已变）→ 市价只能冻结 + `market_entry_unknown`。
这两者的语义差异是我自己推的，请你确认。

---

## 4. 我自查抓到并已处置的（供你复核，不是为了凑数）

| 编号 | 内容 | 处置 |
|---|---|---|
| **F-1** | **v6.1 初版自己引入的回归**：`entry_orders_short` 校验会把 🗑️ 按钮批次**永久挡死**。生产 `cancel_open_orders`（L6896-6897）只截断 `entry_orders` 到 `last_filled_count`、**不动 `target_amounts`** → 那是生产自己创造的合法状态 | 收窄为**精确截断签名**：仅 `0 < len(_eo) == last_filled_count` 放行 |
| C-3 | D6gb **装饰品断言**（只断言副作用，不钉前置条件 → 缺陷版反而通过） | 断言改为把「链已成功」写成前置条件 |
| C-4 | S2c/S2-v5b 裸 `list.index()` → 调用被移除时**崩溃**而非失败 | 换 `seq_index()`，缺失返回 None |
| C-5 | AFTER 无显式返回时 `ret[0]` 抛 TypeError 炸穿套件 | `NO_RETURN` 哨兵 |
| C-6 | gate 第 1 层**只被异常兜底覆盖**：改回 `or []` 后崩溃被吞掉，外在表现与干净拦截完全一致 | 新增 `L3c` catch 模式断言 |
| **C-7** | 见 §2（只给调用不给定义） | 补签名 diff + 日报读取侧 |
| **C-8** | 我新写的 checker **自己先错了一次**：把「文档自带定义」优先级设为最高 → M2 变异被文档里未改动的 helper **副本遮蔽** → 假绿 | 改为 **helper 实现文件 > 文档自带定义 > 生产现状**，并新增【7】判「副本 ≠ 实现」为致命 |

---

## 5. 待你裁定 3 项（我不改，等你指示）

1. **N-1**：`market_confirming` 冻结告警还有 **3 条 fail-silent 残留分支**
   （TERMINAL_ZERO 回滚被拒 / except 通道回滚被拒 / 结算后 clear 未收敛）。
   我目前只堵了 ENTRY gate 一条。三条均有一发 critical 兜底，且触发条件都是
   「已有人工处置入口」的终态。**接受「一发 critical 即足够」，还是 v6.2 补齐？**
2. **N-3**：限价 gate 回滚成功后，监控线程可能把被撤层**误判为「手动撤单」**
   并连带撤掉其余未成交层，且发误导性告警。属生产既有行为（L4809-4856）
   被新路径触发，非 v6.1 引入，且不死锁、不裸仓。**是否要连带改监控？**
3. **D-006**：`_get_today_realized_pnl`（L744-754）同样无条件累加 `net_pnl`，
   `prior_reduction_unknown` 会让它**低估**当日已实现亏损。但被漏掉的那部分
   亏损在任何版本里都不在账本中（App 手动平仓不记账是既有盲区，属 D-008-1 地盘），
   故本改动既不改善也不恶化 D-006 的输入。**我的倾向是不改** —— 让一次已经
   成功平仓的批次反向卡住后续风控闸门，收益不抵复杂度。

---

## 6. 复现方式（全离线，Windows）

> **文件位置变更（v6.1 收尾，影响你复现）**：helper 实现原本放在项目外的
> `G:/tmp/`，不受版本控制 —— 整条验证链挂在一个可能随时消失的临时文件上，
> 这是不可接受的单点。现已把 **7 个附件纳入项目内** `送审附件_v6.1/`：
> `new_helpers_v6.py`（v6.1 正本，880 行）/ `new_helpers_v60.py`（负向基线）/
> `new_helpers_v5.py` / `v4` / `v3` / `v5_after_market_close.py` /
> `new_helpers_v3_entry.py`。
> 3 个 checker 与测试套件的默认路径改为**相对自身 `__file__`**，不再依赖 CWD；
> 变异体注入改为环境变量（`HELPER_PROJECT_DIR` + `V6_HELPER_OVERRIDE`），
> 并保留 `TEST_HOOK` 校验防注入点被悄悄移除。只有变异产物仍写
> `G:/tmp/mut61_auto/`。下方命令可直接从项目根目录复跑。

```text
# 测试套件（133 场景，约 16 秒）—— 注意必须用 .venv，ccxt 只装在那里
.venv/Scripts/python.exe test_close_confirmation_v6.py

# 四道机器检查
python check_doc_code_blocks.py            # 14/14 代码块 ast.parse + 零 Ellipsis
python check_doc_helper_parity.py          # 11/11 helper 与文档 ast.dump 同构
python check_doc_helper_calls.py           # 调用闭包 7 查（C-7 的来源）
python run_mutation_checks_v61.py          # 10 变异体全杀死 + M0 基线（约 2.5 分钟）

# 各 checker 自带变异自测（证明 checker 自己不会假绿）
python check_doc_helper_parity.py --self-test     # M1-M5
python check_doc_helper_calls.py --self-test      # M1,M2,M3,M4,M6
```

当前实测：**133/133 rc=0；四道检查全 rc=0；全量回归 43 个 test_*.py → 41 rc=0**
（2 个 rc=1 是 v3 已撤销机制 `allow_flag_rollback` 的遗留测试，落生产时应归档）。

---

## 7. 我想要的结论

1. §3 的两个事务边界（A / B）**是否闭合**；
2. §5 的 3 项裁定；
3. 若通过，**落生产顺序**是否按「BEGIN → derive → 市价/限价 AFTER → helper 全集 →
   结算/clear → 监控冻结」推进。
