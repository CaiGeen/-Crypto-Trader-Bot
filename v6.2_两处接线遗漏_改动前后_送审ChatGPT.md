# 两处接线遗漏 · 改动前/后源码（送 ChatGPT 独立核实）

> 应你的要求：只有片段，没有解释报告。两处我均已自行对照源码核实过（非仅子代理结论）。
> 若实锤 → 按 A/B/C 裁定打最小补丁；不改则直接 landing。

---

## 问题 1：monitor 的 `is_programmatic` 仍读批量级 flag，而 v6.2 🗑️ 不再写它

### 改动前（staged L4761，与生产 L4752 同一段；v6.1 时代配对成立）

生产 v6.1 的 🗑️（`trader_260725.py` L6872）**写** flag：
```python
        # 🔥 设置标记：这是程序主动撤单
        target_b_data['is_programmatic_cancel'] = True
        self.save_batch_state(target_symbol, batch_id, target_b_data)
```

monitor（staged L4761）**读** flag：
```python
                                latest_all_check = self.load_all_states()
                                latest_b_data_check = latest_all_check.get(symbol, {}).get(batch_id, {})
                                is_programmatic = latest_b_data_check.get('is_programmatic_cancel', False)

                                if is_programmatic:
                                    # 程序主动撤单，不触发手动撤单逻辑
                                    ...
                                else:
                                    manual_canceled_detected = True   # ← 误判入口（staged L4768）
```

v6.2 的 🗑️（`after_candidates_v62.py` L117-118）**删了写入**：
```python
    # 🔒 v6.2（ΔE1）：归因 order-ID scoped——不写 batch-global sticky
    # is_programmatic_cancel（棘轮字段，且会永久关闭 SL/TP 自动补挂）。
```

### 触发链（lfc>0 批次，🗑️ 后批次保持 ACTIVE 是常态路径）

🗑️ 撤掉未成交 ENTRY 条件单 → D-4 下 `entry_orders` 保留 → monitor 下轮 poll 见
`ord_status='canceled'` → flag=False（无人写了）→ 误判「手动撤单」→ staged L4818-4856：
**压缩 `entry_orders` 至已成交层（同版本自破 D-4）+ 写 `is_programmatic_cancel=True` 棘轮（SL/TP 不再自动补挂）**。

### 拟议修复（只加只读回退，8 行；仅在 ENTRY 条件单监控这一处读点）

```python
                                is_programmatic = latest_b_data_check.get('is_programmatic_cancel', False)
                                if not is_programmatic:
                                    # v6.2 ΔE1 后 🗑️ 不写批量级 flag；程序撤单事实按 ID 存于 registry
                                    for _pc_e in (latest_b_data_check.get('protection_registry') or {}).values():
                                        if (isinstance(_pc_e, dict)
                                                and str(_pc_e.get('order_id')) == str(order_id)
                                                and _pc_e.get('state') == 'PROGRAMMATIC_CANCELED'):
                                            is_programmatic = True
                                            break
```

（`order_id` 在该分支已在作用域内，L4754 `terminal_orders.add(order_id)` 即用。）

**范围说明**：同构读点还有 SL/TP 监控两处（staged L5352/L5493），但 TP/SL 只被平仓流程撤销，
平仓流程 BEGIN 已写批量级 flag（`_begin_close_request_if_active` → `is_programmatic_cancel=True`），
**这两处不在断链上，不动**。

---

## 问题 2：🗑️ 零成交复核循环不跳过已确认终结（PC）的层

### 改动前（`after_candidates_v62.py` L158-164；staged 同段 L8092-8098）

主循环已有跳过（L76-78，r6 修复）：
```python
        if str(order_id) in _known_terminal:
            # 已确认程序终结：不撤、不验证、不计入任何失败统计
            continue
```
但同函数的零成交复核循环**没有**跳过：
```python
        unresolved = []
        _filled_found = False
        for order_id in entry_orders:
            verdict, _vo = self._verify_entry_order_terminal(order_id, target_symbol)
            if verdict == 'gone':
                continue
            unresolved.append((order_id, verdict))
```

而 verifier 对 OrderNotFound 的语义（`new_helpers_v62.py` L906-908，v6 已批准纪律）：
```python
            except ccxt.OrderNotFound:
                ...
                return 'unknown', None
```

### 触发链

首次 🗑️：E1 撤成功→PC 落 registry；E2 异常→unresolved → 返回 False。
重试：主循环跳过 E1（PC），但复核循环**再次 verify E1** → 交易所查无此单 → OrderNotFound →
`'unknown'` → `unresolved` 永远非空 → `return False`「撤单后仍有 N 张 ENTRY 未确认终态」→ **该批次永不收敛**。

### 拟议修复（2 行，复用同函数已存在的 `_known_terminal` 集合）

```python
        for order_id in entry_orders:
            if str(order_id) in _known_terminal:
                continue
            verdict, _vo = self._verify_entry_order_terminal(order_id, target_symbol)
```

（`_known_terminal` 在函数入口已从 registry 重建，主循环同款判据，无新增 helper/state/字段。）

---

## 修后动作（若裁定实锤）

只重跑既有测试：green 28 → 29（B-2 补 1 个用例）、staged integration 20、canonical 133/133×2、
M11~M38 中涉及候选代码的既有项 → 全绿即停，直接出 4 项 landing 材料。
**不新增 M39/M40、不新增 checker、不做架构审查。**
