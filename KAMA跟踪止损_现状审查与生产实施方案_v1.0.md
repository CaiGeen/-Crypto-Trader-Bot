# KAMA 跟踪止损与自动保本保护线

## 现状审查与生产实施方案 v1.0（复审修订稿）

> 文档性质：设计审查 + 生产实施规格 + 验收计划
> 设计基线：`.workbuddy/memory/discussions/D-001_最终实施规格_v1.0.md`（设计定稿 v1.7）
> 适用项目：`my-crypto-bot`
> 主要生产文件：`trader_260725.py`、`bot_runner.py`、`watchdog.py`
> 审查日期：2026-09-24
> 复审修订：2026-09-24，逐项核实外部复审建议后修订；不改变已拍板的第 3K 激活、999 根预热、只进不退和固定 TP 共存口径
> 当前上线结论：**不具备实盘启用条件**

---

## 1. 文档目的

本文档用于统一回答以下问题：

1. “KAMA + 跟踪止损”最终应实现什么；
2. 当前仓库已经实现到什么程度；
3. 现有回测代码与最终生产规格有哪些差异；
4. 如何安全接入真实交易、保护单更新与状态持久化；
5. 如何验证无未来数据、自动保本、保护线单调性、并发安全和崩溃恢复；
6. 在满足哪些验收条件后，才允许进入实盘灰度。

本文档不是“功能已经上线”的证明。当前生产交易引擎没有接入 KAMA 跟踪止损，现有相关代码仅属于回测研究原型。

---

## 2. 结论摘要

### 2.1 当前状态判定

| 项目 | 当前状态 | 说明 |
|---|---|---|
| KAMA 数学公式 | 基本具备 | 回测指标中已实现，播种索引需与最终规格复核 |
| ER 除零保护 | 已实现 | volatility 为零时 ER 取 0 |
| no-lookahead 回测 | 基本具备 | 当前根先检查出场，peak 收盘后才更新 |
| KAMA + ATR 回测脚本 | 已存在 | `strategies_backtest/trailing_nolookahead.py` |
| 最终设计规格 | 已完成 | D-001 v1.7，明确标注“未实施” |
| 专项自动化测试 | 缺失 | 当前不存在有效的 `test_trailing_nolookahead.py` |
| 生产交易接入 | 未实现 | `trader_260725.py` 无 KAMA 调用 |
| 自动保本 | 未实现 | 未接入第 3K 保护线替换 |
| 动态保护单更新 | 未实现 | 未实现先挂新、持锁更新、撤旧流程 |
| 状态持久化与恢复 | 未实现 | D-001 状态字段未形成生产闭环 |
| 手动干预状态机 | 未实现 | `/be`、`/sl`、`/tp` 尚未联动 KAMA |
| 实盘上线资格 | 不具备 | 尚未满足最终规格验收条件 |

### 2.2 核心判断

当前实现不能直接称为“KAMA + 跟踪止损已经实现”，准确表述应为：

> KAMA 与混合跟踪止损的**回测辅助原型**已经存在；完整生产方案已经定稿，但尚未接入交易引擎。

现有回测脚本使用：

```text
ATR 跟踪止损 + 突破价底线 + KAMA 过滤线
```

而最终生产目标为：

```text
第 3 根 4H K 收盘自动保本
+
KAMA 盈利方向锁定
+
交易所动态保护单安全替换
+
状态持久化、崩溃恢复和手动干预
```

两者不是同一实现，不能直接将回测函数复制到生产交易引擎。

---

## 3. 现状代码审查

### 3.1 当前已有代码

#### 回测指标

文件：`strategies_backtest/indicators.py`

包含 KAMA 函数，默认参数：

```text
source = close
ER length = 10
fast length = 2
slow length = 30
```

主要公式已实现：

```text
fast_sc = 2 / (fast + 1)
slow_sc = 2 / (slow + 1)
ER = abs(price[i] - price[i-ER]) / volatility
SC = (ER × (fast_sc - slow_sc) + slow_sc)²
KAMA[i] = KAMA[i-1] + SC × (price[i] - KAMA[i-1])
```

ER 分母为零时取 0，避免除零异常。

#### 无未来数据回测脚本

文件：`strategies_backtest/trailing_nolookahead.py`

该脚本：

- 计算 KAMA；
- 使用信号突破价和固定目标价；
- 持仓超过 3 根后启用 ATR 跟踪；
- 将 KAMA 加入多头 `max`、空头 `min`；
- 当前根先检查止损/止盈；
- 当前根结束后再更新 peak。

#### 验证脚本

文件：`strategies_backtest/verify_nolookahead.py`

用于比较有未来数据和无未来数据版本，属于研究验证工具。

### 3.2 生产代码现状

对非归档、非回测的生产 Python 文件进行调用搜索，没有发现：

- `kama`；
- `trailing_nolookahead`；
- KAMA 管理器；
- 第 3K 自动保本触发；
- 4H 收盘驱动；
- `locked_kama` 状态；
- 动态保护线替换流程。

因此生产交易引擎仍使用原有固定 TP/SL 逻辑，不会因为回测目录存在 KAMA 代码而自动启用该策略。

---

## 4. 现回测原型与生产规格差异

| 维度 | 当前回测原型 | 最终生产规格 |
|---|---|---|
| KAMA 角色 | 每根直接读取 `kama[j-1]` | 盈利方向锁定为 `locked_kama` |
| 保本价 | 主要以突破价 `bt` 代替 | 按实际持仓成本及规格规则计算保本价 |
| 激活条件 | 持仓超过 3 根 | 第 3 根 4H K 收盘后 |
| 方向 | 直接取信号方向 | 激活后持久化并锁定方向 |
| 状态 | 无持久化 | 完整 D-001 状态机 |
| 保护线公式 | `max/min(trail_stop, bt, kama)` | `max/min(保本价, locked_kama)` |
| ATR | 整笔持仓固定使用信号根 ATR | 不应未经新规格裁决直接进入生产 |
| 保护单 | 纯回测虚拟止损 | Binance 实盘 STOP_MARKET |
| 替换时序 | 不适用 | 先挂新、确认、持锁写盘、撤旧 |
| 加仓 | 简化统计 | 立即重算保本价并按新实际持仓替换保护单 |
| 减仓 | 未接生产口径 | 价格只进不退，数量按实际持仓更新 |
| 手动干预 | 无 | `/be`、`/sl`、`/tp`、恢复、拒绝 |
| 重启恢复 | 无 | 状态恢复、补触发、孤儿单清理和对账 |
| 异常降级 | 无资金状态变更 | 保留旧保护线，告警并重试 |

### 4.1 混合回测公式的局限

当前多头近似使用：

```python
sl = max(peak - trail_atr * atr, breakthrough, kama)
```

空头近似使用：

```python
sl = min(peak + trail_atr * atr, breakthrough, kama)
```

这只能用于研究。它没有表达：

- 自动保本价的实际成本口径；
- 已锁定 KAMA；
- 保护线只进不退的持久状态；
- 交易所订单状态；
- 新旧保护单并存窗口；
- 崩溃后孤儿单；
- 手动操作与自动更新互斥。

### 4.2 KAMA 播种位置偏差

最终规格要求：

```text
KAMA[9] = price[9]
i < 9 不输出有效值
从 i = 10 开始递推
```

当前 `indicators.py` 使用：

```python
out[er_len] = close[er_len]
```

默认等价于从 `out[10] = close[10]` 播种，和最终规格错一根。1000 根预热下影响可能收敛，但会破坏 TradingView 固定序列对账。实施前必须用 TradingView 样本确认并统一。

---

## 5. 最终业务目标

买入成交后，以成交时所在的 4H K 作为 `entry_kline_open`：

```text
第 0K～第 2K 收盘：
  固定 TP + 初始 SL
  不启动 KAMA 自动保本

第 3K 收盘：
  确认 KAMA 数据
  计算保本价
  锁定有利方向的 KAMA
  安全替换为动态保护线
  标记自动保本和 KAMA 激活完成

后续每根 4H K 收盘：
  计算最新已收盘 KAMA
  仅当新保护线更优时替换
  锁定值与保护线只进不退

固定 TP 或动态保护线触发：
  进入现有结算路径
  清理当前批次全部残留保护单和队列状态
```

做多示例：

```text
上方：固定 TAKE_PROFIT_MARKET
下方：第 3K 后动态 STOP_MARKET = max(自动保本价, locked_kama)
```

做空时方向相反：

```text
动态 STOP_MARKET = min(自动保本价, locked_kama)
```

---

## 6. KAMA 计算与数据管道

### 6.1 参数

固定采用：

```text
source = close
ER length = 10
fast length = 2
slow length = 30
K 线周期 = 4h
```

### 6.2 计算公式

```text
fast_sc = 2 / (2 + 1) = 2/3
slow_sc = 2 / (30 + 1) = 2/31

ER[i] = abs(close[i] - close[i-10])
        / sum(abs(close[j] - close[j-1]), j=i-9..i)

SC[i] = (ER[i] × (fast_sc - slow_sc) + slow_sc)²

KAMA[i] = KAMA[i-1] + SC[i] × (close[i] - KAMA[i-1])
```

当 volatility 为 0：

```text
ER[i] = 0
```

不得直接执行 `0 / 0`。

### 6.3 预热和播种

最终目标：

- 使用至少 999 根已收盘 4H K；常见请求 `limit=1000`，过滤未收盘 K 后通常为 999 根；
- 边界情况下若交易所返回 1000 根且全部已收盘，可全部使用，不得为了凑 999 而丢弃有效已收盘 K；
- 不存在“100 根即可激活 KAMA”的放行条件；少于 999 根时不得把本轮 KAMA 当作可交易依据；
- 不缓存随机种子，每次按需重拉；重启或断线后重拉 999～1000 根是**重新计算当前值**，不是回放历史 K 线补执行每一轮；
- 播种索引必须与 TradingView 固定样本一致：同一 symbol、同一 Binance 合约源、同一固定时间窗、同一组参数 `10/2/30`；
- 固定样本的最近 5 根已收盘 KAMA 与 TradingView 偏差必须小于 0.05%，不得用任意时刻的临时截图替代固定样本验收。

### 6.4 已收盘过滤

若当前 4H 桶起点为 `bucket_start`，最后一根满足：

```text
open_time >= bucket_start
```

则丢弃。

必须使用 `>=`，不能使用 `>`，否则可能把正在形成的新 K 混入。

### 6.5 数据质量门

计算前必须全部满足：

1. 过滤后已收盘 K 数量不少于 999；若边界返回 1000 根且全部已收盘，则 1000 根也合法；
2. 时间戳按升序排列；
3. 相邻 `open_time` 差必须为 4 小时；
4. 存在有限 KAMA 值；
5. 无 NaN/None；
6. 首次锁定 KAMA 时，偏离比较基准必须是本轮**自动取优保本价**，不是旧保护价、市场价或 0：多头允许区间为 `[breakeven × 0.95, breakeven × 1.05]`，空头按反方向执行同一 5% 边界；越界只拦截 KAMA 锁定，不撤销既有保护；
7. 任何失败均不得覆盖当前有效保护线；KAMA 数据失败时只跳过 KAMA 更新，不阻塞已可安全计算的保本保护线。

### 6.6 请求节流

- 使用独立 `fetch_ohlcv` 限流路径；
- 常规运行优先以 `limit=2` 检查是否跨过 4H 桶；
- 跨桶后才拉取全量数据；
- 同一跨桶事件在并发线程中只能由一个执行者处理；
- 拉取失败不得影响原有 SL 和固定 TP。

---

## 7. 状态字段与状态机

### 7.1 批次状态字段

每个批次在本地状态中持久化以下字段：

| 字段 | 含义 |
|---|---|
| `entry_kline_open` | 成交时所在 4H K 的 `open_time` |
| `auto_be_done` | 自动保本是否已执行或被手动 `/be` 跳过 |
| `protective_active` | 动态保护线是否已生效 |
| `kama_active` | KAMA 更新是否处于激活状态 |
| `kama_paused` | 因用户手动干预而暂停 |
| `kama_cancelled` | 用户明确拒绝恢复，本批次永久取消 KAMA |
| `locked_kama` | 已锁定的最有利 KAMA 方向值 |
| `protective_target_price` | 当前已生效的动态保护价格 |
| `pending_cancel_sl_ids` | 已确认应撤、但尚未确认撤销成功的保护单 ID 列表 |

### 7.2 合法状态组合

至少允许并测试以下组合：

| `auto_be_done` | `protective_active` | `kama_active` | `kama_paused` | `kama_cancelled` | 含义 |
|---|---|---|---|---|---|
| false | false | false | false | false | 第 3K 前正常持仓 |
| true | false | false | false | false | 手动 `/be` 已执行，但尚未进入动态保护线 |
| false | true | true | false | false | 自动保本和 KAMA 正常激活 |
| true | true | true | false | false | 手动 `/be` 后已动态激活 |
| false | false | false | true | false | 3K 前手动 `/sl` 或 `/tp` 后暂停 |
| true | true | true | true | false | 手动干预后暂停，保留现有保护线 |
| true/false | true/false | false | true | true | 用户拒绝恢复，本批次不再自动更新 |

启动恢复时必须校验状态组合；非法组合应告警并进入安全降级，不得猜测性自动修复资金状态。

### 7.3 核心单调性不变量

1. 多头：`protective_target_price` 只允许上升；
2. 空头：`protective_target_price` 只允许下降；
3. 多头：`locked_kama` 只允许取更大值；
4. 空头：`locked_kama` 只允许取更小值；
5. KAMA 短暂回撤不得使保护线变得更差；
6. 数据质量失败不得改变任何已持久化保护状态；
7. 新保护单未确认有效前，不得更新 `current_sl_id`；
8. 动态保护线激活后，任何普通重建路径不得降回 `stop_steps` 初始价。

---

## 8. 自动保本与 KAMA 更新流程

### 8.1 自动保本价计算

批次口径使用剩余净持仓，而不是历史毛成交量：

```text
qty = 本批次当前剩余净数量
E = 剩余净成本 / qty
```

用户界面统一称为“预估保本价”。含费预估公式为：

```text
多头：breakeven = E × (1 + f_in) / (1 - f_out)
空头：breakeven = E × (1 - f_in) / (1 + f_out)
```

其中 `f_in` 是入场费口径，`f_out` 是动态 STOP_MARKET 触发后的预估退出费口径。若已有可归属的实际入场手续费，优先按金额精确计算：

```text
多头：breakeven = (剩余净成本 + 剩余入场费) / (qty × (1 - f_out))
空头：breakeven = (剩余净成本 - 剩余入场费) / (qty × (1 + f_out))
```

规则约束：

1. 费率或金额缺失、非有限、无法归属时，不得用 0 伪装实际保本价；本轮保留既有保护线并记录原因，后续重检；
2. 严禁因为“到点但无法证明保本”而擅自市价平仓；
3. 名义保本价为 `E`，含费预估保本价按上述公式计算；自动取优保本价在多头取二者较高值、空头取二者较低值；
4. 名义保本价只用于展示和自动取优比较，不能在手续费未知时被包装成“实际保本”；
5. 资金费、资金费率预测可作为后续增强，不属于 v1 上线门槛；
6. 加仓后立即按新 `qty/E/fee` 重算，不等待下一根 4H K。

### 8.2 第 3K 激活条件

同时满足以下条件才执行：

```text
entry_kline_open 有效
最新已收盘 4H K 的 open_time == entry_kline_open + 3 × 4h
server_time >= entry_kline_open + 4 × 4h（即第 3K 收盘时刻，entry_kline_open + 16h）
auto_be_done == false
kama_paused == false
kama_cancelled == false
protective_active == false（补激活场景可按规格例外处理）
```

### 8.3 激活步骤

1. 记录当前 4H K 桶基准并保证同一收盘事件只处理一次；
2. 拉取并过滤 4H K，要求不少于 999 根已收盘数据；
3. 校验时间戳连续性、有限值和 KAMA 播种结果；
4. 先计算本批次剩余净持仓的预估保本价；若费用或数量无法安全归属，保留原保护线并记录，严禁市价平仓；
5. 若 KAMA 数据不可用，仅执行 D-001 情况 A 的纯保本保护线：设置 `auto_be_done=true, protective_active=true, kama_active=true`，但 `locked_kama=None`；下一根已收盘 K 再重试 KAMA 锁定；
6. 根据方向取更优值：多头取 `max(breakeven, locked_kama)`，空头取 `min(breakeven, locked_kama)`；`locked_kama` 从未成功锁定时才允许纯保本价兜底；
7. 若 KAMA 首次锁定相对本轮自动取优保本价偏离超过 5%，只拦截本轮 KAMA 锁定并告警，`locked_kama` 保持未锁定；若预估保本价有效，仍可建立纯保本保护线，且不撤销既有保护线；
8. 调用安全替换保护单；
9. 新保护单有效后持久化 D-001 状态和订单 ID；
10. 发送 Telegram 通知；
11. 后续再撤旧保护单。

### 8.4 常规更新条件

每根新的 4H K 收盘后：

```text
protective_active == true
kama_active == true
kama_paused == false
kama_cancelled == false
批次仍有可保护的实际持仓
```

新保护线只有在严格优于旧价时才进入替换流程：多头要求 `candidate > protective_target_price`，空头要求 `candidate < protective_target_price`；相等时不重复挂单。

### 8.5 重启错过的第 3K

若重启时已经超过第 3K，且 `entry_kline_open` 有效、`auto_be_done == false`、未暂停、未取消，则立即补触发，不等待下一根 K。补触发必须先于普通保护线缺失恢复路径执行。老批次若没有 `entry_kline_open`，必须默认跳过。

---

## 9. 保护单安全替换方案

### 9.1 设计原则

动态保护线不得使用“先撤旧、再挂新”的裸奔方式。必须采用：

```text
先挂新 → 确认新单 → 持锁更新状态 → 撤旧 → 登记/消费待撤 ID
```

允许新单和旧单短暂并存，以保证持仓任何时刻至少有一张有效保护线。

### 9.2 统一入口

所有动态保护线变更必须经过 `_replace_protective_sl(batch, target_price, reason, ...)`。该函数负责价格和方向校验、批次锁、实际持仓读取、挂出新 STOP_MARKET、交易所状态确认、状态持久化、旧单撤销、待撤 ID 消费以及告警审计。调用方不得自行直接改动态保护单。

候选价计算规则：

1. D-001 唯一经济公式保持不变：多头 `max(breakeven, locked_kama)`，空头 `min(breakeven, locked_kama)`；
2. `locked_kama` 缺失时不参与比较，不得用 0/None 代替；若预估保本价有效，则允许按 D-001 情况 A 用纯保本价兜底；
3. 预估保本价缺失或不可证明时，中止本轮替换并保留原保护线，后续重检；
4. 当前交易所侧已确认有效的保护价只作为“不得放宽”的工程护栏：新价不得比它更差；它不改变 D-001 唯一公式的定义，也不得被误当作新的经济候选价。

### 9.3 挂单时序

```text
T0：读取最新批次状态、实际剩余持仓和当前有效保护单
T1：分别计算目标价与目标数量；价格按 tick 归一化，数量按 step/最小数量归一化
T2：获取 per-batch KAMA 锁
T3：再次读取状态，验证没有更高优先级手动操作，且目标价不会放宽现有有效保护
T4：先持久化最小替换意图（batch、symbol、side、旧单 ID、目标价、目标量、参数指纹；新单 ID 允许暂缺），并把旧单登记到 pending_cancel_sl_ids
T5：创建新 STOP_MARKET
    - params 必须从该批次 params_base.copy() 派生
    - Hedge Mode：保留 positionSide=LONG/SHORT，不同时追加 reduceOnly
    - One-way Mode：追加 reduceOnly=true
    - 任何创建路径均不得使用 closePosition=true
    - amount=归一化后的剩余净持仓数量
T6：查询新订单状态
    - open：接受为当前保护线
    - triggered：只代表条件已触发，不等同于成交；先确认真实成交/仓位归零，未确认前不登记为在场保护线，也不得提前撤销其他有效保护
    - closed/filled：按真实成交事实进入正常结算
    - canceled：视为挂单失败，旧线继续
T7：确认 open 后写盘 current_sl_id、protective_target_price 和 pending_cancel_sl_ids
T8：撤销旧保护单
T9：持久化删除已成功撤销的 pending ID
```

若 T4～T9 任一步中断，下一轮只能先对账并补完本次替换，不得在未知状态下发起新一轮候选价替换。若 Binance USDM 算法单接口在当前锁定 `ccxt==4.68` 下验证支持并回显稳定 `clientOrderId`，可将客户端幂等 ID 写入 T4 的替换意图；未完成实测前只作为增强项，不作为唯一归属依据。

### 9.4 锁要求

- `self._kama_lock` 保护单批次状态机；
- `_state_lock` 保护状态文件读写；
- 锁内 API 使用 `_fast_api_call`：单次调用、临时短超时不超过 10 秒、读取全局冷却后快速失败，不使用带重试链的 `_safe_api_call`；
- 禁止持 KAMA 锁执行无界等待任务。

手动 `/sl`、`/be`、限价平仓监控和 SL 丢失检测必须与自动替换使用同一批次互斥边界。

### 9.5 状态合并保护

`save_batch_state` 必须在 `_state_lock` 内重读磁盘最新状态，防止旧式调用者用陈旧快照覆盖 D-001 字段。建议增加显式合法写者参数 `explicit_d001_fields`；保护字段默认以磁盘最新值为准，只有白名单路径才能覆盖。

---

## 10. 手动干预状态机

### 10.1 `/be`

无论 `protective_active` 是否已为真，都必须无条件设置 `auto_be_done = true`。第 3K 前执行时，第 3K 自动保本与 KAMA 自动激活按既定口径跳过；`protective_active == false` 时按手动保本流程更新初始 SL。`protective_active == true` 时不重挂保护线，也不暂停或清除既有 KAMA。重复执行必须幂等。若入场费、净数量或预估退出费无法安全归属，不得伪造保本价，更不得市价平仓；保留现有保护线并记录待重检。

### 10.2 `/sl` 与 `/tp`

成功更新手动止损或固定 TP 后，均设置 `kama_paused = true`。现有交易所保护继续有效，后续自动 KAMA 不覆盖。固定 TP 修改成功后的 Telegram 回执必须明确追加“KAMA 已暂停，可通过恢复按钮恢复”，避免用户误以为动态跟踪仍在更新。

### 10.3 恢复

用户同意恢复后：清除 `kama_paused`，立即按最新已收盘 4H K 重算；只有新保护线严格更优时才安全替换；不得为了恢复 KAMA 而降低现有保护线。

### 10.4 拒绝

用户拒绝恢复后设置：

```text
kama_active = false
kama_cancelled = true
```

本批次不再询问、不再自动更新，但已有固定 TP 和保护线继续有效。

### 10.5 重复点击

所有回调必须先检查状态。已生效则直接回复“已经处理”，不得重复挂单或重复写状态。



---

## 11. 加仓、部分减仓和止损补挂

### 11.1 新层成交

若已激活动态保护：按新实际剩余净持仓和交易成本立即重算预估保本价，再与当前 `locked_kama` 执行 D-001 唯一公式；不等下一根 4H K，立即安全替换保护单；保护线不得退化；不重置 `entry_kline_open`。价格更新和数量更新在同一安全替换事务中完成，但触发原因必须分别记录：价格变化按 tick 判断，数量变化按交易所 step/最小数量判断。

### 11.2 部分减仓

保护价格保持不变；数量更新是独立触发路径。必须在部分减仓成交确认后重新读取本批次剩余净持仓，把保护单数量更新为归一化后的净剩余量。不得用历史毛量、下单请求量或未确认账本量替代；不得因减仓降低多头保护价或提高空头保护价。若剩余净数量与当前保护单数量在交易所精度下相等，则只更新本地账本，不重复挂单。

### 11.3 SL 丢失恢复

若 `protective_active == true`，补挂价格必须使用 `protective_target_price`，禁止回退到 `stop_steps`；数量使用最新实际剩余净持仓；补挂前执行未登记 STOP_MARKET 归属判定，只允许清理可证明属于本程序且属于本批次的孤儿单。未激活时沿用原初始阶梯止损逻辑。

---

## 12. 重启、崩溃和孤儿单恢复

### 12.1 状态恢复

启动时恢复全部 D-001 字段及 `current_sl_id`、`pending_cancel_sl_ids`，并校验状态组合。

### 12.2 待撤孤儿单优先清理

重启后首先处理 `pending_cancel_sl_ids`：查询每个 ID；已不存在则移除；仍 open 且不是当前保护线则尝试撤销；撤销失败则保留记录并告警。未完成前禁止无对账地批量挂新单。

### 12.3 未登记保护单扫描与归属分级

任何挂新保护线前，扫描同 symbol、同方向的未登记 `STOP_MARKET` 保护单。扫描在锁外完成，最终挂新和持锁更新进入互斥区。扫描结果必须分三级处理：

| 归属级别 | 判定依据 | 处理 |
|---|---|---|
| 本程序/本批次可证明孤儿 | 出现在 `pending_cancel_sl_ids`、订单 registry 或 T4 最小替换意图；且 symbol、side、方向参数、价格、数量、类型指纹唯一匹配 | 可按既有流程撤销或补完替换 |
| 已知合法订单 | `current_sl_id`、`tp_order_id`、`limit_close_order_id` 或其他已登记保护单 | 保留，不得误撤 |
| 外部/无法归因订单 | 无持久化意图或唯一指纹；可能是用户手工单、其他程序单或交易所异常残留 | 只隔离和告警，不自动撤销、不自动认领、不挂第三张单 |

这是对 D-001 S29“其余全撤”措辞的安全收窄。实施前应把该裁决回填到 D-001 验收基线；在基线升版前，任何实现都不得恢复“未知即撤销”的默认行为。

### 12.4 挂新成功、写盘前崩溃

恢复时读盘仍可能指向旧保护单。启动扫描发现新单未登记后，只有在持久化替换意图、订单 registry、symbol、side、Hedge/One-way 参数、数量、价格和订单状态全部唯一匹配时，才允许登记为 `current_sl_id`。若 `clientOrderId` 已通过锁定版本实测并被交易所稳定回显，可作为额外强证据，但不得替代完整指纹。无法唯一归因时，保留既有保护、冻结本轮替换并告警，不得直接创建第三张重复保护单。

### 12.5 保护状态缺失

若 `protective_active == true` 但 `protective_target_price` 缺失，按预估保本价和 `locked_kama` 重算并安全重挂，发送 critical 告警。`locked_kama` 缺失时不参与公式，可至少以有效预估保本价保护；若预估保本价也不可证明，则保留当前有效保护线并告警，后续重检，严禁市价平仓。

### 12.6 停机和恢复不得放宽保护

重启、断线、回滚或人工暂停恢复时，只重新计算和对账，不因进程状态变化撤销固定 TP 或当前 SL；恢复价必须满足只进不退。断线期间错过的历史 K 线不逐根回放执行，只在当前已收盘数据上计算一次最新状态，并执行 D-001 规定的第 3K 补触发例外。

---

## 13. 异常降级策略

| 故障 | 必须行为 | 禁止行为 |
|---|---|---|
| 4H K 拉取失败 | 保留现有保护线，退避重试 | 清空状态或把保护价置零 |
| KAMA 含 NaN | 跳过本轮 | 使用 0/None 挂单 |
| K 线缺口 | 跳过并告警 | 强行递推 ER |
| KAMA 首次严重偏离 | 风险拦截 | 接受 flash 行情 |
| KAMA 不可用但预估保本价有效 | 按 D-001 情况 A 保留/建立纯保本保护，下一根 K 重试 KAMA | 因 KAMA 失败撤销原保护或市价平仓 |
| 预估保本价无法安全计算 | 保留原保护线、记录原因并后续重检 | 用 0 手续费冒充实际保本，或擅自市价平仓 |
| 新单返回 triggered | 先确认真实成交和仓位归零，再结算 | 把触发状态直接当成交，或提前撤其他有效保护 |
| 新单创建失败 | 旧线保持有效 | 先撤旧单 |
| 新单状态未知 | 进入对账，不重复挂单 | 盲目登记为 open |
| 状态写盘失败 | 告警并恢复对账 | 继续撤旧单 |
| 替换事务中断 | 下一轮先对账并补完本次替换 | 在未知状态下发起新一轮候选价替换 |
| 无法归因的 STOP_MARKET | 隔离、critical 告警、冻结本轮替换 | 默认撤销、默认认领或挂第三张单 |
| 账户模式/方向参数不匹配 | 拒发订单并告警 | Hedge 省略 positionSide、单向省略 reduceOnly，或用 closePosition=true 创建保护单 |
| 撤旧失败 | 保留旧单和 pending | 丢失待撤 ID |
| Telegram 失败 | 不影响订单和状态 | 回滚已成功保护线 |
| 手动与自动竞争 | 串行化并重读状态 | 双写或陈旧覆盖 |

所有降级必须保住已有防守，不能为了状态整洁主动暴露风险。

---

## 14. 结算和平仓清理

任何退出路径都必须在 `clear_batch_state` 前重新读取最新状态并处理 `current_sl_id`、`pending_cancel_sl_ids`、疑似孤儿 STOP_MARKET、`tp_order_id` 和 `limit_close_order_id`。

结算触发时先确认真实状态和成交，再撤其他保护单。`STOP_MARKET` 返回 `triggered` 只说明条件被触发，不证明实际平仓成交；必须结合成交记录、订单终态和仓位归零确认。覆盖保护线止损、固定 TP、持仓归零、市价平仓、限价平仓、手动平仓、监控线程 finally 和进程退出。平仓后不得残留 TP、SL、限价单和待撤记录。

---

## 15. 并发与一致性设计

1. 同一 4H 收盘事件只能由一个 KAMA 管理任务处理；可用每 batch 任务或锁内 `last_processed_kline_open` 去重。
2. 手动操作优先。自动更新持锁后必须重读状态，不得用陈旧 KAMA 覆盖。
3. 状态写入必须在 `_state_lock` 内重读磁盘，临时文件加 `os.replace` 原子落盘。
4. `pending_cancel_sl_ids` 使用增量合并和去重，禁止陈旧整表覆盖。
5. 每批次独立状态和锁，不同批次不互相阻塞；共享交易所 API 仍受全局限流控制。
6. v1 手动优先以“同一批次锁 + 写前重读 + 字段保护合并 + 合法写者白名单”为准；新增 `state_version` 可作为未来多进程增强，但不是本版验收必需字段。

---

## 16. 推荐代码结构

```text
kama_trailing.py
  - kama()
  - filter_closed_4h_bars()
  - validate_ohlcv_sequence()
  - BreakevenPricePolicy
  - KamaTrailingState
  - calculate_estimated_breakeven()
  - calculate_candidate_price()
  - should_replace_protection()
  - validate_state_combination()
  - migrate_legacy_batch()

trader_260725.py
  - fetch_ohlcv_kama_data()
  - 第 3K 补触发和 4H 收盘管理器
  - _replace_protective_sl() / _fast_api_call()
  - build_protection_order_params()
  - classify_untracked_stop_market()
  - build_replacement_intent_fingerprint()
  - D-001 状态持久化
  - 手动干预、加减仓、补挂和结算清理接线

test_kama_trailing.py
  - 固定向量、数据过滤、缺口、状态组合和单调性

test_kama_order_replacement.py
  - 挂新、撤旧、pending、故障注入和并发

test_kama_integration.py
  - 第 3K、重启、手动操作、加减仓和结算
```

---

## 17. 分阶段实施方案

### 阶段 A：冻结规格基线

修复文档边界，明确 TradingView 固定播种样本，建立专项测试目录和命名约定；同步回填 D-001 S29 的孤儿单安全收窄裁决，再冻结实施基线。

### 阶段 B：纯 KAMA 数据组件

实现独立 `kama_trailing.py`、999 根已收盘过滤、质量门、预估保本价公式和纯函数测试；不依赖 Telegram 或交易所写操作。

### 阶段 C：状态机与持久化

加入 9 个 D-001 字段、状态组合校验、字段保护合并、合法写者白名单、老批次兼容和恢复测试。

### 阶段 D：保护单安全替换

实现 `_fast_api_call`、`_replace_protective_sl`、Hedge/One-way 参数契约、新单状态确认、T4 替换意图、pending 持久化、孤儿归属分级和故障注入。

### 阶段 E：生产主循环接线

接入 4H 收盘、第 3K 激活/补触发、常规更新、SL 丢失恢复、加减仓、Telegram 提示和手动按钮。

### 阶段 F：影子运行

只记录 `expected_candidate`、旧目标、决策原因、数据质量和状态快照，不修改交易所挂单。至少观察一个完整 4H 周期。

### 阶段 G：小范围实盘灰度

建议单 symbol、极小仓位、详细审计；人工核对每张动态保护单，完成至少 3 次更新、一次手动暂停/恢复和一次重启恢复演练后再扩大范围。

---

## 18. 测试与验收矩阵

| 编号 | 场景 | 预期 |
|---|---|---|
| T01 | KAMA 固定 symbol、固定时间窗、固定参数样本对账 TradingView | 最近 5 根已收盘 KAMA 偏差 < 0.05% |
| T02 | ER 长度不足 | 返回 NaN，不负索引 |
| T03 | volatility 为 0 | ER=0，不崩溃 |
| T04 | 未收盘 K 的 `open_time == bucket_start` | 被过滤 |
| T05 | 4h 时间戳缺口 | 跳过并告警 |
| T06 | fetch 失败 | 旧 SL/TP 不变 |
| T07 | 第 0K～2K 收盘 | 不激活 |
| T08 | 第 3K 收盘 | 挂新、持锁更新、撤旧 |
| T09 | 重复第 3K 事件 | 幂等，不重复挂单 |
| T10 | 重启已超过第 3K | 立即补触发一次，不回放历史每根 K |
| T11 | 老批次无 `entry_kline_open` | 不误触发 |
| T12 | 多头 KAMA 上涨 | 保护价上调 |
| T13 | 空头 KAMA 下跌 | 保护价下调 |
| T14 | 多头 KAMA 回撤 | 保护价不降 |
| T15 | 空头 KAMA 反弹 | 保护价不升 |
| T16 | KAMA 首次相对自动取优保本价偏离 >5% | 拦截 KAMA 锁定并告警，不撤既有保护 |
| T17 | 新保护单创建失败 | 旧单保持 |
| T18 | 新单 canceled | 不登记 current_sl_id |
| T19 | 新单 open | 登记后撤旧 |
| T20 | 新单立即 triggered | 不等同成交；先核实成交/仓位，再结算，不当作在场保护线 |
| T21 | 新单成功、写盘前崩溃 | 重启扫描恢复，不挂重复单 |
| T22 | 状态写盘失败 | 保留双单证据并对账，不撤旧 |
| T23 | 撤旧失败 | 保留 pending，重启优先清理 |
| T24 | 手动 `/be` | 3K 前跳过自动激活；激活后幂等且不暂停 KAMA |
| T25 | 手动 `/sl` | `kama_paused=true` |
| T26 | 手动 `/tp` | `kama_paused=true` 且回执明确提示 |
| T27 | 恢复 KAMA | 立即按最新已收盘 K 重算，且不得放宽保护 |
| T28 | 拒绝恢复 | 本批次永久取消 KAMA |
| T29 | 新层成交 | 立即按剩余净持仓重算保本并更新数量 |
| T30 | 部分减仓 | 价格不变、数量按净剩余量更新 |
| T31 | SL 丢失 | 使用 `protective_target_price` 补挂 |
| T32 | 自动更新与手动操作并发 | 不双写、不互相覆盖 |
| T33 | 两个批次同时收盘 | 状态隔离，API 仍受限流 |
| T34 | 固定 TP 与保护 SL 竞争 | 最终状态一致，无残留重复单 |
| T35 | 各平仓路径 | 无残留 TP/SL/限价单/pending |
| T36 | 状态组合非法 | 拒绝猜测性修复并 critical 告警 |
| T37 | 第 3K KAMA 不可用但预估保本价有效 | 执行纯保本保护，下一根 K 重试 KAMA，不市价平仓 |
| T38 | 手续费、净数量或预估退出费缺失 | 保留原保护线并重检，不用 0 伪装实际保本 |
| T39 | Hedge/One-way 保护单参数 | Hedge 含 `positionSide`；One-way 含 `reduceOnly`；均不以 `closePosition=true` 创建 |
| T40 | `locked_kama` 或保本价缺失 | 缺失项不参与 max/min；不得用 0/None 代替 |
| T41 | 加仓/部分减仓触发更新 | 价格按 tick、数量按 step 独立判断，使用成交确认后的净剩余量 |
| T42 | 替换事务中断 | 下一轮只对账并补完本次替换，不发起新一轮替换 |
| T43 | 无法归因的外部 STOP_MARKET | 隔离告警，不自动撤销、不自动认领 |
| T44 | 停机/重启/回滚 | 不撤固定 TP 或当前 SL，不放宽保护，不回放历史 K 线 |
| T45 | 预估保本价公式 | 多头/空头含费公式与实际入场费优先规则均通过固定向量 |
| T46 | 100～998 根已收盘 K | 不得作为 KAMA 激活依据；可保留既有保护并重试 |

---

## 19. 上线门槛

全部满足后才允许实盘灰度：

- [ ] TradingView KAMA 固定样本对账通过；
- [ ] 999 根预热与未收盘过滤通过；
- [ ] T01～T46 全部通过；
- [ ] Python 语法编译通过；
- [ ] 核心状态机测试无未解释失败；
- [ ] 故障注入覆盖所有持久化临界点；
- [ ] 影子模式至少完成一个完整 4H 周期；
- [ ] 手工核对交易所订单与本地状态一致；
- [ ] `pending_cancel_sl_ids` 恢复演练通过；
- [ ] 手动干预与重启演练通过；
- [ ] 回滚开关默认关闭，异常时可停用 KAMA 而不撤出现有保护线；
- [ ] 关键状态和订单 ID 写入交易日志；
- [ ] 第二次独立代码审查无 P0/P1 资金安全问题。

上线启用应采用显式开关，不建议在代码合入时默认打开：

```text
KAMA_TRAILING_ENABLED=false
```

先完成影子模式，再人工切换。

---

## 20. 回滚与降级

运行时发生异常时：

1. 关闭新的 KAMA 更新入口；
2. 不主动降低现有保护线；
3. 保持当前有效 `current_sl_id` 和固定 TP；
4. 保留所有状态与审计证据；
5. 清理或冻结待撤孤儿流程，避免误撤当前保护线；
6. 告警说明是“停止自动更新”还是“保护状态异常”；
7. 人工核对交易所挂单后再恢复。

禁止把“删除全部 KAMA 状态并重置为初始 SL”作为常规回滚手段，因为这可能立即降低保护线。

---

## 21. 风险清单

| 风险 | 等级 | 控制措施 |
|---|---|---|
| 未收盘 K 混入导致 KAMA 漂移 | P0 | `open_time >= bucket_start` 过滤 |
| 首次 KAMA 极端偏离 | P0 | >5% 拦截和 critical 告警 |
| 先撤旧单造成裸奔 | P0 | 先挂新、确认、写盘，再撤旧 |
| 挂新后崩溃造成双 SL | P0 | T4 替换意图 + pending + 启动归属对账 |
| 无法归因订单被误撤 | P0 | 三级归属判定；外部订单只隔离告警 |
| Hedge/One-way 参数错配 | P0 | params_base 派生、参数断言和 T39 |
| triggered 被误认为成交 | P0 | 成交/仓位双确认后结算 |
| 保本费口径缺失 | P0 | 保留原保护并重检，不用 0 伪装实际保本 |
| 状态被旧写者覆盖 | P0 | 字段保护合并和合法写者白名单 |
| KAMA 回撤导致止损变差 | P0 | 锁定最有利值和单调不变量 |
| 重启错过第 3K | P1 | 启动补触发 |
| 手动操作被自动覆盖 | P1 | 同一批次锁和手动优先 |
| 加仓后保护数量错误 | P0 | 每次替换读取实际持仓 |
| 减仓后保护价降低 | P0 | 价格只进不退 |
| 数据缺口导致 ER 错位 | P0 | 连续性校验后 fail-close |
| Telegram 故障影响订单 | P1 | 通知与交易路径隔离 |
| 回测与生产公式混淆 | P0 | 分文件、分接口、分验收 |
| 无专项测试却宣称完成 | P0 | 测试与上线清单强制门禁 |

---

## 22. 最终结论

当前仓库的 KAMA 代码只能作为研究基础，不能直接作为实盘跟踪止损方案上线。

完整生产实现的核心不是简单地把 KAMA 接到主循环，而是建立一套围绕保护单和本地状态的崩溃安全协议：

```text
已收盘 KAMA
→ 计算预估保本价和只进不退候选价
→ 批次互斥
→ 持久化替换意图与待撤旧单
→ 先挂新保护单
→ 确认交易所状态
→ 原子持久化
→ 撤旧保护单
→ 归属对账
→ 重启可恢复
```

建议严格按“纯组件 → 状态机 → 保护单替换 → 主循环 → 影子运行 → 小仓灰度”的顺序实施。任何阶段未通过，不得通过修改阈值或忽略测试跳过上线门槛。

---

## 23. ChatGPT 复审建议逐项裁决

### 23.1 核实依据

本节只依据已拍板规格、生产代码事实和可验证接口约束裁决：

- D-001 第 3K 激活：`D-001_最终实施规格_v1.0.md:147-170`；
- D-001 唯一保护价公式与 5% 首次锁定校验：`132-143`；
- D-001 降级三分支：`173-180`；
- D-001 手动状态机：`258-273`；
- D-001 保护线激活后 `/be` 语义：`221`；
- D-001 原孤儿单扫描口径：`248-252`；
- 生产账户模式：`trader_260725.py:5497-5522`，开仓前查询账户模式，Hedge Mode 写入 `params_base['positionSide']`，失败即阻断；
- 生产保护单参数：`5596-5599`、`5645-5648`、`7585-7588`、`9327-9332` 均从 `params_base.copy()` 派生，One-way 才追加 `reduceOnly`；
- 生产平仓参数：`11028-11034`、`11780-11784` 明确 Hedge 用 `positionSide`、One-way 用 `reduceOnly`，两者不同时塞入；
- `closePosition`：生产代码只在 `5765-5781` 用于识别潜在手工全仓保护单，不用于创建保护单；
- 依赖基线：`requirements.txt` 锁定 `ccxt==4.68`；本机实测 `ccxt 4.77`。涉及 `clientOrderId` 的接口行为必须以锁定版本和实测为准，不能由版本猜测。

### 23.2 裁决矩阵

| # | 复审建议 | 裁决 | 修订结论与理由 |
|---|---|---|---|
| 1 | 保本与 KAMA 激活解耦：手动 `/be` 不应阻塞 KAMA；KAMA 数据故障不应阻塞保本 | **部分采纳** | “KAMA 故障不阻塞纯保本保护”已按 D-001 情况 A 采纳并写入 8.3/13/T37。手动 `/be` 语义不能笼统解耦：3K 前 `/be` 置 `auto_be_done=true`，按 D-001 既定口径跳过第 3K 自动激活；动态保护激活后 `/be` 不重挂、也不暂停既有 KAMA，写入 10.1/T24。 |
| 2 | 到期不能证明保本时，保留原保护线并记录，后续重检，不擅自市价平仓 | **采纳** | 已写入 8.1、10.1、12.5、13/T38。费用、净数量或退出费不可证明时，不得用 0 伪装实际保本，更不得市价平仓。 |
| 3 | 候选保护价按多头取 max、空头取 min；缺失项不参与、不用 0；当前有效止损也应参与保护性比较 | **部分采纳** | 缺失项规则和 0/None 禁令全部采纳。当前有效交易所保护价被采纳为“不得放宽”的工程护栏；但不把它加入 D-001 唯一经济公式，公式仍为多头 `max(breakeven, locked_kama)`、空头 `min(breakeven, locked_kama)`。见 9.2。 |
| 4 | 价格更新和数量更新分离；加仓/减仓后按成交确认量更新，分别做 tick/step 归一化 | **采纳** | 已写入 9.3、11.1、11.2/T41。价格与数量可同处一个安全替换事务，但触发条件和归一化必须独立。 |
| 5 | Hedge Mode 创建保护单必须传 `positionSide`，不能用 `reduceOnly` 或 `closePosition=true` | **条件采纳** | 参数方向正确，但需按生产事实精确落地：保护单参数从 `params_base.copy()` 派生；Hedge 保留 `positionSide`，One-way 才追加 `reduceOnly`；两者不得混用。已核实生产代码没有用 `closePosition=true` 创建保护单，该字段仅用于识别潜在手工单。已写入 9.3/13/T39。 |
| 6 | 未知保护单不应默认撤销；创建前先持久化最小替换记录和客户端幂等标识 | **条件采纳** | “未知即撤销”存在误撤外部手工单风险，本报告将 D-001 S29 收窄为三级归属：可证明孤儿可清理，无法归因只隔离告警。创建前先持久化 T4 替换意图已采纳；`clientOrderId` 只有在锁定 `ccxt==4.68` + Binance USDM 算法单接口实测稳定后才可作为增强证据，不作为唯一归属依据。见 9.3、12.3、12.4/T43。 |
| 7 | 恢复不得放宽保护；停机不能只撤 TP/SL；`triggered` 不等于成交；替换未完成时暂停下一轮替换 | **采纳** | 已分别写入 9.3、12.6、13、14、20/T20/T42/T44。回滚默认只停止自动更新，不撤既有 TP/SL。 |
| 8 | 窄项：`/tp` 不应静默暂停 KAMA；断线不回放全部 K 线；TradingView 用固定样本；预热统一 999；5% 基准写清；手动优先写前重读/版本号 | **部分采纳** | 采纳固定样本、999 根、5% 基准、断线不回放和 `/tp` 明确通知。拒绝“`/tp` 不暂停 KAMA”的改动，因为 D-001 已明确 `/tp` 成功置 `kama_paused=true`；只能增加用户提示。状态版本号不是 v1 必需，本版以同批次锁、写前重读、字段保护合并和合法写者白名单为准。见 6.3、6.5、10.2、12.6、15。 |
| 9 | 保本公式：多头 `E(1+f_in)/(1-f_out)`，空头 `E(1-f_in)/(1+f_out)`；界面称“预估保本价”；资金费可选 | **条件采纳** | 公式作为“预估保本价”采纳；若已有可归属实际入场费，优先用费用金额精确计算。资金费预测不纳入 v1 门槛。费用缺失时不得置 0 冒充实际保本。见 8.1/T45。 |

### 23.3 对既定口径的保护

本次复审没有修改以下已拍板规则：

1. 第 3K 收盘激活，触发时刻为 `entry_kline_open + 16h`；
2. 批次内新层成交不重置 `entry_kline_open`；
3. KAMA 预热门槛统一为不少于 999 根已收盘 4H K；
4. 首次锁定 KAMA 的异常阈值仍为相对自动取优保本价 5%；
5. 多头保护价只升、空头保护价只降；
6. 固定 TP 与动态保护 SL 共存，且动态保护只在 4H 收盘更新；交易所端盘中触价执行；
7. D-001 唯一保护价公式不变；
8. 本次只修订报告和验收基线建议，不修改任何生产代码。

唯一需要回填 D-001 的基线变化是 12.3 对 S29 的安全收窄：无法归因的 STOP_MARKET 不得默认撤销。实施前应先将该裁决同步到 D-001，再进入代码阶段。

---

## 附录 A：相关文件

- `.workbuddy/memory/discussions/D-001_最终实施规格_v1.0.md`
- `.workbuddy/memory/discussions/D-001_复查稿_AI可读版.md`
- `strategies_backtest/indicators.py`
- `strategies_backtest/trailing_nolookahead.py`
- `strategies_backtest/verify_nolookahead.py`
- `trader_260725.py`
- `bot_runner.py`
- `watchdog.py`

## 附录 B：术语

| 术语 | 含义 |
|---|---|
| 4H K | 4 小时 K 线 |
| ER | Kaufman Efficiency Ratio，效率比 |
| KAMA | Kaufman Adaptive Moving Average，考夫曼自适应移动平均 |
| 保护线 | 交易所中的动态 STOP_MARKET 止损保护单 |
| 保本价 | 退出成本基准的统称；不得在未说明费用口径时宣称“实际保本” |
| 预估保本价 | 按批次剩余净成本、可归属入场费和预估退出费计算的保本价，UI 统一使用该名称 |
| `locked_kama` | 激活后锁定的最有利 KAMA 值 |
| `pending_cancel_sl_ids` | 已决定撤销但尚未确认撤成的保护单 ID |
| no-lookahead | 回测或计算不读取当时不可见的未来数据 |
| 补触发 | 重启或事件漏处理后立即补执行本应发生的动作 |
| 影子模式 | 只计算和记录，不修改交易所挂单 |
| fail-close/fail-safe | 数据或状态不可信时停止更新，保留已有保护 |
