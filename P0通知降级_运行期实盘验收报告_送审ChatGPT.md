# P0 通知降级 —— 运行期实盘验收报告

> 生成时间：2026-08-29 17:26（GMT+8）
> 运行实例：watchdog 17:10:07 启动，bot_runner PID 12240
> 运行代码：**e953d79**（P0 已加载）
> 取证方式：**全部只读**（未调用任何写入方法；交易所侧仅查询 open orders / positions）

---

## 一、结论速览

| 项目 | 结果 |
|---|---|
| **P0 `safe_reply` 降级触发** | ✅ 真实触发（17:23:59 BadRequest → 17:24:00 降级重发） |
| **TG 最终送达** | ✅ 用户确认收到完整通知 |
| **降级文本正确性** | ✅ 剥 `**`/反引号、保留 `_` 与 emoji，逐字复现 |
| **根因模型定量锁定** | ✅ 双点命中（139 / 138，差值 1 字节归因明确） |
| **新批次挂单** | ✅ 2 层条件单，交易所可查，与账本一致 |
| **交易所侧无孤儿/重复单** | ✅ 8 条件单 ↔ 账本 8 条，普通单 0，持仓 0 |
| **新发现（未修，仅记录）** | ⚠️ 止盈价方向性校验存在边界缺口（详见第五节） |

**P0 至此完成 RED → GREEN → 上线 → 运行期实测全闭环。**

---

## 二、运行期实测证据链

### 2.1 终端日志（三重时间戳）

```
17:23:53  新批次 batch_20260829_172353_dd6431 建立（入场 80521 / 80701，TP 80000）
17:23:55  🚫 [D-005] 重复信号已拦截 [ce6983f0]: 上次执行 2 秒前
          （batch: batch_20260829_172353_dd6431，状态 SUCCESS），拦截窗口剩 597 秒
17:23:59  ERROR  ⚠️ 回复消息给 Telegram 失败（BadRequest）:
          Can't parse entities: can't find end of the entity starting at byte offset 138
17:24:00  INFO   ℹ️ [TG回复] Markdown 解析失败，已降级纯文本发送
```

关键：17:23:59 失败 → **0.54 秒后** 17:24:00 降级重发成功。
`ℹ️ [TG回复] Markdown 解析失败` 是 P0 新增的日志行 —— **该行出现本身即为新代码已加载并执行的运行时硬证据**（强于此前的时间序推断）。

### 2.2 用户 TG 实际收到（降级后纯文本）

```
🛡 重复信号已拦截（D-005 幂等保护）
🧬 指纹：ce6983f0
📊 上次执行 2 秒前（batch: batch_20260829_172353_dd6431，状态 SUCCESS），拦截窗口剩 597 秒
💡 同参数信号 10 分钟内视为重复（防快捷指令双击/信号重发导致重复开仓）。
如确需再次开仓：
1️⃣ 先核对交易所当前挂单与持仓（防上次执行部分成交）
2️⃣ 发送 /force ce6983f0 放行
3️⃣ 在 5 分钟内重发原信号
```

### 2.3 降级行为离线复核（与实收逐字一致）

```
保留下划线(batch号可读): True   ← batch_20260829_172353_dd6431 完整
剥掉 **                : True
剥掉反引号             : True   ← /force ce6983f0 无反引号
保留 emoji             : True   ← 🛡 / 1️⃣2️⃣3️⃣ 完好
```

**「绝不剥 `_`」这条约束在真实世界被证明是对的** —— 若当初为图省事连下划线一起剥，
用户收到的会是 `batch20260829172353dd6431`，`/force` 指令无法复制，等于修一个 bug 造一个新 bug。

---

## 三、根因模型双点定量验证（新脚本 `verify_d005_offset_model.py`）

模型：`batch_20260829_172353_dd6431` 含 3 个下划线，legacy Markdown 下第 1、2 个配成斜体，
**第 3 个开启的实体找不到闭合** → Telegram 报错位置 = 第 3 个下划线的字节偏移。

| 场景 | 真实报错 offset | 模型预测 | 该偏移字节 | 判定 |
|---|---|---|---|---|
| 16:05:21（旧，未降级） | 139 | **139** | `_` | ✅ 命中 |
| 17:23:59（新，已降级） | 138 | **138** | `_` | ✅ 命中 |

**差值归因（关键反证）**：

```
旧消息：上次执行 94 秒前  → '94' 占 2 字节
新消息：上次执行 2 秒前   → '2'  占 1 字节
→ 139 - 138 = 1，与观测逐位吻合
```

两次独立样本、不同批次号、不同时长数值，同一模型精确预测 ——
**根因已由「合理推断」升级为「定量锁定」，排除巧合。**

> 附带实证：D-005 拦截通知自 2026-08-27 上线至 17:10 重启前，**从未成功送达过一次**；
> 若非本次新建批次时恰好触发，该盲区可能长期存在。

---

## 四、交易所侧核对（只读，无下单/撤单）

```
账本 entry_order_id                8 条（批次1 四层 + 批次2 两层 + 批次3 两层）
交易所 BTCUSDT 条件单(algo)        8 个  → ID 逐个相同
交易所 普通单                       0
实际持仓                            0
双向差集                            空        rc=0
```

新增批次3 的 `3000002163724624` / `3000002163724627` 均已在交易所，
与终端日志打印的 ID 一致 → **新批次挂单正确，无重复挂单、无孤儿单**。

当前活跃批次：**3**（批次1 / 批次2 / 批次3）。

---

## 五、⚠️ 新发现：止盈价方向性校验存在边界缺口（仅记录，未修）

### 5.1 现象

新批次参数：入场 80521 / 80701，**止盈 80000**，当前市价 77591.8。

**止盈价低于全部入场触发价** —— 但校验通过：

```
🔍 [止盈价合理性校验中...]
✅ 止盈方向性校验通过！
```

### 5.2 源码定位

`trader_260725.py` L2861 `_validate_take_profit(signal, current_mark_price)`：

- L2880 BUY 分支只校验 `tp_price <= current_mark_price`
- L2867 注释原文：
  > 不用 TP > max(所有层入场价)：阶梯入场场景 TP 低于未成交层触发价是合法的（ChatGPT 结论）

该结论对「TP 低于**部分未成交高层**触发价」成立，但**未覆盖「TP 低于最低入场触发价」**这一情形。

### 5.3 依据链

依据注释自身的推理（L2865-2866）：

```
条件单触发价必 > 市价，成交价 ≥ 触发价
⇒ 首次成交价 ≥ min(所有入场触发价)
⇒ 若 TP ≤ min(入场触发价)，则成交瞬间 TP ≤ 成交价
⇒ TAKE_PROFIT_MARKET 卖单 stopPrice ≤ 现价
⇒ 币安确定性 -2021 Order would immediately trigger
```

本例：`min(80521, 80701) = 80521 > TP 80000` → **命中该缺口**。

### 5.4 后果评估（非资金安全级，但不为零）

TP 挂单失败路径已实证（trader L6810-6840）：

```
-2021 → ExchangeError → 归类「确定拒绝」 → registry FAILED + fail_count+1
     → 经闸门重试，连续 5 次 → HARD_LOCK 🚨 + 停止自动重挂（需人工解锁）
```

- **下行保护不受影响**：SL（75001 / 75002）独立挂出，方向正确
- **上行保护缺失**：TP 挂不上 → 只能靠 SL 或人工退出
- **告警噪音**：4 次重试后触发 HARD_LOCK 级 🚨 critical 通知

### 5.5 触发条件与当前风险

需 BTC 从 77591.8 上涨约 **3.8%** 至 80521 才会触发。当前不紧迫，但批次3 会持续占用 1/3 批次名额。

### 5.6 建议（待裁决，未实施）

1. **立即**：关闭批次3 释放名额（顺带可测一次「零成交批次的 TG 平仓」路径）
2. **候选校验增强（需 ChatGPT 裁定后再动）**：
   BUY 时改为 `TP > max(当前市价, min(入场触发价))`；SELL 对称改为 `TP < min(当前市价, max(入场触发价))`
3. 该缺口**不影响 D-005 / D-006 / D-009 任一既有结论**

---

## 六、ChatGPT 三步计划进度

| 步骤 | 状态 |
|---|---|
| 第一步：重启让 P0 生效 + 实测 | ✅ **完成**（本报告） |
| 第二步：强杀 bot_runner → watchdog 自动恢复 | ⬜ 待执行 |
| 第三步：不再为测试额外制造批次 | ⚠️ 已制造批次3，建议关闭后遵守 |

### 第二步执行清单（强杀测试）

```powershell
# 1. 记录当前 PID（应为 12240）
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
    Select-Object ProcessId, ParentProcessId, CreationDate | Format-Table

# 2. 强杀 bot_runner 进程树（保持 watchdog 20044 存活）
taskkill /PID 12240 /T /F

# 3. 等待 watchdog 自动拉起（约数秒），然后验证：
#    ① 新 PID ≠ 12240，watchdog.log 出现「主进程已启动 (PID: <新>)」
#    ② 终端出现「✅ [启动检测] 历史任务恢复校验完成！系统 READY」
#    ③ 活跃批次 = 3（或关闭批次3 后 = 2）
#    ④ 无 🚨 / 🚫 告警
```

验证命令（均只读）：

```bash
.venv/Scripts/python.exe d009_gray_watch.py            # 期望 rc=0
.venv/Scripts/python.exe verify_no_duplicate_orders.py # 期望 rc=0，且条件单 ID 不变
.venv/Scripts/python.exe verify_d005_offset_model.py   # 回归护栏，期望 rc=0
```

**强杀测试的核心验收点**：交易所条件单 ID **保持 8 个且不变**（不得重挂），
以及 `trade_state.json` 在断电瞬间未损坏（D-009 fsync 链的近似验证）。

---

## 七、证据复现命令（全部只读）

```bash
# P0 根因双点定量验证
.venv/Scripts/python.exe verify_d005_offset_model.py

# 交易所 ↔ 账本一致性核对（需 .env 的 BINANCE_KEY / BINANCE_SECRET + 代理）
.venv/Scripts/python.exe verify_no_duplicate_orders.py

# 灰度只读检查器
.venv/Scripts/python.exe d009_gray_watch.py

# P0 专项测试（GREEN）；指向改前备份即 RED
.venv/Scripts/python.exe test_tg_reply_fallback.py
TG_FALLBACK_TARGET=backups/p0_tg_fallback_20260829_163026/bot_runner.py \
    .venv/Scripts/python.exe test_tg_reply_fallback.py
```

---

## 八、状态总表

| 项目 | 结论 |
|---|---|
| D-005 重复信号判定 | ✅ 逻辑通过（16:05 + 17:23 两次） |
| D-005 拦截动作 | ✅ 通过 |
| D-005 通知触发 | ✅ 通过 |
| P0 Markdown → 纯文本降级 | ✅ **运行期实测通过** |
| Telegram 通知最终送达 | ✅ **用户确认收到** |
| D-006 最大 3 批次 | ✅ 实盘通过 |
| 3 → 2 → 3 名额释放与回收 | ✅ 实盘通过 |
| 部分成交 → 保护单 → 平仓 → 收敛 → 墓碑 | ✅ 实盘通过（观察项②） |
| 孤儿 TP/SL | ✅ Binance App 确认无 |
| D-009 观察项③（自然异常重启） | ⬜ 由第二步强杀测试近似覆盖 |
| 止盈价校验边界缺口 | ⚠️ 新发现，待裁决（不影响上述任一结论） |
