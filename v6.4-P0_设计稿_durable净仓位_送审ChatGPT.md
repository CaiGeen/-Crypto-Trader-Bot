# v6.4-P0 设计稿 v4：durable 净仓位+成本基准 + crash-phase 分型 + 冻结至保护就位 · 一页

> 2026-09-01 ｜ v4（吸收最后一个 blocker：crash-phase durable distinction）｜ 范围一句话不变：**批次指定部分平仓（bot 指令），净仓位+成本基准持久化，聚合持仓只做守恒校验。**

## 0. v3 → v4 变更（最后 1 个 blocker：crash 阶段不可分辨）

v3 的单一 `partial_closing` 无法区分崩溃发生在哪一步——重启后磁盘上三种情况完全同形，但处置截然相反：

| 崩溃阶段 | 事实 | 重启处置 |
|---|---|---|
| A. BEGIN 后、MARKET 未发 | 副作用未知（不能证明未发） | **loud / 人工核实**，绝不自动续跑 |
| B. MARKET 已发、结果未确认/未提交 | 订单结果未知 | **loud**，绝不重复 MARKET |
| C. 净账本已 CAS、只差 resize | 账本是权威事实 | **可安全自动续跑 resize** |

**修法（零新字段/零新状态机，复用 `limit_creating → limit_pending_normal` 既有模式）**：`close_reason` 承载事务进度——

```
BEGIN：close_reason = partial_closing        （transient，不进 quiet；重启 = loud）
exact confirm + 净账本 CAS（同一次原子提交）：close_reason → partial_resize_pending
                                             （进 quiet + transition_sources；
                                               唯一允许自动续跑的阶段：只续 resize，绝不重发 MARKET）
resize confirmed → 最终 CAS → ACTIVE
```

## 1. 持久化模型（2 个新字段，单写者——不变）

```
pre_net_qty  = Σ target_amounts[:lfc] − realized_reduce_amount
pre_net_cost = Σ (target_amounts×filled_details)[:lfc] − realized_reduce_cost
reduce_cost_delta = confirmed_amount × pre_net_cost / pre_net_qty     ← 净均价，非 gross avg
提交：realized_reduce_amount += confirmed_amount ；realized_reduce_cost += delta
净仓位 = pre_net_qty − confirmed_amount ；净均价 = (pre_net_cost − delta)/(pre_net_qty − confirmed_amount)
```

验证：1@100 → 减 0.5（delta=50）→ 净 0.5@100 ✓ → 买 1@200 → pre_net 1.5@166.67 → 再减 0.5（delta=83.33）→ 剩 1@**166.67** ✓

## 2. PARTIAL 事务（v4：reason 分型 + 冻结至保护就位）

```
BEGIN：close_op_id=uuid · close_phase=1 · pending_close=True
       · is_programmatic_cancel=True · close_reason='partial_closing'
       （transient：不进 quiet；close_phase>=1 冻结保护维护；重启 = loud/人工）
→ 市价减单（Hedge Mode positionSide 正确，量 = 用户指定）
→ exact confirmation（现有）
→ 【同一次原子 CAS，仿 rollback 模板 L7217】：
     锁内重读 → 校验 close_op_id/phase
     → realized_reduce_amount/cost += （§1 公式）
     → close_reason = 'partial_resize_pending'
     → _persist_states
   ※ 此刻起：MARKET 副作用已确认、净账本已 durable，剩余工作 = 已知净量的 resize
→ 保护单 resize：撤旧 SL/TP → 按净 qty 重挂
   （G1/G2 owner exception：partial_resize_pending + matching close_op_id + resize 操作）
   ※ 旧单先撤、净量后挂 = 极短保护空窗——AGENTS.md ponytail 注释记 ceiling
     （避免旧 oversized SL 在多批次环境错平其他 batch，两害取其轻）
→ 确认新保护就位 → 最终 CAS：close_phase=0 · pending_close=False
   · is_programmatic_cancel=False · close_reason 清空 → ACTIVE · pending ENTRY 保留
```

**重启恢复规格（窄分支）**：
- 见 `partial_resize_pending` → 续跑：撤旧 SL/TP → 按持久化净 qty 重挂 → 确认 → 最终 CAS ACTIVE（绝不重发 MARKET）
- 见 `partial_closing` → **loud critical + 人工核实**（不自动续跑、不自动撤保护单）
- 异常路径：MARKET/确认异常 → reason 切 loud——`partial_resize_pending` 已在 transition_sources，覆盖合法；`partial_closing` 阶段异常经既有 freeze-alert 路径响

## 3. 手续费（非 blocker，实现稿定义最小规则）

原则一条：**不得把完整历史 `total_entry_fee` 塞给剩余净仓位**（fee 本身是模型化值 `price×amount×TAKER_FEE_RATE`）。不新增第三个持久化字段，除非实现时源码证明必需。

## 4. 明确不做

成本明细表/FIFO/LIFO/lot/fee engine、App 模糊减仓自动归属、/reconcile、LIMIT partial、墓碑扫描、全量审计、通用 `allow_during_close`、partial_phase/partial_order_id/partial_intent 新状态机。

## 5. 测试预算（5 个封顶）

1. 首次部分平仓 → 净 qty/cost 正确、SL/TP 按净量就位**之后**才回 ACTIVE、`is_programmatic_cancel=False`、reason 迁移 `partial_closing → partial_resize_pending → 清空`
2. 部分平仓后 → close/coverage/survey/保本 按净 qty+净成本（helper 换点抽查 ≥3）
3. `partial_resize_pending` 重启 → 只续 resize 绝不重发 MARKET；`partial_closing` 重启 → loud；非本 op_id 的保护单操作仍被 G1/G2 拒绝（owner exception 窄度）
4. **partial → 新层不同价成交 → 再 partial → 剩余净均价正确**（净均价扣减钉死）
5. 部分平仓后外部 App 再减仓（聚合 < Σnet）→ detector critical（Fail-Closed 兜底）
