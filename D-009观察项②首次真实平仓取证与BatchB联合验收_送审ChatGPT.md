# D-009 观察项②「首次真实平仓」取证报告 + Batch B 联合验收（v1.1）

> 生成时间：2026-08-29 16:50（GMT+8）　**v1.1 修订：2026-08-29 16:58**
> 运行实例：PID 13512 / 15028 / 18232 / 18800，启动于 15:23:55
> 运行代码：**580f077（未重启）** —— 因此本次取证同时构成 P0 通知修复的**修复前对照样本**
> 取证方式：**全部只读**（未调用任何写入方法、未调用交易类 API）
>
> **v1.1 修订说明**（拿到完整终端日志后）：
> 1. **修正 v1.0 的一处归因错误** —— 墓碑里"未收敛"的 `1119893808642` 不是保护单，
>    而是**限价平仓单本体**，其终态为 `filled`（成交）而非 `canceled`。见第四节重写。
> 2. 补齐**完整订单生命周期表**（7 个订单 ID 逐一归因）
> 3. 新增**第六节：D-006 名额释放实证**（3→2 后新信号放行，源码 + 生产账本双验证）
> 4. 新增**第七节：新观察项 —— 成交后 SL/TP「预生成 → 撤销 → 重挂」**

---

## 一、结论速览

| 项目 | 结果 |
|---|---|
| D-009 观察项①（有持仓下状态保存） | ✅ 通过（批次3 持仓期间账本持续正常写入） |
| **D-009 观察项②（首次真实平仓）** | **✅ 已触发并通过**（人工 TG 最优价平仓路径） |
| Batch B 收敛证明 + clear proof 门 | ✅ 真实平仓路径首次验证通过（`L1=0 L2=0 L3=0 scope=FULL`） |
| **孤儿保护单（交易所外部事实验证）** | **✅ Binance App 确认无孤儿 TP/SL** |
| close_phase=3 唯一写入点 | ✅ 确认（墓碑 close_phase=3） |
| **D-006 名额释放（3 → 2）** | **✅ 源码 + 生产账本双实证，新信号放行** |
| 幂等保护（重复结算） | ✅ 日志「[限价平仓已处理] 跳过重复结算」 |
| D-005 / D-006 风控判定 | ✅ 均 PASS（通知层 P0 已修，待重启） |
| D-009 观察项③（自然异常重启） | ⏳ 未发生，继续观察 |

---

## 二、事件时间线（含证据出处）

| 时刻 | 事件 | 证据来源 |
|---|---|---|
| 16:03:37 | 批次3 建立：`batch_20260829_160337_79d97e`，2 层条件单（77520 / 77700），TP=80000，止损 75001 / 75002 | 用户终端日志 |
| — | 价格上涨触发 **L0 成交：0.001 BTC @ 77520.0**（结算均价 77558.76 含手续费口径） | 终端日志 `第 1 层买单成交！实际成交价: 77520.0` |
| 成交后 +0s | 预生成 SL `…675086` / TP `…675128` 挂出 | 终端日志 |
| 成交后 +~1s | 「同步维护独立风控」**撤销**上述两单 → 重挂 SL `…675155` / TP `…675199` | 终端日志（见第七节，新观察项） |
| ~16:2x | **用户在 TG 点击平仓按钮 → 选择「最优价平仓」**（人工触发，非 TP/SL/移动止损） | 用户确认 |
| 平仓指令 | 撤第 2 层开仓单 `…658295`、撤 TP `…675199` → 挂限价平仓单 **`1119893808642`** @ 77554.0 | 终端日志 |
| **16:30:03** | **限价平仓单成交 @ 77559.6，`net_pnl = -0.0534`** | `trade_stats.json` |
| **16:30:07** | clear 通过 proof 门 → 墓碑落盘 `close_phase=3` → 删除 state | 墓碑 `cleared_at=1787992207.96`；`trade_state.json.bak` mtime 16:30:07（两者精确吻合） |

`trade_stats.json` 原文：

```json
{
  "time": "2026-08-29 16:30:03",
  "batch_id": "batch_20260829_160337_79d97e",
  "symbol": "BTCUSDT",
  "side": "BUY",
  "amount": 0.001,
  "avg_price": 77558.76,
  "exit_price": 77559.6,
  "net_pnl": -0.0534,
  "mode": "限价平仓"
}
```

---

## 三、Batch B 链路证据

墓碑全文（`trade_tombstones.json`）：

```json
{
  "symbol": "BTCUSDT",
  "side": "BUY",
  "cleared_at": 1787992207.9582167,
  "converged_order_ids": [
    "3000002163658292", "3000002163658295",
    "3000002163675155", "3000002163675199"
  ],
  "known_order_ids": [
    "3000002163675199", "3000002163675155",
    "1119893808642",
    "3000002163658292", "3000002163658295"
  ],
  "close_phase": 3
}
```

`close_phase=3` 是全库唯一写入点（trader L1506，G-B9 正则锚定），因此它的出现本身即证明：
`_verify_clear_proof` 返回 None（proof 门通过）→ 才可能走到墓碑落盘分支。

清理后账本：1 个 symbol / **2 个批次**（批次1、2 仍在挂单阶段，filled 全 0）/ 活跃 2 个；墓碑 1 条。
检查器 `d009_gray_watch.py` 输出 `rc=0`，`_state_corrupted=False`、`_tombstones_degraded=False`。

---

## 四、`known_order_ids` 比 `converged_order_ids` 多 1 个 —— 归因（v1.1 修正）

> **v1.0 的错误**：我曾推断差值订单 `1119893808642` 是「成交后挂出的真实保护单（SL 或 TP）」。
> 拿到完整日志后可以确定：**它是限价平仓单本体**，证据是终端日志原文：
> ```
> 👀 [限价平仓监控] 批次 batch_20260829_160337_79d97e 订单 1119893808642 监控启动...
> ✅ [限价平仓监控] 批次 batch_20260829_160337_79d97e 限价单已成交，价格: 77559.6
> ```
> 推断方向对（它是普通订单、终态不在交易所），但**具体身份错了**。修正后结论比 v1.0 更强。

### 4.1 完整订单生命周期（7 个 ID 逐一归因）

| 订单 ID | 角色 | 终态 | 是否进 known | 是否进 converged | 依据 |
|---|---|---|---|---|---|
| `3000002163658292` | L0 开仓条件单 | **filled**（成交） | ✅ | ✅ | 日志「第 1 层买单成交」 |
| `3000002163658295` | L1 开仓条件单 | **canceled**（平仓时撤） | ✅ | ✅ | 日志「已撤销开仓挂单」/「程序撤单」 |
| `3000002163675086` | SL 初版（预生成） | canceled（维护重挂时撤） | ❌ | ❌ | 日志「已撤销旧止损单 → registry ABSENT」 |
| `3000002163675128` | TP 初版（预生成） | canceled（维护重挂时撤） | ❌ | ❌ | 日志「已撤销旧止盈单 → registry ABSENT」 |
| `3000002163675155` | SL 正式 | **canceled**（`close_settled_canceled`） | ✅ | ✅ | 日志「已撤销止损单 → SL registry 已终结」 |
| `3000002163675199` | TP 正式 | **-2011 幂等**（`close_settled_canceled_tp`） | ✅ | ✅ | 日志「[B0] TP 已不在交易所（-2011 幂等，事实终态）」 |
| **`1119893808642`** | **限价平仓单本体** | **filled**（成交） | ✅ | ❌ | 日志「限价平仓监控…订单 1119893808642」→「限价单已成交」 |

**关键点**：`known_order_ids` 是 **clear 时刻的存活快照**（5 个），而 `…675086`/`…675128` 在更早的维护阶段就已终结并从快照移除，所以 known 是 5 而非 7。

### 4.2 为什么「成交单」不在 converged 是完全正确的语义

`converged_order_ids` 记录的是**被撤销 / 被终结的挂单**；而 `1119893808642` 的终态是
**filled（成交）**——成交的单子物理上已经不存在于开放订单列表中，**不需要也不应该被撤销**。

这一步恰好印证了 Batch B 的 `B0` 设计：TP `…675199` 在平仓时已不在交易所，
程序没有把它当错误处理，而是识别 `-2011` 为**事实终态**并正常终结 registry。
同一套语义下，成交的平仓单也走「事实终态」而非「收敛撤销」。

### 4.3 收敛性的真正判据（不依赖墓碑字段）

`_verify_clear_proof`（trader L7238-7262）只校验：
`proof['exchange_scan'] == 'zero'`（L7254）、`position_zero is True`（L7252）、scope 匹配（L7257-7261）。

其中 `exchange_scan='zero'` 由 converge 的 **D-B2 单次复扫**产出——撤单后重新扫描交易所，
确认本批次相关单在交易所已无残留。日志实证：

```
✅ [B1] 批次 batch_20260829_160337_79d97e(BTCUSDT) 收敛证明生成：L1=0 L2=0 L3=0 scope=FULL
🧹 批次 [...] 状态归档/清理完毕（proof 门通过，墓碑已登记 close_phase=3，7 天防复活）
```

**外部事实交叉验证**：用户在 Binance App 实际查看，**未发现任何孤儿止盈/止损单**，
只剩另外两个批次的 6 个正常未触发开仓单 —— 与 `L1=0 L2=0 L3=0 / scope=FULL` 完全一致。

→ **无孤儿保护单风险。这是首次由"交易所外部事实"而非仅日志/离线测试证明的收敛。**

---

## 五、并行完成：P0 通知可靠性修复（commit e953d79，已推送）

本次观察期间发现 D-005 重复拦截通知**从未送达**：消息中裸奔的批次号含奇数个 `_`
（`batch_20260829_160337_79d97e` → 下划线 3 个）被 legacy Markdown 解析为未闭合实体，
服务器整条拒收：`can't find end of the entity starting at byte offset 139`（该偏移已用字节流重建精确定位到第三个下划线）。

而 bot_runner `safe_reply` 的 BadRequest 分支只记日志、不重发；同类的
`trader.send_tg_notification` 却有降级重发——两条通道容错能力不一致。

修复（ChatGPT 裁定只做 P0，不改任何风控判定逻辑）：`bot_runner.py` **+24/-0 纯插码**

- 新增 `_strip_markdown()`：只剥 `**` 与反引号，**绝不剥 `_`**（剥掉会把批次号变成
  `batch20260829155232f49f2e`，等于修一个 bug 造一个新 bug）
- `safe_reply` BadRequest 分支：`parse_mode is not None` 时同消息降级纯文本重发一次；
  `reply_markup` 保留；降级也失败仅记日志不抛出；`parse_mode=None` 时不重发（防无限循环）

验证：新增 `test_tg_reply_fallback.py`（17 断言）**RED 6/12 rc=1 → GREEN 17/17 rc=0**；
全量回归 36 文件 **35/36 rc=0**（`test_orphan_guard.py` rc=42 为既有环境型，本批未改动）。
已推送 `01bb44f..e953d79 → main`。

---

## 六、v1.0 三问的答复（用户已提供完整答案）

| 问题 | 答复 | 附带结论 |
|---|---|---|
| ① 平仓触发方式？ | **用户在 TG 点击平仓按钮 → 选择「最优价平仓」** | 人工路径，非 TP/SL/移动止损。说明 Batch B 的清算链在**人工介入路径**下同样成立，而不只是自动路径 |
| ② TG 是否收到平仓通知？ | 用户确认平仓流程的 TG 交互（按钮、限价平仓单已挂出、结算通知）**均正常** | 重要对照：进程仍是旧代码（未加载 P0），**但平仓链路的 Markdown 消息全部送达** → 说明平仓/结算链路走的是 `send_tg_notification`（自带降级），**与 D-005 的 `safe_reply`（无降级）确实是两条独立通道**。这实证了 P0 修复的边界判断：问题只出在 `safe_reply`，不需要动 `send_tg_notification` |
| ③ P0 重启时机？ | 待定（见第八节建议） | — |

### 6.1 附带确认：预计盈亏 vs 净盈亏 语义成立

```
⚠️ 警告：平仓价 77554.0 不高于均价 77558.76，可能亏损
✅ 限价单已成交，价格: 77559.6
🔴 最终净盈亏：-0.05 USDT（-0.07%）
```

挂单瞬间按当时报价 `77554 < 77558.76` 提示潜在亏损；实际成交 `77559.6 > 77558.76`，
价差为正（约 +0.00084 USDT），但因手续费 0.0543 USDT 最终净亏 0.0534。
**「预计盈亏」= 下单瞬间估算，「净盈亏」= 真实成交结算** —— 两者语义不同且都正确，不需要改。

---

## 七、新增：D-006 名额释放实证（3 → 2 后新信号是否放行）

ChatGPT 提出的关键测试点：**批次3 关闭后，名额是否真正释放，而非永久锁死。**

已用**生产真实账本 + 生产真实判定函数**（不复制逻辑、不 mock 判定）离线验证：

| 场景 | 断言 | 结果 |
|---|---|---|
| 1. 生产账本计数 | 活跃批次 == 2 | ✅ `total=2, symbols=['BTCUSDT']` |
| 2. 当前账本下新信号 | `allowed is True` | ✅ 放行（reason 为空） |
| 3. 人为补第 3 个批次（回归护栏） | `allowed is False` | ✅ `活跃批次总数 3 已达上限 3` |
| 4. `is_active=False` 的僵尸批次 | 不计入名额 | ✅ `total=2` |
| 5. `close_phase=1`（平仓进行中） | **仍计入** → 拒绝新信号 | ✅ `total=3, allowed=False`（保守方向） |

**计数源码**（trader L712-723）：只遍历 `all_states`，条件为 `b_data.get('is_active')`。
批次3 已被 `clear_batch_state` 从账本物理删除 → 不再被遍历 → 名额自然释放。

**场景 5 特别值得记录**：平仓**进行中**（`close_phase>=1`）的批次仍占用名额，
即名额在**批次完全清理后**才释放，而非发起平仓时就释放 —— 这是**保守方向**的设计，正确。

验证脚本：`G:/tmp/verify_d006_slot_release.py`（用 `CryptoTrader.__new__` 绕过 `__init__`，零 API 调用）

---

## 八、新观察项：成交后 SL/TP 被「预生成 → 撤销 → 重挂」

这是完整日志暴露出的一处**此前未被记录的行为**（v1.0 因缺日志未发现）：

```
🎯 第 1 层买单成交！实际成交价: 77520.0
  └─ ⚡ 预生成止损单已挂出: 75001.0 (ID: 3000002163675086)   ← 第一次挂
  └─ ⚡ 预生成止盈单已挂出: 80000.0 (ID: 3000002163675128)   ← 第一次挂
  └─ 🔧 补挂待处理止损层: [1]

⚡ [批次 ...] 同步维护独立风控...
  └─ 已撤销旧止损单: 3000002163675086 → registry ABSENT     ← 撤销
  └─ ✅ 止损单已挂出: 75001.0 (ID: 3000002163675155)         ← 重挂
  └─ 已撤销旧止盈单: 3000002163675128 → registry ABSENT     ← 撤销
  └─ ✅ 止盈单已挂出: 80000.0 (ID: 3000002163675199)         ← 重挂
```

**机制**（trader L5616）：`need_update_sl = (batch_filled_count > last_filled_count) or need_recover_sl`。
成交检测模块先"预生成"挂出 SL/TP，随后主循环风控更新检测到 `filled_count` 变化，
**又把刚挂出的两单撤销并重挂一次**，价格完全相同（75001.0 / 80000.0）。

**风险评估（结论：非缺陷，但值得记录）**

- ✅ **不会永久裸奔**：撤销成功后若创建被仲裁拦截（L5738-5752），会置 `current_sl_id=None`，
  下一轮循环由 `need_recover_sl` 检测到缺失并补挂 —— Fail-Closed 有兜底。
- ✅ **撤销失败不创建**：L5704-5707，撤销异常时 `continue`，保留原单（防双单）。
- ⚠️ **存在无保护窗口**：撤销成功 → 创建成功之间，仓位无 SL。正常路径为毫秒~秒级，
  但本批次把它**人为执行了两遍**（预生成一次 + 重挂一次），窗口翻倍。
- ⚠️ **4 次冗余 API 调用**：2 撤 + 2 建，在 API 限流预算下是纯浪费。

**是否需要修？** 按当前裁定「不再改设计、不做破坏性演练」，**建议只记录不修**。
若未来要优化，方向是：成交检测模块在已确认后续主循环会重挂时**跳过预生成**，
或主循环识别 SL/TP 价格未变化（intent 指纹已存在，见 L5761 `_build_intent`）时**跳过 replace**。
后者更安全——`_build_intent` 已经在算指纹，可直接复用做幂等跳过。

---

## 九、下一步建议（需你 / ChatGPT 裁决）

1. **继续灰度观察**剩余 2 个批次（均未成交，filled 全 0），等待自然成交 → 自然平仓，
   看第二、第三次是否复现同样的收敛结果。
2. **D-006 名额释放的实盘终验**：现在活跃数 = 2，可以发一个新信号 → 应放行 → 3。
   （离线已证明逻辑正确，实盘只是再确认一次；**但会占用一个批次名额**，
   若不想占用真实仓位，可发一个远离市价的信号。）
3. **P0 重启时机**（仍待定）：
   - 现在重启 → D-005 通知立即恢复，代价是消耗 D-009 观察项③「自然异常重启」
   - 等自然重启 → 保住观察项③，期间重复信号通知仍丢
   - **折中方案**：观察项③ 要的是「异常」重启（崩溃/断电），而人为的**干净重启不影响它的语义**——
     但当前活跃批次会走启动恢复链（D-009 已验证），属于低风险。请裁决。
4. **第八节的 SL/TP 重复重挂**：建议仅入档，不修。

---

## 十、证据复现命令（均为只读）

```bash
# 灰度检查器（只读，退出码 0 正常 / 1 WARN / 2 ERROR）
.venv/Scripts/python.exe d009_gray_watch.py

# 墓碑与账本摘要取证
python G:/tmp/inspect_state.py

# D-006 名额释放验证（5 场景，rc=0 全过）
.venv/Scripts/python.exe G:/tmp/verify_d006_slot_release.py

# P0 专项测试（GREEN）；指向改前备份即为 RED
.venv/Scripts/python.exe test_tg_reply_fallback.py
TG_FALLBACK_TARGET=backups/p0_tg_fallback_20260829_163026/bot_runner.py \
    .venv/Scripts/python.exe test_tg_reply_fallback.py
```

### 当前生产状态（16:58 快照）

- 运行代码：**580f077**（进程未重启，P0 未加载）
- 进程：4 个 python.exe，PID 13512 / 15028 / 18232 / 18800，启动 15:23:55
- 账本：`trade_state.json` 1 symbol / **2 批次**（`…155232_f49f2e`、`…155343_cfdf77`），均 `is_active=True`、`close_phase=0`、`filled` 全 0
- 墓碑：1 条（`…160337_79d97e`，`close_phase=3`，`cleared_at=1787992207.96`）
- 检查器：`rc=0`，`_state_corrupted=False`、`_tombstones_degraded=False`
