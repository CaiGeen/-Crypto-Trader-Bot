# v6.2 设计稿 v2.3（最终收敛增量）— 待用户过目后送审，未动生产

> 本稿是 **v2.2 的极小 delta**（ChatGPT：v2.2 主体全部批准；新发现 1 个确定性 P0 +
> 3 个接缝）。按铁律逐条独立源码核实：**3 项声明全实锤 + freeze-alert 事实沿用，
> 零证伪**（§零）。字段职责从此三分，不再互相代偿。
> 文档纪律：`python` 块完整可解析（零 Ellipsis）；生产摘录用 `python-frag`。

---

## §零 本轮核实结果

| # | ChatGPT 声明 | 独立核实证据 | 结论 |
|---|---|---|---|
| 1 | **确定性 P0**：`is_programmatic_cancel` 是棘轮字段，`pop + save_batch_state` 清不掉 → v2.2 zero-filled unresolved 路径磁盘仍 True → monitor `break` → 活 ENTRY 无人管辖 | trader L41 `_MERGE_RATCHET_BOOL_FIELDS = ('pending_close', 'is_programmatic_cancel', 'settled_by_limit_close')`；**L1627-1629** `for f in ...: if disk.get(f) and not snap.get(f): merged[f] = disk[f]  # 磁盘 True 快照 False → 保留 True`——pop 后快照缺失同样命中；monitor break 判据 L4862-4863 `is_programmatic_cancel or pending_close`。这正是事故 B「布尔棘轮→回滚死代码」的同一机制作用于新路径 | ✅ 成立（确定性，非 race） |
| 2 | 高→低撤单应**每层** verifier，不只 -2011（cancel 成功返回 ≠ terminal 事实；`_safe_api_call` 成功只透传底层结果，不确认订单事实） | 逻辑核实：INV-1v2 自有措辞「cancel ≠ 终态」；ChatGPT 明示未从源码证明「cancel 成功后再成交」现实样本——按自定不变量统一，逻辑完整 | ✅ 采纳（规格一致性，非新 P0） |
| 3 | hole 硬门须先验 `len(filled_details) == len(target_amounts)`，否则 tail 不可证 | v6.1 helper L244 只查 `len(filled_details) < last_filled_count`——长度 ∈ [lfc, 计划层数) 时通过且尾段无槽位可查。**补充发现**：截断批次重启后 L4562 会把 filled_details 重灌为 len(entry_orders) 长度 → shape gate 恰好抓到这种损坏态（尾段成交状态不可知 = 不可信台账） | ✅ 成立 |
| 4 | `limit_creating` 正常 transient 会撞上 monitor poll 假 critical（`_freeze_alerted` 新 batch 无 key 默认 0 → 首见即 critical）；seed 进程内 grace 即可，crash 后 grace 自动消失 | 送审稿 L2407 `now - self._freeze_alerted.get(batch_id, 0) >= 3600` + L2425 `self._freeze_alerted = {}`（纯进程内存，不落盘） | ✅ 成立，方案可行 |

**字段职责三分（本稿核心，ChatGPT 终局要求）**：
- `is_programmatic_cancel` = **归因**（撤单来源是程序，勿误报人工撤单）——棘轮字段，永不参与生命周期判断；
- `pending_close` = **生命周期终止资格**（撤单事实全部确认后才置 True）；
- `close_phase` = **close transaction 状态**。

---

## §ΔA zero-filled 退出资格只看 `pending_close`（修 P0）

### A-1 monitor 退出判据（生产 L4858-4865 替换）

```python
                # 🔥 程序撤单/pending_close 且无成交：退出监控（finally 块会清理状态）
                if batch_filled_count == 0:
                    latest_all_pc = self.load_all_states()
                    latest_b_data_pc = latest_all_pc.get(symbol, {}).get(batch_id, {})
                    if latest_b_data_pc and latest_b_data_pc.get('pending_close', False):
                        # 🔒 v2.3（ΔA）：zero-filled 退出资格**只看 pending_close**。
                        # is_programmatic_cancel 是 False→True 单向棘轮
                        # （L1627-1629，pop+save 清不掉），职责 = 撤单来源归因；
                        # 生命周期终止只由 pending_close 承载——v2.2 的
                        # 「pop + 保持 ACTIVE」在棘轮下是死代码，磁盘仍 True
                        # → monitor break → 活 ENTRY 无人管辖。
                        print(f"🚨 [批次终止] 本批次未建仓，撤单已全部确认终态，正在退出监控...")
                        break
```

### A-2 🗑️ zero-filled unresolved 路径删除无效 pop（替换 v2.2 §ΔC 对应行）

```python
            if unresolved:
                # 🔒 v2.3（ΔA）：不再 pop is_programmatic_cancel——它是棘轮字段
                # （L1627-1629 证明 pop+save 清不掉，写出来只是伪装能清）。
                # 保留 True 恰好正确：归因 = 程序撤单；生命周期由
                # pending_close=False + close_phase=0 表达 → monitor 不退出。
                self.save_batch_state(target_symbol, batch_id, target_b_data)
                return False, (f"⚠️ 批次 `{batch_id}` 撤单后仍有 "
                               f"{len(unresolved)} 张 ENTRY 未确认终态"
                               f"（{[u[0] for u in unresolved]}，ID 已保留，监控继续）\n"
                               f"💡 请重试或人工核对")
```

### A-3 行为变化披露（ΔA 连带，须送审确认）

1. **monitor 自身手动撤单检测路径**（L4809-4820，zero-filled + clear 失败时）：旧代码靠 L4858 的 `is_programmatic_cancel` break 退出（带未清状态交给重启恢复）；ΔA 后改为**继续轮询重试** converge/clear 直到成功——线程生命周期延长至 clear 成功，持久 UNKNOWN 时按冻结告警节奏 critical（Fail-Closed 语义，与卡死批次一致）。
2. **finally 块（L6318-6334）无冲突**：`is_programmatic_cancel or pending_close` 的 converge+clear 只在**线程退出后**执行，ΔA 只改变何时退出，不改变退出后的清理语义。
3. **L4890（持仓归零 + flags → 跳过结算）保持 `or` 不改**：其语义是「零持仓批次的结算跳过」（🗑️ 部分成交后用户手动平仓 → 须靠 is_programmatic_cancel 触发清理），不是线程生命周期——职责三分后各自归位。

---

## §ΔB 高→低撤单每层统一 verifier（替换 v2.2 §ΔA 撤单循环）

**每一层**：cancel 尝试 → `_verify_entry_order_terminal` → `gone` 才允许继续下一低层；`filled` → critical + 停；`open/unknown` → 停。不再依赖「Binance cancel 正常返回 = 已 terminal」的隐含假设（`_safe_api_call` 成功只透传底层结果）。producer #1/#2 同一纪律。

```python
        cancel_failed_ids = []
        already_terminal_ids = []
        for idx in reversed(range(last_filled_count, len(entry_orders))):
            order_id = entry_orders[idx]
            try:
                self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol, params={'stop': True})
                cancelled_ids.append(order_id)
                cancelled_layers.append(idx + 1)
                print(f"  └─ 已撤销第 {idx + 1} 层挂单: {order_id}")
            except Exception as e:
                if not ('-2011' in str(e) or 'Unknown order' in str(e)):
                    cancel_failed_ids.append(order_id)
                    print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单失败: {order_id} ({e})"
                          f"（ID 保留在台账，监控继续管辖）")
                    break
                # -2011 = 无需再撤；终态事实交由下方 verifier 统一定案
            # 🔒 v2.3（ΔB）：每层统一 verifier——cancel 成功返回 ≠ terminal
            # 事实（INV-1v2）。gone 才允许继续向更低层撤。
            verdict, _vo = self._verify_entry_order_terminal(order_id, target_symbol)
            if verdict == 'gone':
                continue
            if verdict == 'filled':
                self.send_tg_notification(
                    f"🚨【资金安全】ENTRY 在撤单前已成交！\n"
                    f"🆔 批次: `{batch_id}`\n📌 订单: {order_id}\n"
                    f"⚠️ 高层成交 → 成交位可能已不连续（hole），"
                    f"已停止向更低层撤单，请立即人工核对持仓与台账！",
                    level='critical')
            cancel_failed_ids.append(order_id)
            break  # filled / open / unknown 一律停止（不制造 hole）
```

（成本：用户主动撤单路径每层多 1 次 `fetch_order`，ChatGPT 已裁定值得。）

---

## §ΔC hole 硬门前置 shape 校验（补完整向量）

`_derive_close_txn_vars` 在 v2.2 §ΔB 硬门**之前**加：

```python
        # 🔒 v2.3（ΔC）：向量完整性门。filled_details 与 target_amounts 必须
        # 等长（正常初始化 L3250 同长全 0，成交才写价）。长度不等 = 台账
        # 不完整——tail 有无成交价无从证明（含截断批次重启后 L4562 重灌
        # 成 len(entry_orders) 的损坏态），hole 检查整体不可信 → Fail-Closed。
        if len(filled_details) != len(target_amounts):
            return False, None, (f'filled_details_shape_invalid（成交明细长度 '
                                 f'{len(filled_details)} != 计划层数 '
                                 f'{len(target_amounts)}——台账不完整，'
                                 '无法证明尾段无成交，请人工 reconcile）')
```

- v6.1 旧校验 `filled_details_short`（len < lfc）被本门**覆盖**（len != len(target_amounts) 是更严的等价+超集），正式 diff 时旧键由新键取代，测试同步；
- **133 套件 fixture 对齐**：D6e/D6g 截断形状正向用例的 `filled_details` 长度须等于 `len(target_amounts)`（截断后**未重启**的物理正确形态；F-1 旧 producer 只截 entry_orders，不动 target_amounts/filled_details）。

---

## §ΔD `limit_creating` transient grace（消除正常流程假 critical）

BEGIN `limit_creating` 成功后，**当前进程内**预置冻结告警窗口（不新增持久化字段、不碰 merge）：

```python
        # 🔒 v2.3（ΔD）：limit_creating 是 transient 态，正常几秒内 commit 为
        # limit_pending_normal。进程内 seed grace → monitor poll 撞上 transient
        # 窗口不误报「平仓流程卡死」；进程 crash 后内存 dict 消失 → 重启后
        # 磁盘仍 limit_creating 立即 critical（grace 自动失效，恰好只对
        # 真崩溃 loud）。
        self._freeze_alerted[target_symbol + ':' + batch_id] = time.time()
```

（键名以生产 `_freeze_alerted` 实际键格式为准——送审稿改动 4 现用 `batch_id` 单键，正式 diff 对齐；若现有键就是 `batch_id`，seed 用同一键。真正卡死时：调用路径自身有 error critical（B-lite / commit 失败通道），且重启后立即暴露——ChatGPT 已认可该取舍。）

---

## §ΔG 测试增量（4 个，接 v2.2 §ΔF）

| 编号 | 场景 | 必须证明 |
|---|---|---|
| R1-l | `batch_filled_count=0` + 磁盘 `is_programmatic_cancel=True, pending_close=False`，**真跑 monitor 退出判据**（改动 A-1 的 AFTER 块） | **不得 break**——棘轮字段不再有生命周期效力（锁死本轮 P0 事故链；R1-g 相应升级为此用例） |
| R1-m | `cancel(E5)` 正常返回，但 `verifier(E5)` = `filled` / `unknown`，E4 尚 active | **`cancel(E4)` 调用次数 == 0**——每层 verifier 统一（比 v2.2 R1-i 更严：不再只盯 -2011 路径） |
| R1-n | `target_amounts` len=5，`filled_details` len=3，`last_filled_count=3` | derive = False `filled_details_shape_invalid`，绝不产生 close amount；且正常等长批次不误报（双向） |
| R3-c | 当前进程 BEGIN `limit_creating` 后（已 seed `_freeze_alerted`）→ monitor poll 不 critical；模拟重启（清空 dict）→ 同一磁盘态**立即** critical | transient grace 只存在于存活进程；crash 后自动失 grace |

（v2.1/v2.2 的 R1-f~k、R2-f/g、R3-crash-state 全部保留。）

---

## §Gate 状态

- **全部原则批准**（ChatGPT）：主架构四条主线、D-4/D-5、registry 恢复算法 + hardening、-2011 verifier 门控、hole 执行硬门、zero-filled verifier 事实制、first-abnormal-wins 两组模型、`limit_creating` 迁移 + source-state guard、B-lite、MARKET/LIMIT 对称 rollback CAS、helper 13；
- **本稿闭环最后一环**：字段职责三分（归因 / 生命周期 / 事务态）——`is_programmatic_cancel` 棘轮 × monitor break 的确定性冲突从根上拆除；
- helper 计数仍 **13**；生产零改动（HEAD=e953d79）。

## §下一步

1. 用户过目本增量稿 → 送审 ChatGPT（只核 §ΔA-§ΔD + 4 个新反例 + A-3 行为变化披露）；
2. 批准后出正式 v6.2 送审稿 diff（AFTER 块全量 + 落点表 + checker 名单 13 helpers + `cancel_open_orders`/monitor 段入 `PROD_SIGNATURE_OVERRIDES`）；
3. RED→GREEN（v2 §六 + v2.1 §Δ9 + v2.2 §ΔF + 本稿 §ΔG）→ 五道门 + 变异 → 二次送审 → 五段式落生产。
