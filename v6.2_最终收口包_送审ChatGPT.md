# v6.2 r6 最终收口包（Scope Freeze 执行完毕 · 送 ChatGPT landing 终审）

> **日期**：2026-09-01 ｜ **范围冻结执行**：五道门 + M11~M38，无新增设计/checker/mutation/生产功能
> **生产三文件**：零改动（HEAD `e953d79`，`git diff --stat HEAD` = 空）——本包只申请终审，未落生产

---

## 1. 五道门最终输出（r6 权威稿 `v6.2_正式diff_r6_送审ChatGPT.md`）

| 门 | checker | rc | 备注 |
|---|---|---|---|
| ① code_blocks | `check_doc_code_blocks.py` | **0** | 全代码块 ast 可解析（签名片段已标注 python-frag） |
| ② parity | `check_doc_helper_parity.py` | **0** | 13-helper 全量 AFTER 在稿内；self-test M1~M5 rc=0 |
| ③ calls | `check_doc_helper_calls.py` | **0** | 55 项调用点核对（r6 稿 × v6.2 helper） |
| ④ landing_symbols | `check_doc_landing_symbols.py` | **0** | 5 个自由变量提示为片段语境固有局限（staged 集成 20/20 无 NameError 实证） |
| ⑤ 结构 surface | `verify_r6_surface.py` | **0** | prod↔staged direct-method delta = 恰好 13；对 M37 变异产物 rc=1（自证会失败） |

## 2. M11~M38 汇总（最终全量：**29/29 KILLED**）

### Transaction mutation · Green 层（M11~M31，判据 `test_v62_green.py`）— **21/21 KILLED**

| M | 杀死用例 | M | 杀死用例 |
|---|---|---|---|
| M11 | g_R1_a_positional_and_attribution | M22 | g_R1_d_integration（+前缀不一致判据） |
| M12 | g_R1_i_dash2011_then_filled | M23 | g_R3_d_tp_network_blocked, g_R3_f_tp_filled_blocked |
| M13 | g_R1_i_dash2011_then_filled | M24 | g_R1_s_producer2 |
| M14 | g_R1_b_g_zero_filtered | M25 | g_M25_crash_loud_fresh_process（fresh process） |
| M15 | g_R1_n_derive_shape | M26 | g_R1_t_u_derive_invalid（+非正尾段判据） |
| M16 | g_R1_a_positional_and_attribution | M27 | g_R3_f_tp_filled_blocked |
| M17 | g_R1_p_entry_detection | M28 | g_R3_g_coverage_blocked |
| M18 | g_R2_f_first_abnormal_wins | M29 | g_R3_h1_coverage_nan |
| M19 | g_R2_g_rollback_failed | M30 | g_R3_e_endpoint_routing |
| M20 | g_R3_b_commit_behavior | M31 | g_R3_h2_survey_topology（+账本矛盾判据） |
| M21 | g_R3_c_begin_seed | | |

**本轮 4 个存活的处置**（按裁定「存活→查具体问题」，未发散）：
- **M14**：变异体校准问题——原锚点在 else 终止分支，zero-filled 未确认终态场景走提前 return，变异体是死代码。移到实际执行路径（提前 return 前）→ KILLED。生产无缺口。
- **M22**：真实判据缺口——g_R1_d 只测了恢复成功路径，没测前缀不一致。补：registry E3→E9 伪造 → `recoverable=False` 断言。
- **M26**：fixture 判据缺口——尾段含正数 77003，两种逻辑都拦，-1 逃逸暴露不出来。尾段改纯非正（-1.0, 0.0）。
- **M31**：fixture 判据缺口——B 的 details 前缀 0.0 先被拓扑门拦下，到不了「计划量合计 ≤0」门。改：拓扑合法（前缀 70000+尾 0）但 amounts 前缀和 0。

### Transaction mutation · staged 集成层（M32a/M32b/M33/M34/M35/M36/M37/M38）— **8/8 KILLED + checker self-test 通过**

| M | 内容 | 杀死用例 |
|---|---|---|
| M32a/M32b | B-lite cancel 端点回退 conditional / verify 回退 conditional | t_limit_commit_fail_blite |
| M33 | first-abnormal-wins 覆盖 | t_market_first_abnormal_wins_midflight |
| M34 | create 计数（非幂等） | t_limit_tp_gate_blocked |
| M35 | 删 PROGRAMMATIC_CANCELED 豁免 | t_r6b_gate_skips_known_terminal |
| M36 | builder 退回 ast.walk（13→19 污染） | t_r6d_staged_helper_surface_exact13 |
| M37 | 删 batch-not-found guard | t_r6c_missing_batch_guard_no_exchange_call |
| M38 | 豁免扩成 PC+ABSENT+FAILED | t_r6e_absent_failed_not_skipped |

## 3. 最终 staged SHA

| 项 | 值 |
|---|---|
| 文件 | `G:/tmp/v62_staged/trader_260725_v62_staged.py` |
| 行数 | **9311** |
| SHA-256 | `46b75f2768ab21b449cb718ab2f1ca2e0f0ea7900e111d8ee88f89b4d64c2c4f` |
| 复现 | `build_v62_full.py` 从 HEAD 生产 + r6 附件机械生成（你上一轮已独立确认 byte parity） |

## 4. 准备落生产的 git diff（**只读生成，未应用**）

- 文件：`送审附件_v6.2/v62_production_landing.diff`（2086 行）
- 范围：**仅 `trader_260725.py`**（+1650 / -198）；`bot_runner.py` / `watchdog.py` **零改动**——无顺手变更
- 生成方式：`git diff --no-index trader_260725.py <staged>`，即 staged 全文就是 landing candidate，无 staged 之外的额外改动
- 重点自查（对应你的清单）：helper 恰好 13 ✅｜MARKET/LIMIT 与通过测试的 staged 同构（同一文件）✅｜B-lite normal endpoint ✅｜batch guard / PC skip 保留 ✅｜无 debug/test-only 代码混入（diff 内无 print 调试语句新增，测试代码全在项目根 test_*）✅

## 5. 一次性 legacy read-only 检查（`legacy_preflight_check.py`，不进生产代码库）

```
active 批次总数: 2
命中 LEGACY_UNRECOVERABLE: 0
✅ 部署条件满足：LEGACY_UNRECOVERABLE == 0（可上线）
```
判据按 r6：`len(target_amounts) > last_filled_count` AND `len(entry_orders) == last_filled_count` AND registry 链不可恢复。无自动 backfill、无 ID 猜测。

## 附：全量门禁快照（终审时可直接复核）

| 线 | 结果 |
|---|---|
| GREEN（helper/候选 SUT） | **28/28** |
| RED 基线（判别力） | **23/23** |
| staged transaction integration | **20/20** |
| canonical helper regression（v6.2 helper） | **133/133** |
| canonical（v6.1 批准基线，未动） | **133/133** |
| mutation M11~M38 | **29/29 KILLED** |
| 生产三文件 | diff=0 冻结 |

## 待你终审

若 landing diff 无新实锤 blocker → 下一步是**小仓真实 Hedge Mode 验证**（1 次 MARKET close + 1 次 LIMIT close，按你第三步 B 的清单），通过后落生产。在你终审通过前，生产三文件继续冻结，我不做任何改动/提交。
