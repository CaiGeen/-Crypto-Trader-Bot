# v6.2 正式送审稿 diff（精确改动脉冲）· 修订 r5 — 生产零改动

> **r5 修订说明**：ChatGPT 复审 r4 四项修正全通过（并独立复现 26/26 块自检）；再确认
> **最后 1 个 P0**：coverage 的 cross-batch hole 盲区——`_survey_same_side_batches`
> 对其他同方向批次只做 `sum(target_amounts[:lfc])`、无 shape/topology 校验，且
> `filled<=0: continue` 会把「声明 lfc>0 却算出 ≤0」的账本矛盾静默跳过 → hole 批次
> 用错误 prefix 量证明不存在的 coverage（cross-batch 错平）。修补：survey 对**所有**
> 纳入 coverage 的批次（含 target）执行与 derive 同一套 prefix/hole invariant，
> 不成立 → `(-1,-1,-1)` Fail-Closed；不新增 helper（计数仍 13）。
> 另吸收 2 个整理项：删「sticky bool 职责=归因」旧注释；10.6 normal 分支不传
> `params=None`（保持原调用形态）。**ChatGPT 裁定：修完这三点，直接授权 RED-first。**
>
> **基线**：本 diff 描述的是「生产 HEAD=e953d79 **叠加 v6.1 送审稿全部改动后**」的目标态
> 再叠加 v6.2 的增量。落点列同时给出 **生产原始行号**（定位用）与
> **v6.1 送审稿 AFTER 块锚点**（diff 应用位置）。
> **当前生产未做任何改动**；本文档是送审材料与后续 RED→GREEN 的实现规格。
> 文档纪律：`python` 块完整可解析（零 Ellipsis，过 `check_doc_code_blocks`）；
> 生产原文摘录用 `python-frag`。所有新 helper 走 parity/calls/landing 三道门。

---

## §零 交付总表

### 0.1 改动清单与落点

| 改动 | 目标 | 生产定位 | v6.1 送审稿锚点 | 对应测试 |
|---|---|---|---|---|
| 1 | `cancel_open_orders`（producer #1）：撤单循环高→低+每层 verifier、不写 sticky flag、按单归因、zero-filled 先验证后终止、D-3=False、entry_orders 永不压缩 | L6860-6943 | 无（v6.1 未涉及） | R1-a/b/e/g/i/j/l/m/r |
| 2 | monitor 手动撤单（producer #2）同纪律；**r2**：`canceled_layers` 后移到 verifier=gone 之后（否则 filled 层被 L4668-4669 永久挡在成交识别外） | L4826-4846 | 无 | R1-c/m/s |
| 3 | monitor zero-filled 退出判据只看 `pending_close` | L4858-4865 | 无 | R1-l |
| 4 | monitor ENTRY canceled 检测归因 order-scoped | L4749-4763 | 无 | R1-o/p |
| 5 | SL/TP 消费者归因 order-scoped（state-only） | SL L5324-5337 / TP L5465-5478 | 无 | R1-o/p |
| 6 | 市价 close：derive rollback 失败 CAS / 六态分支统一 CAS / TERMINAL_ZERO rollback 拒 CAS / post-create except CAS / pre-create rollback 失败 CAS / cleanup PENDING CAS+文案 | L6947-7143 | 送审稿 L406-651 | R2-f/g、R2 出口③④⑤⑥⑦ |
| 7 | 市价 close：撤 TP/SL 加 `terminal_ok` + 归因写入 | L7000+（撤保护单段） | 送审稿 L606-620 | R1-q |
| 8 | 市价/限价 ENTRY gate：registry 恢复视图 + 删除 orphan scan | helper 层 | 送审稿 L1943-2060 | R1-d/f |
| 9 | 限价 close：**TP factual gate（r2 新增，create 前硬门）** / BEGIN `limit_creating` + seed grace / durable commit 4 字段 + source-state guard / B-lite / outer except 双 CAS | L7470-7680（TP 段 L7559-7584） | 送审稿 L799-846 + 限价段 | R3-b/c/d/crash-state |
| 10 | helper 层：`_derive_close_txn_vars` shape+hole 硬门（**r2 精确化：finite-positive prefix + exact-zero tail**）/ `_pending_entry_ids_for_gate` 新增 / `_commit_limit_close_order_if_current` 新增 / `_set_close_reason_if_current` first-abnormal-wins / **reason 双集合（r2）** / BEGIN seed grace | `送审附件_v6.1/new_helpers_v6.py` | 送审稿 L183-303 / L359-393 / L1469+ | R1-k/n/t/u、R2-f、R3-b/c/crash-state |

### 0.2 checker 与 helper 同步（v6.2 落地前必须完成）

**helper 名单：11 → 13**

| 变化 | helper |
|---|---|
| 保留 | v6.1 既有 11 个（含 `_verify_entry_order_terminal`、`_set_close_reason_if_current`、`_derive_close_txn_vars`、`_cancel_and_verify_entry_orders`、`_begin_close_request_if_active`、`_rollback_close_request_if_active` 等） |
| **新增** | `_pending_entry_ids_for_gate`（改动 8/10） |
| **新增** | `_commit_limit_close_order_if_current`（改动 9） |
| **删除（v2 设计稿提出过，最终不落地）** | `_persist_checked`、`_verify_no_untracked_stop_orders`、`_freeze_reason_is_normal` |

同步项：
- `check_doc_helper_parity.py`：helper 文件函数名单 13；**前导下划线未申报函数 Fail-Closed**（L199）自动覆盖新 helper；
- `check_doc_helper_calls.py`：`PROD_SIGNATURE_OVERRIDES` 登记本方案改动的生产方法——
  `cancel_open_orders`、`_begin_close_request_if_active`（新增 seed 逻辑，签名不变）；
- `check_doc_landing_symbols.py`：新 helper 与全部 AFTER 块零自由变量；
- `run_mutation_checks_v61.py`：变异清单按 §十一重排（新增 R1-* 反例对应的变异体）。

### 0.3 复用声明（不新造轮子）

- ENTRY 终态判定**一律复用** `_verify_entry_order_terminal`（v6.1 helper，L732-777）；
- registry 读写**一律复用** `_find_registry_identity_by_order_id`（L3608-3624）+ `_update_registry`（L3659-3675）；
- reason 写入**一律复用** `_set_close_reason_if_current`（唯一 reason 写入口）。

### 0.4 r2 修订记录（ChatGPT 复审 3 P0 + 2 P1，全部独立源码核实实锤）

| # | 问题 | 核实证据 | 修补 |
|---|---|---|---|
| P0-1 | `limit_creating` 被放进冻结静默白名单 → crash 后磁盘 `limit_creating` 被 quiet，crash-loud 语义被打穿 | 本稿 10.5 单集合双职责（自有文档矛盾）；`_freeze_alerted` seed 是进程内存，crash 即消失，唯一防线就是「不在 quiet 集」 | 拆 `REASON_TRANSITION_SOURCES` / `FREEZE_QUIET_REASONS`（10.4/10.5） |
| P0-2 | producer #2 在 verifier 前置位 `canceled_layers[idx]=True` → verifier=filled 时该层被 L4668-4669 永久挡在成交识别外（filled_details/费用/持仓漏记） | 生产 L4656-4670：`if filled_layers[idx]: ... continue; if canceled_layers[idx]: continue` | bitmap 后移到 verifier=gone 之后（改动 2）+ R1-s |
| P0-3 | 限价 TP cancellation 只是「尝试」不是 create gate：`-2011 → _tp_terminal_ok=True` 且无论结果都 create LIMIT → TP 已触发成交时仍挂 LIMIT，hedge `positionSide=LONG` 无 reduceOnly 会减到 aggregate 敞口（错平其他批次） | 生产 L7559-7573 + L7575-7584 | 改动 9.0 TP factual gate（仅 cancel 成功或 TP 不存在才 proceed）+ R3-d/e |
| P1 | hole gate 的 tail 检查 `>0` 会让 None/'corrupt'/-1/NaN 逃逸；prefix 的 `<=0` 对 NaN 为 False | 自有代码语义 vs 文字契约（finite-positive prefix + exact-zero tail） | 10.3 精确化 + R1-t/u |
| P1 | R1-j 与实现不一致：非 -2011 的 cancel 异常在 verifier 前 break，「网络失败但实际已撤」永不验证 | 改动 1 原稿 `if not (-2011...): break` | r2：所有 cancel 异常一律走 verifier（gone→continue 但不宣称 programmatic 归因） |

### 复审第三轮（r2 复审，3 项全实锤）

| # | 问题 | 核实证据 | 修补（r3） |
|---|---|---|---|
| P0-1 | TP factual gate 不是 factual：r2 只把「cancel 抛异常也继续」修成「cancel 没抛异常就继续」，cancel-success 后无任何事实确认 → TP 与新 LIMIT 仍可能重叠减仓 | r2 9.0 原文 `try: cancel ... if _tp_gate_ok: # 继续 create`，无 fetch/verifier | 9.0 重写：cancel attempt → `_confirm_close_filled()` 六态，**仅 TERMINAL_ZERO 放行**；+ R3-f / M27 |
| P0-2 | LIMIT 无 coverage guard，与 MARKET 安全等级不对称：`_close_amount_guard` 仅市价调用（送审稿 L469），限价直挂 `amount=current_filled_amount`；BEGIN single-flight 不能证明 aggregate 敞口 ≥ 台账合计 → 错平他批次 | helper L677-728（guard 定义，多批次 actual<sum_all→None）；送审稿限价段无调用；生产 L7584-7593 直挂 | 9.0b：create 紧前调用 guard，`safe_amount == ledger` 才放行（LIMIT 无 partial-PnL 传播），否则 `limit_amount_conflict` Fail-Closed；+ R3-g / M28 |
| P1 | producer #1 的 `cancelled_ids/cancelled_layers` 在 cancel 返回后立即 append（动作统计冒充事实统计），verifier=filled 时文案会出现「已撤 1 张 + 未确认 1 张」的矛盾 | r2 改动 1 原文 | r3 三套统计分离：`cancel_requested_ids`（动作）/ `programmatic_gone_ids`（归因）/ `unresolved_ids`（事实）；文案只用事实集合 |

### 复审第四轮（r3 复审，4 项全实锤）

| # | 问题 | 核实证据 | 修补（r4） |
|---|---|---|---|
| P0-1 | TP 六态门复用**错误的交易所查询端点**：`_confirm_close_filled → _fetch_close_order_state` 只走 normal 端点（无 stop=True），TP 是 conditional 条件单 → 恒 not_found → NOT_CONFIRMED → **有 TP 的正常限价平仓被系统性 Fail-Closed**（上线阻断级）；Fake 不区分端点还会假绿 | helper L443-472（docstring L452「平仓单走 normal 端点」+ fetch 无 params）；生产 `_verify_order_created` L3377-3381 明载「conditional 必须带 stop=True 走 algo 端点，否则恒 not_found 假阴性（C5 事故根因）」且**该方法本身已有 order_kind 参数化先例** | 10.6：两 helper 参数化 `order_kind`（默认 'normal' 零变化，TP 传 'conditional'）；9.0 调用同步；helper 仍 13；+R3-e 事实矩阵（含 endpoint-routing 正反例）/ M30 |
| P0-2 | coverage guard NaN/Inf **fail-open**：`_read_position_amt` 累加只捕 TypeError/ValueError，NaN 直通；guard 三个 NaN 比较全 False（单批返回 NaN / 多批返回台账）；r3 最终门 `abs(NaN-x) > 1e-8` 亦 False → UNKNOWN→PASS | helper L437-440 累加逻辑 + L709/713/719 比较链数值推导 | 10.7：`_read_position_amt` NaN/Inf→None（惠及全部调用方）+ `_close_amount_guard` 对 ledger/actual/sum_all finite 校验；+R3-h1 / M29 |
| P1-1 | r3 写「guard 调用点必须持有写侧锁」是反的：`_state_lock` 是非重入 `threading.Lock()`（生产 L202），锁内零交易所 API 是铁律，guard 第一步就是 fetch_positions | 生产 L202 + BEGIN/CAS helper「锁内零 API」契约 | 9.0b 文案改为「不得持有 `_state_lock`，锁外调用、create 紧前执行」 |
| P1-2 | R3-e 把 `-2011` 又偷偷变成阻断判据，与「action result 不下结论」状态机冲突 | r3 测试表原文 vs 9.0 六态状态机 | R3-e 重写为事实矩阵（4 行，cancel 结果与 fetch 事实正交），Fake 建模端点路由 |

### 复审第五轮（r4 复审，1 P0 + 2 整理项，全实锤）

| # | 问题 | 核实证据 | 修补（r5） |
|---|---|---|---|
| P0 | coverage 的 cross-batch hole 盲区：`_survey_same_side_batches` 对其他同方向批次只 `sum(target_amounts[:lfc])`、无 shape/topology 校验，且 `filled<=0: continue` 静默跳过账本矛盾 → hole 批次用错误 prefix 量证明不存在的 coverage（A derive 查 hole ✅ / B survey 不查 ❌ 不对称） | v6.1 helper L661-667 原文；决定性反例：B `[0.1,10.0]/lfc=1/filled_details=[0,70000]` → 算出 0.1、sum_all 低估 → LIMIT 平 A 侵占 B 剩余敞口 | 10.8：survey 对**所有**纳入 coverage 的批次（含 target）执行 shape+prefix-topology+tail-zero 判据（局部闭包，不新增 helper），不成立 → `(-1,-1,-1)`；`lfc>0 且 filled<=0` = 账本矛盾 Fail-Closed（不再 continue）；+ R3-h2 / M31 |
| P1 | 改动 3 旧注释「is_programmatic_cancel 职责=撤单来源归因」不符合最终模型 | 与 v2.4 收口裁定（归因=registry order-ID 事实）冲突 | 注释改为「已彻底退出归因用途，legacy 辅助字段」 |
| P1 | 10.6 normal 路径显式传 `params=None`，与「零变化」目标不符（CCXT params 默认 `{}`） | r4 10.6 原文 | normal 分支不传 params（保持原调用形态） |

---

## 改动 1：`cancel_open_orders`（producer #1）

**落点**：生产 L6860-6943（函数体）；被删/被改的既有行为：
- L6871-6873 sticky `is_programmatic_cancel=True` 写入 → **删除**；
- L6876-6893 撤单循环（低→高、失败只 print、无条件截断） → **改写**；
- L6895-6897 `entry_orders` 截断 → **删除**（D-4 永不压缩）；
- L6921-6929 zero-filled 分支（无条件 `entry_orders=[]` + 终止标志） → **改写**；
- L6943 `return True` → 部分失败改 False（D-3）。

### AFTER（完整替换 L6860-6943 的函数主体，从 pending_count 判断之后开始）

```python
        # 🔒 v6.2（D-4 语义降级）：pending_count 仅表示「待撤尝试区大小」，
        # 不再表示「仍有 pending 单」——entry_orders 永不压缩后，尾段可能
        # 全是已终态 ID（重撤幂等，-2011/-Unknown 由下方 verifier 吸收）。
        pending_count = len(entry_orders) - last_filled_count

        if pending_count <= 0:
            return False, f"ℹ️ 批次 `{batch_id}` 没有未成交的挂单"

        # 🔒 v6.2-r3：三套统计严格分离（动作 ≠ 事实 ≠ 归因）：
        #   cancel_requested_ids  = cancel 调用成功返回（动作统计，仅日志/文案）
        #   programmatic_gone_ids = cancel 成功 + verifier=gone → 可写 registry 归因
        #   unresolved_ids        = verifier 非 gone（未确认终态，Fail-Closed 源）
        #   already_terminal_ids  = cancel 异常但 verifier=gone（事实 gone，
        #                           但无法证明是本程序终结 → 不写归因）
        cancel_requested_ids = []
        requested_layers = []
        unresolved_ids = []
        already_terminal_ids = []
        programmatic_gone_ids = []

        # 🔒 v6.2（INV-3a）：从最高层往最低层撤 + 遇阻即停。
        # canceled 层恒在所有 active 层之上 → 成交位保持前缀连续，
        # 不主动制造 hole（last_filled_count 的 prefix 假设才成立）。
        for idx in reversed(range(last_filled_count, len(entry_orders))):
            order_id = entry_orders[idx]
            _cancel_ok = False
            try:
                self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol, params={'stop': True})
                _cancel_ok = True
                cancel_requested_ids.append(order_id)
                requested_layers.append(idx + 1)
                print(f"  └─ 已请求撤销第 {idx + 1} 层挂单: {order_id}")
            except Exception as e:
                # 🔒 v6.2-r2：**所有** cancel 异常（含 -2011/网络失败）一律交给
                # verifier 定案 —— 不区分异常类型、不在 verifier 前下结论。
                # （-2011 只证明「无需再 cancel」，网络失败可能实际已撤成功。）
                print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单请求异常: {order_id} ({e})，交由逐 ID 验证定案")
            # 🔒 v6.2（INV-1v2 / ΔB）：每层统一 verifier ——
            # cancel 成功返回 ≠ terminal 事实（_safe_api_call 只透传底层结果）。
            verdict, _vo = self._verify_entry_order_terminal(order_id, target_symbol)
            if verdict == 'gone':
                if _cancel_ok:
                    # cancel 调用成功 + 交易所确认 canceled → 程序终结事实成立
                    programmatic_gone_ids.append(order_id)
                else:
                    # cancel 请求异常但事实已 canceled：不宣称「程序终结」
                    # （无法排除用户手工撤单竞态），归因留空 → Fail-Noisy
                    already_terminal_ids.append(order_id)
                continue
            if verdict == 'filled':
                self.send_tg_notification(
                    f"🚨【资金安全】ENTRY 在撤单前已成交！\n"
                    f"🆔 批次: `{batch_id}`\n📌 订单: {order_id}\n"
                    f"⚠️ 高层成交 → 成交位可能已不连续（hole），"
                    f"已停止向更低层撤单，请立即人工核对持仓与台账！",
                    level='critical')
            unresolved_ids.append(order_id)
            break  # filled / open / unknown 一律停止（不制造 hole）

        if not programmatic_gone_ids and not already_terminal_ids and unresolved_ids:
            # 全部未确认终态：台账原样，批次保持现状，监控继续管辖这些层
            return False, (f"⚠️ 批次 `{batch_id}` 挂单全部未确认终态"
                           f"（{len(unresolved_ids)} 张，ID 已保留），"
                           f"请重试或人工核对")

        # 🔒 v6.2（ΔE1）：归因 order-ID scoped —— 不再写 batch-global sticky
        # is_programmatic_cancel（棘轮字段，且会永久关闭 SL/TP 自动补挂）。
        for order_id in programmatic_gone_ids:
            _ident = self._find_registry_identity_by_order_id(target_symbol, batch_id, order_id)
            if _ident:
                self._update_registry(target_symbol, batch_id, _ident,
                                      state='PROGRAMMATIC_CANCELED',
                                      order_id=order_id, id_known=True,
                                      terminated_reason='cancel_open_orders')
            else:
                print(f"  └─ ⚠️ 撤单归因：{order_id} 在 registry 中无 identity"
                      f"（撤单事实已完成，归因降级为 manual 语义）")

        # 🔒 v6.2（D-4）：entry_orders 作为 positional/audit ledger 永不压缩——
        # 已终态的 ID 一并留在原位，层号零漂移；terminal 与 pending 状态
        # 不再由 list 长度推断。
        remaining_orders = entry_orders
        target_b_data['entry_orders'] = remaining_orders

        pending_sl = target_b_data.get('pending_sl_orders', [])
        pending_sl = [idx for idx in pending_sl if idx < last_filled_count]
        target_b_data['pending_sl_orders'] = pending_sl

        current_持仓 = sum(target_b_data.get('target_amounts', [])[:last_filled_count])

        if last_filled_count > 0:
            self.save_batch_state(target_symbol, batch_id, target_b_data)
            result_msg = (
                f"🗑️ **撤单完成**\n\n"
                f"🆔 批次：`{batch_id}`\n"
                f"🪙 标的：`{target_symbol}`\n"
                f"📊 本轮待撤尝试：{pending_count} 层\n"
                f"├─ 已确认终态：{len(programmatic_gone_ids) + len(already_terminal_ids)} 张\n"
                f"├─ 未确认终态：{len(unresolved_ids)} 张（ID 已保留）\n"
                f"📊 当前持仓：{current_持仓}\n\n"
                f"💡 {last_filled_count} 层已成交，止盈止损单已保留，监控继续运行"
            )
        else:
            # 🔒 v6.2（Δ2 / P0）：zero-filled 终止标志的前置 = 逐 ID terminal 确认。
            # 旧代码无条件 entry_orders=[] + pending_close/close_phase=1，
            # 而 monitor 只看磁盘标志（L4858-4865）→ 活 ENTRY 失去管辖。
            unresolved = []
            _filled_found = False
            for order_id in entry_orders:
                verdict, _vo = self._verify_entry_order_terminal(order_id, target_symbol)
                if verdict == 'gone':
                    continue
                unresolved.append((order_id, verdict))
                if verdict == 'filled':
                    _filled_found = True
            if _filled_found:
                self.send_tg_notification(
                    f"🚨【资金安全】撤单确认期间发现 ENTRY 已成交！\n"
                    f"🆔 批次: `{batch_id}`\n"
                    f"⚠️ 批次不再是无持仓状态，已保持 ACTIVE 由监控接管，"
                    f"请立即人工核对持仓！",
                    level='critical')
            if unresolved:
                # 绝不写 pending_close/close_phase/is_programmatic_cancel
                self.save_batch_state(target_symbol, batch_id, target_b_data)
                return False, (f"⚠️ 批次 `{batch_id}` 撤单后仍有 "
                               f"{len(unresolved)} 张 ENTRY 未确认终态"
                               f"（{[u[0] for u in unresolved]}，ID 已保留，监控继续）\n"
                               f"💡 请重试或人工核对")
            # 全部 gone → 才获得终止资格
            target_b_data['pending_sl_orders'] = []
            target_b_data['pending_close'] = True
            target_b_data['close_phase'] = 1
            self.save_batch_state(target_symbol, batch_id, target_b_data)

            result_msg = (
                f"🗑️ **撤单完成**\n\n"
                f"🆔 批次：`{batch_id}`\n"
                f"🪙 标的：`{target_symbol}`\n"
                f"📊 已确认终态：{len(entry_orders)} 张\n"
                f"📊 当前持仓：0\n\n"
                f"💡 批次已无成交层，监控将自然退出"
            )

        if unresolved_ids:
            # 🔒 v6.2（D-3 裁定）：部分失败就是失败（统计口径 = 事实，非动作）
            return False, (f"⚠️ 批次 `{batch_id}` 撤单部分完成："
                           f"{len(programmatic_gone_ids) + len(already_terminal_ids)} 张已确认终态，"
                           f"{len(unresolved_ids)} 张未确认终态（{unresolved_ids}，"
                           f"ID 已完整保留，监控继续）\n💡 请重试或人工核对")

        return True, result_msg
```

---

## 改动 2：monitor 手动撤单路径（producer #2）

**落点**：生产 L4826-4846（`manual_canceled_detected` 且 `batch_filled_count > 0` 分支）。

### AFTER

```python
                        cancelled_count = 0
                        programmatic_gone_this_round = []
                        # 🔒 v6.2：高→低 + 每层 verifier + 遇阻即停（同 producer #1）
                        for idx in reversed(range(len(entry_orders))):
                            if filled_layers[idx] or canceled_layers[idx]:
                                continue
                            order_id = entry_orders[idx]
                            _cancel_ok = False
                            try:
                                self._safe_api_call(self.exchange.cancel_order, order_id, symbol,
                                                    params={'stop': True})
                                _cancel_ok = True
                                print(f"  └─ 已撤销第 {idx + 1} 层挂单: {order_id}")
                            except Exception as e:
                                # 所有异常一律交 verifier 定案（同 producer #1 r2）
                                print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单请求异常: {order_id} ({e})，交由逐 ID 验证定案")
                            verdict, _vo = self._verify_entry_order_terminal(order_id, symbol)
                            if verdict == 'gone':
                                # 🔒 v6.2-r2（P0 修正）：bitmap 只在「交易所确认 gone」
                                # 之后才置位 —— verifier=filled 时若已置
                                # canceled_layers=True，下一轮 `if canceled_layers[idx]:
                                # continue`（生产 L4668-4669）会把已成交层永久挡在
                                # 成交识别之外（filled_details/费用/持仓全部漏记）。
                                canceled_layers[idx] = True
                                cancelled_count += 1
                                if _cancel_ok:
                                    programmatic_gone_this_round.append(order_id)
                                continue
                            if verdict == 'filled':
                                self.send_tg_notification(
                                    f"🚨【资金安全】ENTRY 在撤单前已成交！\n"
                                    f"🆔 批次: {batch_id}\n📌 订单: {order_id}\n"
                                    f"⚠️ 已停止向更低层撤单；该层保持未标记状态，"
                                    f"下一轮监控将按成交事实入账，请立即人工核对持仓与台账！",
                                    level='critical')
                            break

                        # 🔒 v6.2（ΔE1-2）：不再写 sticky is_programmatic_cancel，
                        # 台账 entry_orders 原样保留（删除旧 L4842-4843 截断）。
                        latest_all = self.load_all_states()
                        latest_b_data = latest_all.get(symbol, {}).get(batch_id, {})
                        if latest_b_data:
                            self.save_batch_state(symbol, batch_id, latest_b_data)
                        for _oid in programmatic_gone_this_round:
                            _ident = self._find_registry_identity_by_order_id(symbol, batch_id, _oid)
                            if _ident:
                                self._update_registry(symbol, batch_id, _ident,
                                                      state='PROGRAMMATIC_CANCELED',
                                                      order_id=_oid, id_known=True,
                                                      terminated_reason='monitor_cancel_remaining_entries')
```

---

## 改动 3：monitor zero-filled 退出判据只看 `pending_close`

**落点**：生产 L4858-4865。

### AFTER

```python
                if batch_filled_count == 0:
                    latest_all_pc = self.load_all_states()
                    latest_b_data_pc = latest_all_pc.get(symbol, {}).get(batch_id, {})
                    if latest_b_data_pc and latest_b_data_pc.get('pending_close', False):
                        # 🔒 v6.2：退出资格只看 pending_close。
                        # is_programmatic_cancel 是 False→True 棘轮（L1627-1629），
                        # pop+save 清不掉；**它已彻底退出归因用途**（权威归因 =
                        # protection_registry 的 order-ID 事实），此处仅作为
                        # legacy 辅助字段继续存在，不得再参与任何生命周期或
                        # 订单事件判断。
                        print(f"🚨 [批次终止] 本批次未建仓，撤单已全部确认终态，正在退出监控...")
                        break
```

**operational 语义（§ΔE4）**：zero-filled unresolved 后批次保持 ACTIVE + `pending_close=False` +
`close_phase=0`，ID 完整保留、monitor 在线；**不会自动转 terminal**（归因仍为 program 语义，
不置 `manual_canceled_detected`），需用户再点一次 🗑️ 或人工 reconcile 完成终止。
本轮不新增自动 terminal 状态机。

---

## 改动 4：monitor ENTRY canceled 检测归因 order-scoped

**落点**：生产 L4749-4763。

### AFTER

```python
                                _en_ident = self._find_registry_identity_by_order_id(
                                    symbol, batch_id, order_id)
                                _en_reg = ((latest_b_data_check.get('protection_registry') or {})
                                           .get(_en_ident) if _en_ident else None)
                                _en_prog = (isinstance(_en_reg, dict)
                                            and _en_reg.get('state') == 'PROGRAMMATIC_CANCELED')
                                if _en_prog:
                                    print(f"ℹ️ [程序撤单] 第 {idx + 1} 层开仓条件单已由程序终结 (ID: {order_id})")
                                    # 不设置 manual_canceled_detected
                                else:
                                    manual_canceled_detected = True
                                    print(f"⚠️ 🛑 [手动撤单提醒] 第 {idx + 1} 层开仓条件单被撤销 (ID: {order_id})")
                                    self.send_tg_notification(
                                        f"⚠️ 🛑 **[撤单提醒]** 批次 `{batch_id}` 第 {idx + 1} 层条件单已被手动撤销/失效。"
                                    )
```

（registry 无条目 → 按 manual 语义提醒：Fail-Noisy 方向，宁可误报也不静默。）

---

## 改动 5：SL / TP 消费者归因 order-scoped（state-only）

### 5.1 SL（落点 L5324-5337）

```python
                            # 🔒 v6.2：归因 order-ID scoped。batch-global sticky bool
                            # 会因一次 🗑️ 永久为 True，导致 SL 被外部撤销时永不补挂。
                            # 授权只认 registry state（与 _assert_create_allowed 同规范），
                            # 不解析 terminated_reason 字符串。
                            _sl_ident = self._find_registry_identity_by_order_id(
                                symbol, batch_id, current_sl_id)
                            _sl_reg = ((latest_b_data_check.get('protection_registry') or {})
                                       .get(_sl_ident) if _sl_ident else None)
                            _sl_prog = (isinstance(_sl_reg, dict)
                                        and _sl_reg.get('state') == 'PROGRAMMATIC_CANCELED')
                            if _sl_prog:
                                print(f"ℹ️ [程序撤单] 批次 {batch_id} 止损单已由程序终结 (ID: {current_sl_id})")
                                current_sl_id = None
                            elif user_modified:
                                print(f"ℹ️ [用户主动修改] 批次 {batch_id} 止损单已被用户撤销，不再自动补挂")
                                current_sl_id = None
                            else:
                                print(f"⚠️ ⚠️ [风控异常] 止损单已在外部撤销，准备按策略自动补挂...")
                                current_sl_id = None
                                need_recover_sl = True
```

### 5.2 TP（落点 L5465-5478，同构）

```python
                                _tp_ident = self._find_registry_identity_by_order_id(
                                    symbol, batch_id, tp_order_id)
                                _tp_reg = ((latest_b_data_check.get('protection_registry') or {})
                                           .get(_tp_ident) if _tp_ident else None)
                                _tp_prog = (isinstance(_tp_reg, dict)
                                            and _tp_reg.get('state') == 'PROGRAMMATIC_CANCELED')
                                if _tp_prog:
                                    print(f"ℹ️ [程序撤单] 批次 {batch_id} 止盈单已由程序终结 (ID: {tp_order_id})")
                                    tp_order_id = None
                                elif user_modified:
                                    print(f"ℹ️ [用户主动修改] 批次 {batch_id} 止盈单已被用户撤销，不再自动补挂")
                                    tp_order_id = None
                                else:
                                    print(f"⚠️ ⚠️ [风控异常] 止盈单已在外部撤销，准备按策略自动补挂...")
                                    tp_order_id = None
                                    need_recover_tp = True
```

---

## 改动 6：市价 close 的六个 reason 出口（INV-2 闭环）

**落点**：v6.1 送审稿市价 AFTER 块（L406-651）；生产对应 L6947-7143。
**统一纪律**：所有 reason 写入只走 `_set_close_reason_if_current()`；异常出口若磁盘仍属本事务
（`close_op_id` 匹配 + `close_phase>=1`），`close_reason` 必须落在异常集；CAS 本身失败时
critical 文案如实追加「切换失败」。

### 6.1 derive 失败分支（落点：送审稿 L775-786）

```python
        _vars_ok, _txn_vars, _vars_why = self._derive_close_txn_vars(_claimed, batch_id)
        if not _vars_ok:
            _rb_ok, _rb_why = self._rollback_close_request_if_current(
                target_symbol, batch_id, close_op_id)
            if not _rb_ok:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'txn_aborted_rollback_failed')
                self.send_tg_notification(
                    f"🚨【资金安全】平仓中止且回滚失败，批次冻结！\n"
                    f"🆔 批次: `{batch_id}`\n💡 派生失败原因: {_vars_why}\n"
                    f"🔄 回滚失败原因: {_rb_why}\n"
                    + ('' if _rs_ok else
                       f"⚠️⚠️ close_reason 切换也失败（{_rs_why}），"
                       "冻结告警可能不再周期触发，请立即人工处置！"),
                    level='critical')
                return False, f"❌ 平仓中止（{_vars_why}）；回滚失败已冻结待人工处置"
            self.send_tg_notification(
                f"🚨【资金安全】平仓中止：claimed 快照不能用于下单。\n"
                f"🆔 批次: `{batch_id}`\n💡 原因: {_vars_why}\n"
                f"🔄 回滚本次平仓标记: 成功\n"
                f"⚠️ 未发出任何订单，请人工核对账本与批次状态。",
                level='critical')
            return False, f"❌ 平仓中止（{_vars_why}）"
```

### 6.2 六态非确认分支（落点：送审稿 L548-552，删 `except: pass`）

（摘录：落点为六态 if/else 的 else 分支，落地时随该段整体过检。）

```python-frag
            else:
                # PARTIAL / PENDING / UNKNOWN / NOT_CONFIRMED —— 一律不回滚
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'market_confirm_unknown')
                raise RuntimeError(
                    f"市价平仓单结果未确认（{_verdict}）：{_detail}。"
                    f"不回滚，保持冻结等人工处置"
                    + ('' if _rs_ok else
                       f"；⚠️ close_reason 切换失败（{_rs_why}），"
                       "冻结告警可能不再周期触发，请立即人工核查！"))
```

### 6.3 TERMINAL_ZERO rollback 被拒（落点：送审稿 L537-539）

```python
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'rollback_rejected')
                raise RuntimeError(
                    f"平仓单未成交且回滚被拒绝（{_rb_why}），转人工处置"
                    + ('' if _rs_ok else
                       f"；⚠️ close_reason 切换失败（{_rs_why}），"
                       "冻结告警可能静默，请立即人工核查！"))
```

### 6.4 outer except：`close_order_placed=True`（落点：送审稿 L628-634）

```python
            if close_order_placed:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'settlement_error')
                self.send_tg_notification(
                    f"🚨【资金安全】市价平仓单已发出但后续结算异常（未回滚关闭标记）！\n"
                    f"🆔 批次: {batch_id}\n💡 原因: {str(e)[:150]}\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n")
                    + f"⚠️ 请立即人工核对持仓与挂单！",
                    level='critical')
                return False, f"❌ 市价平仓结算异常（平仓单已创建，close_phase 保持）: {e}"
```

### 6.5 outer except：pre-create rollback 失败（落点：送审稿 L644-649）

（摘录：落点为 outer except 内 `if close_order_placed: ... else:` 的 else 分支。）

```python-frag
            else:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'txn_aborted_rollback_failed')
                self.send_tg_notification(
                    f"🚨【资金安全】市价平仓失败且回滚被拒绝！\n批次: `{batch_id}`\n"
                    f"原因: {_rb_why}\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），"
                       "冻结告警可能静默，请立即人工处置！\n")
                    + f"请立即检查仓位是否仍有 SL 保护！",
                    level='critical')
            return False, f"❌ 市价平仓失败: {e}"
```

### 6.6 cleanup=PENDING（落点：生产 L7100-7104 / 送审稿结算段）

```python
            if _cleanup_pending:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'settlement_stuck')
                result_msg += (
                    f"\n\n⚠️ **[残单收敛提示]** 市价平仓已成交（持仓=SUCCESS），"
                    f"但批次清理暂未收敛（cleanup=PENDING，proof 未通过）。"
                    f"系统仍会继续尝试收敛；若告警持续存在，请人工检查残单。"
                    + ('' if _rs_ok else
                       f"\n⚠️ close_reason 切换失败（{_rs_why}），请人工关注该批次。"))
```

**限价路径（生产 L7656-7665）同构补齐 6.4 / 6.5 两段**（文案前缀改为「限价平仓…」，reason 值相同）。

---

## 改动 7：市价撤 TP/SL 加 `terminal_ok` + 归因（落点：送审稿 L606-620）

```python
            _tp_terminal_ok = False
            if _txn_vars.get('tp_order_id'):
                try:
                    self._safe_api_call(self.exchange.cancel_order, _txn_vars['tp_order_id'],
                                        target_symbol, params={'stop': True})
                    _tp_terminal_ok = True
                    print(f"  └─ 已撤销止盈单: {_txn_vars['tp_order_id']}")
                except Exception:
                    pass

            _sl_terminal_ok = False
            if _txn_vars.get('current_sl_id'):
                try:
                    self._safe_api_call(self.exchange.cancel_order, _txn_vars['current_sl_id'],
                                        target_symbol, params={'stop': True})
                    _sl_terminal_ok = True
                    print(f"  └─ 已撤销止损单: {_txn_vars['current_sl_id']}")
                except Exception:
                    pass

            # 🔒 v6.2：只有各自 cancel 正常返回的那一张才写 PROGRAMMATIC_CANCELED。
            # 异常（含 -2011/OrderNotFound）= 不知道它为何消失 → 不记「程序已终结」，
            # 交 converge / 后续事实判断（与 ENTRY verifier 同一保守哲学）。
            for _oid, _ok in ((_txn_vars.get('tp_order_id'), _tp_terminal_ok),
                              (_txn_vars.get('current_sl_id'), _sl_terminal_ok)):
                if not _oid or not _ok:
                    continue
                _ident = self._find_registry_identity_by_order_id(target_symbol, batch_id, _oid)
                if _ident:
                    self._update_registry(target_symbol, batch_id, _ident,
                                          state='PROGRAMMATIC_CANCELED',
                                          order_id=_oid, id_known=True,
                                          terminated_reason='close_requested_canceled')
```

---

## 改动 8：ENTRY gate 的 registry 恢复视图（落点：送审稿 L1977-1981 / `_cancel_and_verify_entry_orders` 头部）

```python
        entry_orders = b_data.get('entry_orders', []) or []
        if (len(b_data.get('target_amounts', []) or []) > last_filled_count
                and len(entry_orders) == last_filled_count):
            # 🔒 v6.2：legacy 截断形状 → registry 链恢复（不靠 open-orders 快照：
            # 快照看不见已成交的遗失 ENTRY）。symbol-wide orphan scan 已删除
            # （同 symbol 其他合法批次会被误判）。
            rec_ids, recoverable, _chain = self._pending_entry_ids_for_gate(
                symbol, batch_id, b_data, last_filled_count)
            if not recoverable:
                self.send_tg_notification(
                    f"🚨【资金安全】截断台账无法与 protection_registry 对上链！\n"
                    f"🆔 批次: {batch_id}\n"
                    f"⚠️ registry ENTRY 链与 target_amounts/entry_orders 前缀不一致，"
                    f"不能归因缺失层，请立即人工 reconcile！",
                    level='critical')
                return False
            pending_ids = list(rec_ids)
        else:
            pending_ids = [oid for idx, oid in enumerate(entry_orders)
                           if idx >= last_filled_count and oid]
        if not pending_ids:
            return True
```

（后续逐 ID cancel + `fetch_order` 终态验证循环不变——数据源换成 `pending_ids`。）

---

## 改动 9：限价 close（TP factual gate / BEGIN / commit / B-lite / outer except）

### 9.0 TP factual gate（create LIMIT 之前的硬门，v6.2-r3 精确化）

**生产实锤**（L7559-7573）：cancel TP 失败时 `_tp_terminal_ok=False` 仅 print「TP 可能仍在场」，
`-2011/Unknown order` 直接 `_tp_terminal_ok=True`（注释「已离开交易所 = 事实终态」）；
**L7575-7584：无论 True/False 都继续 create LIMIT**。hedge 模式 LIMIT 参数为
`positionSide=LONG/SHORT`、无 reduceOnly（L7578-7582）——TP 未撤/已触发成交时仍挂 LIMIT，
会减到同 symbol 同方向的 **aggregate 敞口（错平其他批次）**。

**r3 规格**（ChatGPT 裁定）：cancel 动作结果（成功/异常）都**不下结论**；TP 必须经
既有六态确认器 `_confirm_close_filled()` 到达**事实判定**，**只有 TERMINAL_ZERO 放行**：

```text
tp_order_id 不存在（原本无 TP）   → proceed
有 TP → cancel attempt（成功/异常都只算动作）
     → _confirm_close_filled(TP_ID, ...)
     TERMINAL_ZERO（canceled/expired/rejected 且权威 filled==0） → proceed
     CONFIRMED_FULL / PARTIAL（TP 已成交/部分成交——仓位事实已变化） → 禁止 + 冻结
     PENDING（仍活着）                                            → 禁止 + 冻结
     UNKNOWN / NOT_CONFIRMED（无法证明安全）                       → 禁止 + 冻结
```

（复用六态而非新造保护单 verifier：TP 的成交事实变化属于「仓位事实已变化」，
必须冻结转人工，而非程序自动裁决。**r4 端点路由（P0）**：TP 是 conditional 条件单，
六态确认内部必须走 algo 端点——`order_kind='conditional'`（见改动 10.6）；
生产 `_verify_order_created`（L3377-3381）早已记载：不带 `stop=True` 查条件单
会命中普通端点 → 恒 not_found → 假阴性（C5 事故 12 处误判 24 孤儿单的根因）。
`terminated_reason`/registry 终态仍按 N14 延后写入——只在真正走到 create 之后才写，
禁止 create 时不污染 registry。）

```python
            _tp_old_id = target_b_data.get('tp_order_id')
            if _tp_old_id:
                _tp_cancel_note = 'cancel 正常返回'
                try:
                    self._safe_api_call(self.exchange.cancel_order, _tp_old_id, target_symbol,
                                        params={'stop': True})
                except Exception as _tp_cancel_e:
                    # 🔒 v6.2-r3：cancel 动作结果（成功/异常）都不下结论，
                    # 事实由下方六态确认器定案。
                    _tp_cancel_note = f'cancel 异常（{_tp_cancel_e}）'
                _tp_verdict, _tp_detail, _tp_filled = self._confirm_close_filled(
                    target_symbol, side,
                    target_b_data.get('is_hedge_mode', False),
                    _tp_old_id, current_filled_amount, current_filled_amount,
                    order_kind='conditional')
                if _tp_verdict != 'TERMINAL_ZERO':
                    # 🔒 v6.2-r3（P0）：只有「明确终态且 filled==0」才允许挂 LIMIT。
                    # CONFIRMED_FULL/PARTIAL = 仓位事实已变化；PENDING = 仍活着；
                    # UNKNOWN/NOT_CONFIRMED = 无法证明安全 —— 一律 Fail-Closed。
                    _rs_ok, _rs_why = self._set_close_reason_if_current(
                        target_symbol, batch_id, close_op_id, 'limit_tp_unresolved')
                    self.send_tg_notification(
                        f"🚨【资金安全】限价平仓中止：旧止盈单 {_tp_old_id} 未确认安全终结！\n"
                        f"🆔 批次: `{batch_id}`\n"
                        f"📌 {_tp_cancel_note}；六态判定 = {_tp_verdict}（{_tp_detail}）\n"
                        f"⚠️ TP 可能仍在场或已触发成交——此时挂 LIMIT 会减到同方向"
                        f" aggregate 敞口（错平其他批次）\n"
                        f"🚫 未发出任何平仓单，批次冻结待人工处置\n"
                        + ('' if _rs_ok else
                           f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n")
                        + f"💡 请人工核对该 TP 与当前持仓后再决定处置",
                        level='critical')
                    return False, (f"❌ 限价平仓中止（旧 TP 六态={_tp_verdict}，"
                                   f"已冻结防止错平其他批次）")
```

（`ENTRY` 撤单段维持 v6.1 改动 1d 的 gate 语义不变；TP gate 插在 ENTRY gate 之后、
create LIMIT 之前。）

### 9.0b create 紧前 coverage guard（v6.2-r3 新增 P0 修正）

**不对称实锤**：`_close_amount_guard`（v6.1 helper L677-728，多批次
`actual < 台账合计 → None 禁止自动平`）只在**市价** AFTER 调用（送审稿 L469）；
限价路径直挂 `amount=current_filled_amount`（生产 L7584-7593），无 coverage 检查。
BEGIN 的同方向 single-flight 只能保证「无其他自动 close 在途」，不能证明
「交易所 aggregate 敞口 ≥ 同方向台账合计」——手工减仓/SL 未同步/ADL/状态漂移
都会造成决定性反例。**r3 规格**：TP gate 通过后、create 紧前复用同一 guard，
且 `safe_amount` 必须与台账量**相等**才放行（LIMIT 无 partial-PnL 传播，
不等即 Fail-Closed 转人工）：

```python
            # 🔒 v6.2-r3（P0）：LIMIT 与 MARKET 同等 coverage 安全等级。
            # 复用 v6.1 既有 _close_amount_guard（多批次 actual < 台账合计
            # → None；单批次 min 合法域）。LIMIT 本轮不做 partial-PnL 传播，
            # 因此 safe_amount != 台账量也一律 Fail-Closed 转人工。
            safe_amount, _amt_detail = self._close_amount_guard(
                target_symbol,
                side,
                target_b_data.get('is_hedge_mode', False),
                current_filled_amount,
                batch_id,
            )
            if safe_amount is None or abs(safe_amount - current_filled_amount) > 1e-8:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'limit_amount_conflict')
                self.send_tg_notification(
                    f"🚨【资金安全】限价平仓中止：平仓数量与 aggregate 敞口冲突！\n"
                    f"🆔 批次: `{batch_id}`\n"
                    f"📌 {_amt_detail}\n"
                    f"📌 台账量 {current_filled_amount}，guard 判定 {safe_amount}\n"
                    f"⚠️ 此时挂 LIMIT 可能减到属于其他批次的剩余敞口（错平）\n"
                    f"🚫 未发出任何平仓单，批次冻结待人工 reconcile\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n"),
                    level='critical')
                return False, (f"❌ 限价平仓中止（coverage 冲突，"
                               f"已冻结防止错平其他批次）")
```

（**r4 修正**：guard 调用点**不得持有 `_state_lock`**——它是非重入 `threading.Lock()`
（生产 L202），锁内零交易所 API 是项目铁律，而 guard 第一步就是
`_read_position_amt → fetch_positions`。与 MARKET 现有调用方式一致：锁外调用、
create 紧前执行即可。）

### 9.1 BEGIN reason 改 `limit_creating` + 进程内 seed grace（落点：送审稿 L813-814 + helper）

```python
        _begin_ok, close_op_id, _begin_why, _claimed = self._begin_close_request_if_active(
            target_symbol, batch_id, 'limit_creating')
```

helper 侧（`_begin_close_request_if_active` 内，persist 之前）：

```python
        _prev_freeze_alerted = self._freeze_alerted.get(batch_id)
        if reason == 'limit_creating':
            self._freeze_alerted[batch_id] = time.time()
        _ok = self._persist_states(all_states)
        if not _ok:
            if reason == 'limit_creating':
                if _prev_freeze_alerted is None:
                    self._freeze_alerted.pop(batch_id, None)
                else:
                    self._freeze_alerted[batch_id] = _prev_freeze_alerted
            return False, _op_id, 'persist_failed', None
```

（磁盘一旦可见 `limit_creating`，同活进程必有 grace；crash 后内存 dict 消失 → 重启立即 critical。）

### 9.2 durable commit 取代原 `save_batch_state`（落点：生产 L7596-7602）

```python
            order_id = order['id']

            # 🔒 v6.2（INV-3）：专用窄 helper 做 durable commit + 原子切正常态。
            _cm_ok, _cm_why = self._commit_limit_close_order_if_current(
                target_symbol, batch_id, close_op_id, order_id, limit_price, price_mode)
            if not _cm_ok:
                # B-lite：尝试撤掉这张无人管辖的活单，用既有六态确认器按事实定案；
                # 无论结果如何都不自动 rollback（close_order_placed 纪律）。
                try:
                    self._safe_api_call(self.exchange.cancel_order, order_id,
                                        target_symbol, params={'stop': True})
                    _cx_note = "已发出撤单请求"
                except Exception as _cx_e:
                    _cx_note = f"撤单请求异常（{_cx_e}）"
                _verdict_c, _detail_c, _filled_c = self._confirm_close_filled(
                    target_symbol, side,
                    target_b_data.get('is_hedge_mode', False),
                    order_id, current_filled_amount, 0.0)
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'limit_persist_failed')
                self.send_tg_notification(
                    f"🚨【资金安全】限价平仓单 {order_id} 账本写盘失败，已按 B-lite 处置！\n"
                    f"🆔 批次: `{batch_id}`\n💰 价格: {limit_price}\n"
                    f"🔄 {_cx_note}；六态确认 = {_verdict_c}（{_detail_c}）\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n")
                    + f"🚫 批次保持冻结（close_phase=1），绝不自动回滚，"
                      f"请立即人工核对该限价单与持仓！",
                    level='critical')
                return False, (f"❌ 限价平仓单账本写盘失败"
                               f"（六态={_verdict_c}），批次冻结待人工处置")
```

### 9.3 限价 outer except（落点：生产 L7656-7665，与改动 6.4/6.5 同构）

```python
            if close_order_placed:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'settlement_error')
                self.send_tg_notification(
                    f"🚨【资金安全】限价平仓单已挂出但后续流程异常（未回滚关闭标记）！\n"
                    f"🆔 批次: {batch_id}\n💡 原因: {str(e)[:150]}\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），冻结告警可能静默\n")
                    + f"⚠️ 限价单仍在交易所，请立即人工核对！",
                    level='critical')
                return False, f"❌ 限价平仓后续流程异常（平仓单已创建，close_phase 保持）: {e}"
            else:
                _rb_ok, _rb_why = self._rollback_close_request_if_current(
                    target_symbol, batch_id, close_op_id)
                if not _rb_ok:
                    _rs_ok, _rs_why = self._set_close_reason_if_current(
                        target_symbol, batch_id, close_op_id, 'txn_aborted_rollback_failed')
                    self.send_tg_notification(
                        f"🚨【资金安全】限价平仓失败且回滚被拒绝！\n批次: `{batch_id}`\n"
                        f"原因: {_rb_why}\n"
                        + ('' if _rs_ok else
                           f"⚠️ close_reason 切换失败（{_rs_why}），请立即人工处置！\n")
                        + f"请立即检查仓位是否仍有 SL 保护！",
                        level='critical')
                return False, f"❌ 限价平仓失败: {e}"
```

---

## 改动 10：helper 层（`送审附件_v6.1/new_helpers_v6.py` → v6.2 helper 文件）

### 10.1 新增 `_pending_entry_ids_for_gate`（第 12 个 helper）

```python
    def _pending_entry_ids_for_gate(self, symbol: str, batch_id: str,
                                    b_data: dict, last_filled_count: int):
        """gate 的 pending ENTRY ID 视图：从 claimed 台账内 registry 恢复 ID 链。

        🔒 v6.2：registry 的 layer 是【原始 signal.entries 层号】（创建循环
        enumerate(signal.entries) + 跳层 continue），而 entry_orders 只 append
        成功层 = 压缩列表，两者坐标系不同。先例 _rebuild_entry_orders_from_registry
        按 entry['layer'] 升序重建——本 helper 复刻同一坐标系：
          ① role=ENTRY 且有 order_id（不限 state：真实创建过的单）
          ② layer 非 int / bool / 重复 → Fail-Closed（绝不静默解释成 layer 0）
          ③ 按 layer 升序 → chain
          ④ 一致性证明：len(chain) == len(target_amounts)
             且 entry_orders == chain[:len(entry_orders)]
          ⑤ 待验证 = chain[last_filled_count:]
        只读 claimed 快照（D-5），零交易所 API、零新增生产依赖。
        返回 (ids: list, recoverable: bool, chain: list)。
        """
        reg = b_data.get('protection_registry', {})
        target_amounts = b_data.get('target_amounts', []) or []
        entry_orders = b_data.get('entry_orders', []) or []
        if not isinstance(reg, dict):
            return [], False, []
        confirmed = []
        seen_layers = set()
        for _identity, entry in reg.items():
            if not isinstance(entry, dict) or entry.get('role') != 'ENTRY':
                continue
            oid = entry.get('order_id')
            if not oid:
                continue
            layer = entry.get('layer')
            if not isinstance(layer, int) or isinstance(layer, bool):
                return [], False, []
            if layer in seen_layers:
                return [], False, []
            seen_layers.add(layer)
            confirmed.append((layer, str(oid)))
        confirmed.sort(key=lambda x: x[0])
        chain = [oid for _, oid in confirmed]
        if len(chain) != len(target_amounts):
            return [], False, chain
        if entry_orders != chain[:len(entry_orders)]:
            return [], False, chain
        return chain[last_filled_count:], True, chain
```

### 10.2 新增 `_commit_limit_close_order_if_current`（第 13 个 helper）

```python
    def _commit_limit_close_order_if_current(self, symbol: str, batch_id: str,
                                             close_op_id: str, order_id: str,
                                             limit_price: float, price_mode: str):
        """限价平仓单 ID 的 durable commit（专用窄入口，禁止泛化）。

        🔒 v6.2（INV-3）：durable commit = normal-state transition。
        迁移：limit_creating →（本 helper）→ limit_pending_normal。
        校验：op_id CAS + close_phase==1 + pending_close + 未被限价成交结算
        + **迁移源状态 == limit_creating**（否则 abnormal→normal 违反
        first-abnormal-wins）。只允许写 4 个字段（3 个订单字段 + reason）。
        锁内 load → 校验 → 修改 → _persist_states 必返回 True。
        返回 (ok, why)。why ∈ 'state_unreadable（{异常}）' / 'batch_missing'
        / 'op_id_mismatch' / 'not_in_close' / 'already_settled'
        / 'reason_changed' / 'persist_failed' / 'committed'。
        """
        with self._state_lock:
            try:
                all_states = self.load_all_states()
            except Exception as e:
                return False, f'state_unreadable（{e}）'
            b = (all_states.get(symbol, {}) or {}).get(batch_id)
            if not isinstance(b, dict):
                return False, 'batch_missing'
            if (b.get('close_op_id') or '') != (close_op_id or ''):
                return False, 'op_id_mismatch（已有其他操作接管，不覆盖）'
            if int(b.get('close_phase', 0) or 0) != 1 or not b.get('pending_close'):
                return False, 'not_in_close'
            if b.get('settled_by_limit_close'):
                return False, 'already_settled'
            if b.get('close_reason') != 'limit_creating':
                return False, 'reason_changed（迁移源状态不符）'
            b['limit_close_order_id'] = order_id
            b['limit_close_price'] = limit_price
            b['limit_close_mode'] = price_mode
            b['close_reason'] = 'limit_pending_normal'
            if not self._persist_states(all_states):
                return False, 'persist_failed'
            return True, 'committed'
```

### 10.3 `_derive_close_txn_vars`：shape 门 + hole 硬门（追加于 `target_amounts_short` 校验之后）

**v6.2-r2 精确化**：文字契约是「prefix = finite positive numbers；tail = exact numeric zero」，
实现必须忠实——**NaN 自不等（`v != v` 检出）、±inf 显式排除、None/字符串/负数一律 Fail-Closed**
（旧写法 `float(v) <= 0` 对 NaN 为 False、tail 只查 `> 0`，会让 NaN/None/'corrupt'/-1 全部逃逸）：

```python
        # 🔒 v6.2：向量完整性门。filled_details 与 target_amounts 必须等长
        # （正常初始化同长全 0，成交才写价）。长度不等 = 台账不完整
        # （含截断批次重启后被重灌成 len(entry_orders) 的损坏态）
        # → tail 有无成交价无从证明，Fail-Closed。
        if len(filled_details) != len(target_amounts):
            return False, None, (f'filled_details_shape_invalid（成交明细长度 '
                                 f'{len(filled_details)} != 计划层数 '
                                 f'{len(target_amounts)}——台账不完整，'
                                 '无法证明尾段无成交，请人工 reconcile）')

        def _finite_pos(v):
            # finite positive number（非 bool、非 NaN、非 inf）
            return (isinstance(v, (int, float)) and not isinstance(v, bool)
                    and v == v and v != float('inf') and v != float('-inf')
                    and v > 0)

        def _finite_zero(v):
            # exact numeric zero（非 bool、非 NaN、非 inf）
            return (isinstance(v, (int, float)) and not isinstance(v, bool)
                    and v == v and v != float('inf') and v != float('-inf')
                    and v == 0)

        # 🔒 v6.2（INV-3a 执行硬门 / prefix integrity）：监控用纯计数保存
        # last_filled_count，重启还会把 filled_layers 前 N 位压平
        # （生产 L4568-4570）—— 内存 bitmap 不可靠，filled_details 是唯一
        # 持久化事实源：prefix 必须全为 finite 正数（成交价），
        # tail 必须全为 exact 0（未成交）。任何其他值 = 台账损坏。
        if not all(_finite_pos(v) for v in filled_details[:last_filled_count]):
            return False, None, ('ledger_invalid（前 '
                                 f'{last_filled_count} 层存在非有限/非正数成交价——'
                                 '台账损坏，请人工核对）')
        if not all(_finite_zero(v) for v in filled_details[last_filled_count:]):
            return False, None, ('entry_fill_hole（成交位不连续：'
                                 f'第 {last_filled_count} 层之后存在成交价或非法值'
                                 '——prefix 假设失效，自动平仓口径不可信，'
                                 '请人工核对持仓与台账）')
```

（`filled_details_short`（len < lfc）校验被 shape 门覆盖，正式实现时由新键取代；
v6.1 返回的 11 键契约不变。）

### 10.4 `_set_close_reason_if_current`：first-abnormal-wins（写字段前插入）

```python
            cur_reason = b.get('close_reason')
            if cur_reason not in REASON_TRANSITION_SOURCES:
                # 🔒 v6.2：第一个真正的 failure reason 赢。
                # REASON_TRANSITION_SOURCES 之外的 reason 已是异常根因，
                # 通用 except 的 settlement_error 不得覆盖第一现场
                # （market_confirm_unknown / rollback_rejected / ...）。
                # 返回 True：磁盘已 abnormal，本次切换目标已达成。
                return True, f'reason_already_abnormal（{cur_reason}）'
            b['close_reason'] = reason
```

### 10.5 reason 双集合（v6.2-r2 P0 修正：拆分 transition 与 freeze 静默两职责）

```python
# 🔒 v6.2-r2（P0 修正）：一套集合曾被错误地承担两个职责——
#   A. 哪些 reason 可以被第一个 failure reason 覆盖（transition source）
#   B. 哪些 reason 在 freeze monitor 中允许静默（quiet）
# limit_creating 对 A 应允许覆盖、对 B 绝不能永久静默——否则
# create↔commit 之间崩溃后，重启看到磁盘 limit_creating 会被当
# 正常态静默，v2.1 专门引入的 crash-loud 语义被自己打穿。
REASON_TRANSITION_SOURCES = {
    'market_confirming',
    'limit_pending_normal',
    'limit_creating',
}

FREEZE_QUIET_REASONS = {
    'market_confirming',
    'limit_pending_normal',
}
```

- `_set_close_reason_if_current`（10.4）用 `REASON_TRANSITION_SOURCES`；
- 冻结告警分型（送审稿改动 4）用 `FREEZE_QUIET_REASONS`：白名单内只 print，
  **白名单外（含 `limit_creating`、异常态、None、缺失、未知字符串）→ 周期 critical**；
- 告警窗口沿用 `_freeze_alerted` **3600 秒**（生产/送审稿 L2407），**不是** 3 轮
  （3 轮是 `_converge_alert` 的独立机制）；
- 组合语义：活进程 `limit_creating` + seed grace → 不误报；crash 重启 → 内存 seed 消失
  且 `limit_creating` 不在 quiet 集 → **立即 loud**（R3-crash-state 语义自洽）。

### 10.6 端点路由参数化（v6.2-r4 P0：`order_kind` 默认 'normal' 零变化）

**实锤**：v6.1 `_fetch_close_order_state`（helper L443-472，docstring L452「平仓单走
'normal' 端点」、L457/L464 fetch 无 params）对 TP 这类 conditional 条件单会命中普通端点 →
恒 `not_found` 假阴性（C5 事故根因，生产 `_verify_order_created` L3377-3381 记载）。
对两个既有 helper 参数化（不新增 helper，计数仍 13；parity 两侧同步）：

```python
    def _fetch_close_order_state(self, order_id, symbol, retry_not_found: int = 3,
                                 not_found_delay: float = 2.0, order_kind: str = 'normal'):
        # 🔒 v6.2-r4：order_kind 参数化（对齐生产 _verify_order_created L3377 的
        # 既有范式）。'normal' = 普通限价/市价单（默认，MARKET/LIMIT 现状零变化，
        # 且**保持原调用形态——不显式传 params**，CCXT 公开接口 params 默认 {}）；
        # 'conditional' = STOP/TAKE_PROFIT 条件单，fetch_order 必须带
        # params={'stop': True} 走 algo 端点，否则恒 not_found 假阴性。
        try:
            if order_kind == 'conditional':
                order = self._safe_api_call(self.exchange.fetch_order, order_id, symbol,
                                            params={'stop': True}, retries=1)
            else:
                order = self._safe_api_call(self.exchange.fetch_order, order_id, symbol, retries=1)
            return 'success', order
        except ccxt.OrderNotFound:
            for _ in range(retry_not_found):
                time.sleep(not_found_delay)
                try:
                    if order_kind == 'conditional':
                        order = self._safe_api_call(
                            self.exchange.fetch_order, order_id, symbol,
                            params={'stop': True}, retries=1)
                    else:
                        order = self._safe_api_call(
                            self.exchange.fetch_order, order_id, symbol, retries=1)
                    return 'success', order
                except ccxt.OrderNotFound:
                    continue
                except Exception:
                    return 'unknown', None
            return 'not_found', None
        except Exception:
            return 'unknown', None
```

`_confirm_close_filled(..., order_kind='normal')` 同参数化并透传至
`_fetch_close_order_state`；市价/限价主平仓单调用点**零变化**（默认 normal），
仅 9.0 TP factual gate 传 `order_kind='conditional'`。

### 10.7 coverage 数值 finite 校验（v6.2-r4 P0：NaN/Inf Fail-Closed）

**实锤**：`_read_position_amt`（helper L437-440）`total += abs(float(...))` 只捕
`TypeError/ValueError` —— `float('nan')` 是合法 float 不抛异常 → NaN 直通；
`_close_amount_guard` 三个 NaN 比较全 False（单批次返回 NaN、多批次返回台账量）；
r3 最终门 `abs(NaN - x) > 1e-8` 亦为 False → **UNKNOWN→PASS 绕过 coverage 门**。

两处修补（`_read_position_amt` 修补同时惠及 MARKET 等全部调用方）：

（第一处为函数内部插入片段，独立不可解析 → 标 `python-frag`；第二处为完整谓词块。）

```python-frag
        # 🔒 v6.2-r4（_read_position_amt 累加处）：NaN/Inf = 不可判定，
        # 绝不能当数值参与 coverage 比较（NaN 与任何数比较全 False → fail-open）。
            _v = abs(float(pos.get('contracts', 0) or pos.get('positionAmt', 0) or 0))
            if _v != _v or _v == float('inf') or _v == float('-inf'):
                print(f"  ⚠️ 读取持仓失败：contracts 值非有限数（{_v}），不可判定")
                return None
            total += _v
```

```python
        # 🔒 v6.2-r4（_close_amount_guard 开头）：三个量全部必须为有限非负数，
        # 否则 Fail-Closed（NaN 会让下方所有比较变 False → UNKNOWN→PASS）。
        def _finite_nonneg(v):
            return (isinstance(v, (int, float)) and not isinstance(v, bool)
                    and v == v and v != float('inf') and v != float('-inf')
                    and v >= 0)

        if not _finite_nonneg(ledger_amount) or ledger_amount <= 0:
            return None, (f"台账量非法（{ledger_amount}），无法确定平仓数量"
                          f"（Fail-Closed，不发单）")
```

（`actual = self._read_position_amt(...)` 返回 None 已 Fail-Closed；经 10.7 修补后
`actual` 必为有限非负或 None。`sum_all` 来自 `_survey_same_side_batches`，
同样在 guard 内加 `_finite_nonneg(sum_all)` 校验，失败 → None。`_close_amount_guard`
与 `_read_position_amt`、`_survey_same_side_batches` 均为 v6.1 既有 helper，
本轮为语义修补、不新增计数。）

### 10.8 cross-batch topology gate（v6.2-r5 最后 P0：survey 的 hole 盲区）

**实锤**（v6.1 helper L661-667）：`_survey_same_side_batches` 对每个同方向批次只做
`filled = float(sum(target_amounts[:int(last_filled_count)]))`——无 shape/topology 校验；
且 `if filled <= 0: continue` 会把「声明 lfc>0 却算出 ≤0」的账本矛盾**静默跳过**。
coverage 证明依赖**所有**同方向台账可信：hole 批次（如 `target_amounts=[0.1,10.0]`、
`lfc=1`、真实 `filled_details=[0,70000]` → 算出 0.1）会用错误 prefix 量证明一个
不存在的 coverage → LIMIT 平 A 可能侵占 B 的剩余敞口。**修补**：对**所有**纳入
coverage 的批次（含 target）执行与 `_derive_close_txn_vars` 同一套 invariant，
不成立 → `(-1,-1,-1)`（调用方既有 Fail-Closed 语义）。**不新增 helper**（拓扑判据
在 survey 内局部定义，刻意不提取成模块级符号）：

```python
        # 🔒 v6.2-r5（最后 P0）：coverage 证明依赖所有同方向台账可信。
        # 对每个纳入 coverage 的批次（含 target）先验证 fill topology
        # （与 _derive_close_txn_vars 硬门同一套判据），再求和。
        # 判据以局部闭包实现，刻意不提取成模块级符号（不新增 helper）。
        def _topology_ok(amounts, details, n):
            if not isinstance(amounts, list) or not isinstance(details, list):
                return False
            if n < 0 or n > len(amounts) or len(details) != len(amounts):
                return False

            def _finite_pos(v):
                return (isinstance(v, (int, float)) and not isinstance(v, bool)
                        and v == v and v != float('inf') and v != float('-inf')
                        and v > 0)

            def _finite_zero(v):
                return (isinstance(v, (int, float)) and not isinstance(v, bool)
                        and v == v and v != float('inf') and v != float('-inf')
                        and v == 0)

            return (all(_finite_pos(v) for v in details[:n])
                    and all(_finite_zero(v) for v in details[n:]))

        for bid, b in batches.items():
            if not isinstance(b, dict):
                continue
            if b.get('side', 'BUY') != side:
                continue
            try:
                _n = int(b.get('last_filled_count', 0) or 0)
            except (TypeError, ValueError):
                return -1, -1, -1
            _ta = b.get('target_amounts') or []
            _fd = b.get('filled_details') or []
            if not _topology_ok(_ta, _fd, _n):
                # shape 不齐 / prefix 含无效价 / tail 存在成交 = 账本矛盾
                # → coverage 不可证明 → Fail-Closed（绝不能用错误 prefix 量
                # 证明不存在的 coverage）
                print(f"  ⚠️ 勘察中止：批次 {bid} 成交位拓扑损坏"
                      f"（lfc={_n}，amounts={len(_ta)}，details={len(_fd)}），"
                      f"coverage 不可证明（Fail-Closed，人工 reconcile）")
                return -1, -1, -1
            filled = float(sum(_ta[:_n]))
            if _n > 0 and filled <= 0:
                # 🔒 v6.2-r5：声明 lfc>0 却算出 ≤0 = 账本矛盾。
                # 旧代码 `filled <= 0: continue` 会静默跳过 → 假 coverage。
                print(f"  ⚠️ 勘察中止：批次 {bid} 声明 {_n} 层成交但计划量合计 "
                      f"{filled} <= 0，账本矛盾（Fail-Closed，人工 reconcile）")
                return -1, -1, -1
            if filled <= 0:
                # lfc==0 的合法零敞口批次：无 coverage 占用，跳过
                continue
            # v5：target 与任何 close_phase 的批次都计入 coverage
            sum_all += filled
            if bid == target_batch_id:
                continue
            if int(b.get('close_phase', 0) or 0) >= 1 or b.get('pending_close'):
                blocking += 1
            others += 1
        return others, sum_all, blocking
```

（**F-1 兼容性**：🗑️ 旧 producer 截断只动 `entry_orders`、不动
`target_amounts/filled_details` → 截断批次的 topology 判据不受影响；
r5 拦截的是 `filled_details` 拓扑真实损坏的批次——那正是「归因不可证明」的
Fail-Closed 场景。`_derive_close_txn_vars` 硬门（10.3）与 survey 门（本节）
对 target 批次形成双保险。）

---

## §九 checker 与生产签名同步清单

| 项 | 内容 |
|---|---|
| helper 名单 | 13 个（11 保留 + `_pending_entry_ids_for_gate` + `_commit_limit_close_order_if_current`）；parity 的「前导下划线未申报函数 Fail-Closed」自动覆盖 |
| `PROD_SIGNATURE_OVERRIDES` | 新增登记 `cancel_open_orders`（签名不变，语义变更）；`_begin_close_request_if_active` 签名不变（新增 seed 逻辑）；`_set_close_reason_if_current` / `_derive_close_txn_vars` / `_cancel_and_verify_entry_orders` 属 helper，已在名单内 |
| 文档自检 | 本稿全部 `python` 块须过 `check_doc_code_blocks`（零 Ellipsis）+ `check_doc_landing_symbols`（零自由变量）+ `check_doc_helper_calls`（闭包 7 查）+ `check_doc_helper_parity`（13 helper 逐函数 ast.dump 相等） |
| 生产零改动 | 本文档交付时 HEAD=e953d79，未改任何生产文件 |

---

## §十 RED→GREEN 测试清单（先 RED 后 GREEN，双向验证）

| 编号 | 场景 | GREEN 断言 |
|---|---|---|
| R1-a | 不等金额 5 层，E4 cancel 成功 / E5 失败 | `entry_orders` 原列表不压缩——E5 仍对应 0.005（positional invariant） |
| R1-b | `lfc=0`，E1 成功 / E2 失败 | E2 ID 不被清；台账原样；返回 False |
| R1-c | monitor `manual_canceled_detected` 撤剩余 ENTRY，某张失败 | 失败 ID 仍可追踪（台账原样） |
| R1-d | 截断账本 + registry 有 E4/E5，E5 `fetch_order → filled` | gate=False（杀掉「只扫 open orders」） |
| R1-e | 截断形状 + registry 缺条目 | Fail-Closed gate=False + critical |
| R1-f | signal layer0 被 skip，registry layers=[1,2,3] | 按 layer 排序恢复正确 ID，不查 ENTRY layer0 |
| R1-g | `lfc=0`，E1 gone / E2 unresolved | 不写 `pending_close/close_phase`；批次 ACTIVE |
| R1-h | hole：E4 canceled + E5 成交 | monitor 检测 critical 暴露；producer 不再制造新 hole |
| R1-i | `cancel(E5) → -2011` + `fetch(E5) → filled` | **`cancel(E4)` 调用次数 == 0** |
| R1-j | cancel 网络失败但 verifier=gone | 不永久 unresolved；全 gone 可进终止形态 |
| R1-k | `filled_details=[p1,p2,p3,0,p5]`，N=4 | derive=False `entry_fill_hole`；正常批次不误报 |
| R1-l | 真跑 monitor 判据：`lfc=0` + `is_programmatic_cancel=True` + `pending_close=False` | **不得 break** |
| R1-m | cancel(E5) 正常返回但 verifier= filled/unknown | **`cancel(E4)` 调用次数 == 0** |
| R1-n | `target_amounts` len=5 / `filled_details` len=3 / N=3 | derive=False `filled_details_shape_invalid`；等长正常批次不误报 |
| R1-o | 有持仓+SL，曾 ENTRY-only cancel，随后 SL 外部 cancel | **`need_recover_sl=True`**（sticky bool 版本必 RED） |
| R1-p | E5 程序 cancel+verifier gone；E4 用户手工 cancel | E5→programmatic（不提醒）；E4→manual（提醒） |
| R1-q | 市价：TP cancel 成功、SL cancel 网络失败 | TP registry==PROGRAMMATIC_CANCELED；**SL registry != PROGRAMMATIC_CANCELED** |
| R1-r | ENTRY cancel 成功但 verifier= filled/open/unknown | 该 ID registry **不得**出现 PROGRAMMATIC_CANCELED |
| R1-s | **monitor producer #2**：cancel(E5) 正常返回 + verifier(E5)=filled | **`canceled_layers[E5] == False`** + `cancel(E4)` 调用次数 == 0；下一轮普通 monitor 仍能把 E5 识别为成交入账 |
| R1-t | `filled_details` tail 含 None / 'bad' / -1 / NaN（N=3） | derive=False（hole 门 Fail-Closed，非法值不逃逸） |
| R1-u | `filled_details` prefix 含 NaN / inf | derive=False（ledger_invalid） |
| R2-f | `market_confirm_unknown` 已落盘 → outer generic except | reason 仍 `market_confirm_unknown`（first-abnormal-wins） |
| R2-g | BEGIN→pre-create 异常→rollback persist 失败（outer except 真调用序列） | 磁盘 reason==`txn_aborted_rollback_failed`；CAS 再失败文案含「切换失败」 |
| R2-出口 | 出口③④⑤⑥⑦ 各一例 | 落盘 reason ∈ 异常集；CAS 失败时 critical 文案如实 |
| R3-b | op_id/phase/pending_close/settled 全匹配但 reason 已 abnormal | commit 返回 `(False, 'reason_changed…')`，绝不回写 normal |
| R3-c | 当前进程 `limit_creating` + seeded `_freeze_alerted` → 不 critical；模拟重启清空 dict → 立即 critical | transient grace 只存在于存活进程 |
| R3-crash-state | 磁盘 phase=1 + `limit_creating` 且无 `limit_close_order_id` | 冻结分类为 abnormal（周期 critical），非 normal 静默 |
| R3-d | 限价：TP cancel 网络异常 / TP 仍 open | **create LIMIT 调用次数 == 0** + `limit_tp_unresolved` 冻结 |
| R3-e | 限价：TP cancel → `-2011` | **r4 拆成事实矩阵（action outcome 不重要，fact outcome 才重要）**：<br>① `-2011` + conditional fetch → NOT_CONFIRMED → **BLOCK**<br>② `-2011` + conditional fetch → canceled 且 filled==0 → **PROCEED**<br>③ cancel success + fetch → filled/open/unknown → **BLOCK**<br>④ cancel success + fetch → canceled 且 filled==0 → **PROCEED**<br>（FakeExchange 必须建模端点路由：不带 stop=True → not_found；带 → canceled filled=0——②顺便杀掉端点 bug） |
| R3-h1 | `fetch_positions → contracts=NaN / inf` | `_read_position_amt` → None → `_close_amount_guard` → None → **create LIMIT == 0**（UNKNOWN 不得 PASS） |
| R3-h2 | A 台账=1.0（target close）；B `target_amounts=[0.1,10.0]`、`lfc=1`、`filled_details=[0,70000]`（hole）；aggregate actual=1.5 | survey 检测 B topology 损坏 → `(-1,-1,-1)` → guard None → **create LIMIT == 0**；旧 survey 会算 sum_all=1.1 假放行（正向：B `filled_details=[70000,0]`/N=1 topology 合法 → 正常进入 coverage 计算） |
| R3-f | 限价：TP cancel **正常返回**，但 `_confirm_close_filled(TP)` = filled/open/unknown | **create LIMIT 调用次数 == 0**（cancel-success ≠ 事实；仅 TERMINAL_ZERO 放行） |
| R3-g | A 台账=0.001、B 台账=0.001、aggregate LONG 实际=0.001；ENTRY/TP gate 均过 | `_close_amount_guard` → conflict → **create LIMIT 次数 == 0** + `close_reason == limit_amount_conflict`；单批次 actual==台账 正常放行（双向） |

**回归**：既有 133 场景全绿；43 个 `test_*.py` → 41 rc=0（2 个 v3 遗留 rc=1，落生产时归档）。
**基建缺口**：`FakeExchange.cancel_order` 需加 `cancel_seq`（repeat-last 失败注入）；registry 需内存态 Fake（现有测试未覆盖 registry 读取路径）。

## §十一 变异清单（撤销任一防护 → 对应用例必须失败）

| 变异 | 目标 | 必须杀死的用例 |
|---|---|---|
| M11 | 恢复「撤单成功即截断/不 verifier」 | R1-a/b/i/m/r |
| M12 | `-2011 → continue` 不经 verifier | R1-i |
| M13 | 撤单方向改回低→高 / 遇阻不停 | R1-h/i |
| M14 | zero-filled 恢复「先写终止标志」 | R1-g/l |
| M15 | derive 删除 shape 门 / hole 门 | R1-k/n |
| M16 | 归因改回 sticky bool（写 `is_programmatic_cancel=True`） | R1-o/p |
| M17 | E1-3 改回无条件写 PROGRAMMATIC_CANCELED | R1-q |
| M18 | first-abnormal-wins 删除（无条件覆盖 reason） | R2-f |
| M19 | 各出口 CAS 删除（只 critical 不切 reason） | R2-g / R2-出口 |
| M20 | commit 删除 source-state guard | R3-b |
| M21 | BEGIN 不 seed `_freeze_alerted` | R3-c |
| M22 | registry 恢复改回逐 idx 构造 identity / 删除 hardening | R1-d/f |
| M23 | 删除 TP factual gate（恢复「无论结果都 create LIMIT」/-2011→safe） | R3-d/e |
| M24 | producer #2 恢复「cancel 成功先置 canceled_layers」 | R1-s |
| M25 | FREEZE_QUIET_REASONS 混入 `limit_creating`（单集合回退） | R3-crash-state |
| M26 | hole gate 回退为 `float(v) > 0` 判 tail（NaN/None/-1 逃逸） | R1-t/u |
| M27 | TP gate 改回「cancel 正常返回即 proceed」（删六态确认） | R3-f |
| M28 | 删除 LIMIT create 前 coverage guard | R3-g |
| M29 | 删除 finite exposure guard（NaN/Inf 逃逸） | R3-h1 |
| M30 | 删除 conditional 端点路由（order_kind 参数化回退，TP 走 normal 端点） | R3-e② |
| M31 | 删除 survey 的 cross-batch topology gate / 恢复 `filled<=0: continue` | R3-h2 |

（M0 基线变异仍须 rc=0；变异体一律落 `G:/tmp`，不进版本控制。）

