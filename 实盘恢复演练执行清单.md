# 实盘恢复演练执行清单

日期：2026-08-20 ｜ 版本：v1.1（外部复审修订版）｜ 依据：ChatGPT 终审 5 步恢复前置 + 《安全不变量_系统宪法》§七故障注入验收清单（15 场景）

> **使用方法**：按 Step 2 → 3 → 4 → 5 顺序执行，每步一张"操作卡"。每张卡含：操作步骤、观察点（从源码提取的**精确日志关键字**）、通过标准、异常判据。执行完在 §九记录表打勾并记录证据（时间/截图/日志行）。
> **总原则**：演练中任何一步出现"立即停止条件"→ 停止进程 → 保留现场（日志+trade_state.json 快照）→ 报告分析，**禁止热修**。
> **代码版本**：f337199（96b94ed + 终审补强 v3 A/B/C/D + E1/E2）

## v1.1 修订记录（2026-08-20 外部复审 7 点）

| 编号 | 修订 | 处理 |
|---|---|---|
| P0-1 | Step 3 删除"重复信号无告警风暴"误导表述 | ✅ 已改（R1 直发 critical 无去重，属预期） |
| P0-2 | Step 4 明确 /tp 成功 ≠ 标记清除 | ✅ 已改，且源码核实**比复审结论更进一步**：见 4.8 深度说明 |
| P0-3 | Step 5 放宽 TP/SL 距离避免瞬时触发 | ✅ 已改（TP +0.8% / SL -0.5%） |
| P1-1 | 互斥测试改双 watchdog | ✅ 已改，并修正复审细节：互斥体在 bot_runner（watchdog 自身无互斥体；TRADER_LOCK 是进程内 asyncio 锁，非跨进程判据） |
| P1-2 | trade_state.json 注入改停机修改 | ✅ 已改（stop → backup → edit → start） |
| P1-3 | 增加 R3 熔断专项 | ✅ 以"离线已覆盖 + 实盘不可注入原因"说明卡呈现（Step 4.5），不设实盘注入操作 |
| P1-4 | 保护单窗口 ≤3 秒 | ✅ 已改（成交瞬间实时观察，3s 记录 / 10s 停止） |

---

## 一、演练总览与交叉映射

| 恢复步骤 | 状态 | 对应宪法 §七场景 | 验证层 |
|---|---|---|---|
| Step 1 清事故遗留仓位（对账） | ✅ 完成 22:37 | —（reconcile_pre_launch.py） | 交易所↔本地一致性 |
| Step 2 启动恢复测试 | ⬜ | #1 kill→重启 READY 链 / #4 recover=False 拦截 / #15 过期批次检测 | SG1/R3 |
| Step 3 错误 TP 演练（R1） | ⬜ | 无对应（TP 参数校验专项，宪法清单未覆盖） | R1 开仓前阻断 |
| Step 4 改价自愈演练 | ⬜ | #7 用户手动修改不被覆盖 / #8 TP 被人工删除→补挂 | R2/自愈 |
| Step 4.5 R3 熔断专项（离线已覆盖） | ✅ 说明卡 | 宪法 #9/#10（网络断开重复单）同源机制 | R3 熔断 |
| Step 5 小仓灰度（最小 1 层） | ⬜ | #3 SL 恢复链（顺带）/ #6 有仓必有 SL / #13 timeout reconcile | 全链路闭环 |

未在本次覆盖的宪法场景（#2/#5/#9/#10/#11/#12/#14 属故障注入类，需断网/状态损坏等主动注入，**建议灰度稳定 48h 后另行专项演练**，不阻塞本次恢复）。

---

## 二、Step 1 记录（已完成）

- 2026-08-20 22:37 `reconcile_pre_launch.py` 对账通过：本地 0 批次、交易所 0 挂单 0 持仓、无孤儿单无裸仓
- batch_142951 / batch_142633 已彻底清除（用户 app 端平仓+撤单，trade_state.json 空）

---

## 三、Step 2 操作卡：启动恢复测试

**目标**：验证冷启动链路 watchdog → bot_runner → 互斥体 → recover → READY，空状态下无异常告警。
**前置**：当前零 python 进程（Get-CimInstance 确认无残留）；trade_state.json 为空。

### 操作步骤

1. PyCharm 运行 `watchdog.py`（它会拉起 bot_runner 子进程）
2. 等待 1-2 分钟启动完成
3. **互斥体验证（真实重复启动路径）**：再运行一次 `watchdog.py` 模拟"系统恢复重复拉起"——第二个 watchdog 会拉起第二个 bot_runner，后者应被**命名互斥体**拒绝（bot_runner L141-157，Fail-Closed）
4. 确认第一个实例不受影响后，关闭第二个 watchdog（**v1.1 修复后**：第二 watchdog 识别退出码 42 后会自行干净退出，无需手动关闭；若其存活超过一个初始化周期 ~10s 属异常）

> ⚠️ **D-004 修订（2026-08-20 23:28 首次执行结果）**：首次执行本卡时暴露三层守护进程缺陷（拒绝路径 GBK 编码崩溃 → 退出码 42 变 1 → watchdog 无限重启 → crash_alert 通知风暴），已按 **Watchdog 安全补丁 v1** 修复（R1 编码保护 / R2 启动熔断 / R3 告警去重），详见 `D-004_Watchdog重复启动风暴_事故档案.md`。**本卡须在补丁提交后重做**，新增观察点 2.7/2.8。

### 观察点（PyCharm console / watchdog.log / TG）

| # | 期待出现 | 关键字（源码精确文本） | 位置 |
|---|---|---|---|
| 2.1 | watchdog 正常拉起 | watchdog.log 新增一条启动记录（仅一条） | watchdog.log |
| 2.2 | **READY 达成** | `✅ [启动检测] 历史任务恢复校验完成！系统 READY` | console（bot_runner L1810） |
| 2.3 | 空状态正常 | 无任何"批次接管/恢复批次"日志（本地无批次） | console |
| 2.4 | 无资金安全告警 | **不出现** `🚨【资金安全】` 前缀消息 | TG |
| 2.5 | 互斥体生效 | 第二个 bot_runner 打印互斥体拒绝日志（GBK 控制台下 emoji 显示为 ? 属正常，**不崩即可**）并退出；第一实例照常运行 | console |
| 2.6 | 日报定时正常 | 次日 08:05 收到日报 | TG |
| 2.7 | **D-004 修复：拒绝路径不崩** | 第二实例退出码为 **42**（无 UnicodeEncodeError traceback）；第二 watchdog 打印 `🚫 主程序因单实例锁拒绝启动（已有其他实例在运行），watchdog 停止。` 后**自行退出** | console / watchdog.log |
| 2.8 | **D-004 修复：零告警风暴** | 全程**零** crash_alert（无 `💥 已发送崩溃报警` / TG 崩溃消息 / 崩溃邮件）；watchdog.log **无重启循环**（"启动主程序"仅第二 watchdog 名下 1 次） | watchdog.log / TG / 邮箱 |

> 互斥体归属说明：权威判据是 **bot_runner 的 Windows 命名互斥体**（watchdog 自身无互斥体；`TRADER_LOCK` 是 bot_runner 进程内 asyncio 锁，不承担跨进程防重）。故双 watchdog 测试实际验证的是"重复拉起 → 子进程被互斥体拒绝"这条真实链路。

### 通过标准
2.2 出现 + 2.4 未出现 + 2.5 第二实例被拒且第一实例无扰动 + **2.7 拒绝链干净走通（42 → 第二 watchdog 自行退出）+ 2.8 零告警风暴**。

### 异常判据（立即停止条件）
- `🚨 [启动检测] 历史任务恢复失败！recover_active_batches 返回 False`（bot_runner L1815）
- `🚨【资金安全】启动恢复失败/异常` TG critical
- **两个实例同时进入 READY**（互斥体失效，最危险状态）→ 立即全部停止
- watchdog.log 出现**两条及以上**启动记录且进程树成双（疑似双实例，按 CommandLine 父子链核查，勿按 python.exe 数量判断）
- 任何 `UnicodeEncodeError` traceback（R1 编码保护失效）
- 第二 watchdog 存活超过一个初始化周期（~10s）仍未退出 / watchdog.log 出现重启循环 / 收到任何 crash_alert（R2/R3 失效）
- READY 未达成时发出任何开仓信号 → 观察 `🚫 [SG1] 系统未就绪，拒绝新信号`（trader L2028）是否拦截（这本是 #4 场景验证点，正常启动不应出现）

---

## 四、Step 3 操作卡：错误 TP 演练（R1 最重要验证）

**目标**：开仓信号携带方向性错误 TP（BUY 时 TP < 现价）→ 程序在开仓前阻断，零 Binance create。
**前置**：Step 2 通过、系统 READY；从 app 查当前 BTC 现价（下文以 70000 为例，**实操时替换为真实现价**）。

### 操作步骤

1. 查现价 P（app 或任意行情源）
2. TG 发送（TP=P−1000，方向性错误；单层最小仓，即使误放行损失也可控）：

   ```
   /signal BTCUSDT BUY 20 P+50 0.001 P-500 P-1000 P-400
   ```
   （格式：Symbol Side Leverage EntryPrices Amounts StopLosses TP InitialSL；entries 只给 1 层）

3. 等 30 秒，观察 console + TG + app 挂单列表

### 观察点

| # | 期待出现 | 关键字 | 说明 |
|---|---|---|---|
| 3.1 | R1 校验启动 | `🔍 [止盈价合理性校验中...]` | trader L2087 |
| 3.2 | **方向错误拦截** | `❌ 止盈价方向错误！...做多时止盈价必须 > 当前市价（当前 xxx <= xxx），币安将确定性拒绝（-2021）` | trader L2002-2008 |
| 3.3 | TG critical 1 条（**单次信号**） | `🚨 挂单被阻断！` | trader L2091 |
| 3.4 | **交易所零痕迹** | app 挂单列表无任何新订单（开仓条件单也未挂） | R1 在 params 校验段（L2088），早于一切 create |
| 3.5 | 无批次状态 | trade_state.json 未新增该 batch_id | 阻断 return None |

### 通过标准
3.2 命中 + 3.3 单次信号恰 1 条 + 3.4 零挂单 + 3.5 无状态。

> **R1 告警语义边界（v1.1 修订，防审计误判）**：R1 阻断走 critical **直发**（trader L2091），不进入 `_gate_alert_notify` 去重体系。R1 的保护目标是**阻止错误订单进入系统**，不承担信号入口的告警去重——重复发送同一错误信号会产生等量 critical TG（每发一次拦一次），属预期行为，由用户侧约束，非缺陷。审计时不应将"重复错误信号产生多条 TG"记为 R1 缺陷。

### 异常判据
- app 出现任何新挂单（含开仓条件单）→ R1 防线失效 → **立即撤单 + 停止**
- console 出现 `-2021` 且有重试循环 → R1 未拦住（不该发生）

---

## 五、Step 4 操作卡：改价自愈演练（R2 标记 → /tp 修正 → 恢复）

**目标**：批次成交后 TP 变为"相对现价不合理"（BUY TP ≤ 现价）→ R2 拦截补挂 + 1 次 critical + 写 `tp_param_invalid` 标记 + 静默 → 用户 `/tp` 改合理价 → 行为自愈闭环。

**关键事实（源码核实）**：
1. `/tp` 命令路径（`update_take_profit` L1240+）**无 R1/R2 前置校验**——直接打交易所，不合理价会被交易所 -2021 拒绝但不写标记
2. `tp_param_invalid` 标记只在**监控循环补挂路径**（`_tp_update_blocked` L726+）写入
3. **标记清除时机（v1.1 深度核实）**：唯一清除点在 `_tp_update_blocked` 内部校验通过时（L768）。`/tp` 修复成功后：TP 单健康 + `user_modified=True` → 监控循环不再进入 TP 更新路径（L3893 要求 `not user_modified`，L3836 要求订单缺失）→ `_tp_update_blocked` **根本不会被调用** → **标记会在 trade_state.json 中长期留存**。这是无害的（标记只在该函数被调用时短路"告警+create"，参数已合理则放行），但**演练不应以"标记从文件中消失"作为通过判据**——该发现已列入送审改进项（建议 `update_take_profit` 成功时对称调用 `_clear_tp_param_invalid`）

**前置**：需一个已成交的小批次（与 Step 5 灰度开仓**合并执行**最省：先按 §七开仓成交，回来做本卡，再做灰度观察收尾）。

### 操作步骤（停机注入法，v1.1 修订：不依赖时间窗口）

1. 灰度批次成交、TP 正常挂出后：
   a. **停止 watchdog**（PyCharm 停止按钮，进程树一并结束）
   b. 人工在 app **撤销该批次 TP 单**
   c. 备份：`copy trade_state.json trade_state.json.bak.drill`
   d. 手动编辑 `trade_state.json`：该批次 `take_profit_price` 改为一个不合理值（BUY 批次改为**低于现价的值**）
   e. 重新启动 `watchdog.py`
2. 等待 1-2 个监控周期（60-120s），观察拦截与标记
3. TG 发送 `/tp <该批次ID> <合理新TP>`（如现价 +0.8%）
4. 再等 1-2 个监控周期，观察行为自愈

### 观察点

| # | 期待出现 | 关键字 | 说明 |
|---|---|---|---|
| 4.1 | 检测 TP 缺失 | `⚠️ ⚠️ [风控异常] 止盈单已在外部撤销，准备按策略自动补挂...`（L3859）或 `⚠️ [TP 补挂] 批次 xxx 止盈单缺失(未创建或创建失败)，准备补挂...`（L3895） | 监控循环 |
| 4.2 | **R2 拦截（不打 API）** | `BUY 止盈价 xxx 无效：需 > 现价 xxx（防-2021 立即触发）且 >= 持仓成本 xxx（允许保本退出）`（L675）；console 前缀 `❌ [R2 预检] 止盈更新被跳过`（L765） | 现价维度拦截 |
| 4.3 | critical 恰 1 次 | TG `🚨` TP 参数无效告警（含批次号+原因） | `_mark_tp_param_invalid`（60min 去重） |
| 4.4 | 标记落盘 | trade_state.json 该批次出现 `"tp_param_invalid": {"reason": ..., "ts": ...}` | L690 |
| 4.5 | 后续周期静默 | `🤫 [TP参数无效] 批次 xxx 60 分钟内已告警，不重复`（L708）+ `止盈价仍不合理，静默跳过挂单（已有标记，等待用户修正）`（L763） | 去重生效 |
| 4.6 | 用户改价成功 | TG `✅ 批次 xxx 止盈已修改为 xxx` + console `✅ 批次 xxx 止盈单已成功修改为 xxx USDT (ID: xxx)`（L1344） | /tp 链路 |
| 4.7 | **行为自愈闭环**（v1.1 修订判据） | /tp 后下一监控周期起：该批次**无任何 TP 相关告警/补挂尝试/静默跳过日志**（TP 单在 app 内在位） | 替代"标记消失"判据 |
| 4.8 | 标记留存说明（预期，非失败） | trade_state.json 中 `tp_param_invalid` 字段**可能留存**（TP 健康+user_modified=True 时 `_tp_update_blocked` 不被调用，L3893/L3836 判定）——无害，批次终态时由 clear_batch_state 一并清理 | 已列送审改进项 |
| 4.9 | SL 始终在位 | 全程 app 内该批次 SL 单未被触碰 | 不变量② |
| 4.10 | user_modified 保留 | 状态中 `user_modified: true`，程序不再自动覆盖该 TP（宪法 #7/R13-B） | L1336 |

### 通过标准
4.2 + 4.3 恰 1 次 + 4.4 + 4.5 静默 + 4.6 + 4.7 行为闭环 + 4.9 全程 SL 在位。（4.8 标记留存为预期行为，不作失败判据。）

### 异常判据
- 标记写入后 TG 持续重复告警（>2 次/小时）→ 去重失效 → 停止
- `/tp` 改合理价后仍持续出现该批次 TP 补挂尝试/告警 → 自愈失效 → 停止
- 演练结束必须收尾：平仓结束该批次（批次终态清理会顺带清掉留存标记），**不留人为注入状态过夜**

---

## 六、Step 4.5 说明卡：R3 熔断专项（离线已覆盖，实盘不注入）

**结论**：R3 熔断逻辑已由 `test_tp_validation.py` T1 专项 **6 场景离线覆盖**（熔断触发/熔断后零 create API/TG 恰 1 次/去重/成功恢复/计数清零），实盘演练**不重复注入**，原因（源码核实）：

1. **参数注入不可行**：补挂路径 TP 价格经 `price_to_precision` 归一（L3993/L4327 上游），无法构造"通过 R2 却被交易所确定性拒绝"的价格参数
2. **断网注入无效**：网络异常属 NetworkError → PENDING_VERIFY 仲裁路径（B2 状态机），**不递增 `tp_fail_count`**——该计数器只统计交易所明确拒绝（ExchangeError，L4425-4440"余额不足/无效参数等"）
3. **代码注入违反纪律**：为制造 FAILED 而临时改代码 = 演练热修，违反总原则

如未来需要实盘观察 R3，合法途径是等待真实交易所拒绝事件（届时 `🔥 [熔断保护]`（L736）+ `🚨 止盈补挂已熔断！`（L744）会自然出现）。

---

## 七、Step 5 操作卡：小仓灰度（最小 1 层完整闭环）

**目标**：最小数量 1 层实盘，验证 开仓成交 → SL/TP 预生成挂出 → 监控 → 止盈/止损出场 → 批次清理 完整闭环。
**前置**：Step 2/3/4 通过；杠杆建议 5-10x（非平时 20x）；数量 0.001 BTC 单层。
**参数设计（v1.1 修订）**：entry 贴近现价确保成交，但 TP/SL 距离放宽至真实交易量级——TP **+0.8%** / SL **-0.5%**，避免"成交即触发"绕过真实运行状态。以现价 70000 为例：Entry 70070 / TP 70600 / SL 69650（约 1.6R，贴近实盘形态；不追求"马上结束"）。

### 操作步骤

1. TG 发送（示例，按实时价格 P 换算）：

   ```
   /signal BTCUSDT BUY 10 P+70 0.001 P-350 P+560 P-250
   ```
   （Entry=P+0.1%，SL=P-0.5%，TP=P+0.8%，InitialSL=P-0.36%）
2. 等开仓条件单成交
3. **成交瞬间实时观察保护单窗口**（app 持仓页 + 挂单页并排，v1.1 修订：≤3 秒）
4. 等待 TP 触发出场（或价格回落 SL 出场，任一即可）
5. 观察批次清理

### 观察点

| # | 期待出现 | 关键字 | 说明 |
|---|---|---|---|
| 5.1 | R1 校验通过 | `✅ 止盈价方向性校验通过！`（L2020） | 合法信号放行 |
| 5.2 | 预生成挂单提示 | `💡 说明：止盈与止损挂单参数已预生成，成交后立即挂出（1秒内）。`（L2367） | 下单确认 |
| 5.3 | **成交 → SL 落位 ≤3 秒**（v1.1 收紧） | app：持仓出现瞬间计时，SL 单在 **3 秒内**出现在挂单列表 | 核心不变量"有仓必有 SL"；3-10 秒=记录观察，**>10 秒=停止并人工补挂** |
| 5.4 | TP 同步挂出 | `⚡ 预生成止盈单已挂出: xxx (ID: xxx)`（L5003）+ 对应止损单挂出日志 | 与 SL 同窗口 |
| 5.5 | 有仓必有 SL（全程） | 成交后任意时刻 app 内该仓位 SL 单在位（宪法 #6/SG2） | 全程巡检 |
| 5.6 | TP 触发出场 | `🎉 [止盈触发] 批次 [xxx] 专属止盈单已触发成交！净盈亏: +x.xx USDT`（L3935） | 或 SL 出场对应日志 |
| 5.7 | 批次终态清理 | `🧹 批次 [xxx] 状态归档/清理完毕。`（clear_batch_state） | trade_state.json 该批次移除（含 Step 4 留存的 tp_param_invalid 标记一并清理） |
| 5.8 | 出场后无残留 | app 挂单清空（无孤儿 SL/TP） | reconcile 再跑一次确认 |

### 通过标准
5.1-5.8 全部命中；出场后再跑一次 `reconcile_pre_launch.py` 应再次对账通过（0 挂单 0 持仓 0 批次）。

### 异常判据
- 成交后 SL **超过 10 秒**未挂出 → 停止并人工补挂保护（3-10 秒记录为观察项）
- 出场后出现孤儿挂单 → 记录订单 ID，人工撤销 + 停止
- 任何 `🚨【资金安全】` TG 消息 → 立即停止

---

## 八、演练后判定

| 结果 | 动作 |
|---|---|
| 4 步全过 | 恢复常规实盘（建议再观察 48h 后安排宪法 #2/#5/#9/#10/#11/#12/#14 故障注入专项） |
| 任一步失败 | 停止 → 保留现场（console 全文 + watchdog.log + trade_state.json 副本 + 订单截图）→ 整理送审，**不热修** |

## 九、执行记录表（边执行边填写）

| Step | 执行时间 | 结果 | 证据（日志行/截图/订单ID） | 备注 |
|---|---|---|---|---|
| 1 | 2026-08-20 22:37 | ✅ | reconcile 退出码 0，0仓0单 | |
| 2 | | ⬜ | | |
| 3 | | ⬜ | | |
| 4 | | ⬜ | | |
| 4.5 | 离线已覆盖 | ✅ | test_tp_validation.py T1 6 场景（24/24 全绿） | 实盘不注入（见 §六） |
| 5 | | ⬜ | | |

---

## 十、送审遗留问题（v1.1 新增，随下轮材料送 ChatGPT）

1. **`update_take_profit` 成功路径未调用 `_clear_tp_param_invalid`**（已调 `_gate_alert_clear` L1333）：导致用户改价修复后 `tp_param_invalid` 标记在 state 中长期留存（无害，见 §五 4.8 深度说明）。建议对称补一行清除，属注释级小改动，不阻塞演练。
2. **R1 阻断 critical 直发无去重**：重复错误信号产生等量 TG（§四说明）。如需入口级去重可复用 `_gate_alert_notify` 体系，属可选优化。

## 附：本清单引用的源码锚点（f337199，行号随代码演进需同步）

- READY：bot_runner L1810 ｜ SG1 拒绝：trader L2028 ｜ 命名互斥体：bot_runner L141-157（watchdog 无互斥体；TRADER_LOCK L94 为进程内锁）
- R1 校验：trader L1981-2020（调用于 L2088，critical 直发 L2091，不进 gate 去重）
- R2 校验：trader L656-681（`_check_tp_viability`）
- 标记写入/清除/静默：L690 / **L768（唯一清除点，仅 `_tp_update_blocked` 被调用且校验通过时）** / L708+L763
- TP 熔断：L725-752（`_tp_update_blocked` 熔断分支）
- TP 更新路径进入条件：L3836（订单缺失）/ L3893（要求 `not user_modified`）——TP 健康+user_modified 时 `_tp_update_blocked` 不被调用
- tp_fail_count 递增：L4425-4440（仅 ExchangeError 确定性拒绝；NetworkError 走 PENDING_VERIFY 不计数）
- /tp 命令：bot_runner L840-868 → trader `update_take_profit`（无 R1/R2 前置校验，直连交易所；成功写 user_modified L1336）
- gate 告警去重：L626-650（`_gate_alert_notify`，3 次上限）
- TP 价格精度归一：L3993 / L4327 上游 `price_to_precision`
- 批次终态清理：`clear_batch_state`（含 E2 breaker 键释放）
