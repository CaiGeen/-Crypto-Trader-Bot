# v6.2 组装收口 · staged trader 全量组装完成 + 双层测试证据（送审 ChatGPT）

> **第 2 轮（GREEN 终审后收口）已完成 —— 摘要见 §0。以下 §1 起为第 1 轮正文。**

## §0. 第 2 轮收口（回应 GREEN 终审：1 个 B-lite P0 + 3 个证据缺口 + 2 个门禁）

### 0.1 🔴 B-lite P0 已修：撤普通 LIMIT 改 normal endpoint
判定成立，且这是 **r5 规格文本自身的缺陷**（改动 9.2 B-lite 写死
`params={'stop': True}`），我此前忠实复现了规格。已修：
```python
# 修前（规格原文）：普通 LIMIT 却走 conditional 端点 → 真单撤不掉
self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol,
                    params={'stop': True})
# 修后：normal endpoint（与生产 _cancel_limit_close_order 一致）
self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol)
...
_confirm_close_filled(..., order_id, current_filled_amount, 0.0,
                      order_kind='normal')   # 显式写出便于审计
```
⚠️ **待裁定**：规格层（改动 9.2）同处需同步更正——我只改 staged 产物，
**未擅自改动已评审的规格正文**。

**静态签字依据**（`G:/tmp/verify_blite_ast.py`，AST 级，非字符串匹配）：

| 检查项 | 实测 |
|---|---|
| B-lite `cancel_order` | `_safe_api_call(cancel_order, order_id, target_symbol)` — **无 params** ✅ |
| B-lite `_confirm_close_filled` | **`order_kind='normal'`** ✅ |
| TP gate（对照） | **`order_kind='conditional'`** — 条件单未被误改成 normal ✅ |
| 生产 `_cancel_limit_close_order` | `cancel_order(limit_id, symbol)` **无 params** ✅ |

⚠️ 过程教训（如实记录）：我第一次用字符串匹配自查，因**注释正文里也含有
`params={'stop': True}` 字样**、且打印窗口截断在 `order_kind` 之前，得到
「仍有 stop=True」的**假阴性**。改用 AST 断言（`_safe_api_call` 的首参
`self.exchange.cancel_order` + 无 `params` 关键字）后才得到可信结论。
行为侧另有 `t_limit_commit_fail_blite` 的 `cancel_normal:OID1==1` 佐证。

### 0.2 Fake cancel endpoint routing + B-lite endpoint 断言
`FakeExchange62.cancel_order` 增加 `cancel_normal:{id}` / `cancel_conditional:{id}`
（保留旧 `cancel:{id}` 兼容既有断言）。`t_limit_commit_fail_blite` 现锁定
`cancel_normal:OID1==1` / `cancel_conditional:OID1==0` / `fetch_normal:OID1>=1` /
`fetch_conditional:OID1==0`，并以 `cancel_conditional:E2/TP1>=1` 作对照
（证明不是把端点一刀切成 normal）；OID1 终态改放 `order_seq`。

### 0.3 gate 失败 `create==0` / happy·B-lite `create==1`
`t_limit_tp_gate_blocked` / `t_limit_coverage_conflict` /
`t_limit_entry_gate_fail_rollback` 各加 `create_order == 0`；
`t_market_happy_path` / `t_limit_happy_path` / `t_limit_optimal_price_path` /
`t_limit_commit_fail_blite` 各加 `create_order == 1`。

### 0.4 first-abnormal-wins 改为真正 mid-flight 集成
原用例更名 `t_market_frozen_batch_begin_rejected`（诚实说明只证明 BEGIN 层不覆盖）；
新增 **`t_market_first_abnormal_wins_midflight`**：BEGIN 成功 → 六态 UNKNOWN 先 CAS
写 `market_confirm_unknown` → raise → outer except **确实尝试** `settlement_error`
（hook 记录每次 CAS）→ 被拒 `reason_already_abnormal` → 磁盘保留首因。

### 0.5 变异检查（证明新断言不是装饰品）— 4/4 全部杀死
`G:/tmp/mutate_v62_integration.py`：M0 基线 15/15 绿；
`M32a`(B-lite cancel 强加 stop=True) / `M32b`(verify 改 conditional) /
`M34`(TP gate 放行非 TERMINAL_ZERO) / `M33`(首因保护失效) 全部 RED。

### 0.6 canonical **133@staged v62** → **133/133**
直接把 v6.1 套件指向 v6.2 helper 得 **115/133**（18 红）。逐条归因确认
**18 条同源**：v6.2 新增的 `filled_details` 等长门拒绝了 v6.1-era fixture 的
「短/缺 filled_details」形态。`G:/tmp/verify_shape_gate.py` 证明是
**fixture 陈旧，不是功能死**：生产合法形态（等长、prefix 成交价 + tail 0）仍放行，
🗑️ 截断态得到完全相同结果（amt=0.002, entry_orders=['E1','E2']）；
生产合法态下「ID 丢失」仍被精确拦为 `entry_orders_short`。

据此生成 **v6.2 canonical 变体** `test_close_confirmation_v62.py`（**副本**，
v6.1 批准基线 `test_close_confirmation_v6.py` 未改动）→ **133/133**。
变体含 14 处 fixture 补丁，其中 **2 处是语义再基线，需裁定**：

1. **`_derive_close_txn_vars` 不含 `protection_registry`**：v6.2 gate 需要它做
   registry 链恢复，而 v6.1 套件把 `txn_vars` 当 `b_data` 传给 gate → 恒不可恢复。
   变体已对齐 staged trader 真实范式（传 **claimed 快照**）；
   staged trader 本身传 `target_b_data(=_claimed)`，**无此问题**。
2. **🗑️ 截断语义变化（改动 8）**：v6.1 下 🗑️ 签名 → pending_ids 恒空、**零撤单**；
   v6.2 改为从 registry 取回被截断层 ID 并**撤掉**。变体把 D6gb 期望从 `[]`
   再基线为 `['cancel:E3','cancel:E4','cancel:E5']`。
   ⚠️ 附带风险：**registry 不完整的 🗑️ 批次会被永久 Fail-Closed**
   （legacy 无 registry 批次无法自动平仓）—— 新的 F-1 候选，请裁定是否可接受/需兜底。

### 0.7 最新 staged trader 本体
**9292 行**，SHA-256 `2dd72eea61ce5ec01a3f71473d560cdeb2f0e56b0604588bcfd586224f46ce7c`
（随本轮一起上传，供逐行复核）。

### 0.8 收口后门禁状态
| 门 | 结果 |
|---|---|
| 组装后 staged 集成 | **15/15**（endpoint / create 计数 / mid-flight） |
| 集成变异检查 | **4/4 杀死** |
| GREEN 层（helper/候选） | **28/28** |
| RED 基线 | **23/23**（独立保持） |
| canonical 133@staged v62 | **133/133** |
| canonical 133（v6.1 基线，未改动） | **133/133** |
| 43 scripts 回归 | 41 rc=0 + 2 个 v3 遗留 rc=1 |
| 生产三文件 | 零改动冻结 |

**待你裁定 3 项**：① 规格 9.2 B-lite endpoint 更正；② 🗑️ 批次 registry 不完整
永久 Fail-Closed 是否可接受；③ 是否满足进入五道门 + mutation。

---

# v6.2 组装收口 · 第 1 轮正文

> **日期**：2026-08-31（第 1 轮） ｜ **生产三文件零改动**（`git diff --stat HEAD` 为空）
> **本轮完成**：MARKET（改动 6/7）与 LIMIT（改动 9）全流程事务组装进
> `G:/tmp/v62_staged/trader_260725_v62_staged.py`，并补全 R1-q、R2 异常出口、
> LIMIT happy path、最优价路径、B-lite 集成 GREEN 证明。
> **组装修正（终检抓出）**：v6.1 doc 的 LIMIT BEGIN 块只取 `current_price`，而最优价路径
> （`price=None`）使用 `ask`/`bid`（生产 L7499-7506 语义，doc 文本注明「仅多取 bid/ask」）
> → 组装修正补回 `bid/ask` 定义 + 新增 `t_limit_optimal_price_path` 锁定。

---

## 1. 交付物与指纹

| 项 | 值 |
|---|---|
| staged trader | `G:/tmp/v62_staged/trader_260725_v62_staged.py` |
| 行数 | **9281**（生产 7859 → +1422） |
| SHA-256 | `0b086685c6a1be6be7795c539249c732450700f64f4ce8918268e1a0a4e8e951` |
| 生产 HEAD | `e953d79`（`git diff --stat HEAD` = 空，零改动） |
| 生产方法 | `close_position_market` L8126-8460（335 行）／`close_position_limit` L8790-9098（309 行） |
| 集成测试 | `test_v62_staged_integration.py`（已入项目根目录，14/14） |

## 2. 组装清单（builder：`G:/tmp/build_v62_full.py`，可复跑）

| 补丁 | 内容 | 状态 |
|---|---|---|
| P1 | 13 个 v6.2 helper 插入 CryptoTrader（含 `_pending_entry_ids_for_gate`、`_commit_limit_close_order_if_current`、`_begin_close_request_if_active` 的 limit_creating seed grace） | ✅ |
| P2 | `cancel_open_orders` → v6.2 改动 1（高→低 + 逐层 verifier + order-scoped 归因 + 永不压缩） | ✅ |
| P8 | monitor freeze 块 → r4 双集合（`FREEZE_QUIET_REASONS`=market_confirming/limit_pending_normal，之外周期 critical） | ✅ |
| P9 | `__init__` 初始化 `self._freeze_alerted = {}`（crash 重启 AttributeError P0） | ✅ |
| P12 | `_record_realized_pnl` 签名补 `pnl_partial=False`（v6.1 1b 追加 2 落地阻断项） | ✅ |
| **P10** | **MARKET 组装**：批次查找头 → ticker → BEGIN(`market_confirming`) → claimed 快照派生 → 平均成本/PnL → `try:`[pos_before → `_close_amount_guard` → create MARKET(params_base 派生) → 六态确认 → CONFIRMED_FULL/TERMINAL_ZERO(rollback)/else(异常) → ENTRY gate（clear gate）→ 撤 TP/SL 用 `_txn_vars` + terminal_ok 归因 → 结算(confirmed_filled_amount 贯穿 + `_cancel_limit_close_order` + phase=2 + converge+clear) → record_pnl(pnl_partial)] → `except:`[close_order_placed→settlement_error / else→rollback→txn_aborted_rollback_failed] | ✅ |
| **P11** | **LIMIT 组装**：批次查找头 → ticker（**含 bid/ask，组装修正**）→ BEGIN(`limit_creating` + grace) → claimed 快照派生 → 价格确定（最优价/custom） → `try:`[ENTRY gate（失败→回滚）→ TP factual gate（`_confirm_close_filled(order_kind='conditional')`，仅 TERMINAL_ZERO 放行）→ coverage guard（safe_amount==台账 才放行）→ create LIMIT → durable commit 4 字段 + B-lite → N14 TP registry 终态 → PnL 预估 → `_monitor_limit_close` 线程] → `except:`[close_order_placed→settlement_error / else→rollback→txn_aborted_rollback_failed] | ✅ |

## 2b. landing symbols 终检（AST 自由变量扫描）

`G:/tmp/check_free_vars.py`：两个组装方法除 `ask`/`bid`（已修正）外无未解析自由变量；
剩余候选（`TAKER_FEE_RATE`/`MAKER_FEE_RATE`=模块常量、`_rb_err`/`_tp_cancel_e`/`_cx_e`/`e`=except 绑定、内建/import）均为假阳性，已逐项核实。

## 3. 双层测试证据

### 3.1 既有 GREEN 层（helper/候选 SUT）— **28/28**
`test_v62_green.py`（RED 基线 23/23 独立保持，未触碰）

### 3.2 组装后 staged 集成层（新增，`test_v62_staged_integration.py`，已入项目根目录）— **14/14**
直接对组装后的 `close_position_market` / `close_position_limit` 方法（ast 提取 + FakeSelf62 绑定）跑真实调用链：

| 用例 | 验证点 | 结果 |
|---|---|---|
| t_market_happy_path | BEGIN→guard→create→CONFIRMED_FULL→ENTRY gate→撤 TP/SL+归因→结算→True | ✅ |
| t_market_r1q_sl_cancel_fail_no_attribution | **撤 TP 成功、撤 SL 失败 → SL registry 绝不写 PROGRAMMATIC_CANCELED**（TP=PROGRAMMATIC_CANCELED / SL=CONFIRMED） | ✅ |
| t_market_exit34_post_create_exception | 结算期异常 → outer except → settlement_error + 冻结(phase≥1) + critical | ✅ |
| t_market_exit2_confirm_unknown | 六态 UNKNOWN → market_confirm_unknown（**不再 `except: pass` 静默**） | ✅ |
| t_market_exit3_terminal_zero_rollback_rejected | TERMINAL_ZERO + rollback persist 失败 → rollback_rejected | ✅ |
| t_market_exit5_entry_gate_fail | ENTRY gate False → market_entry_unknown + SL/TP 保留 | ✅ |
| t_market_exit6_precreate_rollback_failed | create 前异常 + rollback 失败 → txn_aborted_rollback_failed | ✅ |
| t_market_first_abnormal_wins | abnormal reason 不被 settlement_error 覆盖（first-abnormal-wins） | ✅ |
| t_limit_happy_path | BEGIN(limit_creating)→ENTRY gate→TP gate(TERMINAL_ZERO)→coverage→create→**durable commit 4 字段**→limit_pending_normal→True + N14 | ✅ |
| t_limit_optimal_price_path | **最优价路径（price=None）→ ask=77001.0 挂单 → commit → limit_pending_normal**（bid/ask 补回修正的回归锁定） | ✅ |
| t_limit_tp_gate_blocked | TP PENDING → limit_tp_unresolved + **零 create LIMIT** | ✅ |
| t_limit_coverage_conflict | aggregate 0.001 < 台账 0.002 → limit_amount_conflict | ✅ |
| t_limit_commit_fail_blite | **create 成功但 commit persist 失败 → B-lite 撤 OID1 + 六态确认 + limit_persist_failed + 绝不 rollback ACTIVE** | ✅ |
| t_limit_entry_gate_fail_rollback | ENTRY gate 失败 → 未挂单 + CAS 回滚 → 批次回 ACTIVE | ✅ |

### 3.3 旧 43 套件全量回归（`G:/tmp/reg_v62_full.txt`）
**41/43 rc=0**；2 个 rc=1（`test_ast_rollback_guard.py` / `test_merge_rollback_semantics.py`）
为 v3 已撤销机制的遗留测试，与既有基线一致（落生产时归档）。

## 4. 与 v6.2 正式 diff 的逐项对照（r5 规格 → 组装产物）

- **改动 6（六出口闭环）**：`market_confirm_unknown` / `rollback_rejected` / `settlement_error` /
  `txn_aborted_rollback_failed` / `settlement_stuck` / `market_entry_unknown` 全部 CAS 走
  `_set_close_reason_if_current`，`except: pass` 已删除（r5 P0-2 闭环）。
- **改动 7（terminal_ok 归因）**：撤 TP/SL 用 `_txn_vars`（claimed 快照）来源 + 仅 cancel 正常返回者写
  PROGRAMMATIC_CANCELED；异常（含 -2011）不归因（R1-q 实证）。
- **改动 9.0（TP factual gate）**：create LIMIT 前硬门，`_confirm_close_filled(order_kind='conditional')`
  走 algo 端点（r4 P0-1 端点路由），仅 TERMINAL_ZERO 放行。
- **改动 9.0b（coverage guard）**：create 紧前锁外调用，`safe_amount==台账` 才放行（r3 P0-2）。
- **改动 9.2（durable commit）**：`_commit_limit_close_order_if_current` 校验源状态 limit_creating 后
  4 字段原子落盘，失败转 B-lite（r5 P0-3 闭环）。
- **组装修正（本稿新发现）**：LIMIT 最优价路径缺 `ask`/`bid` 定义（v6.1 doc BEGIN 块只取
  `current_price`）→ 已补回 + `t_limit_optimal_price_path` 锁定；生产零改动不受影响。

## 5. 已知边界（如实记录）

1. 集成测试中 `_converge_batch_orders_before_clear` / `clear_batch_state` 为桩（返回 proof/True 不删批次），
   聚焦平仓事务语义；converge 本身由既有 133 回归覆盖。
2. `_monitor_limit_close` 为桩（避免真实线程）；其行为由既有 `test_r10_limit_close_alert` 等覆盖。
3. staged 文件在 `G:/tmp/v62_staged/`（工作区外），生产三文件仍冻结；集成测试已入项目根目录。

## 6. 待 ChatGPT 终审确认项

- [ ] 组装产物与 r5 规格逐项对照是否通过（含 bid/ask 组装修正）
- [ ] 双层测试证据（28/28 + 14/14 + 41/43）是否满足 GREEN 终审门槛
- [ ] 是否可以进入「正式 diff 落生产」流程（仍需单独评审落地方式与回滚预案）
