# v6.2 修复方案（针对 ChatGPT 终审 3 个 P0）— 初稿 v1，待用户裁定，未送审

> 状态：设计稿。本文档不含对生产文件的任何改动（HEAD=e953d79 保持零改动）。
> 按终审要求：**不扩 v7**，只集中解决三个点，测试只加针对三个反例的 RED→GREEN。
> 文档纪律：`python` 块均为完整代码（零 Ellipsis）；生产原文摘录用 `python-frag`。

---

## §零 ChatGPT 终审结论与独立核实证据

终审：事务边界 A（BEGIN persist 纳入 claim）与边界 B（限价 gate 失败优先 CAS rollback）**正式批准**；v6.1 **不批准落生产**，因 3 个 P0。我方按铁律逐条独立源码核实，全部实锤：

| # | P0 | 独立核实证据 |
|---|---|---|
| P0-1 | F-1「截断签名=安全事实」被生产源码证伪 | `cancel_open_orders`（trader L6879-6897）：撤单失败只 print；`cancelled_ids` 非空（≥1 张成功）即整体截断 `entry_orders = entry_orders[:last_filled_count]`，失败 ID 从账本消失但交易所仍 active。**攻击链已独立复现**（`G:/tmp/verify_f1_attack.py`，零改动交付物）：截断账本 [E1..E3]/lfc=3 下 derive=True → gate 的 `pending_ids` 为空直接 `return True` → entry 相关交易所调用=0 → 照常撤 TP1/SL1、流程「成功」走完；对照组完整账本 [E1..E5] 下 gate 正常撤 E4/E5（gate 本身有效，击穿它的是「截断」） |
| P0-2 | 异常冻结出口不止 3 条，「一发 critical 就够」被否决 | 第四类实锤：送审稿 L775-786（市价）/ L828-839（限价）——derive 失败 + CAS rollback 失败时只发 critical 就 return，磁盘 `close_reason` 仍是 BEGIN 写入的正常态（`market_confirming`/`limit_pending_normal`）→ 按告警分型被当「正常等待」**静默**。另：六态非确认分支 `except: pass` 写盘（送审稿 L548-552），写失败同样静默 |
| P0-3 | `limit_close_order_id` 持久化不检查写盘成功 | trader L7596-7602：`limit_close_order_id = order_id` 后直接 `save_batch_state(...)`（void，内部 L1427 不查 `_persist_states()` 返回值），继续 N14/计算盈亏/启动监控/`return True` —— 写盘失败时账本无单、交易所有单 |

终审其他裁定（本稿遵守）：D-006 日亏损限额=0 时不阻断；N-3 定 P1 可延期；「Mutation testing 能证明断言守住当前模型，不能证明当前模型描述了真实生产世界的全部状态」。

---

## §一 P0-1 修复：`cancel_open_orders` 的订单事实**保存/验证**语义

终审原文：*「撤销 F-1『截断形状=安全事实』的假设。修 `cancel_open_orders` 的订单事实保存/验证语义，再重写 D6e/D6g。」*

### 1.1 新不变量 INV-1

> **任何 pending ENTRY 订单 ID，在其被交易所确认为终态（canceled/expired/rejected）之前，不得从批次可追踪状态（`entry_orders` 及其派生视图）中消失。**

F-1 的本质是 producer（`cancel_open_orders`）违反 INV-1；D6e/D6g 的本质是 verifier（derive 校验）把「违反 INV-1 的产物形状」当成了安全签名。所以修复必须**两半一起做**：

- **保存半边（改动 1a）**：producer 不再丢失失败 ID → 新形状重新可信；
- **验证半边（改动 1b）**：gate 对「截断形状」做交易所侧对账 → 同时兜住存量形状与 producer 未来回归。

### 1.2 生产现状（BEFORE，`python-frag` 摘录，trader L6879-6897）

```python-frag
        for idx in range(last_filled_count, len(entry_orders)):
            order_id = entry_orders[idx]
            try:
                self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol, params={'stop': True})
                cancelled_ids.append(order_id)
                cancelled_layers.append(idx + 1)
                print(f"  └─ 已撤销第 {idx + 1} 层挂单: {order_id}")
            except Exception as e:
                print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单失败: {e}")

        if not cancelled_ids:
            # 撤销失败，清除标记
            target_b_data.pop('is_programmatic_cancel', None)
            self.save_batch_state(target_symbol, batch_id, target_b_data)
            return False, f"⚠️ 批次 `{batch_id}` 挂单撤销失败，请检查订单状态"

        # 从状态中移除已撤销的订单
        remaining_orders = entry_orders[:last_filled_count]
        target_b_data['entry_orders'] = remaining_orders
```

缺陷链（三连）：①失败只 print，无任何状态记录；②`remaining_orders = entry_orders[:last_filled_count]` 只要 ≥1 张成功就**整体截断**；③失败 ID 无处可寻 → ENTRY gate 的逐 ID 归因视野永久失去它。

### 1.3 改动 1a（producer）：失败 ID 保留台账，只移除确认撤成功的 ID

```python
        cancel_failed_ids = []
        for idx in range(last_filled_count, len(entry_orders)):
            order_id = entry_orders[idx]
            try:
                self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol, params={'stop': True})
                cancelled_ids.append(order_id)
                cancelled_layers.append(idx + 1)
                print(f"  └─ 已撤销第 {idx + 1} 层挂单: {order_id}")
            except Exception as e:
                # 🔒 v6.2（P0-1 / INV-1）：撤单失败的 ID 必须保留在台账。
                # 它在交易所仍 active，成交后会形成无保护仓位；
                # 从 entry_orders 消失 = 从 ENTRY gate 的逐 ID 归因视野消失。
                cancel_failed_ids.append(order_id)
                print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单失败: {order_id} ({e})"
                      f"（ID 已保留在台账，平仓时将重撤并逐 ID 验证）")

        if not cancelled_ids and cancel_failed_ids:
            # 全部 pending 层撤单失败：台账不动（INV-1），批次保持现状，
            # 监控线程继续管辖这些层（与手动撤单分治的 >0 分支同语义）。
            target_b_data.pop('is_programmatic_cancel', None)
            self.save_batch_state(target_symbol, batch_id, target_b_data)
            return False, (f"⚠️ 批次 `{batch_id}` 挂单全部撤销失败"
                           f"（{len(cancel_failed_ids)} 个 ID 保留在台账），请检查订单状态")

        # 🔒 v6.2（P0-1 / INV-1）：只移除确认撤单成功的 ID；失败 ID 原位保留。
        # 被移除的 ID 全部位于 idx >= last_filled_count 的尾段，因此失败 ID
        # 在截断后仍落在 idx >= last_filled_count 区间 —— ENTRY gate 的
        # pending_ids 推导（idx >= last_filled_count）天然覆盖它们：
        # 下次平仓会重撤这些 ID 并做逐 ID 终态验证。
        cancelled_set = set(cancelled_ids)
        remaining_orders = [oid for oid in entry_orders if oid not in cancelled_set]
        target_b_data['entry_orders'] = remaining_orders
```

语义说明：

- **层序索引不变**：成功撤除的 ID 全在尾段（idx ≥ last_filled_count），列表压缩后失败 ID 的索引仍 ≥ last_filled_count，`pending_ids` 推导不受影响；
- **🗑️ 按钮语义更正确**：现状「部分撤失败」会谎报成功并丢 ID；修复后 TG 文案如实报告失败数，且监控线程继续管辖失败层（它们仍是可能成交的活单）；
- **全撤成功**时 `remaining_orders` 恰为 `entry_orders[:last_filled_count]`，与现状形状一致——该形状从「碰巧安全」变成「由遵守 INV-1 的 producer 产生的真签名」。

### 1.4 改动 1b（verifier）：ENTRY gate 对「截断形状」做交易所侧对账

derive 是只读、锁内零交易所 API 的（既有契约），不可能在 derive 里查交易所。所以验证半边放在 ENTRY gate——它本来就是逐 ID 事实验证的所在。插入点：gate 的 `if not pending_ids: return True` 之前。

```python
        entry_orders = b_data.get('entry_orders', []) or []
        pending_ids = [oid for idx, oid in enumerate(entry_orders)
                       if idx >= last_filled_count and oid]
        if not pending_ids:
            # 🔒 v6.2（P0-1 验证半边）：pending 为空有两种成因——
            # ① 本就无未成交层（计划层 == 已成交层，合法放行）；
            # ② 截断形状：计划层 > 已成交层，但台账只剩已成交层。
            #    形状 ② 在 v6.1 被当作「🗑️ 截断签名」放行（D6e/D6g），
            #    但 F-1 攻击链证明它可能是「producer 丢 ID」的产物。
            #    台账说「没有 pending ENTRY」，交易所侧必须证明确实
            #    不存在无主 stop 条件单，gate 才允许放行。
            if (len(b_data.get('target_amounts', []) or []) > last_filled_count
                    and len(entry_orders) == last_filled_count):
                return self._verify_no_untracked_stop_orders(symbol, batch_id)
            return True
```

第 13 个 helper（新增）：

```python
    def _verify_no_untracked_stop_orders(self, symbol: str, batch_id: str) -> bool:
        """ENTRY gate 的对账半边：证明「台账之外、本 symbol 之内」无无主 stop 单。

        🔒 v6.2（P0-1）：归因集合 = 同 symbol 全部活跃批次的
        entry_orders + tp_order_id + current_sl_id（锁内读内存态）。
        已 clear/墓碑的批次不在态里，其遗留挂单会表现为 unknown —— 这正是
        本 helper 要抓的「孤儿保护单」类；用户手动下的同 symbol 条件单
        也会被拦（Fail-Closed，critical 明示 ID 供人工放行）。
        """
        try:
            remaining = self._safe_api_call(
                self.exchange.fetch_open_orders, symbol, params={'stop': True})
        except Exception as e:
            print(f"  └─ ⚠️ ENTRY 对账快照查询异常: {e}")
            self.send_tg_notification(
                f"🚨【资金安全】ENTRY 对账快照不可判定！\n"
                f"🆔 批次: {batch_id}\n⚠️ fetch_open_orders(stop) 异常: {e}\n"
                f"⚠️ 无法排除无主开仓条件单，禁止进入撤保护/clear！",
                level='critical')
            return False
        if not isinstance(remaining, list):
            # v4 契约：禁 or []，None/非 list = Fail-Closed
            self.send_tg_notification(
                f"🚨【资金安全】ENTRY 对账快照不可判定！\n"
                f"🆔 批次: {batch_id}\n"
                f"⚠️ fetch_open_orders 返回 {type(remaining).__name__}，"
                f"无法排除无主开仓条件单，请立即人工核对！",
                level='critical')
            return False

        known = set()
        with self._state_lock:
            try:
                all_states = self.load_all_states()
            except Exception:
                all_states = None
        if isinstance(all_states, dict):
            sym_states = all_states.get(symbol, {}) or {}
            for _b in sym_states.values():
                if not isinstance(_b, dict) or not _b.get('is_active'):
                    continue
                for oid in (_b.get('entry_orders') or []):
                    if oid:
                        known.add(str(oid))
                for k in ('tp_order_id', 'current_sl_id'):
                    if _b.get(k):
                        known.add(str(_b.get(k)))

        unknown = [str(o.get('id')) for o in remaining
                   if isinstance(o, dict) and str(o.get('id')) not in known]
        if unknown:
            print(f"  └─ 🚨 ENTRY 对账发现台账外 stop 条件单: {unknown}")
            self.send_tg_notification(
                f"🚨【资金安全】发现台账外的条件单（疑似截断孤儿）！\n"
                f"🆔 批次: {batch_id}\n📌 无主订单: {unknown}\n"
                f"⚠️ 这些挂单成交后将形成无保护仓位，请立即人工处理！",
                level='critical')
            return False
        return True
```

### 1.5 D6e/D6g 重写（derive 校验的依据反转）

`_derive_close_txn_vars` 的放行条件**保持不变**（`0 < len(_eo) == last_filled_count` 放行——它仍是 v6.1 的存量形状，且修复后是合法签名），但**安全论证整体重写**（docstring 替换），放行资格从「形状=安全事实」反转为：

> 放行该形状的依据 = ①producer 已修复为遵守 INV-1（改动 1a）+ ②gate 对该形状强制交易所侧对账（改动 1b）。derive 自身零 API，不做也无法做交易所验证；形状本身不再被宣称「安全」。

配套收紧一点：v6.1 注释中「未成交层已被有意移除，pending_ids 恒空，gate 无单可撤自然通过」这句**删掉**——它在改动 1b 之后不再成立（gate 有对账）。

### 1.6 决策项 D-1：对账的触发范围

| 选项 | 内容 | 取舍 |
|---|---|---|
| A（推荐） | 仅「截断形状」（计划层 > 已成交层且 `len(entry_orders)==last_filled_count`）触发对账 | 精确覆盖 F-1 反例与存量形状；普通 close 不加 API 调用；误报面最小 |
| B | 每次 close 的 ENTRY gate 无条件对账 | 更强，但用户手动在同 symbol 挂的条件单也会拦 close（Fail-Closed 烦扰）；且新增一次 API 调用 |

---

## §二 P0-2 修复：N-1 反转——白名单正常态 + 统一异常出口纪律

### 2.1 新不变量 INV-2

> **任何异常退出路径，若磁盘批次仍带本事务的 `close_op_id` 且 `close_phase>=1`，则其 `close_reason` 必须为异常态（即不在白名单内）；若连 reason 切换的写盘也失败，该次 critical 通知必须显式声明「reason 切换失败，冻结告警可能静默」。**

### 2.2 异常出口穷举表（BEGIN 之后，市价/限价共通结构）

| # | 出口 | v6.1 现状 | v6.2 处置 |
|---|---|---|---|
| 1 | BEGIN 失败（未取得所有权） | critical + return | 不变（磁盘不属于本事务，无 reason 责任） |
| 2 | derive 失败 + rollback 成功 | critical + return（磁盘已回 phase=0） | 不变（INV-2 不适用，磁盘已脱离本事务） |
| 3 | **derive 失败 + rollback 失败（第四类）** | critical 但 reason 停在**正常态** → 静默卡死 | **改动 2a**：CAS 写 `txn_aborted_rollback_failed` |
| 4 | 市价确认非 CONFIRMED_FULL 分支写 `market_confirm_unknown` | `except: pass`，写失败静默 | **改动 2b**：persist-aware，失败提示随 raise 进 critical |
| 5 | ENTRY gate 失败 | 已 CAS 写 `market_entry_unknown`（v6.1 已修） | 不变；CAS 失败文案已如实（v6.1 N-2） |
| 6 | 结算段通用 except 通道 | critical（fail-noisy） | 不变；改动 2c 保证其 reason 已是异常态时不再静默 |

### 2.3 改动 2a：第四类出口（市价与限价两处同构修改）

```python
        _vars_ok, _txn_vars, _vars_why = self._derive_close_txn_vars(_claimed, batch_id)
        if not _vars_ok:
            # claimed 快照显示无需平仓 / 账本残缺 → 撤销这次 claim 再退出
            _rb_ok, _rb_why = self._rollback_close_request_if_current(
                target_symbol, batch_id, close_op_id)
            if not _rb_ok:
                # 🔒 v6.2（P0-2 / INV-2）：rollback 失败 = 磁盘仍带本事务
                # close_op_id 且 phase>=1，reason 却停在 BEGIN 的正常态 →
                # 冻结告警按白名单静默。必须 CAS 切异常态；CAS 再失败
                # （双重故障）也绝不静默，critical 文案如实声明。
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'txn_aborted_rollback_failed')
                self.send_tg_notification(
                    f"🚨【资金安全】平仓中止且回滚失败，批次冻结！\n"
                    f"🆔 批次: `{batch_id}`\n💡 派生失败原因: {_vars_why}\n"
                    f"🔄 回滚失败原因: {_rb_why}\n"
                    + ('' if _rs_ok else
                       f"⚠️⚠️ close_reason 切换也失败（{_rs_why}），"
                       "冻结告警可能不再周期触发，请立即人工处置！"),
                    level='critical')
                return False, f"❌ 平仓中止（{_vars_why}）；回滚失败已冻结待人工处置"
            self.send_tg_notification(
                f"🚨【资金安全】平仓中止：claimed 快照不能用于下单。\n"
                f"🆔 批次: `{batch_id}`\n💡 原因: {_vars_why}\n"
                f"🔄 回滚本次平仓标记: 成功\n"
                f"⚠️ 未发出任何订单，请人工核对账本与批次状态。",
                level='critical')
            return False, f"❌ 平仓中止（{_vars_why}）"
```

（原 v6.1 的合并文案拆成「回滚成功 / 回滚失败」两支：失败支先 CAS 切 reason。限价同构，仅文案前缀不同。）

### 2.4 改动 2b：六态非确认分支 persist-aware

（摘录：落点在市价 AFTER 块六态分支内，正式 diff 中随该块整体过检。）

```python-frag
            else:
                # PARTIAL / PENDING / UNKNOWN / NOT_CONFIRMED —— 一律不回滚
                _pc_ok, _pc_why = self._persist_checked(
                    target_symbol, batch_id, {'close_reason': 'market_confirm_unknown'})
                raise RuntimeError(
                    f"市价平仓单结果未确认（{_verdict}）：{_detail}。"
                    f"不回滚，保持冻结等人工处置"
                    + ('' if _pc_ok else
                       f"；⚠️ close_reason 写盘失败（{_pc_why}），"
                       "冻结告警可能不再周期触发，请立即人工核查！"))
```

### 2.5 改动 2c：冻结告警分型反转为白名单

```python
_CLOSE_REASON_NORMAL = {'market_confirming', 'limit_pending_normal'}


def _freeze_reason_is_normal(reason):
    # 🔒 v6.2（P0-2 / N-1 反转）：白名单正常态。除这两个值以外的一切
    # reason（异常态、None、缺失、未知字符串）一律按异常周期处理。
    # v6.1 的「黑名单枚举异常 reason」模型被终审否决：枚举必然遗漏
    # （第四类出口即证明），反转后缺失/未知值默认 fail-noisy。
    return reason in _CLOSE_REASON_NORMAL
```

监控线程冻结告警（送审稿改动 4）的分型判断改为调用 `_freeze_reason_is_normal`：True → 只 print；False → 周期 critical（3 轮去重不变）。

---

## §三 P0-3 修复：`limit_close_order_id` persist-aware durable commit

### 3.1 新不变量 INV-3

> **交易所侧已存在的本事务订单（如限价平仓单），其账本落盘未确认成功前，函数不得以正常状态返回；宁可冻结 + critical，绝不「账本无单、交易所有单」地静默成功。**

### 3.2 改动 3a：`_persist_checked` 原语（第 12 个 helper，P0-2/P0-3 共用）

写路径与已批准的 `_set_close_reason_if_current` 完全同构（锁内 `load_all_states()` → 修改 → 检查 `_persist_states()` 返回值），绕开 `save_batch_state` 的 merge-ratchet（那是 P0 事故回滚失效的机制，事务字段写入不应经过它）：

```python
    def _persist_checked(self, symbol: str, batch_id: str, updates: dict):
        """锁内把 updates 写入批次态并**检查持久化返回值**（durable commit 原语）。

        🔒 v6.2（P0-2/P0-3）：save_batch_state 是 void 且内部不查
        _persist_states()（trader L1427）——「写了」≠「写进磁盘了」。
        本 helper 是事务字段的唯一合法写入口之一（另一为
        _set_close_reason_if_current），两者同构：锁内重读磁盘禁旧快照、
        锁内零交易所 API、持久化失败显式返回 False。
        返回 (ok, why)。why ∈ 'updates_empty' / 'state_unreadable（{异常}）'
        / 'batch_missing' / 'persist_failed（{详情}）' / 'persisted'。
        """
        if not isinstance(updates, dict) or not updates:
            return False, 'updates_empty'
        with self._state_lock:
            try:
                all_states = self.load_all_states()
            except Exception as e:
                return False, f'state_unreadable（{e}）'
            b = (all_states.get(symbol, {}) or {}).get(batch_id)
            if not isinstance(b, dict):
                return False, 'batch_missing'
            b.update(updates)
            if not self._persist_states(all_states):
                return False, 'persist_failed（_persist_states 返回 False）'
            return True, 'persisted'
```

### 3.3 改动 3b：限价路径接入（BEFORE 见 §零 P0-3 证据行）

```python
            order_id = order['id']

            # 🔒 v6.2（P0-3 / INV-3）：persist-aware durable commit。
            # 有限重试 1 次（_persist_states 失败多为瞬时替换失败）；
            # 仍失败绝不作为正常 limit_pending 返回成功。
            _pc_ok, _pc_why = self._persist_checked(
                target_symbol, batch_id,
                {'limit_close_order_id': order_id,
                 'limit_close_price': limit_price,
                 'limit_close_mode': price_mode})
            if not _pc_ok:
                _pc_ok2, _pc_why2 = self._persist_checked(
                    target_symbol, batch_id,
                    {'limit_close_order_id': order_id,
                     'limit_close_price': limit_price,
                     'limit_close_mode': price_mode})
                _pc_ok, _pc_why = _pc_ok2, _pc_why2
            if not _pc_ok:
                # close_order_placed 已为 True（L7594 既有纪律：平仓单已创建
                # → 绝不回滚关闭标记），且此时撤单风险大于收益（撤单可能
                # 已成交，账本又没记 → 更多竞态）→ 处置 = 冻结 + critical。
                # 限价单留在交易所但已 loud 报警，由人工接管。
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'limit_persist_failed')
                self.send_tg_notification(
                    f"🚨【资金安全】限价平仓单 {order_id} 已挂出，"
                    f"但账本写盘失败！\n"
                    f"🆔 批次: `{batch_id}`\n💰 价格: {limit_price}\n"
                    f"⚠️ 账本无此单（limit_close_order_id 缺失），"
                    f"确认循环将无法管辖它\n"
                    f"🔄 异常态切换: {'成功' if _rs_ok else '失败（' + _rs_why + '），冻结告警可能静默'}\n"
                    f"🚫 批次保持冻结（close_phase=1），本轮禁止进入 clear\n"
                    f"⚠️ 请立即人工核对该限价单！",
                    level='critical')
                return False, ("❌ 限价平仓单已挂出但账本写盘失败，"
                               "批次冻结待人工处置")

            # 🔥 N14 落盘（P0 Batch A）……（以下原逻辑不动）
```

### 3.4 设计张力说明（为什么不「撤单后回滚」）

L7594 既有纪律 `close_order_placed=True → 此后失败绝不回滚关闭标记`，其存在理由正是「撤单/回滚期间订单可能已成交，回滚 = 伪装没平过」。写盘失败时若选择撤单 + 按六态验证后再回滚，需要把六态确认器前置到这条路径——超出本轮「集中三个点」的范围，且引入新的竞态面。故推荐**冻结 + critical**（fail-noisy，与 INV-2 的冻结语义一致），把「撤单 + 六态验证后回滚」登记为后续可选项。

---

## §四 RED→GREEN 测试设计（只加三个反例，不扩 v7）

| 组 | 用例 | RED（v6.1 行为） | GREEN（v6.2 行为） |
|---|---|---|---|
| R1（P0-1 producer） | FakeExchange.cancel_order 注入「E4 成功 / E5 抛异常」，执行改动 1a 的 AFTER 块 | `entry_orders` 被截断为 [E1..E3]，E5 消失 | `entry_orders` == [E1..E3, E5]，E5 保留；全部失败时返回 False 且台账不动 |
| R1（P0-1 verifier） | 截断形状 + `fetch_open_orders` 返回含无主 stop 单 → gate；另测返回 [] / 异常 / 非 list | gate 恒 True（零对账） | 无主单 → False + critical；[] → True；异常/非 list → False |
| R2（P0-2 出口③） | derive 失败 + rollback 失败（`persist_ok=False`） | reason 停在 `market_confirming` | 落盘 reason == `txn_aborted_rollback_failed`；CAS 再失败时 critical 文案含「切换失败」 |
| R2（P0-2 出口④） | 六态分支 persist 失败 | 静默 except: pass | raise 消息含「close_reason 写盘失败」 |
| R2（P0-2 分型） | `_freeze_reason_is_normal` 纯函数四例 | —（新函数） | `market_confirming`/`limit_pending_normal` → True；`market_confirm_unknown`/`None`/`'garbage'` → False |
| R3（P0-3） | 限价 create 后 `persist_ok=False` | return True，账本无单 | 不 return True；critical；批次冻结；重试 1 次后仍失败走冻结分支 |
| R3（P0-3 正向） | `persist_ok=True` | — | 正常 limit_pending 路径 GREEN |

**双向验证纪律**：每个新断言在 v6.2 文档（正常）下 GREEN、在对应变异（回退 producer / 删对账分支 / persist 失败仍 return True）下 RED；变异清单新增 3 项，进 `run_mutation_checks` 体系。

**测试基建缺口披露**：现 `FakeExchange.cancel_order` 永远成功且无失败注入通道（F-1 盲区在同桩上的又一实例——「Fake 桩必须建模生产契约」）。需增加 `cancel_seq`（与 `order_seq` 同款 repeat-last 语义）。

---

## §五 对既有 133 场景与五道门的影响

1. **场景回归**：现有 gate 用例 `tb` 均为 `len(entry_orders) > last_filled_count`（pending 非空），不触发对账分支；`oo_seq=[[]]` 下即使触发也无 unknown → 预期零回归。D6e/D6g 键保留（放行条件不变），仅 docstring 重写。
2. **helper 计数 11 → 13**（`_persist_checked` + `_verify_no_untracked_stop_orders`）：parity / calls / landing / mutation 四个 checker 的 helper 名单同步更新；landing 的 `MODULE_DUNDERS` 不变。
3. **`PROD_SIGNATURE_OVERRIDES`**：`cancel_open_orders` 作为「本方案改动的生产方法」登记，三处同步（送审稿完整定义 + calls 检查【6】+ 登记表）。
4. **新 AFTER 块纳入五道门**：改动 1a/1b/2a/2b/3b 的块均为完整 `python` 块，落地 diff 时逐块过 `check_doc_code_blocks`（零 Ellipsis）+ parity + calls + landing + mutation。
5. **回归基线**：43 → 41 rc=0（2 个 rc=1 为 v3 已撤销机制遗留测试，落生产时归档）——不变。

---

## §六 存量数据处置

- **事故批次 2**（`close_phase=1` 卡死）：仍须**落生产前人工处置**，本方案不自动触碰；
- **存量截断形状账本**（旧 producer 的产物，可能含丢失的 active ID）：由改动 1b 的 gate 对账兜住——只要这些批次走程序平仓，对账即暴露无主单；不走平仓的由 D-007 事件日志/对账工具（路线图既有项）覆盖；
- `.bak` 永不自动装载（既有铁律）。

---

## §七 决策项汇总（等用户裁定后出正式送审稿 diff）

| # | 决策 | 推荐 |
|---|---|---|
| D-1 | gate 对账触发范围：仅截断形状（A）vs 无条件对账（B） | A |
| D-2 | P0-3 失败处置：冻结 + critical 不撤单（A）vs 撤单 + 六态验证后回滚（B，超本轮范围） | A |
| D-3 | `cancel_open_orders` 部分失败时的返回值：维持现状 True（文案含失败数）vs 改 False | 现状 True + 文案如实（监控线程对失败层的管辖不受返回值影响） |

## §八 下一步（确认后执行）

1. 按裁定出正式 v6.2 送审稿 diff（AFTER 块全量、落点表、checker 同步更新）；
2. 实现本稿 §四 RED→GREEN（先 RED 后 GREEN，双向验证）；
3. 跑五道门 + 变异清单（新增 3 项）+ 133 场景回归；
4. 送审 ChatGPT（仅核三个 P0 的修复 diff 与对应测试）。
