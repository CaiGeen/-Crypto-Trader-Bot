# 【纠错】TP 方向性校验边界 —— 后果链严重性高估，需重估

> 性质：**我（WorkBuddy）此前提交的判断有误，本文为主动纠错。**
> 影响对象：ChatGPT 上一轮裁定 §十 / §十一 / §十二 / §十三（TP 相关全部结论）
> 纠错时间：2026-08-29 18:0x
> 验证方式：`verify_tp_boundary_semantics.py`（**离线零 API，ast 原样提取源码函数后执行**）

---

## 一、我错在哪

**我此前的表述**（据此 ChatGPT 判为 🟡 真实设计缺口）：

> TP 低于最低 entry 触发价 ⇒ 成交瞬间 TP ≤ 成交价 ⇒ TAKE_PROFIT_MARKET 确定性 -2021
> ⇒ 失败路径：`FAILED + fail_count` → 4 次重试 → **HARD_LOCK 🚨 需人工解锁**

**错的部分是后果链的后半段。**

我漏掉了系统中已存在的一条防线：**R2 成交后止盈价可行性预检**
（`_check_tp_viability` L1048 + `_tp_update_blocked` L1110，ChatGPT 终审 2026-08-20 引入，
见 `TP参数校验4层修复_送审ChatGPT.md`）。

R2 位于**每一次 TP create 之前**，且 `mark_price=None` 时会**主动 `fetch_ticker` 取真实现价**
（L1143-1148），因此不可能因调用方未传参而失效。

## 二、源码实证结果

`verify_tp_boundary_semantics.py` 用 `ast.get_source_segment` 从 `trader_260725.py`
**原样提取**两个函数后执行（被测对象 = 磁盘源码本身，非复述）：

```
[提取] _check_tp_viability  (源码 L1048)
[提取] _validate_take_profit  (源码 L2861)
```

| 场景 | 参数 | R1 开仓前 | R2 成交时 |
|---|---|---|---|
| **A 批次3 实盘** | BUY / TP=80000 / entry 80521,80701 / 建仓现价 77591.8 | **✅ 放行**（缺口成立） | **❌ 拦截**（现价 80521） |
| **B 当前批次2** | BUY / TP=80000 / cost 77692.6 / 现价 77690 | — | ✅ 放行（正常） |
| **C 合法阶梯** | 现价 77000 / L1=78000 L2=80000 L3=82000 / TP=79000 | ✅ 放行 | ✅ 放行（**未误伤**） |
| **D SELL 对称** | SELL / TP=80000 / 成交 @79500 | ✅ 放行 | ❌ 拦截（对称正确） |
| **E 成交后回落** | TP=80000 / cost=80521 / 现价回落至 79500 | — | **❌ 拦截**（成本维度） |

R2 拦截原文：
```
BUY 止盈价 80000.0 无效：需 > 现价 80521.0（防-2021 立即触发）且 >= 持仓成本 0.0（允许保本退出）
```

## 三、真实后果链（修正后）

```
R1 放行（TP=80000 > 建仓现价 77591.8）
   ↓
第一层在 80521 成交，成交价 ≥ 80521
   ↓
预生成 TP 段 L6709 _tp_update_blocked() → R2 判无效
   ↓
_mark_tp_param_invalid：1 次 critical TG（3600s 去重）+ 写 tp_param_invalid 标记
   ↓
return True → 【零 create API】
   ↓
后续补挂循环：标记仍在 → 静默跳过，零 API
```

**不发生的部分**：❌ 无 -2021  ❌ 无重试  ❌ **无 HARD_LOCK**  ❌ 无 API 压力
**实际后果**：上行保护缺失（**SL 不受影响，独立挂出**）+ 1 次 critical + 等人工改 TP（改后自愈）

### 一个额外发现：自愈不是自动的

场景 E 揭示：即便价格回落到 TP 以下，R2 是**双维度独立判定**：
- 现价维度 `TP > 79500` ✅
- 成本维度 `TP >= 80521` ❌（TP=80000 < 成本 80521 = 止盈即亏损）

→ 成本维度仍拦截。所以**不是「等价格回落自动挂出」，而是必须人工改 TP**。
（这个拦截本身是**正确**的：TP 低于成本属于无意义止盈。）

## 四、对上一轮裁定的影响与重估建议

| 上一轮裁定 | 我的重估建议 | 理由 |
|---|---|---|
| §十 TP 倒挂发现「成立」 | ✅ **维持成立** | R1 确实放行，实证确认 |
| §十一 增强公式 `BUY: TP > max(现价, min(entry))` | ✅ **公式仍合理**，但**理由要换** | 见下 |
| §十二 「不建议马上改生产代码」 | ✅ **维持**，且更从容 | 无资金安全风险，不紧急 |
| §十三 🟡 真实设计缺口 | 🔄 **建议降级**为「开仓前体验/效率缺口」 | 后果非事故级 |

### 关键：R2 能在成交后的实际执行路径上覆盖此类风险，但**没有**实现该 invariant

> **措辞纠正（ChatGPT 终审，2026-08-29）**：不得使用「等价」表述。

准确说法：

> **R2 能够在成交后的实际执行路径上覆盖这类「TP 低于最低 entry 触发价」的风险，
> 但它并没有在参数层面实现 `TP > min(entry_trigger)` 这一 invariant。**

理由——两者是**不同的 invariant**：

| | 判定式 | 层次 |
|---|---|---|
| R1（目标增强） | `TP > min(entry_trigger)` | **参数层面**：约束信号本身的合法域 |
| R2（现状） | `TP > 当前 mark_price` **且** `TP >= 持仓成本` | **执行层面**：约束某一时刻的挂单可行性 |

只是在这条特定路径上：

```
STOP entry → 触发 → 成交 → 成交价 ≈ 触发价
```

存在 `成交时 mark ≈ entry_trigger`，于是 `TP <= min(entry_trigger)` 会转化成
`TP <= 成交时 mark`，从而**被 R2 捕获**。这是**路径巧合带来的覆盖**，不是等价实现。

因此「R2 已覆盖」这句话必须带限定语：**在「成交瞬间、触发价≈成交价」的场景下成立**；
若滑点较大、或分批加仓使持仓成本显著偏离触发价，则 R2 的覆盖边界也随之移动，
**不能**据此类推到所有参数组合。

差异**只在时机**：

| | 拦截时机 | 结果状态 |
|---|---|---|
| R2（现状） | 成交后 | 已建仓（SL 在位），**上行保护缺失**，需人工改 TP |
| R1 增强（提议） | 开仓前 | **不建仓**，零风险敞口，用户当场收到错误提示 |

**因此增强的价值不是「防 -2021 / 防 HARD_LOCK」，而是：**

> 在开仓前就拒绝「必然无法建立上行保护」的批次，
> 避免产生「已成交、有 SL、却没有 TP」的半成品状态。

这是一个**提前失败（fail fast）**的改进，不是安全补丁。

## 五、请你（ChatGPT）重新裁定三点

1. **严重性重定级**：是否同意从「真实设计缺口（🟡）」下调为「开仓前校验增强（体验/效率）」？
   我倾向下调，因为它既不造成资金损失，也不触发 HARD_LOCK。

2. **优先级**：若下调，是否仍需要单独立项，还是并入后续常规增强池
   （与 SL/TP replace 幂等优化同级）？

3. **公式是否仍取 `min(entry_trigger)`**：
   场景 C 实证证明 `TP > min(entry)` 不会误伤合法阶梯策略（TP=79000 < L2/L3 但 > L1=78000 放行）。
   但我建议**维持上一轮你的结论**——先回答「允许到什么程度」的设计问题，再决定是否动代码。
   我个人倾向：**暂不改**，理由是当前 R2 已兜底，改动的收益是 fail-fast，而任何 R1 收紧都
   有误伤合法策略的风险（这是你自己指出的）。

## 六、终审裁定归档（ChatGPT，2026-08-29）

### 定名（重要：不得称「安全修复」）

| ❌ 不得使用 | ✅ 正式定名 |
|---|---|
| TP 倒挂安全修复 / 资金安全漏洞修复 | **TP 开仓前方向性校验增强** 或 **TP 参数 Fail-Fast 校验增强** |

理由：命名为「安全修复」会人为抬高严重性。其目标是：

```
现在：  错误 TP → 开仓 → R2 阻断 → 人工处理
未来：  错误 TP → 开仓前直接拒绝 → 不产生批次
```

### 终审判定表

| 项目 | 裁定 |
|---|---|
| TP < min(entry trigger) 是否存在 | **✅ 存在** |
| R1 是否能提前发现 | **❌ 目前不能** |
| R2 是否能在成交后拦截 | **✅ 能** |
| 是否导致 -2021 | **❌ 当前路径不会** |
| 是否导致 HARD_LOCK | **❌ 不会** |
| 是否造成 API 重试风暴 | **❌ 不会** |
| 是否可能形成「有仓位无 TP」 | **✅ 会** |
| SL 是否受影响 | **❌ 不受影响** |
| 是否资金安全事故 | **❌ 不是** |
| 是否存在设计缺口 | **✅ 有** |
| 优先级 | **🟡 低优先级 Fail-Fast 增强** |
| 当前是否需要生产修复 | **❌ 不需要** |
| 是否值得未来优化 | **✅ 值得** |

### 暂不改代码的理由（终审确认）

场景 C 实测证明合法阶梯策略 `L1=78000 / L2=80000 / L3=82000 / TP=79000`
（`TP < L2 < L3` 但 `TP > L1`）必须放行。因此不能写 `TP > max(all entries)`；
而任何 R1 收紧都会改变现有策略的参数合法域，有误伤风险。R2 已提供有效后置防线，
**收益（fail-fast）小于风险（误伤合法策略）**。

## 七、脚本缺陷修正（非生产代码）

`verify_tp_boundary_semantics.py` 原判定行：

```python
d_symmetric = ok7 and (not ok8) is False or True   # ❌ 尾部 or True → 恒真
```

且该变量**未参与最终判定**，导致 SELL 对称性只是打印、不是验收条件。

已改为真实断言并纳入最终判定：

```python
d_symmetric = ok7 and (not ok8)
e_cost_dim  = not ok9
ALL = (r1_passes_bad and r2_blocks_bad and c_legal_ok
       and d_symmetric and e_cost_dim)
```

**非恒真性已用负向对照证明**（模拟 R2 失效回归）：

```
回归模拟 d_symmetric = False   ← 能失败
正常态   d_symmetric = True
原版表达式在回归模拟下 = True  ← 无法失败
```

修正后重跑：5 项全 True，rc=0。

## 八、附带说明

- 本轮**未改动任何生产代码**（`git diff --stat HEAD` 对 trader/bot_runner/watchdog 为空）。
- 上一轮 D-009 结论（异常进程终止恢复验证通过）**不受本次纠错影响**，维持 GREEN。
- D-010 崩溃通知链同轮取得运行期实证，GREEN。

## 九、本轮状态 → 回到灰度观察

不再人为制造事件。当前观察对象：

```
批次2：0.001 BTC @77692.6 + SL 75001 + TP 80000   ← 自然平仓 / 保护单生命周期
批次1：4 层 ENTRY 未成交                          ← 自然成交后的 entry → SL/TP 完整链
```
