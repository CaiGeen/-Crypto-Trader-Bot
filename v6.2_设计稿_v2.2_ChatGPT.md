# v6.2 设计稿 v2.2（收敛增量）— 待用户过目后送审，未动生产

> 本稿是 **v2.1 的增量修正**（ChatGPT：主架构 / registry 恢复算法 / D-4=D-5 /
> 白名单 / `limit_creating` 迁移 / B-lite 全部批准；4 个边界须先修）。
> 按铁律，本轮全部声明已逐条独立源码核实：**4 项全实锤，零证伪**（§零）。
> 只覆盖 4 件事 + registry hardening + 5 个新测试反例；v2/v2.1 其余章节不变。
> 文档纪律：`python` 块完整可解析（零 Ellipsis）；生产摘录用 `python-frag`。

---

## §零 本轮核实结果（4 项全实锤）

| # | ChatGPT 声明 | 独立核实证据 | 结论 |
|---|---|---|---|
| 1 | `-2011` 只证明「无需再次 cancel」，不证明「没有成交」；v2.1 的 `-2011 → continue 向低层` 会把 hole 制造回来（E5 刚成交 → cancel -2011 → 继续 cancel E4 → filled=[T,T,T,F,T]） | v6.1 helper `_verify_entry_order_terminal`（new_helpers_v6.py L732-777）：verdict ∈ {gone, filled, open, unknown}；**OrderNotFound → unknown（Fail-Closed）**；docstring L742-748 明确「OrderNotFound 能证明『不用再 cancel 了』，证明不了『它没有成交』」。v2.1 把 -2011 当 already_terminal 正是该 helper 已否决的语义 | ✅ 成立（v2.1 §Δ3 证伪） |
| 2 | hole 只 critical 不够——系统仍会按 `last_filled_count` 纯计数保存并用 `sum(target_amounts[:N])` 自动平仓；且重启会把内存 bitmap 压平（检测失忆） | ①L6226 `last_filled_count = batch_filled_count`（纯计数）；②**L4568-4570 重启 `for i in range(last_filled_count): filled_layers[i] = True`**（hole 被压平）；③filled_details 是唯一持久化事实源：L3250/L3311/L4562-4563 按完整 `len(entry_orders)` 初始化、**未成交槽 = 0.0**、L4706 成交写 `executed_price(>0)` → hole 可纯推导：前 N 位含 ≤0 或第 N 位之后含 >0 | ✅ 成立，derive 硬门方案可行 |
| 3 | v2.1 zero-filled 把 `cancel_failed_ids` 预塞 unresolved 且永不删除 →「cancel 网络超时但实际已撤」永久假 unresolved 死锁；且自写判据未复用 verifier、fetch 没带 `params={'stop': True}` | v2.1 §Δ2 原文 `unresolved = list(cancel_failed_ids)` 无删除路径；verifier L758 `params={'stop': True}` 内建 | ✅ 成立 |
| 4 | ①generic pre-create rollback 失败无 abnormal reason（与 derive rollback failure 同一不变量不同入口）；②commit helper 缺 source-state guard（reason 已 abnormal 时仍可能 abnormal→normal） | ①送审稿 outer except pre-create 分支 L636-650：`_rb_ok=False` → 只 critical，**无 reason CAS**（磁盘停 close_phase=1 + market_confirming）→ 白名单静默；②v2.1 §4.1 commit 校验无 reason 前置，op_id+phase 匹配即写 `limit_pending_normal` | ✅ 两点均成立 |

**first-abnormal-wins 文案修正（采纳，代码方向不变）**：替换规则分两组——
- **可被 failure reason 替换的 source**（normal + transient）：`market_confirming`、`limit_pending_normal`、`limit_creating`；
- **abnormal root cause（first wins，绝不覆盖）**：`market_confirm_unknown`、`market_entry_unknown`、`limit_entry_unknown`、`rollback_rejected`、`settlement_error`、`txn_aborted_rollback_failed`、`limit_persist_failed`、`settlement_stuck` 等。

v2.1 §Δ6「commit 前异常 → first-abnormal-wins 保持 `limit_creating`」为**文案错误**：`limit_creating` 是 transient 不是 root cause，被 `settlement_error` 替换是**正确**方向（helper 代码本就如此，改文档即可）。

---

## §ΔA `-2011` verifier 门控（修 P0-①，替换 v2.1 §Δ3 撤单循环）

**纪律**：cancel 成功 → 可继续向低层；`-2011` → 必须 `_verify_entry_order_terminal` 定案——`gone` 才可继续、`filled` 立即停止 + critical（prefix 可能已破坏）、`open/unknown` 立即停止（Fail-Closed）。**任何失败路径都 break，绝不向更低层继续。**

```python
        cancel_failed_ids = []
        already_terminal_ids = []
        for idx in reversed(range(last_filled_count, len(entry_orders))):
            order_id = entry_orders[idx]
            try:
                self._safe_api_call(self.exchange.cancel_order, order_id, target_symbol, params={'stop': True})
                cancelled_ids.append(order_id)
                cancelled_layers.append(idx + 1)
                print(f"  └─ 已撤销第 {idx + 1} 层挂单: {order_id}")
            except Exception as e:
                if '-2011' in str(e) or 'Unknown order' in str(e):
                    # 🔒 v2.2（P0 修正）：-2011 只证明「无需再次 cancel」，
                    # 不证明「没有成交」。能否继续向更低层撤，必须由
                    # verifier 按交易所事实定案（INV-1v2：cancel ≠ 终态）。
                    verdict, _vo = self._verify_entry_order_terminal(order_id, target_symbol)
                    if verdict == 'gone':
                        already_terminal_ids.append(order_id)
                        print(f"  └─ 第 {idx + 1} 层挂单已确认终态: {order_id}")
                        continue  # 唯一允许继续向低层的路径
                    if verdict == 'filled':
                        self.send_tg_notification(
                            f"🚨【资金安全】ENTRY 在撤单前已成交！\n"
                            f"🆔 批次: `{batch_id}`\n📌 订单: {order_id}\n"
                            f"⚠️ 高层成交 → 成交位可能已不连续（hole），"
                            f"已停止向更低层撤单，请立即人工核对持仓与台账！",
                            level='critical')
                    cancel_failed_ids.append(order_id)
                    break  # filled / open / unknown 一律停止（不制造 hole）
                else:
                    cancel_failed_ids.append(order_id)
                    print(f"  └─ ⚠️ 撤销第 {idx + 1} 层挂单失败: {order_id} ({e})"
                          f"（ID 保留在台账，监控继续管辖）")
                    # 🔒 v2.1（INV-3a）：从高往低撤，遇阻即停——保持成交区前缀连续
                    break
```

（monitor producer #2 的撤单循环同改：`reversed` + 同款 -2011 verifier 门控 + 失败 break。）

---

## §ΔB derive prefix 硬门（修 P0-②，加入 `_derive_close_txn_vars`）

monitor 的 hole critical 是**观测层**；本节是**交易执行硬门**——两者同时存在。新增校验（置于现有 `filled_details_short` 校验之后）：

```python
        # 🔒 v2.2（INV-3a 硬门 / prefix integrity）：监控线程用纯计数保存
        # last_filled_count（L6226），重启还会把内存 filled_layers 前 N 位
        # 压平（L4568-4570）—— 内存 bitmap 不可靠。持久化 filled_details
        # 是唯一事实源：未成交槽 = 0.0（L3250/L4562-4563），成交槽 =
        # executed_price > 0（L4706）。前 N 位含非正价或第 N 位之后含成交价
        # = 成交位不连续（hole），prefix 假设已失效 → 禁止自动平仓。
        _bad_in_prefix = any(
            (not isinstance(v, (int, float)) or isinstance(v, bool))
            or float(v) <= 0
            for v in filled_details[:last_filled_count])
        _fill_in_tail = any(
            isinstance(v, (int, float)) and not isinstance(v, bool)
            and float(v) > 0
            for v in filled_details[last_filled_count:])
        if _bad_in_prefix or _fill_in_tail:
            return False, None, ('entry_fill_hole（成交位不连续：'
                                 f'前 {last_filled_count} 层存在无效成交价，'
                                 '或其后存在成交价——prefix 假设失效，'
                                 '自动平仓口径不可信，请人工核对持仓与台账）')
```

- Fail-Closed 效果：hole 批次**绝不发出自动平仓单**（close amount 口径不可信），derive 失败 → 现有 rollback/abnormal 通道接管；
- 正常批次不受影响：`filled_details[0:N]` 全为成交价（>0）、`[N:]` 全为 0.0；
- 存量 hole 批次（若存在）落在本门后转人工——这正是 Fail-Closed 的本意；
- 不新增持久化字段（不碰 merge/ratchet，ChatGPT 认可的「直接从已持久化数据推导」）。

---

## §ΔC zero-filled 重写（修 bug ③，替换 v2.1 §Δ2 确认段）

**最终事实只来自 `_verify_entry_order_terminal`**；`cancel_failed_ids` 只用于诊断文案，绝不预塞 unresolved：

```python
            # 🔒 v2.2（P0 修正）：unresolved 从空集开始，只由 verifier 事实填充。
            # v2.1 预塞 cancel_failed_ids 且无删除路径 → 「cancel 网络超时但
            # 实际已撤」会永久假 unresolved 死锁。verdict == 'gone' 即安全，
            # 无论 cancel 动作本身报过什么。
            unresolved = []
            _filled_found = False
            for order_id in entry_orders:
                verdict, _vo = self._verify_entry_order_terminal(order_id, target_symbol)
                if verdict == 'gone':
                    continue
                unresolved.append((order_id, verdict))
                if verdict == 'filled':
                    _filled_found = True
            if _filled_found:
                # zero-filled 批次的 pending ENTRY 成交了 → 已不是 zero-filled
                # 物理事实 → 批次保持 ACTIVE 由监控接管，critical 转人工
                self.send_tg_notification(
                    f"🚨【资金安全】撤单确认期间发现 ENTRY 已成交！\n"
                    f"🆔 批次: `{batch_id}`\n"
                    f"⚠️ 批次不再是无持仓状态，已保持 ACTIVE 由监控接管，"
                    f"请立即人工核对持仓！",
                    level='critical')
            if unresolved:
                # 存在未确认终态 → 批次保持 ACTIVE，监控继续管辖。
                # 绝不写 pending_close/close_phase/is_programmatic_cancel。
                target_b_data.pop('is_programmatic_cancel', None)
                self.save_batch_state(target_symbol, batch_id, target_b_data)
                return False, (f"⚠️ 批次 `{batch_id}` 撤单后仍有 "
                               f"{len(unresolved)} 张 ENTRY 未确认终态"
                               f"（{[u[0] for u in unresolved]}，ID 已保留，监控继续）\n"
                               f"💡 请重试或人工核对")
            # 全部 gone → 才允许进入终止形态（entry_orders 原样保留，D-4）
            target_b_data['pending_sl_orders'] = []
            target_b_data['pending_close'] = True
            target_b_data['close_phase'] = 1
            self.save_batch_state(target_symbol, batch_id, target_b_data)
```

（fetch 循环内建 `params={'stop': True}`——verifier L758 已带，不再自写判据。`result_msg` 同 v2.1。）

---

## §ΔD pre-create rollback 失败统一 CAS + commit source-state guard（修 ④）

### D-1 统一纪律（市价/限价 outer except 同构）

> 任何 `close_order_placed=False` 异常 → 尝试 CAS rollback → 成功回 ACTIVE / **失败必须 CAS 写 `txn_aborted_rollback_failed`** + critical。derive 失败分支（v2 §3.2）与 outer except（本节）共用同一 reason 值——同一不变量，同一落点。

替换送审稿 outer except 的 rollback-else 段（L644-649，限价对应段同构）：

```python-frag
            else:
                _rs_ok, _rs_why = self._set_close_reason_if_current(
                    target_symbol, batch_id, close_op_id, 'txn_aborted_rollback_failed')
                self.send_tg_notification(
                    f"🚨【资金安全】市价平仓失败且回滚被拒绝！\n批次: `{batch_id}`\n"
                    f"原因: {_rb_why}\n"
                    + ('' if _rs_ok else
                       f"⚠️ close_reason 切换失败（{_rs_why}），"
                       "冻结告警可能静默，请立即人工处置！\n")
                    + f"请立即检查仓位是否仍有 SL 保护！",
                    level='critical')
            return False, f"❌ 市价平仓失败: {e}"
```

### D-2 `_commit_limit_close_order_if_current` 增加 source-state guard

迁移已有明确语义 `limit_creating → limit_pending_normal`，必须验证**迁移源状态**——否则同 op_id 已切 `settlement_error` 等异常态后，commit 仍可把它覆盖回正常态（违反 first-abnormal-wins）。在 op_id/phase/pending_close/settled 校验之后、写 4 字段之前插入：

```python
            if b.get('close_reason') != 'limit_creating':
                # 🔒 v2.2（source-state guard）：迁移源不符 = 本事务状态已被
                # 其他路径推进/异常化。commit 只认 limit_creating 起点，
                # 绝不把 abnormal reason 拉回正常态。
                return False, 'reason_changed（迁移源状态不符）'
```

（`why` 枚举补充 `reason_changed`；B-lite 失败分支的 `limit_persist_failed` 写入路径不受影响——它走 `_set_close_reason_if_current`，源 `limit_creating` 在可替换组内。）

### D-3 first-abnormal-wins 文案修正

v2.1 §Δ6 的「commit 前异常 → first-abnormal-wins 保持 `limit_creating`」**删除**，改为 §零 的两组模型：`limit_creating` 属可替换 transient，被 `settlement_error` 替换是正确方向。

---

## §ΔE registry hardening（Δ1 批准的便宜防御，替换 v2.1 §Δ1 扫描段）

```python
        confirmed = []
        seen_layers = set()
        for identity, entry in reg.items():
            if not isinstance(entry, dict):
                continue
            if entry.get('role') != 'ENTRY':
                continue
            oid = entry.get('order_id')
            if not oid:
                continue
            layer = entry.get('layer')
            if not isinstance(layer, int) or isinstance(layer, bool):
                # 🔒 v2.2：缺 layer / 非 int 绝不能静默解释成 layer 0
                # （int(True)==1 的 bool 陷阱一并排除）
                return [], False, []
            if layer in seen_layers:
                # 🔒 v2.2：重复 layer = registry 链已不可信
                return [], False, []
            seen_layers.add(layer)
            confirmed.append((layer, str(oid)))
        confirmed.sort(key=lambda x: x[0])
```

（其余：`len(chain)==len(target_amounts)` + 台账前缀一致 + `[lfc:]` 待验证——v2.1 §Δ1 不变，ChatGPT 已批准。）

---

## §ΔF 测试增量（5 个精准反例，接 v2.1 §Δ9）

| 编号 | 场景 | 必须证明 |
|---|---|---|
| R1-i | `cancel(E5) → -2011`，随后 `fetch_order(E5) → filled`，E4 尚 active | **`cancel(E4)` 调用次数 == 0** + critical——prefix 策略被 verifier 定案立即锁死（`-2011 → continue` 的变异体必须被此用例杀死） |
| R1-j | `cancel(E2)` 抛网络异常，但 verifier 对 E2 返回 `gone` | zero-filled 不得因 `cancel_failed_ids` 残留而永久 unresolved；全部 gone → 可进终止形态 |
| R1-k | `filled_details=[p1,p2,p3,0,p5]`，`last_filled_count=4` | `_derive_close_txn_vars` RED：返回 `entry_fill_hole`，**绝不产生 close amount**；正常批次 `[p1,p2,p3,0,0]+N=3` 不误报（双向验证） |
| R2-g | BEGIN 成功 → pre-create 抛异常 → rollback persist 失败（**outer except 真调用序列**，非单测 helper） | 磁盘 reason == `txn_aborted_rollback_failed`；CAS 再失败时 critical 文案含「切换失败」 |
| R3-b | op_id/phase/pending_close/settled 全匹配，但磁盘 reason 已是 `settlement_error` | commit 返回 `(False, 'reason_changed…')`，**绝不重新写 `limit_pending_normal`** |

（v2.1 的 R1-f/g/h、R2-f、R3-crash-state 保留；R1-g 的 GREEN 断言随 §ΔC 微调——unresolved 集合以 verifier 事实为准。）

---

## §Gate 状态

- **批准**：主架构四条主线、D-4=A、D-5=A、registry 恢复算法（+本稿 hardening）、正常态白名单 / first-failure-reason（两组模型）、`limit_creating → durable commit → limit_pending_normal`、B-lite；
- **本稿闭环**：①-2011 verifier 门控（hole 不再被 -2011 复活）②derive prefix 硬门（hole 批次禁止自动平仓）③zero-filled unresolved 以 verifier 事实为准（无死锁）④pre-create rollback 失败统一 CAS + commit source-state guard；
- helper 计数仍 **13**（`_verify_entry_order_terminal` 是 v6.1 既有 helper，复用不新增）；
- 生产零改动（HEAD=e953d79）。

## §下一步

1. 用户过目本增量稿 → 送审 ChatGPT（只核 §ΔA-§ΔF + 5 个新反例）；
2. 批准后出正式 v6.2 送审稿 diff（AFTER 块全量 + 落点表 + checker 名单 13 helpers + `cancel_open_orders`/monitor 段入 `PROD_SIGNATURE_OVERRIDES`）；
3. RED→GREEN（v2 §六 + v2.1 §Δ9 + 本稿 §ΔF）→ 五道门 + 变异 → 二次送审 → 五段式落生产。
